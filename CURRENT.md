# Current

> **Version**: v3.0.0-beta.228 — "the drain finishes, and the vxreddit renderer" (6 PRs / 6 runtime / 104 range files; no migrations; PR #2469 merged 2026-09-21 22:08Z, main `de26c3f1d`; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.227 — "attachment provenance, the embed diagnostic, and the gateway hardening" (9 PRs / 8 runtime / 116 PR-diff files; no migrations; 2026-09-21 00:34Z, `56e36674d`).

---

## 🙋 Owner queue (the human-only items, consolidated 2026-09-08 — everything below also lives in its long-form bullet under Open items)

- **Smoke pass on dev (one sitting, report pass/fail by number; owner holds it for last)**: (1) ~~`/shapes export` on an imported shape~~ PASS 2026-09-13 (alt account, `adora-mi-keter-malkut` freshly imported under a suffixed slug, 51 memories): README.md first, then config / memories / knowledge-base / user-personalization as JSON+Markdown pairs plus export.json — the first attempt surfaced TASK-958 (an undecryptable stored cookie 500s the list route; `/shapes auth` again fixed it) and TASK-957 (the request log leaks `x-service-auth`); (2) ~~`/history purge` with scope Everyone's from an account WITHOUT Manage Messages~~ PASS 2026-09-13: refused before the modal, the message names Manage Messages; (3) ~~Emily in a FRESH channel or DM after dev has deployed #2412~~ DONE 2026-09-13, PASS on the mechanism: two payloads by accident (cross-channel history ON then OFF) — ON: 0 exclamations/1k, 698 chars, "sweetheart" from the history block; OFF: 4.1/1k, 241 chars, card register back. The anchor and directive work; cross-channel history is the drift vector; `doc-97` Phase 4 (history-side treatment) OPENED, numbers there. (4) The masked-link `/inspect` error render (#2259) is carried as a watch, not asked for.
- **TASK-907 (owner review)**: commitment facts are live on prod since beta.219 with no switch. The tag filter SHIPPED 2026-09-21 (#2467, TASK-959 Done; on dev now, prod with beta.228): `/memory facts character:X tag:commitment:promise` (autocomplete offers the four kinds) lists only those facts, so the Sort A-Z workaround is retired on dev — do they read as commitments? Also note any welcomed pet name that was NOT extracted (the TASK-950 gap in the wild).
- **Rollout state (2026-09-13)**: memory-archive switches ON on dev and prod; Emily (`emily-tzudad-seraph-ditza`) listed in the prod render switch BEFORE her pre-warm (owner call); the 644-row pre-warm enqueued on prod 15:0xZ. **Re-read 2026-09-13 ~22:45Z: gate 85.8% of 95%, NOT READY.** Of 698 rows retrieved in the window: 599 done, 73 failed, 25 dead, 1 never attempted; fact coverage 71.6%; 74 hot rows still selectable. **All 25 dead rows are one class, `no_template`** — terminal and non-billing (the row's content carries no extractable assistant portion, so it renders verbatim). With those 25 permanently out, the ceiling is 96.4%, so the gate is reachable but ONLY if nearly all 73 failures clear — and the sweep report gives failed rows as a bare count with no error-class breakdown, so what they failed on is not readable from it (TASK-971 filed, medium). No jobs were enqueued: the rollout flip stays the owner's. `realMessagesEnabled` confirmed ON on dev and prod. The dev before/after baseline SKIPPED by ruling. The dev stray `archive-summary` probe job removed (C2 cleanup done).
- **Leisure (unchanged, do not re-surface)**: the voice-harness blind review · the slice P 30-row spot check · TASK-104 · ~~TASK-133~~ (round trip DONE 2026-09-13 with the smoke pass) · card-level examples for Emily in her own register.
- **Done 2026-09-13**: the Default `system_prompts` row edited on dev and prod (v1 then v2; long-form under doc-97 Phase 2) · `WEEKLY_AUDIT_GH_TOKEN` set (Dependabot row reads; deletion-safety row fixed by #2413) · `TZUROT_RAILWAY_API_TOKEN_DEV`/`_PROD` in the local `.env` (tooling reads them after the TASK-62 follow-up) · the TASK-798 prod measurement · the stale `.bashrc` OpenRouter key revoked · the Waffles Share-Memories question CLOSED by ruling (deal with it if it recurs) · the privacy-policy z.ai summaries bullet committed.
- **Agent-run, no owner action**: the doc-17 gap-bucket cache read · the Emily gate and dead-row read · TASK-702 probe · TASK-62 env-suffixed token read · TASK-951 `.env.example` guard · TASK-838 cadence ledger then the doc-61 economy pass (ruled 2026-09-13, Opus lane) · the #2270 link-share watcher · the five `state:observable` watches TASK-952–956.

## 🚀 2026-09-20/21 — beta.227 CUT (shipped; PR #2460 `56e36674d`, 00:34Z; the owner ruled cut-now on new information — the ruling, contents and risk appraisal are in the PR body and release notes)

**Carried watches (agent-run, no owner action)**: the first prod `Recent-days digest retention sweep` tick (`48 9 * * *` UTC) · the 401 re-authenticate path on an undecryptable shapes cookie (unsmokeable; observability only) · the redacted `Content-Disposition` on the next account export · the provenance header on the next image/voice turn via `/inspect` (`[Image: <name>]` above its description; `[Voice message: Ns]` plus the transcript wrapper). The vxreddit capture, the manual-mode REST probe (`railway run -s bot-client -e production` — `pnpm ops run --with` cannot read a service-scoped variable), the renderer's five review rounds and the dev smoke all live on TASK-1024 and in git.

## 🚀 2026-09-21 — post-227 drain (Fable driving; all on develop, beta.228 train)

**Merged**: #2461 (vxreddit renderer, above) · #2463 + #2464 (dependabot prod/dev groups; dotenv 18 is safe — we import only `dotenv/config`, which v18 keeps; the embeddings bumps onnxruntime-node 1.30 / transformers 4.3 are the runtime-unverified part, dev boot is the check) · #2466 (TASK-1017, scheduled-job dispatch map; two review findings dispositioned in the body, no code change). **On `main` outside the train**: #2465 (claude-code-action 1.0.230 via main-cut — the first push lost a cycle to the pre-push workflow-sync guard, TASK-1034; `release:finalize` resynced develop, which orphaned the TASK-1017 dispatch base and forced the manual patch transfer; #2462 closed as superseded). **TASK-960 ANSWERED** on the task: sequential turns see the sibling reply, the multi-tag fan-out does not (`Promise.all` in `MultiTagCoordinator.ts`); owner queue, recommendation stay parallel. **Filed**: TASK-1033 (NSFW-gated vision prompt context; the vision prompt carries no platform context and the channel flag never reaches ai-worker; the shared description cache has no framing dimension) · TASK-1034 · TASK-1035 (dotenv 18 stderr line on every ops/test run). **Owner smoke still open**: the vxreddit link on dev (item above). **Later the same day**: #2467 (TASK-959, the facts tag filter) MERGED after three review rounds; TASK-961 ANSWERED (undo restores, the deletion chain is pinned on hard-delete; the soft-clear-should-forget call is in the owner queue); TASK-1036 filed (enforce route-manifest query schemas at the gateway). **#2468 (TASK-962) MERGED** (read-time cost enrichment on both single-log diagnostic routes; owner ruled the line visible to everyone; four review rounds; TASK-1040 filed for the fallback-provider gap on the owner's merge-now call). **The beta.228 drain is complete and the cut criterion is met** (`release:range`: 6 PRs / 6 runtime / 103 files, no migrations). **beta.228 CUT 2026-09-21 22:08Z** (fresh session after `/clear`, Fable driving): the proposal was made from the board's own criterion and the owner ruled cut-now; the bump `c4483277b` carried the commands-table edits (the `tag` filter, the estimated-cost view); PR **#2469**, one holistic review round, no findings (its one unmentioned item, the `knip.json` pino-ignore drop, is #2467's, not the deps bumps'); merged `de26c3f1d`, finalize done, tagged `latest`, 227 demoted. The embeddings bumps are dev-OBSERVED (ai-worker boot 21:23Z on the bumped tree, embeddings produced during the owner's 21:29Z smoke, zero error lines). **Smoke PASSED 2026-09-21 18:48 EDT (owner, prod, #pop-culture-media)**: a vxreddit link (a four-panel Emily fan-art set) and Emily described every panel, the plush and the rubber duck's caption — the Components-V2 render reaches vision on prod; beta.228 is fully verified. Side observation for doc-97, not a finding: the reply in that long-lived channel ran ~1,450 chars with zero exclamation marks of its own and courtroom vocabulary ("demanding testimony", "on record", "the council decided", "curator") — the same-channel drift signature TASK-1039 targets, in a second channel. Observability: the cost line on the next `/inspect`; the tag filter through the TASK-907 review. **After the cut, same session**: the TASK-1039 gate MEASURED on prod (the join holds, coverage 0.92 on the diary thread; task relabelled ready; the doc-97 fallback bullet corrected). The owner ruled TASK-1038 promotes the digest list too (fully automatic, atomic across three writes). **TASK-1038 IN REVIEW — PR #2470** (branch `feat/archive-promotion`, base `0eb754903`, head `bb0a7a530` after the round-1 fixup; CI monitor `bbdb1qieb`): a gateway admin route `POST /api/admin/memory-archive/promote` (grouped coverage SQL, the flip-gate formula moved into common-types, one transaction per promotion, registry entries `archivePromotionOptOutPersonalities` + `archivePromotionEnabled`) and a bot-client 6-hour scheduler that posts the owner-channel embed; living intent: LLD § Slice C3 + MEM-ARCH-033/034/035. Nested dispatch, eight canaries, the opt-out fixture added at component tier before transfer; gates in the main tree green (five package suites, component tier, throttled `pnpm quality`). Owner question in the PR body: no minimum denominator on the gate (recommendation: leave it; the opt-out list covers it). **TASK-1039 IN REVIEW — PR #2471** (branch `feat/same-channel-render-mode`, head `fe9fc9f92`; CI monitor `bc6dk6zuc`): `sameChannelRenderMode` both/summarized/user-only + `sameChannelVerbatimExchanges` default 10, exposed on every settings dashboard's Memory page; the seam is `ContextStep.sourceHistory` on the ai-worker render IR (`StructuredHistoryEntry` already carries the Discord ids — the spec's assembler/`ConversationHistoryEntry` premise was wrong and is corrected in the PR body), the lookup in conversation-history adds the `visibility` filter, `rendered="summary"` marks substituted XML turns. Nested dispatch survived a 529 mid-run (resumed by SendMessage, worktree intact); nine canaries; gates green in the main tree (six suites, component tier, no snapshot moved, quality). PARTIAL on the register clause by construction: the diary-thread with/without read happens after it ships — the beta.229 cut criterion.

## 🔁 FABLE → OPUS HANDOFF (owner call 2026-09-21 evening: Fable burn rate exceeded; tomorrow runs on Opus)

**Done under Fable tonight**: the beta.228 cut, the TASK-1039 gate measurement, the 1038 build + PR, the 1039 spec (the design judgment) and its dispatch. **The Opus lane picks up** (`/tzurot-orchestration` mode table: Opus main loop, nested dispatch for substantive units, the Opus loop's own full-diff read is the gate): (1) **PR #2470 round 1 LANDED** as fixup `bb0a7a530` (five approved findings, one batch; finding 5 is TASK-1041; transferred, gated in the main tree, monitor `bbdb1qieb`). Two orchestrator scope calls, both correct, both due in the amended base body at merge: the dry-run projection now reports the same three-way writes, and the MEM-ARCH-033 spec row reads "not already fully converged". Later rounds per `/tzurot-review-response` § 3a — a fresh nested dispatch from a round spec, ONE fixup per round; autosquash + amend the base body (the specs rows, the `skipped.conflicted` accounting) right before merge; merge when truly ready. (2) **PR #2471 round 1 DISPATCHED** (round landed 22:05Z, four findings, owner approved all four as one batch: newest-first lookup with first-hit-wins so truncation drops the oldest duplicate, the info log on every non-`'both'` turn, the data-source guard test, a shared `partitionExchanges`) in agent worktree `.claude/worktrees/agent-ad567b61c920a5d59` from base `fe9fc9f92`, spec `docs/local/handoffs/spec-task-1039-round1.md`, dispatched at this commit's time with a 25-minute horizon — its report arrives as a task notification in whichever context is open: read the FULL diff, park on `feat/same-channel-render-mode`, `pnpm ops worktree:transfer <path> --base fe9fc9f92`, rebuild `@tzurot/common-types` + `@tzurot/conversation-history`, gates per spec § 7 sequentially, ONE `git commit --fixup=fe9fc9f92`, plain push, arm a monitor (the round-0 monitor has ended). If the worktree is GONE, re-dispatch fresh from the spec file. At merge, the PR body's Observability bullet and Residue bullet 3 are re-derived (fixes 1 and 2 change both); (3) the **mechanical drain** while CI runs: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` — TASK-988 (high) first, then 283 · 318 · 527 · 531 · 559 · 596, plus the 2026-09-21 residue TASK-1035 · 1036 · 1040 · 1030–1032 as their sizes allow; (4) **the beta.229 cut** when the criterion on the board is met (TASK-1039 merged behind its override with the with/without read on the diary thread) — release ops are model-independent; the proposal is three-part per `/tzurot-git-workflow` § Release. **Keep for Fable**: doc-97 Phase 4 slice 2 design (the measured default for N, the facts-vocabulary slice), any council pass, any spec for a semantic unit. The coverage script's `scripts/analysis/` run copy was deleted on the owner's ok; the original stays under `docs/local/handoffs/`. After the cut: doc-97 Phase 4 with TASK-1039 as its first slice (owner's voice-drift report 2026-09-21; TASK-1037 admin-tier flip is the owner's call, TASK-1038 auto-promotion is ready).

## 🔬 2026-09-19/20 — PROD BUG PAIR (Opus driving; diagnosed from owner `/inspect` payloads) — reviewed by Fable 2026-09-20 from the full runtime diff, no blocking findings

**(1) Misattribution FIXED — #2456** (`86768cb47`): `PromptBuilder` built the trigger message with bare prose instead of the `[Image: …]` emitter, so TASK-804's constraint keyed on a marker that path never produced (since `6cdf58fe37`, 2026-01-22). The same PR closed the two forgery vectors a load-bearing header opens (`headerDisplayName` for the filename, `neutralizeHeaderMarkers` for the body). **TASK-804 CLOSED**; clause 1 stays unverified by construction (owner waiver 2026-08-29). Six review rounds, four of which caught false claims in the PR prose. **Fable's note**: every VOICE turn's trigger render changed shape too — header plus the `<voice_transcripts>` wrapper where the bare transcript was inlined — correct by the protected-tags asymmetry, and named in the release notes.

**(2) Embeds — PARTIAL, #2459** (`a82e82b4b`); the character still cannot see the art. vxreddit adopted Discord **Components V2** (Discordbot-UA probe, tree matches the screenshot); `parseEmbed` reads only legacy fields. Shipped: one shared `formatEmbedElement` at all four wrapper sites emitting `<embed rendered="false" keys="...">`, `logEmptyEmbedShape` (numeric types and counts only, four-value adversarial test), and the legacy `url`/`provider`/`video`/`type` render. NOT shipped: the renderer. **CAPTURE 2026-09-21 00:40Z (prod, first turn after the deploy)**: the tree is ON THE EMBED (`embed.components`, `embed.type === "components"`), `message.components` is empty — the inferred location was wrong, and the `embeds: []` worry had no instance. Nothing types it (discord-api-types 0.38.55 is installed AND latest; Discord's docs are silent), the node types are already installed. **TASK-1024 stays open on step 2**, now gated only on the owner's REST probe of the node shape (smoke item above). Residue: TASK-1028 (its trigger has fired).

**(3) TASK-1025 + TASK-841 — #2458** (`b4fa61ac2`): bot-client's PERSISTED placeholder built the same header from the raw filename; all three sites now route through `headerDisplayName`, and the sanitizers, `HEADER_LABELS`, `imageSource` and `imageHeaderLabel` hoist to `common-types/utils/attachmentProvenance.ts`, one definition each. TASK-1025 Done; TASK-841 PARTIAL (two regex consumers still hand-spell the vocabulary — **TASK-1026**); fullwidth brackets pass through — **TASK-1027**, `state:owner`.

**#2457** — adm-zip override → 0.6.1 clears both advisories; `security:advisories` still lists them because alerts sit on the default branch and `classifyScope` short-circuits on transitive — **TASK-947** stays open for the post-`main` re-run. **Opus's own ledger**: four spec defects of one shape (a correct enumeration never checked against the sentence saying why the unit exists) plus a CURRENT.md mis-splice from a stale line number; **TASK-1029** carries the structural fix — state the unit's PURPOSE, canary that first, READ the failure count.

## 🚀 2026-09-18 — beta.226 CUT (shipped; the prod lane is a watch)

**COMPLETE 2026-09-18**: PR #2450 merged 13:42Z, main = develop = `0f1e9cfbc`, tagged `latest`; both slice-1a migrations premigrated before the merge; owner flips ~13:55Z; the owner pair reached `done` at the 14:26Z tick and the first prod trace read **2.04 exclamations/1k** against the OFF arm's 4.1 (`<recent_days>` 2,017 chars, 1,185 completion tokens, 17.1 s). Numbers in the design doc §6 and the release notes. **Two mechanism facts**: a failed digest pair is NOT retried on the next tick (the failure write stamps `source_watermark`); the every-table-dispositioned gate EXISTS in `syncTables.test.ts`. **Parked, needs numbers**: reported speech as third-person paraphrase or double quotes — validator-only until prod tick rejections show the rate (`pnpm ops logs --env prod --service ai-worker --filter '"Recent-days digest"'`).

**After the cut, same day**: #2451 (TASK-1013 + 1014, the digest retention sweep `48 9 * * *` and the export at `formatVersion` 3) and #2452 (process batch) merged under Fable; Opus took over ~17:30Z and the owner DEFERRED the beta.227 cut for the full medium/S drain — since superseded (block above). Drain shipped under Opus: TASK-1015 → #2453 (residue TASK-1020/1021), TASK-968 + 969 → #2454 (the fix made response headers reach the log for the first time and surfaced a real leak, the account-export filename in `Content-Disposition`, now redacted; residue TASK-1022/1023; GitGuardian went red on synthetic fixtures — TASK-856's second data point), TASK-958 → #2455 (exactly one route decrypts, not four). Two non-blocking owner questions recorded in those PR bodies: `Access-Control-Allow-Origin` left unredacted by judgment; TASK-1021 filed rather than applied.

- **Mechanics worth keeping**: `release:premigrate` is `autoMode.soft_deny` at user scope and now also `permissions.ask`, so it prompts to the phone and never reaches the classifier; the `!` handoff is not a fallback when the owner is remote. The merge gate's re-derive step caught the PR body's pre-bump file count (155 vs 175) — run `release:range` AFTER the bump commit.
- **beta.226 watch list (observability, no owner round)**: (1) the first prod `Recent-days digest sweep complete` line with `generated=1` for the owner pair; (2) the first prod `<recent_days>` render in a debug export; (3) carried from 225: the shared-LTM foreign note/fact export (TASK-991/992), `Invalidated persona cache`, the `x-forwarded-for` carrier, the qwen tail (TASK-937), the catalog-outage copy (#2414).
- **Carried from 225**: the prod `INTERNAL_SERVICE_SECRET` rotation done 2026-09-16 (TASK-980/981 unexercised); `worktree:transfer` refuses `no-unpushed-base` after a rebase-merged base — the manual patch path is the fallback; agent worktrees: none.

## 📋 Open items (near-term)

- **beta.219 → beta.221 (2026-09-07 → 09-09), all shipped**: the beta.219 cut (8 PRs / 206 files, three additive migrations premigrated; #2353's workflow hunk landed on main via #2361), #2362 (TASK-912 — settings dashboard renders an empty slug list as `(none)`, new `SettingType.LIST`), and the #2354 / #2363–#2371 / #2373–#2378 drain. Per-PR detail in the release notes and PR bodies. Residue still open: TASK-913 (vitest-5 watch), TASK-918 (partial-invalidity boot wipe), TASK-919 (fifth private `createTestCharacter`).
- **🔥 beta.219 opener, HIGH: character voice drift (`doc-97`)** — owner intake 2026-09-05 with a handoff and debug payload (both under `docs/local/handoffs/`, gitignored). Owner decisions (2026-09-05, binding): cut 218 first (done); the memory overhaul (`doc-8`) is the active epic again through this theme, anchor first and caching Phase 2 behind it; LID starts at the re-entry design pass; backfill back on the table; no model-parameter experiments. Measured from prod memories (read-only, aggregates only): the card register faded through spring and collapsed in August 2026 (exclamations per 1k chars 0.7–1.0 in Apr–Jul → 0.06 Aug → 0.01 Sep; courtroom vocabulary zero through July, then rising; reply length 830 → 1,652 chars). The cross-channel history feature shipped 2026-02-28, so it is not the August trigger alone; August is also the heaviest memory month (529 rows), and the character's preset history (model change?) is unchecked. Phase 1 = a cache-neutral `<voice_anchor>` before the current turn (`PromptBuilder.ts`, targets in the theme doc). The one-off curve script is stored under `docs/local/handoffs/` and copied into `scripts/analysis/` to run (workspace packages resolve only from there; run with `pnpm ops run --env prod --force tsx <path> <slug>`).
- **beta.218 CUT 2026-09-05** — `doc-97` Phase 1 MERGED as #2348 (the `voice_anchor` V-tier section; TASK-900 filed for the catalog-wide size measurement); the Phase 2 directive text is drafted in the theme doc for the owner to apply; validation is smoke item 4 below. `docs/proposals/backlog/memory-archive-format.md` ACCEPTED 2026-09-05 (four-model council + owner pass). TASK-880 stays a prod watch.
- **Memory-archive build (`doc-8`) COMPLETE and LIVE**: slices P/A/B1/B2/C1/C2 + the commitment fact type shipped 2026-09-06→07 (#2349–#2352, #2356–#2358; detail in the PR bodies, the design in `docs/proposals/backlog/memory-archive-format.md` §8, residue in TASK-901/902/904/905/906/910). Owner rulings: commitment facts prompt-level only; retrievals stamped. **Rollout state 2026-09-13**: the enqueue and model switches ON on dev and prod; Emily listed in the prod render switch; her 644-row pre-warm enqueued on prod (`pnpm ops memory:summarize --env prod --personality emily-tzudad-seraph-ditza --dry-run` reads the ≥95% gate; 21.1% at 15:45Z, 4 dead rows to classify); the other characters flip one at a time as their gates read READY (D3, per character on purpose: the summary arm loses ~18 points of correctness to verbatim, so a healthy character pays that for nothing). The privacy-policy z.ai summaries bullet landed 2026-09-13. Dev's stray C2 probe job removed.
- **Real-messages flip (prompt-assembly Phase 2) — ON in prod (log-confirmed 2026-09-08) and dev (owner-confirmed 2026-09-13).** What remains is the doc-17 READING: the multi-day gap-bucket sweep across prod deployments (agent-run; the first 11-hour read was too thin — one `<2 min` turn, 30 in 2–10 min at 36% hit, 23 beyond 10 min at 56%). The dev before/after baseline was SKIPPED by owner ruling 2026-09-13; the snapshot review (the voice harness on Emily, third-person self-reference as the guard) has no pre-flip baseline; the fresh-thread probe (2026-09-13) showed the anchor holding with cross-channel history off and failing with it on, so the review is no longer gated but still lacks the baseline the skip removed — owner call whether it runs at all. Rollback is the same setting back to false, live.
- **beta.219 watch list**: the retrieval stamp PASSED (log-closed 2026-09-08, 974 retrievals, zero `Retrieval stamp tail rejected`); the commitment-facts review is TASK-907 on the owner queue.
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low (used 2026-09-09 → 09-10 for the drain; back on Fable 2026-09-10 evening at ~11% daily headroom against the 14.3%/day pro-rata). TASK-540 (ruled 2026-09-12: 503) and TASK-559 (ruled 2026-09-13: filter against gates before sampling) are both drain-ready.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped**; **second run 2026-09-09 ~20:05Z (owner-approved via AskUserQuestion): 5 purged (1 grace-expired, 4 never-used, 0 characters), 0 skipped, 1 warning DM enqueued** (`retention:notify` prompts and exits 13 from a non-TTY shell without `--force`, unlike purge — folded into TASK-926) — now 187 users; 2 in grace, 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. **Phase 4 SHIPPED in beta.222 (2026-09-11, autonomy ON by owner ruling; kill switch `RETENTION_AUTORUN_ENABLED=false`). First autonomous prod run 2026-09-11 13:39:37Z, 60 s after boot, mode live: attempted 0, purged 0, eligible 0, 1 warning DM sent (0 bounced), no halt, no breaker warning, report delivered — TASK-926 CLOSED on that observation.** Runs hourly; the 23 h cooldown gates the next live one. **Reminder DM shipped 2026-09-11 (#2395, TASK-331; `retention_reminded_at` on dev, prod at the beta.223 premigrate): one reminder at day 23–29 of grace; the first prod reminder cohort can form on or after 2026-10-04.**
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
- **#2379 (TASK-920 Done, beta.221)**: the compound self-header leak is closed by `leadingSelfHeaderLineMatcher`, name-scoped; six review rounds, six real defects, all pre-merge. Residuals TASK-921 (name divergence, medium) and TASK-923 (Done 2026-09-12 via #2400); the durable direction, match the rendered header string rather than guess from the roster name, is recorded on both tasks.
