# Current

> **Version**: v3.0.0-beta.204 — "count-cap hysteresis": the channel-history window's start row now holds still across a chunk instead of sliding every turn (#2124, design §2.5.2), plus prompt-identity fixes — roster speaker-independence (#2123), participant-bio attribution (#2129, real identity bleed), quota retarget after demotion (#2128), model-footer dedupe (#2130), and this release's own CodeQL fix (#2132). 7 PRs / 6 runtime / 91 files. Migration (additive index on `conversation_history`) premigrated to prod BEFORE the merge, owner-authorized via AskUserQuestion; **verified in `pg_indexes`, and both protected pgvector indexes confirmed still present**. Merged to main 2026-08-17 23:52 UTC; `release:finalize` run (develop SHA-aligned); tagged + published; beta.203 demoted.
>
> **Previous**: v3.0.0-beta.203 — "z.ai thinking translation": doc-77 COMPLETE (z.ai `thinking`/`reasoning_effort` translation #2112, save-time validation #2114, GLM-5.3 catalog #2115, `show_thinking` retirement #2110) + riders (#2116 reference-stub preview, #2117 cache observability, #2118 guest-mode footer, #2119 persist durability, #2120 retention dev-footgun — the owner's pre-cut gate). 11 PRs / 9 runtime / 149 files. Migration (data-only JSONB strip) premigrated to prod BEFORE the merge (`release:premigrate --force`, owner-authorized via AskUserQuestion). Merged to main 2026-08-17 00:39 UTC; tagged + published; beta.202 demoted.

---

## 🚀 beta.204 SHIPPED (2026-08-17 EDT) — smoke-verified, three clean release reviews

Three holistic release reviews, no blocking findings. They verified the 17-package bump, traced the cross-PR seams, and confirmed the old `.getChannelHistory(` shape has no dangling call sites.

**✅ PROD MECHANISM CONFIRMED 2026-08-18 00:01Z — the eviction case dev could not produce.** First post-deploy window line, in a baselined channel: `channelId=1481138179917615144 inScopeCount=733 evicted=689 take=44 chunk=13 degraded=false`. 733 in scope, **689 actually evicted** — a genuinely over-cap channel. `chunk=13` ⇒ cap 50 (the real default, unlike dev's raised cap), and `take=44` sits INSIDE the quantized band `(cap−chunk, cap]` = (37,50] rather than pinned at the cap, which is the quantization working. `evicted + take = 733 = inScopeCount`, conservation exact. Zero `Failed to get channel conversation history` in the window.

**✅ PAYOFF MEASURED 2026-08-18 — the hysteresis paid, large.** Channel `1498247824662335608`, 12 pairs: offsets moved from a 14-char band at 27,451-27,465 to a 27,350-97,986 spread, 9 of 12 above 54,000. Channel `1481138179917615144`, 8 pairs: 6/8 at `H chat_log` spanning 32,326-101,954. **Two caveats recorded on TASK-651, not here**: `cache:prefix-diff` emits NO cached-token figure (verified via `--help`), so the plan's "compare tokens, not offsets" is not reproducible with that command — and #2129's constant S1 byte insertion cannot explain a 3x offset move, so the direction and magnitude stand. **The residual is TASK-651** (S1 participants churn, filed 01:57 BEFORE this read; I re-discovered it and filed a duplicate TASK-663, now archived and folded in — see the session block below).

**Dev smoke left three of six runtime PRs unexercised** (#2128 no demotion, #2130 no swap-shape footer, #2123 single-speaker), and the hysteresis BENEFIT needs eviction only prod reaches — hence the prod confirmation above.

**NEXT UNIT**: see the beta.205 session block at the end of this file. TASK-639's instrumentation SHIPPED as #2134.

**Release-process ledger (honest half)**: the release PR body claimed 72 files when the diff was 91 — accurate when `release:range` ran, stale after later commits, and caught by **two** reviewers rather than by me. That is the "re-derive a count whenever the thing it counts changes" rule, violated. CodeQL went red on a `replace(/<[^>]*>/g, '')` that `00-critical.md` explicitly bans, introduced in #2129 with the rule loaded in context and missed by per-PR CodeQL (fixed in #2132; lint-time enforcement filed as TASK-647). And three removal-at-ship misses (TASK-618, TASK-636, doc-77) all surfaced via release-time sweeps rather than at merge.

## ⚙️ 2026-08-18 session — reply-ping parked, catalog refresher + lint rule shipped

**✅ #2134 MERGED (`32f76fd0f`) — model-catalog refresher, TASK-639.** `ModelCatalogRefresher` on TTL/3 (not TTL/2: at a half-interval the retry lands ON the expiry instant), startup warm, stopped before the Redis disconnect. Runtime-confirmed in dev (`Refreshed model catalog from source modelCount=414`). **Prod ran degraded until beta.205**: `openrouter:models` was `EXISTS 0 / TTL -2`, verified with `pnpm ops inspect:redis-key` (#2136, TASK-652) and positive-controlled against dev first. **Process retraction that still stands**: the original "prod probe showed EXISTS 0" claim was made with a BROKEN instrument — `pnpm ops run` injects only `DATABASE_URL`, so `REDIS_URL` fell through to local `.env` and every probe read my laptop. Being accidentally right does not make an unverified claim verified. TASK-650 filed for owner-alert-on-repeated-failure.

**✅ #2135 MERGED (`35fdef952`) — TASK-647, `@tzurot/no-regex-tag-strip`.** A plugin rule, NOT `no-restricted-syntax` (seven `eslint.config.js` blocks set that key and an override replaces the array wholesale, so a cross-cutting ban there silently stops applying). Registered for test files too — the original violation was in a test. Six review rounds; the matcher was rewritten three times as each round found a spelling it missed, so it now **normalizes group syntax away first** and matches ONE shape rather than accumulating spellings. **The binding constraint is `<@!?\d+>`** — Discord's mention matcher, delimited identically — which is why the atom cannot be loosened. Compound-alternation and allow-list-class forms stay open by design, CodeQL as backstop.

**Session process ledger (honest half).** (1) I wrote "a full-repo lint confirmed zero violations" into a PR body when that run had **OOM-killed** — I had seen the OOM two steps earlier. Corrected before CI proved it wrong, which it then did. (2) `FULL_LINT_EXIT=$?` after a pipe read `tail`'s status, not eslint's — the `PIPESTATUS` trap, which is why the OOM looked like exit 0. (3) A verification grep counted its own **filename**: `grep -c "no-regex-tag-strip"` over eslint output matched the path `no-regex-tag-strip.ts`, so "1 hit" was a measurement artifact; re-ran anchored on `error.*@tzurot/`. (4) #2134's PR body still said "half the TTL" after the code moved to a third — the same re-derive-the-number miss as beta.204's file count, caught by the reviewer. The rules that would have caught all four already exist; the failures were in applying them to my own verification commands.

## ⚙️ 2026-08-17 session — beta.204 theme complete + cut (digest; full detail in git history of this file)

#2123/#2124 (hysteresis, 13 rounds) · #2128/#2129/#2130 merged; TASK-641 closed refuting its own premise (doc-17); Arm A retained with corrected header; dev smoke PASS (chunk=25 ⇒ the 100-clamp, not the default — config-dependent prediction miss); #2125→TASK-646, dependabot held. Ledger: the day's dominant failure was PROSE (header-as-status, unread doc pointer, untraced threading claim in #2128's body — reviewer caught all three; round 2 caught the same class one hop further).

## 📋 Open items (near-term)

- **Purge EXECUTED (owner-authorized 2026-08-16)**: 20 never-used accounts erased (cohort had grown 18→20 by run time), 0 characters touched, post-run preview confirms 0 eligible, userbase 228→208. Tombstones propagate to dev on next sync (the #2120-safe direction). Remaining: 1 reachable user awaits the warning DM — `retention:notify` NOT run (outward-facing; separate owner call).
- **S1 (beta.201, #2090 export-clear round-trip) — DEFERRED BY OWNER 2026-08-20 → run in the beta.205 release-kickoff smoke batch.** The full instruction, self-contained for when it's time: on dev, pick a character with at least one genuinely **empty** optional field (or blank one out, e.g. appearance/likes) → `/character export` → `/character import` the JSON back. PASS = import succeeds AND the field is still empty afterward; FAIL = import error, or the field comes back filled with an old value. Judge by any field EXCEPT `customFields` (TASK-590, known-lossy gateway-side, excluded on purpose). Attachment-bearing (needs file upload). This is the ONLY carried attachment smoke item.
- **Housekeeping**: `3f50da50.jsonl` (155MB, fully mined) is a disk-space deletion candidate — owner's explicit okay still needed.

## 🔧 DRAIN CAMPAIGN — resumes now (doc-77 shipped); batching is the method

Backlog composition, measured: ~321 open = 107 `ready` · 97 `observable` · 57 `dependent` · 45 `owner` — only ~1/3 agent-drainable; filing rate (not staleness, measured ~3%) drives growth. **Themed batches over per-task PRs** (4-5 closes per cycle vs 1). Pre-grounded: 199+200 together (bare-run HEALTH_TOOLS shape); 349 and 457 solo (457 has a global-flag design call); held for owner: 527 (copy), 540 (HTTP contract), 559 (pick), 531 (process design); TASK-599 = the batched owner-decision pass over `state:owner`. Query, do not browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (all one shape — a check that can't distinguish measured-fine from didn't-measure)**: publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green (check other probes for `2>/dev/null` in invoke paths).

**Open rule-shaped gap, not yet drafted**: five of #2097's last six defects entered _while fixing a previous finding_ — every countermeasure fires at authoring time, none at correction time. This session added data: #2119 round-2 (asymmetric fix) and #2105's round-4 regression are the same class. Wants a council pass (TASK-531 adjacent).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. Judge preview: A-vs-B 5/5/8, B-vs-B′ control 8/18 (the pre-registered under-power flag may trip — read the noise-floor line before trusting a PASS). Artifacts backed up (`voice-consistency-2026-08-04.tar.gz`). **Owner's leisure — do not re-surface as a recommendation.** Phase 2 (history extraction) + the #1317 cluster + TASK-165 stay gated behind it.
- **Opus trial record**: TASK-513 (owner decision surface) + TASK-487 (Sonnet-tier evidence ledger). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness spinoff; license recorded), doc-65 (private brain), doc-66 (message coalescing), doc-67 (tag-scoped sharing), doc-70/71/72 (tag mgmt/dashboard nav).
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (beta.191 threads; bullmq/ioredis v6 pending, dependabot-ignored until then) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528, key by personaId) fix shapes recorded.
- **Retention is calendar-only**: books as of 2026-08-09 — 221+ users, 5 in earlier grace (~08-26 expiries) + 2 warned (~09-08). Phase 4 autonomy parked BY DESIGN. Dev nag now silenced by #2120's prod gate.

## ⚙️ 2026-08-18 later — dependency drain + the Redis instrument

- **#2126 MERGED** (production deps: 8 patch/minor bumps — pg 8.22→8.23, langchain minors, astro/pglite/tsx patches). Held until beta.204 was on main so its risk did not ride the release; that condition cleared, rebased onto current develop, CI green, branch deleted + ref-verified. **#2127 was CLOSED BY DEPENDABOT ITSELF** after its rebase — "Looks like these dependencies are updatable in another way, so this is no longer needed" — with NO superseding PR. Expected to regenerate on the next scheduled run; if it has not reappeared, check rather than assume handled.
- **#2136 MERGED** (`f8bb0505e`, branch deleted + ref-verified; TASK-652 closed Done) — `inspect:redis-key` + a host fingerprint on every Redis resolve (TASK-652). The fingerprint lands in `getRailwayRedisUrl`, so all five consumers inherit it; **none printed a target before**, so the wrong-instance blindness was never specific to the ad-hoc probe that caused the bad claim. stderr, to keep `inspect:dlq --json` clean. Four review rounds, two of which found REAL defects in code I wrote this session: `size` labeled "bytes" but computed from `String.length` (UTF-16 code units), and a missing `catch` so an unreachable Redis produced a raw stack trace instead of a readable line. Both mutation-tested after fixing.
- **doc-78 — DM Context Isolation: DESIGN SETTLED + GROUNDED.** Enum `shareHistoryAcrossPersonalities: 'always'|'guilds-only'|'dms-only'|'never'`, default `always`; no migration (the leak is read-side, `ContextAssembler.ts:224`). Full design, the 7 registration sites, and the runtime evidence live in tracker `doc-78` — read it there rather than duplicating here.

## ⚙️ 2026-08-18 later still — workflow bump landed, reply-ping design corrected

- **#2138 MERGED TO MAIN** (`1856fa5be`) — claude-code-action 1.0.193 via a main cut, owner-approved. `release:finalize` resynced develop; **all 24 commits verified preserved**, main is an ancestor of develop, guard green on both. **TASK-646 closed** against its re-read acceptance. #2125 closed with the reason on the PR. #2133 rebased onto the realigned develop.
- **TASK-654 filed — `guard:workflow-sync` skips its own primary target.** `release:finalize`'s SHA realignment leaves any pre-release branch with its merge-base at the release point (on main), so the guard reads it as a main cut and reports success. Window: every pre-release branch after every release; dependabot PRs sit in it permanently. The SAME realignment caused a tracker id collision — two tools, one root cause: both infer state from git topology. (It recurred this session: a task filed from a feature branch got a duplicate 657, renumbered to 658.)
- **#2137 (dev deps, 20 updates) is RED and held → TASK-655.** `config-resolver` mutation score 85.97 vs floor 86.17. NOT a quality regression: the PR bumps Stryker 9.6.1 → **10.0.0**, and a major changes which mutants are generated, so the ruler moved rather than the code. That is the `mutation:update-baseline` case, which needs fresh local reports for every tracked package. Held so a baseline move does not ride a 19-package dep bump unreviewed, and because Stryker 10's breaking changes are unread. #2127 was closed by dependabot itself; #2137 is its replacement.

## ⚙️ 2026-08-18 latest — reply-ping ruled out, forward attribution built

- **THE REPLY-PING GATE IS DEAD, on evidence rather than effort.** #2133 CLOSED, TASK-649 ARCHIVED. Full-guild read-only scan: 240 channels, 7000 messages — replies to real users carry the referenced author in `mentions` **276/1318**, replies to character webhooks **0/521**. The ping DEFAULTS to on, so a working signal would show hundreds of hits among those 521; `mentions` is provably not broken there (2 carry a non-empty array from an @-mentioned human); and the union of top-level keys across all 1839 replies holds no field carrying a ping bit. Owner confirmed the last unknown: Discord DOES show the toggle for webhook replies, then discards it. **My failure**: round 1 read ONE field, found it empty, and generalized to "no signal exists" with no positive control — then closed the PR on it. Owner pushed back; the redo is what produced the numbers above. The instrument was fine; the reasoning was not. **Forward is the workaround and already worked** — `message_reference.type` distinguishes Forward from Reply and `PersonalityTriggerProcessor` skips reply+mention resolution for forwards; runtime-confirmed in #rotzot (forward → silence; five replies in the same window → five responses) and documented in `CHANNEL_ACTIVATION.md`.
- **#2141 OPEN — forwarded-message attribution** (TASK-656). Forwards reached the model as `<quote type="forward" from="Unknown">` with no author and no timestamp; Discord omits both from `message_snapshots` but `message_reference.message_id` recovers them. Owner chose the full build: new internal route `POST /conversation/forwarded-origin`, fire-and-forget backfill so nothing blocks job submission, `from_id` carrying the internal personality id. **Consequence by design**: attribution lands for SUBSEQUENT turns, not the one that created the forward.
- **TASK-657 → beta.205 (owner directive)**: characters into the `<participants>` roster. Two scope guards, both load-bearing — characterInfo ONLY, and EXCLUDE the currently-speaking character (it already gets its full card). The second makes the roster **response-scoped, not window-scoped**, so it must be threaded rather than cached per channel. This is what makes #2141's `from_id` actually resolve.
- **TASK-658 filed**: #2141 added a second writer of `message_metadata`, which makes `writeTriggerReferences`' read-modify-write unsafe for keys it does not own. Both comments now state the residual risk instead of claiming it closed. Not fixed here — needs a raw-capable client where the service deliberately holds a narrow one (fast-pool blast radius).

_HANDOFF READY (2026-08-18 late): **#2141 MERGED** (`dd5c92bf7` / `ba3bab1dc` / `e7fb73f28`, branch deleted + ref-verified, TASK-656 Done). Four review rounds, three of which found REAL defects in code written this session: a snowflake in `from_id` where every other producer emits an internal id; cross-channel forwards silently losing attribution because the personality lookup reused a reply-shaped, same-channel fetch; and a row-id divergence caught by its OWN test — the backfill re-read `message.createdAt` while the persist applies a `?? new Date()` fallback, so the ids differed and the write silently no-oped. No migration: the field lives in the existing JSONB column. **Dev auto-deploys from develop** — worth one forward in a non-activated channel to see attribution land on the FOLLOWING turn (by design: the backfill races its own job). MERGED this session: #2136 · #2126 · #2138→main · #2139 · #2140 · #2141. CLOSED: #2133 (reply-ping, ruled out on 0/521 vs a working control of 276/1318; TASK-649 archived with the numbers AND the query shape, so a re-check has a baseline if Discord ever changes). HELD: #2137 (Stryker 10 moved the ruler → TASK-655). **NEXT**: beta.205's theme is an OWNER PICK; **TASK-657** (characters into `<participants>`) is committed to it by owner directive and is what makes #2141's `from_id` actually resolve — note its roster is response-scoped, not window-scoped. Also live: **TASK-658** (the two-writer `message_metadata` clobber race, disclosed in both writers rather than falsely claimed closed), **TASK-659** (schema strictness at the write boundary only — the naive `.datetime()` fix would turn safe degradation into total-metadata loss), **doc-78** (design-settled AND grounded: 7 registration sites mapped, runtime evidence recorded), and beta.204's **post-deploy cached-TOKEN read** — one `pnpm ops cache:prefix-diff --env prod --channel 1498247824662335608` against the recorded baseline, comparing COUNTS/percentages not raw offsets. That read is the only evidence beta.204's headline change paid, and it gets less meaningful the further prod drifts. **Failure shape this session, third instance in one day: wrong-layer verification** — I checked Discord's payload and called a claim confabulated when the layer that mattered was the assembled prompt. Two candidate rules for the next mining pass: verify at the layer the claim is ABOUT, not the nearest layer holding data; and a negative probe is not a finding until the same instrument returns a positive on a known-present case._

## 🎭 2026-08-18 (evening) — beta.205 themed and under construction

**Theme: "Characters know who they're talking to."** Slice A alone is plumbing (the model already saw `from="Kai" role="character"`), so **TASK-660 slice B is the headline**. **CUT BOUNDARY DECIDED by owner 2026-08-19: TASK-660 + TASK-661, with TASK-651 as the SWING ITEM** — in if its grounding holds at `size:M`, carried to beta.206 if the wider blast radius (human roster, second migration) confirms. Cutting between 660 and 661 would ship the headline with a visible hole: 661 is slice A's binding gap in the quote path. Reasoning, riders and the re-estimated ~200-file projection are in `backlog/now.md` § Next Release — authoritative there, pointer here.

**✅ #2143 MERGED — TASK-657 slice A.** Characters render as `<character_participant id><name>` sharing ONE id space with personas; `role="character"` lines carry `from_id`. Membership is decided by `resolveSpeakerInfo` itself, so roster and chat log cannot disagree — which also excluded the currently-speaking character for free. Recency-capped at `MAX_ROSTER_CHARACTERS` (10). Measured: +104 tokens for the first sibling, +57 each after, in the cacheable S1 prefix. Four review rounds; the reviewer caught one real bug of mine (the cap's newest-first walk let the OLDEST occurrence win a renamed sibling's name) and one overclaim (the "humans-only roster is byte-identical" test used ONE human, so the duplicate-name branch could not fire either way).

**✅ #2144 MERGED — TASK-664**, self-vs-sibling by `personalityId` rather than name. A rename past the old name's prefix made a personality read its own history as a sibling, and since slice A that also gave it a roster entry pointing at itself. Threading is wide on purpose: every `resolveSpeakerInfo` caller that branches on assistant-vs-character, or roster and log disagree. `chatLogEnrichmentFor` is the one deliberate exclusion (reads only null-ness and the RAW role).

**⚠️ `Personality.name` HAS NO UNIQUE CONSTRAINT — only `slug` does.** Verified in `prisma/schema.prisma`. A review finding hinged on it and I nearly dismissed it as impossible; what misled me was a comment in `ConversationMessageMapper` calling it "the unique `name`", now corrected in #2144. **Anything deciding identity must key on `personalityId`.**

**✅ MERGED THIS SESSION — #2145 (TASK-658), #2146, #2147 (TASK-654).** 658: `message_metadata` now merges server-side (`COALESCE(col,'{}') || patch`), closing the lost-update race structurally; `RawCapableConversationHistoryClient` joins the capability split, gated by `typecheck:spec`. **Its premise was wrong and the correction shrank the fix** — the third writer had no caller, so it was deleted rather than converted ("present and wired" ≠ "live at runtime"). #2146: `cache:prefix-diff --show-divergence` prints the differing bytes, not just the section name — built to answer TASK-651 and it did so on first use. #2147: `guard:workflow-sync` asks GitHub for the real base; the topology test is **deleted, not refined** (the information is not in the graph — a stale develop-cut branch and a main-cut one are the same shape once `release:finalize` puts main's HEAD on develop's history). Three refusals guard the lookup: develop/main, undeterminable-branch, and non-OPEN PR state.

**🔬 TASK-651 CAUSE FOUND — `<guild_info>` flickers on a STABLE roster entry.** Same participant id, name and position; the block present one turn and absent the next. Neither cause the acceptance anticipated — nobody arrives, nobody is evicted. Roster membership comes from DB history while `guild_info` comes from the extended-context Discord fetch: two windows, one block, and the shorter one wins the cache miss (~78k chars of chat_log discarded). **Owner decision: PERSIST a last-known value**, with anti-rot as a requirement — write-through on every observation, because _expiry deletes while refresh replaces_; a TTL would reintroduce the same flicker on a slower clock.

**🧠 TASK-671 — memory recirculates stale facts, and the cheapest fix is one string.** Owner runtime report (characters asserting a quit habit). Facts have a full supersession chain; **episodes have none and structurally can't** — an episode isn't wrong, just old. Episodes DO render dated, so the model isn't blind to age — but **`MEMORY_ARCHIVE_INSTRUCTION` never mentions time at all** (owner's catch). That's step 0: one string, no migration, testable against the real case. `MemoryFact.sourceMemoryIds` already links facts to their source episodes for the structural half. **Retraction: I read one good retrieval as evidence doc-8 may be unneeded; that says nothing about consistency or staleness.** TASK-671 is a concrete argument on doc-8's FOR side.

**✅ #2148 MERGED — TASK-660 slice B PR 1 of 4**: `hashCharacterCard` (full 64-hex, case-PRESERVING, NFC, whitespace-collapsed; null/empty/blank are ONE absent state) + two additive nullable columns on `personalities`, **dev-migrated**. The checksum's field set is deliberately NOT in the module — it must equal the summarizer prompt's INPUT set, defined together in PR 2 so they cannot drift. A fully-empty card hashes to the sha-256 of the EMPTY STRING (pinned), so the caller must detect "nothing to summarize" by that value and skip the model call. Zero-width stripping DECLINED on merit (U+200D binds emoji sequences and joins letters in several scripts — stripping changes content). PR 2's prompt is drafted in scratchpad with the field set decided: IN name/displayName/characterInfo/traits/tone/age/appearance/likes/dislikes/goals; OUT conversationalExamples (verbatim first-person dialogue invites quoting — removing it beats prompting against it), errorMessage, customFields, birth\*. `characterInfo` is instruction-register for imported characters, so the prompt needs BOTH a data-not-instructions frame AND a third-person requirement (complying then produces visibly wrong output). **Local branches 24→5**; 19 dead `worktree-agent-*` deleted after `git cherry` proved every commit had an equivalent upstream (`git diff develop..b` is the WRONG instrument — symmetric, dominated by develop moving forward). Three parked branches kept, all now backed up remotely. **TASK-676/677/678 filed.**

**📋 Grounded before any code**: TASK-661 (resized S→M — the obvious resolution point is documented pure/sync, and the cheap mechanism `getWebhookPersonality` is UNGATED where the task's own warning is about the gate) · TASK-660 (two premises corrected: the checksum precedent conflates two unrelated functions, and the `definitionPublic` list is a _privacy_ boundary while the checksum answers _staleness_ — it wrongly includes `errorMessage` and omits `name`). **New**: TASK-666/667/668/669/670/671/672.

**🚢 beta.205 gate added**: TASK-670 (S1 tier audit) is a **pre-cut gate** by owner directive — measurement, not a build. The trap it exists to avoid: a stable S1 byte is _cached_ and free after turn one, while a V-tier byte is paid _every_ turn, so relocation only wins for content that genuinely cannot be stabilised at its source.

**Session ledger (honest half) — the dominant failure was PROSE, three PRs running.** Every review finding on #2145 rounds 1-4, #2146 round 3, and #2147 round 3 was a false statement I wrote, with **zero logic findings**. The sharpest: #2147's diff deleted the topology test and left a comment telling the reader the caller "falls back to branch topology". A memory entry already prescribes grepping the old claim's tokens when a premise changes; it did not fire three times, so **TASK-669 files the hook** — the trigger need not be "a premise changed" (unjudgeable) but "this diff removed a named identifier" (a pure diff shape). Separately: **three assertions I wrote could not fail**, all caught by mutation rather than by reading — a clamp fixture too short to discriminate, a `GITHUB_REF_NAME` test that passed with the feature deleted, and an alignment claim (`toContain` is blind to column position). Also: a positive control that silently passed because I edited the working tree while the guard compares HEAD; and a reviewer who correctly caught that my "narrowest scope" comment was true of the permission _type_ but not its blast radius.

**⚡ 2026-08-19 late — TASK-651 SHIPPED (#2152), z.ai reroute discovered (#2153 open).** #2152 merged (branch deleted + ref-verified): `user_guild_infos` keyed `(user_id, guild_id)` — by the HUMAN, not the persona, since one person can wear two personas in a roster and they must not disagree — write-through from ai-worker (bot-client is Prisma-free; its `guildMemberUpdate` half routes through a new internal endpoint), persisted fallback at MemoryRetriever's pick, byte-equality acceptance test, 7+2 canaries. Two corrections to the task's own grounding: the write sites it named were the wrong SERVICE, and `guildId` was already threaded. **The z.ai finding is the bigger one**: a live probe (`requested=glm-4.5-air → served=glm-4.7`) proves z.ai silently reroutes, so the owner's usage chart was RIGHT and the carve-out's "cheapest 1x multiplier" premise is FALSE — guests bill at 4.7's Input 4.6 / Output 16, and the input budget was capped at 64,000 when 100,000 was available. #2153 renames the constant — and after an OpenRouter probe showed `z-ai/glm-4.5-air` still served there as a distinct **PAID** model (131_072 ctx, no `:free` variant), the retired id is **REMOVED, not aliased** (owner directive, no backward compat): an alias would have granted guests a billable model on the strength of z.ai's reroute alone, and dropping the catalog entry sends the id to OpenRouter, the only place it still resolves to Air. It also fixes a REAL vision mis-route: `detectVisionProvider` matched the bare `z-ai/` prefix, sending OpenRouter-hosted `z-ai/<model>:free` to the coding-plan endpoint — reachable via `selectVisionModel`'s priority-2 branch, surfaced by the owner setting `z-ai/glm-5.2:free` as the prod free default. **Session ledger (honest half): three times I stated a mechanism's behaviour from its NAME or CALL SITE instead of its body** — the floor-hop "spend" concern (the floor is free for guests / user-billed otherwise), the weighted-budget concern (the plan METER bounds cost, not the request count; TASK-686 filed then archived on merit), and the context clamp (I told the owner "~3K not 72K"; `clampContextWindow` applies `computeContextCap`, so their 64k→100k was right). Each took under five minutes to check. **Filed**: TASK-685 (post-deploy S1 re-measure) · TASK-687 (z.ai served-model, ANSWERED) · TASK-688 (guildMemberUpdate fan-out). **Owner decisions open**: TASK-645 (latency-vs-rescue on the floor-hop gate) and TASK-670's prod channel id — the latter is the pre-cut gate for beta.205.

**🌙 2026-08-20 overnight — three units shipped; the prod dead-end fixed same-night.** **#2154 MERGED** (TASK-690 + TASK-592 Done): `/chat`+`/random` input 2000→4000 (Nitro parity) with the echo chunked as history rows (last chunk anchors; no wire/worker change); `splitMessage` gained `decorateForcedSplits` covering BOTH synthetic-markup mechanisms ('...' force-split marker AND rebalance fences) so persisted user text carries nothing they didn't type. 4 review rounds; TASK-695 (UUID-seed collision, speculative) + TASK-696 (orphan-chunk cleanup) filed from findings. **#2155 MERGED** (TASK-694 Done): the owner-reported prod bug — guests terminal-erroring when the free default rate-limits — fixed by promoting the floor to hop 1 on ANY null tiered target, plus the guest non-OpenRouter credential root fix (round-2 review's Medium, confirmed by read). **Runtime-UNVERIFIED**: watch `No hop-1 retarget available — promoting the floor to the hop-1 target` on the next prod rate-limit window (now.md entry carries this). TASK-697 = the round-5 edge-hardening follow-up, deferred at the ~6-round cap. **#2156 MERGED** (TASK-655 Done): Stryker 10 + baseline refresh (config-resolver 87.17→86.89, other four UP; measured v9-vs-v10 diff: 56 new mutants, zero flips — the grounding note's empty-expression story was WRONG, corrected; 4 new gaps killed with real cache-behavior tests); TASK-693 = the ~70-survivor seam campaign; `@dependabot rebase` posted on #2137. Also: TASK-692 filed (inspect view for vision descriptions); `/describe` ruled out against doc-11's tool-only design. **Morning smoke additions**: one >2000-char `/chat` alongside the attachment batch. **Ledger (honest half)**: three worker-caught wrong premises in MY specs (an existing constant, a nonexistent test pin, an exactness the splitter never had); my one inline rider (`.catch`) was the round's untested finding; a tracker id collision (693/694 reused) from filing on a branch forked before they landed — renumbered 695/696; and my own `head -30` cut the incident window out of a log grep mid-diagnosis.

**☀️ 2026-08-20 morning — chunked echo RUNTIME-CONFIRMED; three owner decisions recorded.** The >2000-char `/chat` smoke PASSED (10:35 EDT, #rotzot): 2 prefixed echo chunks, clean sentence-boundary seam (no `...`), reply cited details from BOTH halves; the debug log shows exact parity — chunk 1 as a normal `from_id`-bearing history row (prefix stripped), chunk 2 as the prefixed current turn. #2154's 50035 edge fix is thereby runtime-confirmed too. **Owner decisions recorded on tasks**: TASK-645 → option (b) narrow gate widening (buildable, beta.205 rider) · TASK-689 → accept-for-now, task re-scoped to the eventual GuildMemberRemove handler (state:ready, low) · TASK-662 → widen the duplicate-name note to pure-human rosters (buildable). **TASK-670 unblocked differently than expected**: the audit needs a channel with character traffic in the last 24h (diagnostic rows live 24h) — owner is picking an active channel; the two previously-recorded ids may be quiet. **Free-default plan**: owner reverted guests to `glm-4.5-air` (their toggle, confirmed) and will point it at `glm-4.7` when beta.205 reaches prod — deploy note added to now.md (post-#2153, `glm-4.5-air` resolves via OpenRouter as PAID, so the free-guard would silently give guests `openrouter/free`).

**🔬 2026-08-20 midday — TASK-670 AUDIT EXECUTED; the beta.205 pre-cut gate is SATISFIED.** Owner supplied prod channel 498827782219104266; 5 consecutive pairs read via `cache:prefix-diff --show-divergence`. Verdict: **no S1 section belongs in V** — system_identity/identity_constraints/protocol/location are (a) stable, participants is (b) with stabilizers already in the train, datetime already renders in V; no reordering proposed. **Bigger finding: a measurement artifact** — 4/5 pairs 'cut at S1' were CROSS-STREAM comparisons (two interleaved personalities, A/B/A/B roster flips, ~160K/~106K alternating totals); a cross-stream pair is not a real cache miss, so mixed reads overstate S1 churn → **TASK-698** filed (group pairs by personalityId), caution added to TASK-685, and the recorded 2/5 + 2/8 baselines are suspect. The one within-stream pair cut at chat_log 76% — healthy — where vision heal-on-read had replaced an image placeholder with its description (one-time per image, inherent). **Attachment smoke batch clarified**: it is ONE item — the beta.201 S1 export-clear round-trip (line above in this file). **Release state: both cut conditions are met; TASK-645 is the last decided-but-unbuilt rider.**

**⛔ Attachment smoke is blocked until the morning of 2026-08-20.** The owner's Discord restriction blocks file uploads through **slash commands** too, not just `/chat` — `/character import` was tested and failed in every server AND in DMs with the bot. Any attachment-bearing smoke item is un-executable until the restriction expires; non-attachment items are unaffected and can be batched now.
