# Current

> **Version**: v3.0.0-beta.216 — "drain batches 2–3: hooks residue closed, the hysteresis test's cost, `/inspect` finish reason inert" (6 PRs / 2 runtime / 46 range files, no migrations, merged 2026-09-04 ~18:40Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.215 — "ceiling extractions + review residue + the first drain batch + the vision body-timing diagnostic" (13 PRs / 4 runtime, 2026-09-04 ~11:40Z).

---

## 🌙 2026-09-04 (night) — TASK-888 half (2) merged, decided-work drain batches A1/A2 in flight (Fable-driven, nested dispatch)

**Merged: #2333** (TASK-888 Done — `06-backlog.md` § The process-residue default plus the sweep of every hot surface that stated the old universal file-it rule; six claude-review rounds: round 1's "entirely under" scope clause was wrong and round 2 replaced it with substance-under-the-four-directories with tracker/backlog/docs riding along and any runtime or test-infra file disqualifying; rounds 2–5 each found one more copy of the old routing because my sweep grep was three terms, so the final sweep grepped the filing obligation itself across `BACKLOG.md`, `.claude/rules`, `.claude/skills`; round 5 was the owner-set cap and round 6's wording nits were declined in the PR's own Residue section). **Open: #2334** (drain A1, bot-client: `followUpBrowsePageFailure` at five pagination catches, the scoped-key embed with the owner-chosen `Details` field, the ↕️ sort glyph; round 2 found the `!result.ok` silent shape at channel/persona, which my comment-text enumeration could not see — dispatched as a class sweep with seam tests; TASK-890 filed for the character-view sibling the orchestrator found and left alone) · **#2335** (drain A2, ai-worker: TASK-662 widened — the predicate already compared humans, only the conjunct dropped; TASK-398 stated as a decision in the module doc). Specs for 730 / 689 / 206 staged under `docs/local/dispatch/`. Net today so far: 1 filed (TASK-890), 1 closed (TASK-888); 309 · 344 · 294 · 662 · 398 close on merge.

**Honest ledger.** Two sample-based enumerations missed a sibling in one day: the rule PR's prose sweep (four rounds of one-more-copy) and the A1 spec's comment-grep (the `!result.ok` shape). Both fixes were the same: enumerate the obligation or the failure shape, not the text I happened to write. The A1 worker used `sed -i` for a canary against instruction, self-caught; the orchestrator discarded its canaries and re-ran them with hash proof. A1's inner worker also missed the inbound prose sweep once (three test names still said "silent"); the orchestrator caught it on its diff read. The Monitor tool was classifier-blocked once mid-session; a background `gh:ci-gate` served the same purpose. The pr-body-ref gate correctly refused a "filed as TASK-890" claim while the task existed only on the branch, and its claim scan caught a "never fired" I could not cite.

## 🌙 2026-09-04 (evening) — TASK-888 half (1): the priority-low rule-out pass, cleared and landed (Fable-driven; nine Sonnet readers + one Opus merit pass + one Opus docs writer)

**Done: TASK-888 half (1).** All 285 open priority-low tasks read whole, each premise re-verified against the code with a cited grep or log query (nine area-group readers), then a merit pass sorted the 267 survivors by whether they earn a pool slot. Digest published as a private artifact; the owner cleared it in one sitting: four block rulings (archive the 11 verified exits; archive the 14 admission-bar third-instance triggers; supersede 28 rows into doc-18/21/3/8/4; create 8 idea docs + widen doc-74 and supersede 72 more) and 13 single decisions (C1–C13, all on the recommendation; C5 needed a dev/prod query first — both directive names live in the shared "Default" system prompt row, 205 of 208 personalities, no admin write path). Landed on develop in six commits: 155 keeps with dated reason+evidence comments, 20 retargets/narrowings, 13 rulings, 13 owner-decided-but-unbuilt rows promoted to medium for two drain batches; 130 archived, each with its reason on the file and in the commit body. **Pool 285 → 140 low, 432 → 302 open.** Re-measured growth W32–W35: about +36/week net, same order as July — the cut buys about a month; half (2) is the lever. Spend: ~2.1M subagent tokens for the read, ~0.3M for the merit pass, ~0.3M for the docs.

**Honest ledger.** The merit pass claimed doc-3/8/4 "already list" their members; a content-token check showed only doc-18/21 do, and the digest said so before the owner saw it. Its "inflow fell four times" line was wrong too — my own re-measure at close put net growth at the July order, corrected on the board. Spec arithmetic: I told the docs writer 101 superseded ids when the doc union is 102 (doc-18 also points at the archived duplicate TASK-151) and listed "6 existing docs" while naming seven — both caught by the writer. The tracker CLI is hook-blocked from a worktree, so the writer hand-wrote the eight doc files in the CLI's format and proved the search index finds them; spec conflict, resolved by the worker, not by me. Three commitlint trips on one header (103 → 107 → 98 chars: I lengthened it once). Two staging-count checks were wrong because git pairs the archive moves as renames. TASK-2's area relabel is moot (superseded). Shape 15's state:owner relabels were skipped deliberately: the backlog gate demands question/recommendation lines on state:owner rows, and those six carry recorded rulings with nothing pending.

## 📋 Open items (near-term)

- **beta.217 is the shrink release** (🚢 in `backlog/now.md`): **TASK-888 DONE** 2026-09-04 (both halves; pool 285 → 140 low; the admission default merged in #2333). In flight: drain A1 #2334 (round-2 fix dispatched) and A2 #2335 (CI). Next units in order: TASK-730, 689, 206 (specs staged), then infra/tooling 273 · 99 · 183 · 62 · 57, then drain batches 4–5 (grounded candidates on the board). Cut when #2334 and #2335 merge. TASK-880 stays a prod watch. The Opus effort sweep runs on whichever unit the next Opus window takes — mine it with the effort-tag lens.
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 (after a surprise weekly reset) — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped** — now 188 users; 2 in grace (~09-08 expiries are the next event), 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. Phase 4 autonomy parked BY DESIGN.
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
