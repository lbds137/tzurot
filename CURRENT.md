# Current

> **Version**: v3.0.0-beta.147 (released 2026-07-04) — **no migrations**. Headliners: **free-tier vision fix** (#1485 — model-agnostic canonical description cache with tier promotion; hydrator dead-path fixed; filename-bearing failure placeholder), **edit-modal caps** (#1484 — Tone 255→1000, slug single-sourced at 50 with truncate+hash), **slug suffix preservation + cap-message drift** (#1486), **vision log-noise sweep** (#1487 — 11 demotions + structured failure WARN), **reasoning-budget warn** (#1467), **the barrel-kill epic** (#1472–#1483 — 955-export root barrel gone, deep imports + guards, knip audit, tooling lint gate), **CI matrix split** (#1471). 4 release reviews, zero blocking findings. _Prior: v3.0.0-beta.146 (2026-07-03) — provider-failure fixes, Stryker pilot arc, weekly audit cron (11 PRs)._

---

## Unreleased on Develop

- **#1490 fix(ai-worker): deleted memories no longer retrievable in RAG** — visibility guard in the similarity query + sibling expansion (the prod trust bug from the boulder-#3 sweep); rider: memory edits re-embed atomically (content + vector in one transaction, NULL on failure). 4 review rounds, review-hardened (atomicity restructure was a reviewer catch; the hard chunking assertion exposed a silently-skipping test).
- **#1491 fix(bot-client): settings-dashboard closure leak removed** — the per-session handler Map was write-only dead weight (zero readers); Map + `getUpdateHandler` + dead params deleted, −184 net lines. Unblocks design-system D18 (Close-button removal).

## Next Session Goal

**Boulder #1 DONE 2026-07-04, BOTH parts ACCEPTED**: (1) `docs/proposals/backlog/ux-design-system-spec.md` — the normative design system (tokens incl. entity-emoji/badge registries with collision rule, components incl. modal form toolkit + `sendChunkedReply` expand + pending/empty states, verb/option grammar + IA moves, discoverability incl. picker hiding + context-menu Inspect, `/inspect` + owner-surface dispositions; full-trio council GLM 5.2 · Kimi K2.7 · Qwen 3.7 Max; all 21 decisions adopted). (2) `platform-portable-ux-design.md` — the machinery (catalog + outcome-honesty classifier, builders, enforcement), phase-mapped in spec §9. Part 2 exists because the owner caught part 1 covering machinery while missing the actual UX concerns (naming, discoverability, ergonomics). Settings-dashboard closure leak found during the Close-button teardown investigation → Quick Win. 2025-12 SLASH_COMMAND_ARCHITECTURE proposal triaged + deleted. **Boulder #2 DONE 2026-07-05, ACCEPTED**: `docs/proposals/backlog/prompt-assembly-architecture.md` — stability tiers (S0/S1/H/V), `<chat_log>` → real messages (multi-party mapping: assistant = own persona only; attributed user-role for everyone else; no merging — Anthropic auto-combines), volatile tail inside the user message with pinned internal-recall framing, eviction-to-75% hysteresis + guardrails, verified per-provider cache matrix (first-party docs; OpenRouter cache_control pass-through documented; **Qwen-first markers — owner constraint: do not assume Anthropic, not in active use**), o-series rewrite DELETED after fact-check (o-series deprecated; `system` accepted API-wide — owner's staleness challenge triggered the verification pass that caught this), LangGraph adoption gate passed by construction. 4-agent grounding + trio council + fact verification. **Boulder #3 DONE 2026-07-05, ACCEPTED**: `docs/proposals/backlog/memory-architecture.md` — verdict: **evolve in-house on pgvector with named paradigm imports** (Hindsight runner-up behind operationalized triggers; scope-reduction-first fallback). Typed memories (episode/fact/reflection/canon, verbatim texture never deleted), owner's social scoping matrix designed for real (place/community/canon-group pools; community = strict per-user opt-in, derived-lore-not-verbatim), async extraction (turn-count batched, fixed cheap model, fail-to-skip), relationship blocks (256-tok cap, scene-active only), consolidation jobs, RRF hybrid retrieval, **eval harness as the load-bearing gate** (council's unanimous #1 catch), cost guardrails + tripwires. Phases 0+1a+2 = minimum-viable bet; 3–6 evidence-gated. Grounding: OpenMemory rewrite dissection + 2026 landscape survey + current-impl map + owner's 9 curated links + owner's scoping model. **Prod bug surfaced en route** (deleted memories still retrievable — no visibility filter in RAG SQL; filed on the board, fix fast-tracked). Memory-system-overhaul theme rewritten to implement the artifact (its OpenMemory-waypoint plan was dead twice over: upstream retired the architecture; adjudication rejects adoption). **Boulder #4 DONE 2026-07-05, ACCEPTED — and with it the ENTIRE BOULDER AGENDA, two days early**: `docs/proposals/backlog/agentic-scaffolding.md` — hand-rolled tool loop on @langchain/core vocabulary (adjudicated over createAgent via the owner-directed LangChain deep-dive; decisive: invocation-hardening reuse + quality-retries-must-never-re-bill-tools; re-open triggers named); provider seam confirmed thin (3 shims, no abstraction); v1 tools recall_memories → web_search → generate_image; council caught a wire-contract bug in the draft (final-turn protocol now fixes it); all calls unanimous/synthesized, owner-confirmed. **Five accepted artifacts now govern the next several quarters of architecture** (UX system ×2, prompt-assembly, memory, agentic). **Prod-bug sweep DONE 2026-07-05 (#1490 + #1491 merged — see Unreleased).** **Next: config-cascade design session** (user-sequenced 2026-07-05: "production bugs then config cascade design"; PROMOTED on the board) — resolver priority, off-vs-inherit sentinels, profiles-vs-presets, **+ the new required dimension: server/community as a config tier** (per-server settings don't exist today; user directive on the board entry). Anchor: preset-cascade-standardization theme; 5+ parked items defer to it. Same boulder cadence: ground → draft → trio council → owner pass → land. Opus queue unchanged (contract suite doubly load-bearing — agentic Phase 0 prerequisite). Boulder-pass retro candidate: the artifact-per-boulder + trio-council + fact-verification process worked — consider codifying in a skill if design sessions recur.

**Mechanical work queue (Opus-suitable — user directive 2026-07-03: run these on Opus between/after Fable design sessions; every item is build-sized with design decisions already written down):**

1. **Stryker per-package expansion** — recipe in the deterministic-test-quality theme; per-package bounded units (~1–2h each), order: conversation-history → identity → cache-invalidation → clients; services need the viability measurement first.
2. **Job-payload contract suite** (knockout pull #1, agentic prerequisite) — spec is theme candidate 2; every context shape → job-chain → worker consumption, consider fast-check.
3. **CPD campaign 1** (`LlmConfigService` ↔ `TtsConfigService`) — council pass first per the theme (council works fine from Opus), then mechanical extraction under the 2-callback ceiling.
4. **Database-performance-audit Phase 1** (prevention rule PR) — cheap, marked NEXT in its theme.
5. **shapes-inc fetcher hardening** — 6 small well-specified items, untouched.
6. **Small tooling from follow-ups**: `ops find <concept>` (xray+JSDoc+filename search), utility-table symbol guard, backlog-lint outbound-link extension, BYOK/voice-engine theme-archaeology distillation.
7. **Follow-ups table sweep** — knock out 5–10 of the oldest rows (aging escalates; `pnpm ops backlog` surfaces them).
8. **LLM legacy-column Phase A DROP** — mechanical but destructive-migration-bearing (`release:premigrate --allow-destructive` at its release); save for a focused moment, not a filler slot.

**Fable-only (design):** the boulder agenda in `backlog/now.md` — design system → prompt-assembly → memory → agentic, each with council pass + written adjudication.

Weekly audit cron: first ORGANIC scheduled run completed/success 2026-07-04 10:45 UTC (cron drift from 09:00 nominal is normal) — the trigger is proven end-to-end; no further passive checks needed.

## Last Session — the pre-handoff process refit + CI split + boulder sweep (2026-07-03)

**The Fable→Opus handoff-prep marathon** (Fable leaves subscription plans 2026-07-07; capacity hiatus, expected to return). Mined six weeks of session transcripts (~2,770 user messages from 700MB of JSONL) with 6 parallel reader agents + memory-store audit + docs inventory → synthesis (all archived at `~/.claude/projects/-home-deck-Projects-tzurot/mined-corpus/`). Executed the full refit: **#1468** rules/skills operationalization (negative-existence protocol with xray at the decision point, completion-claim gate, promise ledger, human-verification template, merge-approval codification, archaeology diet) — 8 review rounds, 7 real catches; **memory surgery** 56 → 26 files (atomic promote-and-delete now law); **docs purge #1469** (−7,925 lines, 14 scout-verified deletions, postmortems 650→42 lines outcome-shaped, 13 orphans rescued); **log cleanup** (~686MB freed, 2.8MB permanent corpus); **#1470** anti-drift tooling (lines:check ratchet incl. pre-push docs path, guard:gate-parity closing the 9-check local-vs-CI gap + backlog:lint added to CI, ops-health security/margins/orphans sections, session-URL-secrecy rule + commit-msg hook after scrubbing 15 PR bodies on the public repo); **#1471** CI matrix split (test job p50 5.8min → ~3.5min floor, calibration held ±seconds across 3 runs, tier-consistent names: unit-tests (cell) / component-integration-tests / voice-engine-tests; tracked ruleset JSONs de-deadlocked). **Boulder sweep** (agent over all 27 themes + ideas + follow-ups): shortlist confirmed + prompt-assembly architecture promoted as the missed timing-critical peer (shared message-assembly surface with memory + agentic — sequenced BEFORE them); config-cascade semantics parked below the fold; 29 org defects fixed same-day (dead links, stale scrubs, model-config-overhaul rewritten to residuals, barrel-kill promoted to a real theme, follow-ups table repaired, observability-philosophy conflict reconciled). Wish-list capture throughout: message-action affordances (edit/regen/delete/ping + history-memory invariant), lore books, web tools + deep-research agent (user-agent policy on record), quota fairness (owner-protection design in the piggyback proposal), multi-personality v1 slots (activation order, max 5, compact-on-deactivate), provider-caching input-dominance argument. Live lessons: the merge gate caught a grep-skimmed review read; verify-reviewer-claims overturned a 🔴 turbo-filter claim with runtime evidence; commitlint + the new session-URL hook both fired correctly on their author.

## Last Session — beta.146 knockout + finishing push (2026-07-02 → 07-03)

**11 PRs merged (#1456–#1466), 3 themes CLOSED, 2 prod bugs fixed same-day.** The z.ai "routing bug" was diagnosed as fallback-visibility (one seam, two symptoms) → both-fail footer chain #1456 + summary-unwrap #1460. The Stryker pilot arc shipped (config-resolver only — per-package expansion remains open in the theme): pilot #1459 (60.71% score against green line coverage — premise validated), 23 gap-closing tests #1461 (→77.74%, logic classes 97.2%), logger-ignorer ratchet #1463 (baseline-and-hold 87.81, full audit-class ceremony, parallel `mutation-tests` CI job ~3min). The finish_reason-"error" prod bug (OpenRouter provider failure inside HTTP 200 → 1-char reply delivered + LTM poisoned) went screenshot→investigation→fix→merged in ~90min (#1462; stable thrown message for classification safety, provider detail preserved via `response_metadata.openrouter.providerError`). Then the **finishing push** (user: "I definitely like finishing stuff" → memory `finish-partial-themes-first`): human-users-only invariant #1464 (the prior bot-block was structurally UNREACHABLE — flag never passed; now compile-enforced from `GatewayUser.isBot` through `X-User-Is-Bot` to `requireUserAuth`), ops logs incident-dig flags #1465 (pull-and-grep-locally, NOT the unreliable server DSL; review caught a real CAC numeric auto-coercion silent-drop on all-digit IDs — empirically confirmed before fixing), ops:health aggregator + weekly cron #1466 (Layers 5–6; maiden run surfaced 2 perma-red roster candidates → excluded with tuning rows, per "false positives are a death spiral"). Review-cycle note: #1465/#1466 each ran 4–5 converging rounds with genuinely real catches (CAC coercion, verdict-header truncation, roster-rot guard, diagnostic fall-through); the round-cap ASK fired twice — user chose fix-then-merge both times. Also: 402 max_tokens auto-reduction evaluated + DECLINED (input tokens dominate this bot's spend); first-ever claude-review run that completed without posting (re-run posted normally — watch for recurrence).
