# Current

> **Version**: v3.0.0-beta.190 (cut 2026-08-02) — **the caching release.** 17 PRs (#1892–#1908); the centerpiece is the prompt-caching epic Phase 0+1: S0/S1/H cacheable system prefix, all volatile context in the user message (#1907, closed TASK-392's ~1,900-token double-send), anti-cache deletions (#1903), cache telemetry in `/inspect` + logs (#1904), `cache:prefix-diff` (#1906), absolute chat_log timestamps (#1908). Also the memory-search budget pair (#1893/#1902), stickers on referenced messages (#1895), the backtick-run fix (#1892). No migrations. No advisories. Smoke F passed pre-cut on dev: **67% cached tokens on turn 2** (req `fc288277`).

---

## ⏭️ NEXT SESSION STARTS HERE

**beta.190 is CUT and released (2026-08-02).** The caching epic Phase 0+1 is live in prod; the epic's proof already landed pre-cut on dev (67% cached tokens on a same-persona turn 2, req `fc288277`). The Watches below are now armed on prod traffic — cache-hit rate, memory-search budget, roleplay quality.

**The voice-consistency harness is BUILT (2026-08-02: PR #1910 miner + PR #1911 arms/judge/gate, both merged after 4+3 review rounds). NEXT: the operational gate RUN** — owner-in-loop, ~1 sitting:

1. `pnpm ops prompt:mine-voice-probes --env dev --owner <your discordId>` → **veto/approve the printed persona list** (auto-picks 4 most-active from YOUR conversations only — privacy scope is owner-decision, code-enforced; ≥1 JSON + ≥1 legacy protocol forced). Pre-restructure mining window closes **~Sep 1** (30d retention).
2. Persona-model census from the picked list → confirm the judge family is disjoint (default `anthropic/claude-sonnet-4.5` via OpenRouter; override `VOICE_EVAL_JUDGE_MODEL`).
3. `VOICE_EVAL_DRY_RUN=1 pnpm eval:voice-replay` → cost sanity (~1.6–1.9M input tokens ≈ $4–8 + ~$3 judge). Env comes from Railway (owner: "you can always grab them from railway"): `EVAL_MEMORY_DATABASE_URL` = dev pgvector `DATABASE_PUBLIC_URL`, `OPENROUTER_API_KEY`/`ZAI_CODING_API_KEY` from the dev ai-worker service variables.
4. `pnpm eval:voice-replay` → `pnpm eval:voice-judge` → **owner blind-reviews `reports/voice-consistency/judgment-sheet.md`** (~10–15 pairs, check ONE verdict box per pair + HARD BREAK when seen) → `pnpm eval:voice-verdict` → verdict + numbers recorded in `active-epic.md`. FAIL path: `VOICE_EVAL_ARMS=A,C` re-run → gate `VOICE_EVAL_GATE_KIND=CA` (the OUTPUT_CONSTRAINTS-to-tail remedy).
   _Carried runner nits (fold into any runner touch-up, not worth a CI cycle alone): `afterAll` disconnect guard when PrismaClient construction throws; retry-once wastes a cycle on deterministic parse failures._

Phase 2 (history extraction) starts only after the gate passes. Fill-in units: the drain campaign's same-origin clusters (`#1317` 159–162, `#1323` 167–169, `#1321` 163–165, `#1119` 131–133, `#1035` 100–102) and **TASK-403** (dedicated `/inspect` Cache view, low). _TASK-402 shipped 2026-08-03 (#1917: three-token nag lines, markdown-escaped, fail-open schema)._

**Shipped 2026-08-03 (all merged to develop)**: **#1915** TASK-405 — `/preset default set|clear` + `/voice tts default` (breaking rename; Discord's no-nested-groups forced the flat voice shape, owner-approved). **Release-notes obligation for the next cut**: Breaking Changes must list both renames AND state explicitly that running `/voice tts default` with no option **clears** your default (reviewer-flagged surprise risk). `preset/global/set-default.ts` → `global/default.ts` filename fix rode along; BYOK doc drift filed as TASK-408. **#1916** `.claude/agents/opus-implementer.md` (orchestrator-mode spawn contract). **#1917** TASK-402 (nag usernames). **#1918** TASK-404 (`ops logs --since` grepped a time format Railway never renders; TASK-409 filed for the same-class colorize miss). **#1920** TASK-100 (convertMessage extraction, both suppressions retired). **#1921** + deps: dependabot #1914/#1912 merged (TASK-72 closed); **#1913 was RED on bullmq 6 — a breaking major** (`repeat` gone from JobsOptions; Job Schedulers API) → dependabot-ignored via #1921, migration filed as **TASK-410** (must re-verify #1647's 5.x-grounded lock/stall tuning), group recreate requested. _Sequencing note: the #1317 drain cluster (159–162) AND TASK-165 are prompt-assembly content — gated behind the voice-consistency run so arm B stays beta.190's shape._

**TASK-387 is now actionable**: it was deliberately sequenced after TASK-385 (same shared-state seam), and #1894 shipped 385's front-door restructure in this release — re-read 387 against the new pipeline shape before planning it; the restructure may have changed or absorbed its answer.

**Owner-gated, not agent-callable**: **TASK-371** (reasoning chunk cap → 10) is a user-visible-latency call. **TASK-132 + TASK-164** rule-outs are approved on technical merit (councilled 3/3) but not executed; TASK-164's tripwire (`name-match fallback`, shipped #1898) now feeds the decision with prod volume data.

**Post-release watch carried from beta.189's review**: every generation renders + tokenizes each history entry instead of reading a cached count (#1885; 50–120 ms/request measured) — worth a glance at prod generation latency under beta.190's traffic too.

**Owner decisions locked** (still governing): no asset table for attachments — council split 2-1 for one, Gemini tiebroke for Kimi's history-row design · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY`, not any user-facing `maxAge` · direct replies always re-vision regardless of age · extended context re-visions only within retention · an undescribed old image renders a content-free presence note, never a filename stub · extended-context quota exemption ships AFTER persistence · `maxImages` is a spend cap persisted images do not consume, with no second render lever.

**2026-07-27 was retention Phase 3's runtime completion, all owner-delegated**: 45 users purged (33 bystanders incl. the `'unknown'` fossil + 12 notify-bounces), beta.181 released (#1818 BullMQ jobId fix — caught by the dev re-route smoke BEFORE prod tripped it), first prod notify run (21 → 9 in grace, expiry ~2026-08-26; 2 warned users reactivated within the hour). Final books: 227 users, 0 purge-eligible, 7 in grace. **Everything retention is now calendar**: grace expiries ~Aug 26 (daily nag surfaces them); Phase 4 parked. **First rolling-cohort run 2026-08-02 (owner-approved, manual-approval-first working as designed)**: the daily nag surfaced 10 new bystanders (aged past 180d since the July run) → all 10 purged (231 → 221 users, zero character impact, 4.3% vs the 25% ceiling). The 3 reachable-inactive users were sent the warning DM — **all 3 bounced** (batch tally: 0 warned, 3 bounced, 0 failed-without-stamp), were reclassified unreachable by the stamp, and were purged same-night on owner call (July's bounce-purge precedent). **Books after the run: 218 users, 0 purge-eligible, 0 awaiting DM, 5 in grace (~Aug 26)** — everything retention is calendar again. **system-model Phase 0 COMPLETE** (map interview + accuracy review applied; scheduled redraw dropped by owner call — informal sketch-check at next design conversation; freshness = epic-close re-touch rule via #1814). Idle-time ride-alongs whenever: drift audit (Quick Win), GLM generic-unwrapper (now.md Untriaged — ride-along, never a dedicated cut).

**The backlog substrate** → design, councils, owner ratification, and trial record all live in **[`docs/proposals/backlog/backlog-substrate.md`](docs/proposals/backlog/backlog-substrate.md)**. All four steps shipped (#1822 import · #1823 flip · labeling pass · themes/ideas→docs). **The whim constraint still governs the drain campaign**: the owner's priority-jumping is the point — make the haystack cheap to jump around in; acceptance test is "here are the 20 items you could close this week."

## 🧊 Build-complete, calendar-only — Automated Inactivity Retention & Purge (operational record; the active epic is caching → `backlog/active-epic.md`)

Design ACCEPTED 2026-07-23 (council trio + 6 owner calls) → [`inactivity-retention-purge-phase2.md`](docs/proposals/backlog/inactivity-retention-purge-phase2.md).

| Slice                                                                                                                                                                                                                                                                                                                  | State                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| Phase 1 (1a–1d) tracking substrate                                                                                                                                                                                                                                                                                     | ✅ released beta.175; prod backfilled 270 users |
| Phase 1.5 CH sync unification (#1778)                                                                                                                                                                                                                                                                                  | ✅ merged                                       |
| PR-A schema (#1779) — `discord_account_gone_at`, `retention_exempt`, `original_owner_discord_id`, `retention_purge_log`                                                                                                                                                                                                | ✅ merged                                       |
| PR-B mode-aware erasure (#1780) — `AccountEraserService` unifies DB+off-DB (D1); `retention` mode re-homes cross-user characters to the Orphaned-Characters sentinel (D11)                                                                                                                                             | ✅ merged                                       |
| PR-C preview (#1781) — `RetentionPurgeService` owns the single D3/D4 predicate; read-only `GET /internal/retention/preview` + `pnpm ops retention:preview`                                                                                                                                                             | ✅ merged                                       |
| **PR-D1 purge capability** (#1795) — per-user `POST /internal/retention/purge` + `retention:purge`/`reconcile-off-db` CLI, in-tx TOCTOU re-check, audit ledger, 25% server-side ceiling, gone-flag self-heal, privacy policy                                                                                           | ✅ merged 2026-07-26; dev migrated              |
| Dev end-to-end purge smoke                                                                                                                                                                                                                                                                                             | ✅ PASS 2026-07-26 (checklist item 10)          |
| **PR-D2 daily nag** (#1797) — `RetentionNagScheduler` (daily + startup, weekly Redis cooldown, silent at 0, breaker warning + per-env CLI commands in the embed); rode with a `createIntervalScheduler` extraction across all three bot-client schedulers (CPD-forced; also fixed the stray-startup-timer-on-stop gap) | ✅ merged 2026-07-26 — **Phase 2 COMPLETE**     |

**Owner calls locked (D1)**: no 10013 fast-track (gone is an alternative unreachable signal, still ANDed with the 180d window — D13's safety argument assumed an activity-clear that didn't exist; it ships now) · the release-cadence coupling is accepted as Phase 2's scope boundary (the 51 inactive-but-reachable are Phase 3's cohort) · public-then-private characters with historical reach stay retained, because deleting them deletes other users' memories. Earlier locks: orphan sentinel + `original_owner_discord_id` · `retention_exempt` · single `retention_purge_log` · mode-aware `eraseAccount`.

**D15 refined, not followed**: off-DB-before-DB is not implementable (the off-DB work is a function of the transaction's outcome), so DB-first stands and the ledger's `pending` + `off_db_pending` is the retry handle. Recorded in the design doc.

**Open follow-ups**: `sendCustomSuccess`→`sendContractSuccess` internal-routes sweep (deliberately not ridden on the destructive PR) · the purge-concurrency assumption (two members, both gated on Phase 4). _(Struck at beta.186: fake-optional columns shipped #1859; bounded reconcile sweep shipped #1856/beta.185.)_

## UX Epic — Phase 3 IN FLIGHT (2026-07-20 → )

Waves 0–2 ✅ released beta.173 · **Wave 3 (breaking renames) ✅ released beta.174** — six PRs, 15 old→new command rows. Per-PR detail: `cold/epic-log.md` § Phase 3.

**NEXT: Waves 4–6** — PR-6a factory core+pilot → 6b destructive preset → 7 `/deny` redesign → 8 picker hygiene → 9/10 factory sweep + router adoption. Ship as normal minors; post-batch class slices are schedulable (burn-down trigger fired). Command-propagation lag ~1h in prod post-release.

Phases 1–2 are ✅ COMPLETE (released beta.170–172); their per-PR narratives live in `cold/epic-log.md` and git history.

## 🔬 Dev smoke checklist — the beta.190 cut (owner-driven; this file is the source of truth)

**One sitting, F first.** F is this release's only needs-smoke item (the prompt restructure — runtime-only observable); G is optional/low. Items 8, 13, 17 are carried deferrals from earlier cuts, independent, droppable. Closed items from the beta.189 round (16, 18, 19 — all PASS) were evicted at this cut — git has them.

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Status                             |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| F   | **Prompt restructure quality + first cache hit (#1903–#1908) — needs-smoke: roleplay voice is runtime-only.** In dev or prod, either channel type: send **two consecutive turns to the same character** and read the replies for voice/tone drift (the invariant: same content, rearranged containers — replies should feel unchanged). Then `/inspect` the SECOND reply: the Response section should show a nonzero **Cached** token count (turn 1 warms the prefix, turn 2 hits it; z.ai and OpenRouter-cache-capable models only — a 0 on an uncached model is not a failure). Masking: none — every request builds fresh. Failure looks like: audibly flatter/off-voice replies, or Cached stuck at 0 across several same-persona turn pairs on z.ai (then: `pnpm ops cache:prefix-diff --env <env> --channel <id>` names the diverging section). Report: pass/fail + the /inspect screenshot if Cached is 0. **PASS (owner, 2026-08-02 ~21:55Z, dev, req `fc288277`, COLD on glm-5.2)**: voice reads right, and turn-2 `/inspect` shows **Cached Tokens: 9,664 of 14,370 (67%)** — far above the ~15% floor prediction because a same-persona turn-2 is a pure append, so the chat-log prefix hit too, not just S0/S1. The epic's first live proof. Follow-up filed: TASK-403 (dedicated `/inspect` Cache view, owner nice-to-have). | ✅ **PASS**                        |
| G   | **Stickers on referenced/forwarded messages (#1895) — optional, low.** Reply to (or forward) a message that carries a sticker, in a character conversation. Expect the character to react to what the sticker depicts, not its filename. Unit-covered; this only proves the live Discord payload shape matches the fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | ⬜ optional                        |
| 8   | **D.** Wave-3 remainder. **Retroactive prod-log sweep 2026-07-31** over every retained deployment (2026-07-23→07-31, i.e. essentially the whole post-Wave-3 window): **`history purge` ✅** (2 × `DELETE /api/user/history/hard-delete`, 2026-07-26, `deletedCount=2` each). **`/preset override` + `set-default` ❌ never invoked** — their endpoints are `PUT /api/user/model-override[/default]`, zero hits window-wide. **`/random`, `/chime-in`, `/help` are NOT log-checkable at all**: modern deferral-mode commands log nothing on success (`index.ts` only warns on unknown), the first two emit chat traffic identical to a normal turn, and `/help` makes no backend call. Avatar/voice groups ambiguous (`PUT /api/user/personality/{slug}` ×9 can't be attributed). Remaining human work: the two `/preset` writes + avatar/voice groups.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 🟡 partial — 1 of 6 closed by logs |
| 13  | **E.** `/history purge` a channel with a derived unlocked memory → memory flips to `visibility='deleted'` (#1796 runtime proof; released beta.177)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | ⬜ carried from beta.176           |
| 17  | **A. Extended-context images are described again (#1884)** — post a message carrying an image (a **forwarded** one is the sharpest case: a forward is only attachments, so an undescribed one reads as a blank quote), let it fall into the chat log, then in a **NEW** message ask the character what the image showed. Expect a description. Regression tell: the model sees only `[image/webp: name.png]`. Prod-log confirmation without Discord: `extendedContextAttachmentCount` in ai-worker's `Preprocessing results ready` should be **non-zero** when bot-client's `Fetched and processed channel messages` logged `imageAttachmentCount>0` for the same window — that pair was 67-vs-0 before the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | ⬜ not run                         |

Executed in the Wave-3 round: `/voice voices purge` + `/character voice set` (each surfaced a real bug → #1752), `/chat` in an activated thread (works).

## 🔭 Watches

- **Dev-bot Discord quarantine — TERMINAL, not pending.** Rotzot's appeal was **DENIED 2026-07-22** (canned reply; did not engage with the zero-messages-sent / already-fixed facts). The app is permanently unable to join new guilds or DM users it hasn't messaged before. Root cause was boot-time createDM prewarm sweeps (~117 opens × 10+ deploys/day from a 1-server bot — spam-prep shape even with no sends); structurally fixed by `OUTBOUND_DM_ALLOWLIST` (#1650). **Prod Tzurot is a separate app and was never quarantined.** Casualty is dev outbound-DM testing only.
  **Owner leaning: delete & recreate Rotzot as a fresh dev app.** When executed: new app + token → set dev `DISCORD_TOKEN`/`DISCORD_CLIENT_ID` → re-register commands → re-invite to the dev guild → set `PUBLIC_SITE_URL`/`SITE_BRAND=rotzot` → re-run the broadcast smoke with a fresh label. The recurring dev-blast `0 sent, 1 transient-failed` **is** this quarantine surfacing as `discord-20026` (runtime-confirmed); classifier Quick Win open (copy should read "bot quarantined (permanent)").
- **`targeting: 'recent'` rate after #1883 ships** — both trigger-row writes now log which path found the row. A high `recent` share means the exact `triggerMessageId` lookup isn't matching and the robustness win is illusory; grep `Stored built references` / `Updated user message with enriched content` once prod has traffic. (Raised in #1883's sixth review; the fallback itself predates the PR.)
- **Memory-search budget after the next release (#1893 + #1902 ship together)**: grep prod ai-worker for `Input exceeded the model window` (should now fire only on pathological reference/multi-part turns) and `attachmentCharsBeforeBudget` (the cap's bite rate — mined data predicts it bites on the MEDIAN attachment turn).
- **Prompt-cache hit rate after the next release (caching epic Phase 0+1, #1903–#1908)**: two consecutive same-persona prod turns → grep `Generated response` for nonzero `cachedPromptTokens` on turn 2 (the epic's first live proof); a repeated 456ec221-style `/inspect` measures the realized cacheable fraction vs the predicted ~15%; `pnpm ops cache:prefix-diff --env prod --channel <id>` names any unexpected divergence by section. **Roleplay-quality watch on the same release**: the restructure moved protocol/output constraints off the recency tail — if voice drifts, the recorded fallback is OUTPUT_CONSTRAINTS back to the tail (~150 re-billed tokens/turn).
- First prod extraction log post-beta.165 (`valid_from` should stamp source time, not run time) · first deploy-orphan/safety-flush event (#1642 runtime proof) · owner's felt-repetition re-measure (gates 1b slice B + correction detection) · db-sync probes · prod lock-storm · retention-failure.

_Older session logs live in git history._
