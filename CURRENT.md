# Current

> **Version**: v3.0.0-beta.210 — "vision follow-through + z.ai plumbing + integration-tier opening" (10 PRs / 8 runtime / 103 range files, no migrations, merged 2026-08-29 16:39Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.209 — "DM isolation + scope-first /deny + guest vision on flash" (14 PRs / 11 runtime / 193 files, 2026-08-28).

---

## ☀️ 2026-08-29 day — Fable verification pass + mining tail3 + beta.210 cut

**Opus-window verification (owner ask: "mine / verify the Opus work").** Fresh Fable read of all three Opus-window merged diffs (#2248/#2249/#2250): zero findings — shipped logic held everywhere, hedges honest, pins correct (incl. #2248's voice exclusion and #2250's `:free`-first rule-order claim, both re-verified against code). Release preflight clean (0 advisories, repo-settings green, no dependabot, no migrations).

**Mining run (delta 08-28 23:41Z → 08-29 16:09Z; report 08a1ee8b-tail3 in the machine-local corpus).** Headline: every spec defect in the window was the ORCHESTRATOR's, never the worker's ("the one seat no fresh reader occupies"); § 3a round-dispatch decayed within one Opus window (honored #2246/#2247/#2248, abandoned #2249/#2250 — the two longest PRs, both producing the sample-not-inventory loop); claim scan verdict "not crying wolf" (≥5 fires, real catches); count-rot 5/5 gate-caught 0 self-caught; undelivered-close-out crossed its threshold (one occurrence per driver → no longer Fable-specific). Owner approved all four operationalization sets same day: process micro-PR (turn-ends-on-confirmation rule + package-default gate lists + ci-gate § 3a reminder) · owner-decision digest section · PR-body claim scan · small filings (cwd-drift-guard worktree FP task; worktree-stale-base logged upstream). Building next; README dispositions finalize with PR numbers.

**beta.210 CUT + MERGED + PUBLISHED (PR #2251, owner-approved merge).** All checks green, release review read (no blocking findings; its 103-vs-119 file flag resolved by comm — the 16 extras are bump package.jsons); finalize aligned develop to main; beta.209 demoted. **The routed-model instrumentation is now IN PROD — TASK-791's fix half unblocks on the next `empty_response` window.**

**Honest ledger today:** my miner briefing mis-attributed the owner-decision batch to the Fable window (it was Opus — miner corrected from the JSONL); I wrote "12 review rounds across the range" in the release PR body when 12 covers only the newest three PRs — caught on my own re-read before anyone else saw it (first self-catch of the class in two windows), fixed with the scoped number. The REVIEW_ROUND_CAP counter printed "594" on #2251 — TASK-768's implausible-count bug, fourth sighting.

## 🌙 2026-08-28/29 night — mining run + operationalizations + TASK-791 instrumentation (Fable-driven)

**Session mining (delta 08-27 01:01Z → 08-28 23:41Z, owner-triggered "Opus had a streak of mistakes").** Report + SYNTHESIS-2026-08-28 + all dispositions in the machine-local mined-corpus README. Headline: the two driver lanes fail in different places — Fable defects at the spec boundary, caught pre-commit by the orchestrator's diff read; Opus defects in the orchestrator's own INLINE review-round edits, caught only by claude-review post-push (#2242/#2245 both ran 10 rounds; round 3's fix WAS round 10's defect; zero dispatches across ~32 round pushes). Count-rot merge gate caught all ~13 in its jurisdiction; the falsified-prose class (6 instances) lived on the one surface it can't see: added code comments.

**Owner rulings tonight (durable):**

- **No mechanical round cap — ruled out.** "I want the job done even if it takes longer"; rounds stay unbounded/advisory; target defect GENERATION. Future mining runs must not re-propose (recorded in the synthesis + README).
- **Opus-driver nested dispatch for substantive units** (mode table + 10-working-posture, shipped #2246) — the fresh-context layer, not the model tier, is the defect-catcher; untested-at-adoption hedge recorded; **the next Opus-window mining pass measures it** (round counts, self-fed loops).
- **beta.210 riders**: TASK-791 (instrumentation-first) · **TASK-804** (image-description attribution, prod-evidenced from the owner's debug dump — characters read vision prose as user-typed text; prior task-361 fixed only the GENERATION seam) · TASK-790 (preset-slot UX). Plus: **drain filler slices bundle 3–5 same-area size:S per PR**; **failure-surface routing** (Opus windows on mechanical ground; judgment-dense/sanitization surfaces to Fable windows or tight specs). All in now.md § beta.210.
- **Priority: land the Opus posture now** — week tail, Fable low; owner intends Opus windows next. The posture is LIVE on develop; flip drivers at will.

**#2246 SHIPPED + MERGED (mining P1–P4, 4 review rounds, all findings applied).** Driver-agnostic review-round dispatch (§ 3a rewritten; the >~4-rounds step-in trigger deliberately removed) · merge-gate **added-comment claim scan** (`\b`-anchored vocabulary, count-before-cap, fail-open; probe canaried red/green each round) · docs/local/ joins the prior-decision search list · hedged owner values ("probably X") are measurement orders. Round-3 self-referential catch: round 1's own `\b` anchors made the DIFF_CLEAN fixture vacuous — fixed with a standalone token + canary. **The scan fired live on #2247's merge — first real fire: 8 comment lines flagged, all 8 verified true.** That's TASK-810's first data point (watch: FP rate vs earning its keep; co-render probe case folds in if tuning happens).

**#2247 SHIPPED + MERGED (TASK-791 instrumentation half, 3 review rounds).** `routedModel` now reaches: vision failure/success/exhausted-chain logs + `VisionModelError` (via `response_metadata.model_name` — the vision path never runs the OpenRouter extractor, one call site verified) · text-path empty_response/provider-error warn logs · `openrouter.model` metadata · the debug dump beside `modelUsed` (whose false "actually used" doc comment is corrected). Real-LangChain canary pins the `model_name` contract against version bumps. **Scope notes (verified, cited in the PR): attributes the empty-content shape; the zero-choices shape throws before any message exists (LangChain keeps the raw payload local) — unreachable without a signature change, deliberately not taken.** The next prod `empty_response` wave names the routed model; TASK-791's fix half waits on that data.

**Nested-dispatch evidence tonight (3 units, all Fable→Opus-orch):** the fresh-context layer caught TWO spec defects of mine before any code was written — the #2246 fetch-placement ambiguity (resolved correctly against the letter of the spec) and the 791 dead-field premise (`openrouter.*` never populated on the vision path — an Explore ground-map was right about the code, wrong about the wiring; the STOP prevented a silently-dead instrumentation field and surfaced TASK-808/809 as side findings). Round counts 4 and 3 vs the Opus window's 10s.

**Filed tonight (all pushed):** TASK-804 (image attribution, high, riding) · 805 (flash-vs-Qwen blind comparison — promise-ledger catch) · 806 (ZAI_MODEL_CATALOG sync question) · 807 (GitGuardian fixture FP) · 808 (vision path never deletes `__raw_response`, 200-500KB/message) · 809 (`/inspect` Served line compares requested-vs-requested — `state:owner`, two reviewers independently want the render) · 810 (claim-scan FP watch, `state:observable`) · 811 (text-path failure diagnostics drop routedModel — typed-error follow-up).

**Misses tonight, honest ledger:** announced the CURRENT.md close-out and ended the turn without writing it (owner had to ask — this edit is the recovery); one chained command let the merge gate block TASK-811's commit silently (re-run unchained); two stale self-reported counts in PR bodies caught by the merge gate's re-derive prompt (rules 2227→2229; tests 5404→5407) — the gate is now 14-for-14, still never self-caught at authoring time.

## 📋 Open items (near-term)

- **TASK-791 (high)** — instrumentation LIVE IN PROD as of the beta.210 merge (2026-08-29). Fix half = read the routed-model data from the next prod `empty_response` window, then design the tier fix. Prior root-cause candidates BOTH recorded open on the task (neither survived review).
- **beta.209 open clauses (constituent detail in git/notes):** TASK-598 open on its worktree-push clause (close after a quiet stretch) · TASK-764 open on the experiential /deny clause (owner-verifiable only) · TASK-612 open on its runtime invalidation clause · TASK-782 watch (5.3-flash scaffolding shapes in prod logs) · TASK-795 (#2242 coverage nits) · TASK-775 (#2228 coverage nit).
- **Watches carried:** `usage_logs` attribution · `model_not_found (rescued)` ⚠️ embeds (GLM now.md entry clears on first sighting — **still open**: the 2026-08-29 sighting was `timeout (rescued)`, a DIFFERENT category, so it does not clear that entry) · ~~error-channel first prod posts~~ (**CLOSED 2026-08-29** — first enriched embed landed in prod with every #2245 field populated: Source/Stack Hash/Outcome/Request ID/Personality/Model/Provider/Duration/Occurrence, model rendering the `z-ai/glm-5.3-flash → openrouter/free` swap) · glm-5.2 maxTokens saturation + Qwen censoring rates. (The trailing-confirmation watch CLOSED 2026-08-29: threshold crossed, one occurrence per driver — the turn-ends-on-confirmation rule amendment is in the approved process micro-PR.)
- **Usage posture:** ≤10% weekly Fable per day through the Sun 02:00 ET reset (owner directive 2026-08-27); tonight honored via nested dispatch + § 3a. Owner flagged Fable LOW at week tail — Opus windows intended next, new posture live.
- **Housekeeping (CLEARED 2026-08-29, owner okay given):** the mined `015f3cbc`/`0059bca0` JSONLs were already gone from disk — only the active session (`08a1ee8b`, 67MB) and one 0.1MB stub remain, so nothing needed deleting. All four `worktree-agent-*` local branches deleted; two required `-D` because their work transferred by PATCH (different SHAs), and their content was verified superseded first — they held a BROADER draft of the media-provenance constraint covering `<voice>`/`[Voice message: …]`, which develop deliberately narrowed out and test-pins as excluded. Parked branches (`memory-hybrid-retrieval`, `reply-ping-gate`, `task-563`) untouched. Active session mined through 2026-08-28 23:41Z; next delta starts there.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest; +vision/mining filings). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28)** — tasks-per-CI-cycle is the throughput number. Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Open rule-shaped gap, not yet drafted:** defects entering _while fixing a previous finding_ — authoring-time countermeasures, none at correction time. The § 3a driver-agnostic dispatch (#2246) is the first structural answer; mining measures it next window. Council pass still wanted (TASK-531 adjacent).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. Judge preview: A-vs-B 5/5/8, B-vs-B′ control 8/18 (read the noise-floor line before trusting a PASS). Artifacts backed up. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 (owner decision surface) + TASK-487 (Sonnet-tier ledger) + tonight's Opus-nested adoption (untested; next Opus mining pass is the test). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness; license recorded), doc-65 (private brain), doc-66 (coalescing), doc-67 (tag-scoped sharing), doc-70/71/72 (tag mgmt/dashboard nav).
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending, dependabot-ignored until then) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: 200 users; 7 in grace (~09-08 expiries are the next event). Phase 4 autonomy parked BY DESIGN.
- **doc-78 — DM Context Isolation: SHIPPED + smoke-verified**; design + runtime evidence in tracker doc-78.
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
