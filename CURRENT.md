# Current

> **Version**: v3.0.0-beta.199 — the post-beta.198 drain batch: own-voice STT class closed (#2055/#2056, one predicate in common-types), GLM mis-channel watch telemetry (#2057, family-scoped), prerelease-flag surfacing (#2038), health-webhook chunking (#2039), 6 more bounded fixes + contract sweep + deps. No migrations. Released 2026-08-10 ~20:20 EDT via release:publish (flags verified: 199 = latest, 198 demoted).

---

## 🔄 SESSION STATE (2026-08-10 late, Fable 5 driving — beta.199 RELEASED)

**v3.0.0-beta.199 is out** (2026-08-10 ~20:20 EDT): #2058 merged with owner approval after the holistic review ("no blocking findings"), `release:finalize` resynced develop (SHA-aligned at `8eb94ace6`+), `release:publish` tagged + flipped flags (verified: 199 latest, 198 demoted). No migrations, no advisories. Own-voice smoke CLOSED via retro-log (open-threads below). Post-release incident: the deploy window caught one in-flight thread reply (~8s bot-client/gateway boot gap → immediate errored slot) — diagnosed, thread recovered with heal-on-read runtime-confirmed, TASK-515 (submit-path retry) + TASK-516 (ops logs snowflake mangle) filed. **Next per owner directive: TASK-513** (Opus 5 orchestrator revisit — pre-trial tweak review + low-risk work slate; owner at 59% weekly Fable <48h post-reset, wants Fable load shifted).

<details><summary>Shipped in beta.199 (session log, compressed)</summary>

**glm-4.5-air mis-channel RESOLVED as watch (#2057) — Production Issue cleared, TASK-514 is the observable watch.** Measured before designing (prod `llm_diagnostic_logs`, full 24h retention window): 1 genuine mis-channel in 132 GLM requests (glm-4.5-air 1/63; glm-5.2 0/64), and a legit glm-5.2 brief-reply row carried the IDENTICAL length signature — so no automatic promote/retry can act without leaking real meta-reasoning; owner picked watch+WARN via AskUserQuestion. Shipped: family-scoped predicate in `ai-worker/src/utils/reasoningMischannel.ts` + warn in `ResponsePostProcessor.logReasoningTelemetry` (round-2 Medium correctly caught that the noise justification was GLM-scoped while the check was unscoped). 3 review rounds, round-3 "No bugs found". Escalations if the warn recurs are prod-config-only (reasoning-off on the free default, or free default → glm-5.2; free default verified = GLM 4.5 Air reasoning:medium). Ledger misses: mutation-proof cleanup ran `git checkout` and wiped my own uncommitted implementation (recovered from context; the exact ASK-FIRST-table trap); the pre-compaction "~16 runtime PRs" claim overcounted (tag sits at #2035 → real range #2037..#2057). Owner filings: TASK-513, doc-73 (one-click View Reasoning + reasoning persistence on history rows + /inspect window question).

**TASK-512 shipped — the own-voice class is fully closed.** **#2056 merged** (`f852067af`, Sonnet worker): `isOwnPersonaVoice` promoted to `@tzurot/common-types/utils/ownVoice` (one predicate for both services — api-gateway cannot import ai-worker), api-gateway's `processAttachmentsForJobs` skips audio-job creation for assistant-authored references (audio-only; images still dispatch; both reference arms incl. thin-envelope), structural test detection widened + allowlist reason scoped to the reference-pipeline producer after verifying `routes/ai/transcribe.ts` is a second, deliberately-ungated producer. 2 review rounds, round-2 "no blocking findings". Dispatch + render + cache paths now all consult one predicate. Ledger misses this stretch: my worker spec omitted `typecheck:spec` from the gates (pre-push caught a bare-`string` fixture type — second occurrence of this spec gap, now in the standard gate list), and one monitor was armed from a cwd stranded in `services/api-gateway` and died instantly (cwd-drift class, re-armed from worktree root).

**TASK-507 + TASK-511 shipped (post-compaction stretch).** **#2054 merged** (`903de3d16`, TASK-507: `packages/tooling/turbo.json` declares every live-swept repo tree as a cache input for `test`/`test:coverage`, plus `turbo-inputs-coverage.test.ts` importing each guard's scan-root constant so a root change breaks the build instead of un-guarding the cache; 8 review rounds — rounds 3/5/6 each caught readers my enumeration missed, PR body carries the corrected verification history; the round-6 probe proved the common-types glob redundant for cache-busting via package-graph hashing, recorded in TASK-509). **#2055 merged** (`25cf73c6c`, TASK-511 own-voice STT class fix, Sonnet worker unit 6: `ownVoiceGuard.ts` single choke point + dispatch guard in the reference path + render-side redaction on deduped/stored paths + structural `transcribeAudio(` scan with reasoned allowlist; both June guards converted to the shared predicate; review round 1 merge-clean; **TASK-512 filed** — api-gateway still enqueues the STT job for own-voice references, cost gap only). **Needs-smoke at next release** (runtime-unverified, unit-tested): reply to a persona's voice message → the quote shows the static own-voice description ("The character's own voice message — its spoken text is the message content."), no transcript; masking state: a reference already enriched before the fix replays redacted too — that's the belt-and-suspenders path, also fine. Housekeeping: 16 orphaned `worktree-agent-*` local branches deleted (14 via `-d`, 2 via patch-id-verified `-D`). Ledger misses this stretch: the F4 cache probe first appended to a nonexistent `common-types/src/index.ts` (created + deleted a stray file — the removed barrel), and one JSON glob removal left dangling commas that made the guard test fail for the wrong reason before the probe ran.

**Dependabot campaign COMPLETE + TASK-508 shipped (same session, post-compaction).** #2051 (main-cut carrier for #2041's claude-code-action v1.0.187 bump) merged to main with owner approval → `release:finalize` resync (develop SHA-aligned; claude-review live again) → #2041 closed superseded → #2042 (pnpm/action-setup) + #2043 (7 prod deps) + #2052 (13 dev deps; dependabot recreated #2044 rather than rebasing) all merged through green CI (their claude-review/CodeQL "skipping" rows are by-design bot/path guards, verified in the workflow source). Then **TASK-508 shipped as PR #2053** (`eb3ce8a1a`, Sonnet worker): outcome-honest applied-fallback in shared `handleDestructiveModalSubmit` + tailored `appliedNotice` at all four callers — 3 review rounds, round-3 "No bugs found"; the worker caught that my spec's three suggested verify-steers named nonexistent subcommands (corrected pre-commit). Ledger misses: my `beforeEach` fixup initially omitted the vitest import (pre-push typecheck caught it before the remote saw it), and one monitor was armed from a drifted cwd (`services/bot-client`) and died instantly — re-armed from repo root. Residual double-double-failure surface documented in the TASK-508 entry (reviewer round-3 suggestion).

**Drain round three COMPLETE — 3 more PRs merged (Sonnet workers, standing tier).** #2048 (TASK-243: `computeWindowCap` inverted-bounds guard — swapped-pair clamp + warn-once; reviewer's mock-reset finding dismissed with evidence, ai-worker's global `afterEach(clearAllMocks)` already isolates) · #2049 (TASK-305: all ten remaining `routes/internal` handlers swept to `sendContractSuccess` — compile-time schema pinning, zero drift found, round-1 "no actionable items") · #2050 (TASK-234: batchDelete outcome honesty — collector-scoped timeout catch + `read/cancelled/confirm/applied` phase tracker + `CATALOG.error.destructiveApplied`; purge half of the task verified obsolete, client never throws; 3 review rounds). Round-three misses for the ledger: my spec placed `phase='confirm'` before the cancel branch (reviewer round-2 catch — a cancel-ack throw rendered a delete-failure) and my #2050 scope note verified the client-throw claim but not the success-render throw (round-3 catch → **TASK-508 filed, HIGH**: the same class lives in shared `handleDestructiveModalSubmit`, backing account deletion + 3 purges); plus one CI red that was mine, not a flake — the new user-facing string tripped batchDelete's shrink-only raw-literal budget, fixed by the catalog entry, and it passed locally only because turbo caches the tooling live-sweep test without hashing the swept packages (**TASK-507 filed**). Repo-state sweep: 10 verified-vestigial merged remote branches pruned (owner-approved); `feat/memory-hybrid-retrieval` left parked (doc-8); 4 dependabot PRs waiting — #2041 (claude-code-action bump) needs the main-cut cherry-pick treatment.

**Drain round two (same day) — 3 PRs merged, pilot closed.** #2045 (TASK-503: both sibling nag schedulers gate their cooldown on confirmed embed delivery — 1 CI cycle, cleanest unit) · #2046 (TASK-277: releases-page coverage tripwire + `RELEASES_PAGE_SIZE` const; review round added an order-independence test) · #2047 (TASK-347: trailing-slash normalization — env-schema strip for GATEWAY/PUBLIC_GATEWAY/PUBLIC_SITE URLs, `deriveAvatarUrl` local strip [getConfig() deliberately avoided: schema default would fake an unfetchable localhost avatar], transport-layer multi-slash widening covering both `gateway-client.ts` raw reads; 4 review rounds, round-4 verdict "no actionable items"). **Sonnet pilot DECIDED at 5 units: KEEP** (TASK-487 Done — 0 semantic defects across the pilot; skill's "pilot" framing update rides the next .claude/skills PR). TASK-506 filed then closed same-session (absorbed by #2047's transport widening); TASK-505 filed (MultimodalProcessor fallback-vision 5s-timeout CI flake — watch). Round-two misses for the ledger: first worker spawn landed on a STALE worktree-pool base (16 commits behind; worker stopped correctly — specs now branch from explicit `develop` + ancestor check), and the #2047 PR body twice overclaimed sweep completeness before reviewer rounds closed the real gaps (avatar site, tooling client).

**Round one (same morning): 4 PRs merged, 2 tasks closed without code** (owner directive: offload mechanical work to cheaper tiers; Fable was ~39% weekly at kickoff). #2037 (workflow_dispatch escape hatch, TASK-446 — dispatch verified live post-merge) · #2038 (TASK-500 prerelease-flag guard: reconcile WARN + `newestPrerelease`, bot-client `ReleaseFlagNagScheduler` with version-scoped delivered-gated weekly cooldown, `GitHubReleaseSchema` → common-types) · #2039 (TASK-491: `ops health:post-webhook` chunks the weekly report, replacing the 1800-byte shell truncation; ENOENT-narrowed degrades + INCOMPLETE trailer) · #2040 (TASK-435: **seven** CatalogModel fixture copies → one membership-based shared builder — review found 4 copies the original enumeration missed; enumeration lesson: value-based repo-wide grep, not function-name grep). TASK-492 closed as already-shipped (`3322c7749` predated its filing); TASK-501 Done-flip committed. Filed: TASK-503 (both sibling nag schedulers adopt delivered-gated cooldown), TASK-504 (webhook inter-chunk pacing watch + 2 same-file members). Sonnet pilot units 2-4 recorded in TASK-487 (0 semantic defects; tally at 4/5 units, holding).

</details>

## 🧪 OPUS 5 ORCHESTRATOR TRIAL — units 1-2 done (2026-08-11, Opus 5 driving)

**Both units merged.** #2060 (TASK-516: `ops logs` snowflake truncation — cac/mri coerces all-digit values past 2^53, so an incident dig silently grepped a different id; fixed via the existing `rawOptionValue`, plus a source-scanning guard and a class sweep that also caught `retention:purge --exclude`, which CRASHED on its documented single-id case). #2061 (TASK-515: submit-path retry across a deploy window; retry narrowed to CONNECTION failures only because `generate()` creates a paid job and the gateway's 5s dedup window is shorter than the ~14s backoff; three duplicated retry loops extracted to `withGatewayRetry`).

**Trial verdict is the owner's call** — full record in TASK-513's notes. Honest read: 2 substantive reviewer catches across 2 units, both traceable to the ORCHESTRATOR's spec framing (neither spec said the call was non-idempotent), none to the Sonnet worker (TASK-487 unit 12, still 0 worker semantic defects) and none to code the diff-read examined. Recommendation: continue to units 3-5 (TASK-32, 24, 178) with "does the spec state the invariants the code actually has?" added to the spec template, rather than declaring pass or kill at n=2.

**Also this stretch**: two comments I wrote asserted external-library behavior as fact and were wrong both times (cac repeated-flag → returns an ARRAY; `AbortSignal.timeout` → clock starts at `fetch()`, so a timeout does NOT imply delivery). Both reviewer-caught. Operationalized: a memory carrying the code-comment trigger + TASK-520 for the rules wording. Filed this session: TASK-518 (owner: dedup window vs retry span), TASK-519 (guard's non-id-named flag gap), TASK-520.

**Open threads for next session:**

- ~~Own-voice post-release smoke~~ ✅ **CLOSED via retro-log 2026-08-11** (owner-supplied prod diagnostic, requestId `75407676`): all 31 persona `<voice>` tags in the assembled context render the static own-voice description with zero persona transcripts, while user voice messages keep their transcripts — both halves of the invariant runtime-verified in prod.
- **Opus 5 trial is STAGED (TASK-513)**: plan owner-approved 2026-08-11; tweaks PR #2059 merged (settled Sonnet tier citing the completed 11-unit TASK-487 ledger, never-delegate-the-diff-read, deliverable-length habit). The trial starts when the OWNER opens an Opus 5-driven session — open with /tzurot-orchestration + TASK-516, slate and kill criteria in TASK-513's notes. TASK-513 stays open as the trial's per-unit recording surface.
- TASK-514: mis-channel WARN watch (state:observable) — Railway grep "Suspect reasoning mis-channel" on recurrence signals.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the venue-scoping design input either way; if they say OFF and never-talked holds, open an investigation with an /inspect evidence request).
- TASK-498: codify the pre-commit review panel (trial data: #2034 caught 3 pre-commit, CI still found 1 mirror-branch Medium; 2 rounds vs #2032's 9). TASK-499/502/503 filed and open.
- Owner-idea council queue: doc-70 (tag count/group-message), doc-71 (/tag management), doc-72 (dashboard index nav — council pass mandated), doc-73 (reasoning-trace access) + the standing doc-64/65/66/67 set.

_Session-per-release note: this session has now spanned TWO releases (beta.198 → beta.199) + multiple compactions; a fresh session is a clean continuation point whenever convenient — owner's call. beta.199 post-merge sequence complete (merge → finalize → publish, flags verified); watch for the release DMs on the next announce reconcile._

<details><summary>Previous session log (2026-08-09, beta.197/198 — compressed)</summary>

## Previous session log (2026-08-09 evening)

**Resume-sequence items ①–③ are DONE; doc-60 build is IN FLIGHT.**

- ① **TASK-490 shipped** (PR #2030 merged: 8 memory promotions + AskUserQuestion allowlist rider + TASK-470's mutate-and-confirm-red clause as Core Principle 9; the 8 memory files + MEMORY.md lines deleted, store at 24 memories). **TASK-488 shipped** (PR #2031 merged after 3 review rounds: rule § A Blocking Question Goes Through a Formal Channel + `blocking-question-channel-check.sh` Stop-hook, 19-case probe; round-3 leftovers rode onto TASK-494 as riders). Both Done in tracker.
- ② **doc-64 Phase 0 DONE**: portability inventory shipped as **doc-69** (278 rows, 5 classifier agents, 12 borderline adjudications); doc-64's Phase 0 bullet updated. Next for doc-64: council pass consumes doc-69.
- ③ **doc-60 owner decisions locked via AskUserQuestion** (recorded in the doc): owner-authored tags · global namespace · over-cap `/chime-in` samples cap-many with a notice · **the multi-character cap becomes admin-configurable** (owner addition). Plan approved (plan file `~/.claude/plans/eventual-enchanting-lynx.md`): 3 PRs. **PR 1 = #2032 MERGED** (`08c15b69a`, tags schema+gateway+dashboard; 9 review rounds — notable: admin routes' silent tags drop caught+fixed round 5, Identity emoji → 🪪 swept through page titles/labels, import seam tests added). **PR 2 = #2033 MERGED** (`e1161be1e`, `multiTagMaxCharacters` registry setting + cap threading; INCLUDES the mention-only truncation-detection bug fix — categorize as Bug Fix in release notes, PR body has the callout; round 4 added write-path cache invalidation + `MAX_POTENTIAL_MENTIONS` ceiling-parity test). **Dev migration `20260809185820_add_personality_tags` APPLIED** (prod rides the next release's premigrate — additive). Owner AskUserQuestion decisions this stretch: models/limits page split (9 pages) · Identity → 🪪 · tags count toward PARTIAL badge · truncation bug fixed in-PR · Promise.all cap-fetch fold · round-4 trio (cache invalidation, cap=1 grammar, description annotate). **PR 3 = #2034 MERGED** (`e670b1bc4`, tag command flows: /random tag filter, /chime-in character→optional + tag XOR, capped+sampled tag fan-out with 🎲 notice, tag autocomplete; both promised riders landed. 2 review rounds — round-1 Medium: emptyTagPoolDetail unescaped echo, fixed; round-2 LOW: cap description scoped to /chime-in. First pre-commit review-panel trial ran on this PR: 3 findings fixed pre-commit, 1 mirror-branch Medium still reached CI review — trial data recorded in TASK-498; TASK-499 filed from round-1 nit). **doc-60 COMPLETE.**
- Also this session: **doc-68** filed (backlog-surface consistency pass, from owner musing), **TASK-493** (stale `ops context` doc filenames), **TASK-494** (Stop-hook sidechain filtering + 3 round-3 riders), **TASK-497** (db-sync schema-skew guard during dev-ahead windows).

**Tag-flow dev smoke (one pass, ~5 min, guild channel with some recent messages; all other shipped surfaces are high-confidence — CI/mutation/review-covered, no manual round):**

- [x] **S1 — dashboard tags write** ✅ PASSED 2026-08-09: `/character edit` on an owned character → Preferences & Tags → enter `Fantasy, sci-fi` → save. Proves the Discord modal → union-schema round trip (PGLite covered the gateway; the modal path is runtime-only). Expect: view shows normalized `fantasy, sci-fi`. Fail: 400 on save, or raw casing displayed.
- [x] **S2 — tag autocomplete vocabulary** ✅ PASSED 2026-08-09: type `/random tag:` and wait for the dropdown. Proves tags survive the wire into the cached client list. Masking: autocomplete cache is 60s — a just-added tag can lag up to a minute; re-open the picker rather than judging the first paint. Expect: `fantasy — N characters`-style entries, count-sorted. Fail: empty dropdown after 60s+, or `[Unable to load tags — try again]`.
- [x] **S3 — tag-filtered /random** ✅ PASSED 2026-08-09: `/random tag:fantasy` (any tagged character counts — the invariant is "pick stays inside the tag pool", not which character). Expect: ephemeral 🎲 pick notice; a character carrying the tag replies. Fail: an untagged character replies, or "No characters carry the tag" despite S1.
- [x] **S4 — cap + over-cap fan-out** ✅ PASSED 2026-08-09 in two halves: default-cap fan-out (`tag:helluva-boss`, 10-pool → picked 5, all 5 delivered in character, notice intact) + cap-change propagation (`multiTagMaxCharacters` 5→2 via `/admin settings set`, observed within TTL: `tag:hazbin-hotel`, 24-pool → picked 2, exactly 2 responders). Pool counts cross-checked the bulk-tag corrections (10 and 24 both exact). Original item (for reference): run `/admin settings set multiTagMaxCharacters 2`, wait ~60s (admin-settings cache TTL — the masking state), then `/chime-in tag:fantasy`. Expect: EPHEMERAL notice `🎲 3 characters carry fantasy — picked 2 at random: A, B` (invoker-only by design — other members see only the replies), then exactly those 2 reply publicly in completion order. Fail: 3 replies (cap ignored), no notice, or notice clobbered by an error. Also covers the admin-setting write path end to end.

Report: pass/fail per item is enough; screenshot only if something looks wrong. Then: doc-64 council pass (consumes doc-69) or drain fill-in. Tags prod migration rides the next release's premigrate (additive). **RESOLVED: beta.197 release DMs never sent** — the GitHub release sat prerelease-flagged (and 196 undemoted, the inverse of what `release:publish` produces — evidence points to a manual `gh release create` in the PM-3 release session), so the announce pipeline skipped it by design in both envs. Fixed 2026-08-09 ~20:40 EDT by flipping both flags (197 → latest, 196 → prerelease); the :41 reconcile announces it. TASK-500 filed: hourly sweep should WARN when the newest release is prerelease-flagged.

**The voice-consistency sitting (~15 min) is at the owner's leisure — do NOT nag or re-surface it as a recommendation.** No calendar pressure exists (probes backed up, retention deadline defused); it unblocks the prompt-assembly cluster + caching Phase 2 whenever it happens, and it costs nothing by waiting.

Owner-idea docs awaiting promotion (council pass before plan-mode): doc-64 (meta-harness plugin spinoff; license section records the owner constraint + MIT-everywhere recommendation, decision pending), doc-65 (private brain repo), doc-66 (coalesce queued user messages — collapse by user alternation; council pass for a hybrid reply shape at pickup), doc-67 (tag-scoped sharing of context/memories/facts; sequenced after doc-60's tag substrate).

**2026-08-09 PM-3 (Fable, release session): beta.197 CUT, MERGED, RELEASED.** Range verified mechanically: 57 commits / 16 PRs (#2013–#2028), zero runtime files (pathspec-excluded diff empty) → risk floor, no smoke items, no migrations, no advisories. Three holistic release reviews, zero blocking findings; finalize resynced develop onto main; v3.0.0-beta.197 tagged + published. Owner filings this session: **doc-66** (message coalescing — same-author runs concatenate, different author breaks the stack, memory/history rows untouched; hybrid room-reply shape gets a council pass at pickup) and **doc-67** (tag-scoped sharing theme + queue bullet — off/all/tag-set scope unified across context, memories, and facts, consuming doc-60 tags; "pantheon of characters that follow me around"). License question answered + recorded in doc-64. Process note: each develop push restarts a release PR's CI — the monitor was re-armed twice for doc nudges before switching to hold-and-batch local commits; batching is the default while a release PR is open.

**2026-08-09 PM-2 (Fable): three process PRs merged + memory sweep.** **#2026** (bare-token + queued-receipt hooks; round-6 HIGH refuted by probe — 1,991/1,991 enqueues carry content, verbatim line is probe case 16) · **#2027** (token economy) · **#2028** (doc-61 economy pass: rules corpus 166,827 → 142,502 B ≈ 6k tok/session saved, first `--surface rules` DOWN-write, worktree-isolation mandate — shipped after I ran a same-tree file-mutating worker and branch-hopped under it). Memory Section 0 sweep: 6 deleted, 7 trimmed, 8 MIGRATE files await TASK-490; backup `../memory-backup-2026-08-09.tar.gz`. Honest-ledger: the TASK-470 weigh-in was missed during the pass (now TASK-490 rider 2).

</details>

---

**The voice-consistency harness is BUILT (#1910/#1911); REMAINING: the owner's ~15-min sitting.** Probes mined 2026-08-04 (18 probes, 3 personas, all JSON-protocol — legacy-XML coverage deliberately declined, owner call), census done (personas `z-ai/glm-5`, judge `anthropic/claude-sonnet-4.5` — family-disjoint), replay + judge done (54 generations, 36 swapped pairs). **Next: blind-review `reports/voice-consistency/judgment-sheet.md` (15 pairs) → `pnpm eval:voice-verdict`** → verdict + numbers into `active-epic.md`. Judge preview: A-vs-B dead even (5/5/8); B-vs-B′ controls 8/18 — the pre-registered p₀>35% under-power flag may trip, read the noise-floor line before trusting a PASS. One probe (d5a31e4e) generated empty on all three arms (arm-symmetric, unbiased). FAIL path: `VOICE_EVAL_ARMS=A,C` re-run → `VOICE_EVAL_GATE_KIND=CA`. **All artifacts backed up** at `~/.claude/projects/-home-deck-Projects-tzurot/voice-consistency-backup/voice-consistency-2026-08-04.tar.gz` (`/reports/` is gitignored; probes cannot be re-mined once the pre-beta.190 retention window closes). Owner has no committed date — do not re-surface as a recommendation. Carried runner nits (fold into any runner touch-up): `afterAll` disconnect guard when PrismaClient construction throws; retry-once wastes a cycle on deterministic parse failures.

Phase 2 (history extraction) starts only after the gate passes. Fill-in units: the drain campaign — **query, do not browse**: `pnpm tracker task list -l state:ready -l size:S --plain`; `--priority high` alone returns only Done (the work moved to medium), so pair `state:ready` with size labels; cluster-batching is dead (2 startable of 15 re-checked). Still-open holds: the 11 too-big items (TASK-4, 17, 44, 48, 49, 58, 78, 98, 153, 386, 407) need individual treatment; TASK-61 blocked on the voice gate; TASK-107 needs a producer sweep across every test tier incl. `tests/e2e`. **Sequencing**: the #1317 cluster and TASK-165 are prompt-assembly content — gated behind the voice-consistency run so arm B stays beta.190's shape.

**beta.191 open threads**: TASK-425 (deduped-quote type guard — promote when DependencyStep emits File entries), TASK-426 (STT-wrapper no-key log split, only if noisy), TASK-410 (bullmq/ioredis v6 migration; both majors dependabot-ignored until then — dependabot reads config from MAIN), the 55 doubled-transcript rows aging out ~Sep 2, doc-59 (BYOK video processing — owner ask).

**Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a content-free presence note · extended-context quota exemption ships AFTER persistence · `maxImages` is a spend cap persisted images do not consume.

**Retention is calendar-only**: 2026-08-09 manual run (owner-approved): 3 never-used bystanders purged (0 characters), 2 warning DMs enqueued (grace → ~2026-09-08). Books: 221 users, 5 in earlier grace (~2026-08-26 expiries) + 2 newly warned; daily nag surfaces expiries. Phase 4 (autonomy) parked BY DESIGN — manual-approval-first; tonight's ~3-minute rubber-stamp run is exactly the trust-evidence its promote-gate wants, if such runs keep being routine. system-model freshness = epic-close re-touch rule (#1814).
