# Current

> **Version**: v3.0.0-beta.220 — "the tracker drain: eight fixes, the dependency groups, vitest-5 config prep" (11 PRs / 11 runtime / 106 range files; no migrations; PR #2372 merged 2026-09-08 ~19:47Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.219 — "the voice anchor, and the memory-archive build dormant behind switches" (8 PRs / 7 runtime / 206 range files, 2026-09-07 ~22:03Z; three additive migrations).

---

## 🙋 Owner queue (the human-only items, consolidated 2026-09-08 — everything below also lives in its long-form bullet under Open items)

- **Smoke pass on dev (one sitting, report pass/fail by number)**: (1) `/shapes export` on an imported shape → ZIP of per-section JSON+Markdown with a README first; (2) `/history purge` with scope Everyone's from an account WITHOUT Manage Messages → refused before the confirmation modal, naming the permission; (3) voice anchor in a FRESH channel or DM with the drifted character → the card's register when history is not the dominant mass (decides whether `doc-97` Phase 3 moves up); (4) TASK-907 — read the first `commitment:`-tagged facts on dev and prod (`/memory facts`) and say whether they read as commitments. (5) The masked-link `/inspect` error render (#2259) is carried as a watch, not asked for.
- **Rollouts (owner-run, in this order)**: the memory-archive flip sequence on dev (seven steps, long-form bullet below; privacy-policy z.ai bullet before the prod model switch) → the real-messages snapshot review (prod is already flag-on; a dev before/after per the long-form bullet is the only way to get a pre-flip baseline — owner call whether it is worth it).
- **Owner call, low**: TASK-916 — should alias labels render with the configured mention character instead of a literal at-sign (recommendation: yes, five sites, one PR)?
- **Config steps (minutes each)**: `WEEKLY_AUDIT_GH_TOKEN` repo secret (TASK-898 closes on the first run) · `TZUROT_RAILWAY_API_TOKEN` in the local `.env` plus the two `AUTO_DEPLOY_COMMANDS` deletes (TASK-62) · `~/.bash_profile` line 20 → the WebStorm 2026.1 node path · confirm dev's `realMessagesEnabled` on the Operations page (prod is confirmed ON from logs; dev had no traffic to read).
- **Agent-run, no owner action**: the doc-17 gap-bucket cache read (multi-day sweep across prod deployments) · the retrieval-stamp watch (CLOSED 2026-09-08) · the #2270 link-share and #2253 restart-recovery watchers.

## ☀️ 2026-09-10 — retention Phase 4 built (#2385, #2386), TASK-923a #2387 (Opus-driven)

**#2385** (`dd8e5a853`) run lease + non-prod scope; **#2386** (`86602aebd`) the daily job, 5 review rounds, owner rulings: reconcile backlog + breaker warning report. **#2387** (`c3d351c42`) bracket strip narrowed to prompt timestamps. **#2388** (`66963134f`) the raw-log-content lint rule. **Back on Fable from here.** **#2389** (`ab35ee5ab`) the view page-flip failure notice, TASK-890 closed. **#2390** (TASK-901, main-cut, `8a232687`) merged to main with owner approval; finalize re-aligned develop. **#2391** (`60ecb042c`, TASK-894) undo `restoredCount`, 4 rounds; **#2392** (`6d90ba84f`, TASK-808) extractor parity on two paths. doc-99 measured: bookkeeping dominates, TASK-934 filed. Lineup complete; cut proposal next. Misses: a closed-task blind spot (TASK-634), an unthrottled gate OOM, twice-stale dist, two claims written before the thing existed, my after-commit count placement overturned in #2391 r3.

## 🌙 2026-09-09 (late) — beta.222 opens: sharp advisory shipped, two PRs in review, three tasks filed (Opus-driven)

**#2382 MERGED (`281c2d483`) — the `sharp` HIGH advisory (GHSA-rgj7-g3m4-5g8c) closed by a `pnpm.overrides` floor bump.** The board's plan (wait for a Dependabot PR) was written on a wrong label: all four direct declarations already read `^0.35.4`, so the vulnerable copy was TRANSITIVE (`packages/embeddings` → `@huggingface/transformers@4.2.0` → `sharp@0.35.3`) and no PR was ever coming. **TASK-927** fixes the classifier — `isDirect` is name-membership in the declaration set, never the resolved tree. Advisory clears on `main` at the cut, not before.

**#2383 (mining process PR) — THREE review rounds, three genuine false-block classes, all in the new commit-header pre-check, all caught by review.** Each was a different shape of one root error: classifying commit-shaped text from RAW input. R1 an earlier chain flag (`git stash push -m "WIP…"`) supplying the subject; R2 backticked prose in a `sed` (found by it blocking me mid-edit; the trigger is a conjunction — a substitution span AND escaped separators, not backticks alone); R3 the class fix — classification on quote-stripped text, extraction on raw, with an equal-segment-count guard and a safe fallback. R3 also measured a **fail-open coupling**: a raise in the header code took the develop/main gate AND the gated-file check down with it (`VERDICT=$(...) || exit 0`); now isolated, canaried both directions. 44 → 75 probe cases. **TASK-928** filed (the ack-file shape across five hooks). My own miss: R1's residue note claimed this hook could only miss a check, never falsely block — wrong when written, and the review caught it.

**#2384 (TASK-924) — TWO rounds.** Preamble narrowed to decoration-only; review then caught an unbounded emphasis interior reopening the same bug behind `*…*`, and an **exponential ReDoS I introduced** (ambiguous `{1,2}` delimiter tiling: 645ms at 30 chars, >120s at 200, killed) — both fixed, lookahead-disambiguated delimiters plus a permanent adversarial perf test. Round 2's short-action-beat finding (`*sighs softly* [Name — ts]` still stripped) was **measured against develop before deciding**: develop deletes it identically and admits any non-bracket character besides, so this PR narrows and does not regress. Documented and pinned as an accepted residual, behaviour deliberately UNCHANGED — **owner call outstanding**: no structural rule separates an action beat from the leak's own `*previous context*`, so closing it means choosing between stripping the real leak and preserving action beats. **TASK-929** filed (`npx tsx` cannot load ai-worker — three interfaces re-exported without `type`; tsc erases them so every typecheck tier passes).

## 🌃 2026-09-09 (evening) — mining + audit pass, #2380 ride-along, beta.221 CUT PROPOSED, retention tranche 2, Phase 4 unparked (Fable-driven)

**Back on Fable after the Opus drain window.** Session mining over the 09-03 → 09-09 delta (eight Sonnet miners, Opus synthesis; reports machine-local): zero FRUSTRATION/REPEAT for the fifth run; zero worker-origin defects reached develop; count-rot ~15 self-caught / 2 reviewer / 0 owner; the Opus-at-high sweep cannot attribute (the ledger gate shipped before the window). Ten proposals accepted as one set — the process PR spec is at `docs/local/handoffs/spec-process-mining-0909.md` (R1 commit-header pre-check hook, R3–R8/R10 one-sentence rule and skill edits, TASK-922's sentence; R2 no change; R9 = TASK-901 on the next main-cut). **Independent code audit of the range** (Opus, fresh read): nothing blocking, nine findings — three rode along as **#2380 MERGED 2026-09-09 ~20:26Z (`7fb4a6d77`)** (the shapes-list body preview gated, the two vacuous blank-name tests made discriminating, the matcher docstring scoped; one clean review round), the rest filed: TASK-924 (high: the self-header preamble deletes a prose line ending in a self-named aside), TASK-925 (logger-truncation guard; the #2380 review added a vocabulary variant), corrections on TASK-921/922/923/704, TASK-887 re-surfaced, doc-99 (Fable-budget levers, measurement first). **Honest ledger**: the #2379 body's blank-name canary row was true at round 2 and I never re-derived it after round 3 — the audit caught it, not me.

**beta.221 CUT 2026-09-09 ~20:45Z** — PR #2381 (`a6d50433a`), `release:range` 8 PRs / 8 runtime / 96 files, no migrations, one transitive advisory with no fix (adm-zip), `guard:repo-settings` clean, no dependabot PRs; holistic review no findings; owner approved via `AskUserQuestion`; merged without `--delete-branch`, `release:finalize --yes` SHA-aligned develop, `release:publish` tagged `latest` and demoted 220. **The beta.222 plan is on the board with the Opus-drivable lineup in order** (process PR spec and TASK-890 unit B spec under `docs/local/handoffs/`); **post-deploy watches**: the first prod boot's command-registration outcome line and the echo-strip counter (checklist above). **Retention tranche 2 and Phase 4 unparked** — see the calendar line under Standing threads.

**beta.221 smoke checklist (risk-scoped; every item is HIGH confidence — nothing needs a manual round; the observability line is the check):**

1. Self-header strip (#2379/#2380, ai-worker, LIVE — real-messages is on in prod): the `headerLinesStripped` counter and the echo-strip warn in prod ai-worker logs; a compound leak that survives shows as a `[Name — timestamp]` line in a reply. High — CI + six review rounds + the audit probe.
2. Boot command registration guard (#2377, bot-client): the first prod boot logs one of `registered` / `skipped-unchanged` / `skipped-empty-command-set`; the slash-command roster survives the deploy. High — the guard only changes the zero-valid-commands path.
3. Log-preview opt-in (#2373/#2380): inert in prod by construction (deployed services refuse the flag). High.
4. Mention character (#2375): prod's character is the at-sign, so the rendered text is byte-identical there; dev renders the ampersand. High — snapshot-pinned.
5. Browse label truncation (#2374/#2376): a select label containing an emoji at the cut renders whole. High — unit-pinned both sides of the boundary.
   Coverage gap named: nothing composes the two response strips end-to-end (TASK-923 carries the seam test); post-hoc observability (item 1) suffices for the cut.

## 🌇 2026-09-08 — post-OOM resume: #2371 merged, beta.220 CUT on the backstop, the doc-80 owner call, beta.221 planned (Fable-driven)

**Resumed after the Steam Deck OOM reboot**: the round-2 amend of #2371 pushed with `--force-with-lease`, CI green; round 3 returned a Medium (`LOG_PROMPT_ASSEMBLY` blocked on Railway by convention only) and an Info (two raw `process.env.NODE_ENV` reads — correct-as-is: every service entry point calls `getConfig()` at module scope before either read, verified on develop). **Owner call**: merge as is; a deployed-service refusal for ANY local-only flag set true rides the `doc-80` sweep PR. **#2371 merged**, TASK-594 Done. **`release:range` read 11 PRs / 11 runtime** — the dependency groups and the vitest-config chore count as runtime, where my hand count of 7 had excluded them — so the ~10 backstop fired ahead of the plan's waiting-on list; **owner approved the cut**. **beta.220 CUT**: #2372, 11 PRs / 11 runtime / 106 files, no migrations, no advisories, no dependabot PRs, `guard:repo-settings` clean, notes verified exactly-once, holistic review no findings, finalize SHA-aligned, tag `latest`, 219 demoted; the beta.221 plan is in `now.md` with the waiting-on list carried and the sweep PR added. **doc-80 owner call**: one local-only `LOG_CONTENT_PREVIEWS` flag behind a shared helper, lengths always-on, digests where a site diagnoses duplication (recorded in `doc-80`); spec drafted, dispatch next. **Environment**: the reboot swapped WebStorm to 2026.1 and `.bash_profile` still names the 2025.3 node bundle — owner fix, one line; sessions prefix the 2026.1 path meanwhile. **Post-deploy boot check on the six prod services**: see the beta.220 line under Open items.

## 📋 Open items (near-term)

- **beta.219 CUT 2026-09-07 ~22:03Z** — `release:range` 8 PRs / 7 runtime / 206 files (the PR body had said 189; the holistic review counted 206 and it was right — the docs commits after the body was written count); no advisories; three additive migrations premigrated to prod with `release:premigrate --force` (non-TTY) BEFORE the merge; notes verified exactly-once; the holistic review found no blocking findings and one method note (`gh pr diff` 406s past 20,000 lines on a 206-file PR — TASK-911); the review action went silent once on the release PR (TASK-901, fourth data point); owner approved via `AskUserQuestion`; merged without `--delete-branch`, `release:finalize --yes` re-aligned develop (13 skipped cherry-picks, one replayed), `release:publish` tagged `latest` and demoted 218. **What the release changes at runtime today: the voice anchor only.** Every memory-archive switch ships OFF; the rollout sequence below is the owner's. #2353's workflow hunk landed on main via #2361 (main-cut) with develop resynced.
- **#2362 MERGED 2026-09-07 ~23:40Z (TASK-912)** — the settings dashboard renders an empty slug list as `(none)` and lets the modal clear it (a new `SettingType.LIST`; the owner had seen a bare `**` on the Memory Archive page). Three review rounds: round 1 caught a real crash (select-menu descriptions had no 100-char guard, now truncated for every setting type) and a comment naming a caller that did not exist (dead branch removed); rounds 2 and 3 nits, no bugs. Rollback of a character flip now works from the dashboard, not only via `/admin settings set`.
- **2026-09-07 late (Fable) — dependabot + caching grounding**: **The beta.220 drain — #2354, #2363, #2364 (vitest-5 config prep; TASK-913 stays open as the `state:observable` bump watch), #2365, #2366, #2367, #2368, #2369, #2370, #2371 — all merged 2026-09-07/08 and shipped in that release; per-PR detail is in the release notes and git.** **#2373 MERGED 2026-09-08 ~21:38Z (TASK-593 Done; the `doc-80` pass complete)**: nested dispatch (37 min, three inner Sonnet workers), three review rounds all green — round 1 added the empty-references pin, round 2 caught my residue list as head-truncated (corrected from the full grep, reviewer re-swept it) plus two polish nits, round 3 two Low nits both correct-as-is (per-file `vi.mock` hoisting; a `truncated` boolean filters where a numeric compare cannot). The boot refusal is live on dev from this deploy: all three dev services (ai-worker, api-gateway, bot-client) deployed SUCCESS at 21:38Z with neither local-only flag set in the dev environment (keys checked, values never read). **#2375 MERGED 2026-09-08 ~23:13Z (TASK-587 Done; nested dispatch, one review round, LGTM)**: the verification message and DM help reply read `BOT_MENTION_CHAR` at call time; dev sets the ampersand, prod the default. The alias-display sigil question the dispatch surfaced is TASK-916 on the owner queue. **#2374 MERGED 2026-09-09 ~00:21Z (TASK-581 Done; nested dispatch 37 min + three review-round dispatches/inline rounds, four CI cycles)**: browse select labels no longer split a surrogate pair and stay within the UTF-16 ceiling; `codePointTruncation.ts` in common-types is the one home (moved there in round 1 so the log-preview gate could delegate — it had the same raw cut, my miss); `clampEmbedText`, `boundedTag`, the list-embed metadata line, and `contentPreview` delegate; three logged tag-text sites gated. Review ledger: round 1 also caught two vacuous canary fixtures I had specified (cuts on pair boundaries); round 2 a body claim written before its command ran; round 3 a stale docstring; round 4 two back-off copies outside bot-client → **TASK-917** (plus the fast-path nit); the memory-modal `substring(0, 200)` joined **TASK-704**. `release:range`: 3 PRs / 3 runtime / 61 files. **#2376 MERGED 2026-09-09 ~00:52Z (TASK-917 Done; nested dispatch, no inner worker, one review round LGTM)**: the clients error-body cut delegates to the shared UTF-16 helper, the ai-worker note splitter imports the exported surrogate range constants, the code-point cap gains an exact length fast-path; `grep 0xd800` outside tests now matches only the helper. The truncation theme is closed. beta.221 range: 4 PRs / 4 runtime. **#2377 MERGED 2026-09-09 ~03:37Z (TASK-896 Done; nested dispatch, four review rounds)**: a boot that loads zero valid command files now skips the PUT instead of wiping every registered slash command; the guard gates on the hash store's presence, so the shell script keeps its deliberate wipe. Round 2 surfaced the larger pre-existing bug — both skip paths resolved normally, so the boot logged "deployed successfully" one line after the error; `deployCommands` returns a three-value outcome and the boot logs it. Round 3 swept the `count` → `discoveredFiles` rename to its twin in `CommandHandler.ts`. Review ledger, all mine and all in PR-body prose: two causes claimed with one pinned, "never reads or writes" with only the write pinned, a field renamed in one place without its twin, and "Residue: None" while the partial-invalidity variant sat unfiled → **TASK-918** (medium; a boot loading 5 of 50 files still deletes the other 45, and the store keeps no count to detect the shrink). `release:range`: 5 PRs / 5 runtime / 70 files. **#2378 MERGED 2026-09-09 ~14:29Z (single-hop Sonnet dispatch, 13 min; two review rounds)**: the character view page builders, `PAGE_BUILDERS`, `ViewPageResult`, `buildRedactedViewPage` and the `buildCharacterViewPage` dispatcher move verbatim to `viewPages.ts`; `view.ts` goes 399 → 234 ESLint-counted lines, clearing the 400-line error that made TASK-890 unlandable. My yesterday spec was falsified at grounding: no page builder has a direct test (they all run through the dispatcher), so moving the dispatcher too is what makes the test split a verbatim block move instead of a rewrite — otherwise `viewPages.ts` would be logic-bearing with no colocated test. Canary was test-count equality: 60 before, 36 + 24 after. `browse.ts` was a sixth importer the spec did not know about; the builder-import allowlist grant moved from `view.ts` to `viewPages.ts` at an unchanged net pair count. Round 1 caught a real miss of mine — `view.test.ts` kept the header `Tests for Character View Page Building` while `viewPages.test.ts` carried the identical text, and I had read both headers during my diff review without noticing one had gone false; fixed by amend + force-push rather than a fixup, saving a CI cycle. Round 2 clean, and it confirmed the `no-raw-content-literals` budget of 3 correctly stayed put (the rule exempts embed composition; the one counted literal lives in `handleExpandField`, which never moved). Residue **TASK-919** (low): a fifth private `createTestCharacter` now exists across the bot-client character tests. **TASK-890 unit B is spec'd and ready** at `spec-890b-fix.md`: grounding established the task's own fix shape names the wrong target — `ackUpdate` stamps `deferKind: 'update'` and the `ackMethodFor` matrix routes that to an ephemeral followUp, so `replySpecSafe` plus the already-imported `classifyGatewayFailure` is four touched lines with no browse-helper reach-across and no rename. `release:range`: 6 PRs / 6 runtime / 78 files.
- **🔥 beta.219 opener, HIGH: character voice drift (`doc-97`)** — owner intake 2026-09-05 with a handoff and debug payload (both under `docs/local/handoffs/`, gitignored). Owner decisions (2026-09-05, binding): cut 218 first (done); the memory overhaul (`doc-8`) is the active epic again through this theme, anchor first and caching Phase 2 behind it; LID starts at the re-entry design pass; backfill back on the table; no model-parameter experiments. Measured from prod memories (read-only, aggregates only): the card register faded through spring and collapsed in August 2026 (exclamations per 1k chars 0.7–1.0 in Apr–Jul → 0.06 Aug → 0.01 Sep; courtroom vocabulary zero through July, then rising; reply length 830 → 1,652 chars). The cross-channel history feature shipped 2026-02-28, so it is not the August trigger alone; August is also the heaviest memory month (529 rows), and the character's preset history (model change?) is unchecked. Phase 1 = a cache-neutral `<voice_anchor>` before the current turn (`PromptBuilder.ts`, targets in the theme doc). The one-off curve script is stored under `docs/local/handoffs/` and copied into `scripts/analysis/` to run (workspace packages resolve only from there; run with `pnpm ops run --env prod --force tsx <path> <slug>`).
- **beta.218 CUT 2026-09-05** — `doc-97` Phase 1 MERGED as #2348 (the `voice_anchor` V-tier section; TASK-900 filed for the catalog-wide size measurement); the Phase 2 directive text is drafted in the theme doc for the owner to apply; validation is smoke item 4 below. `docs/proposals/backlog/memory-archive-format.md` ACCEPTED 2026-09-05 (four-model council + owner pass). Owner steps outstanding: the `WEEKLY_AUDIT_GH_TOKEN` secret (TASK-898 closes on the first run with it), the `TZUROT_RAILWAY_API_TOKEN` in the local `.env` plus the two `AUTO_DEPLOY_COMMANDS` deletes (TASK-62 closes on that run). TASK-880 stays a prod watch.
- **Memory-archive build (`doc-8`) COMPLETE 2026-09-06 → 07, all merged, every switch OFF** — slice P #2349 (the render pilot: S beat F on every recall metric, S loses ≈18 points of correctness to V; six amendments in `memory-archive-format.md` §8; owner items: the 30-row spot-check under `reports/render-pilot/`, amendment 3's possible reopening of open call 3) · slice A #2350 (split render behind the `archiveSplitRenderPersonalities` slug list, inert by default; LID pilot live under `docs/intent/memory-archive/`) · B1 #2351 (summarizer write side: nine additive `memories` columns, the `archive-summary` queue, coding plan with reasoning off, three switches on a new Memory Archive settings page) · B2 #2352 (read side: summaries render in split mode, lazy retrieval enqueue, `/memory view` Summary field) · commitment fact type #2356 (**owner ruling: prompt-level only, no `fact_type` column**; TASK-907 = owner review of the first commitment-tagged rows before trusting them) · C1 #2357 (**owner ruling: stamp retrievals** — `last_retrieved_at`/`retrieval_count`, environment-local, fire-and-forget) · C2 #2358 (`pnpm ops memory:summarize --personality <slug> --env dev|prod [--dry-run|--estimate] [--include-cold] [--limit N] [--window days]` with the 95% gate report; dev dry run: 2,161 non-chunk rows for the drifted character, cold-50 ≈ 100k tokens). Round-by-round catches and residue live in the PR bodies and TASK-901/902/904/905/906/910. **Next: the owner-run rollout below.**
- **Memory-archive rollout (owner-run, dev first; every build slice is merged)** — the design's phase table ends at C, so what remains is the flip sequence D5/D7 describe:
  1. Dev switches: `/admin settings set archiveSummaryEnqueueEnabled true`, then `archiveSummaryModelEnabled true` (cap stays 2000). New memories start summarizing on write; retrievals in split mode do not yet exist (no character listed). The sweep refuses to run while the enqueue switch is off.
  2. Pre-warm the drifted character: `pnpm ops memory:summarize --env dev --personality <A> --dry-run` (the hot set is empty until retrievals accrue on the C1 stamp — expect `GATE n/a` at first), then `--include-cold --limit 500` to summarize the newest 500 rows now (≈2k tokens per row, so ≈1M input tokens on the coding plan); watch the ai-worker logs for `archive_summary` usage rows and re-run `--dry-run` for dead rows by error class.
  3. When the gate line reads READY for the window (or the owner accepts a lower share on dev), `/admin settings set archiveSplitRenderPersonalities <A>`; send A a message → `/inspect` shows split notes carrying summaries; `/memory view` on a summarized row shows the Summary field. Failure = verbatim notes with the switch on, or a summary rendering under the wrong name.
  4. D7 (b) voice: re-run the voice harness on A against the pre-flip baseline (no further drift, no regression; third-person self-reference rate is the guard). D7 (c) fallback exposure: `verbatimFallbackNotes` and `summaryNotes` on the `Memory archive render composition` log line.
  5. TASK-907 review rides along: read the first commitment-tagged facts in dev before trusting them.
  6. Prod: the next cut premigrates the three additive migrations (GIN index, nine summary columns, two stamp columns); then the same sequence on prod for A, then the rest one at a time (D3: the render flip is per character on purpose). **Before the prod model switch flips**: `docs/legal/PRIVACY_POLICY.md`'s z.ai bullet describes response generation only; the summarizer sends stored memory content to z.ai asynchronously, which is a new processing use the policy should name (owner call; the release-notes doc sweep found it, and it does not block the cut because every switch ships OFF).
  7. Dev cleanup from the C2 review: one `archive-summary` job enqueued on dev by the round-1 probe (`--limit 1`) carries no removal options; once the model switch flips and it completes, remove it (`pnpm ops inspect:queue --env dev --queue archive-summary` shows it; the job id is the memory id) or that memory can never be re-enqueued.
- **Real-messages flip (prompt-assembly Phase 2, artifact PR 2.5) — PROD IS ALREADY ON.** Owner report 2026-09-08 ("flipped a while ago"), log-confirmed the same day: across the beta.219 ai-worker deployment window (08:56Z–19:48Z, 114 `System prompt composition` lines) no system prompt carries a `chat_log` section while history is selected (45 of 45 on the sampled turn), which is the flag-on shape (`layoutSections` drops only an EMPTY section, and 45 selected messages flag-off render a non-empty log). The board had carried the flip as a future owner step on a stale premise. Dev: unverified — no generations in the current dev deployment window (no traffic), so the dev value stays unknown until the next dev conversation. What remains of the runbook is the READING, not the flip: (a) the first gap-bucket read is too thin — 55 turns in 11 hours, ONE turn in the `<2 min` bucket (hit; 0.58 depth), 30 in 2–10 min (36% hit, mean depth 0.53), 23 beyond 10 min (56% hit, depth 0.80) — so the doc-17 rollout read needs a multi-day sweep across deployments before it says anything (agent-run, no owner action); (b) the snapshot review (step 4 below, the voice harness on A) is still owed and now has no pre-flip prod baseline — the dev baseline runbook below is the way to get one if the owner wants it. Steps 1–3 and 5 below are kept for dev only:
  1. Before: on dev, hold a 6+ turn conversation with one character in one channel (any kind), then I run `pnpm ops cache:prefix-diff --env dev --channel <id> --limit 5` for the baseline (expected: divergences at `H chat_log`, the system prompt re-billed from the log's head on a full window).
  2. Flip: `/admin settings set realMessagesEnabled true` (Operations page; live, no deploy). Leave `headerSpoofNeutralizeEnabled` at its default.
  3. After: same channel, 6+ more turns. `/inspect` on a reply shows the history as real user/assistant messages after the system message (cross-channel history as its own user message when present) and no `<chat_log>` in the system prompt; `promptHashHistoryStable` is present (re-keyed flag-on, TASK-729). Failure shapes: a speaker misattributed in a multi-user channel (the design's named regression risk), a reply that opens with a header-shaped line (the output-side strip should have removed it), or `<chat_log>` still rendered (the flag was not captured). Prefix-diff flag-on should read the system message byte-stable across turns (not yet verified in that mode — it diffs system prompts, so the history reading moves to the hash and to `cachedPromptTokens` on the diagnostic rows).
  4. Snapshot review per the artifact's §6: the voice harness on A flag-on against the pre-flip baseline — the same instrument as D7 (b); third-person self-reference is the guard.
  5. Prod: the same flip; read the `<2 min` gap bucket hit rate and the hit-depth distribution over the first day (doc-17's rollout read: the shallow-hit bucket should move toward ≥90%), never the headline ratio. Rollback = the same setting back to false, live.
- **beta.219 watch list (owner call 2026-09-07 to cut with the memory-archive build dormant behind switches)** — two paths run live from the prod deploy without a switch; both are observability-closable, no manual round requested:
  1. The retrieval stamp: after the ai-worker deploy, no `Retrieval stamp tail rejected` warn in prod logs over the first day (one fire-and-forget UPDATE per retrieval; a systematic failure warns once per hour). Log-closable by me from Railway. **Status: PASS — log-closed 2026-09-08 ~20:25Z: the beta.219 ai-worker deployment logged 974 memory retrievals and zero `Retrieval stamp tail rejected` lines across the 08:56Z–19:48Z window (the 5000-line cap; the server-side filter matched nothing even on a known-present token, so the first 11 hours are unread — a systematic failure would persist into the read window). The beta.220 deployment: 83 lines, no retrievals yet.**
  2. Commitment facts: TASK-907's review of the first commitment-tagged facts on dev and prod (`/memory facts`, filter by the `commitment:` tag). Owner-run. **Status: pending.**
     Everything else in the range is switch-gated and is exercised by the rollout sequence above, not by release smoke. Prod premigrate carries three additive migrations.
- **beta.218 smoke checklist (needs-smoke tier only; high-tier items are CI + review covered)** — run in dev after the deploy, report pass/fail per number:
  1. `/shapes export` on one already-imported shape (any channel kind) → a ZIP download whose entries are per-section JSON and Markdown files with a README first; failure = a single JSON/Markdown file or a missing section. Masking state: none, the export regenerates on every run. **Status: pending.**
  2. `/history purge` with `scope: Everyone's` from an account WITHOUT Manage Messages in that channel → refused before the typed-confirmation modal opens (the message names the permission); failure = the modal opens. Invariant: the gate fires at the option, not at submit. **Status: pending.**
  3. After the bot-client deploy, the boot log carries one registration decision per service (`skipped, unchanged` on the second boot, the PUT on the first); failure = no line, or a PUT on every boot. Log-closable by me from Railway, no owner action unless asked. **Status: PASS — log-closed 2026-09-06 (latest dev deployment logged one `Slash commands unchanged since the last registration; skipping` line at boot, no PUT).**
  4. **Voice anchor (#2348), after the dev redeploy that carries it** — send the drifted character the same message that triggered the debug log (the one under `docs/local/handoffs/`), in the same channel kind as the original (extended context either way; the anchor renders on every turn). Masking state: none, the anchor is built from the card at assembly time, but the card must carry traits/tone/examples in dev (sync from prod if the dev row is bare — an empty card renders no anchor, which is the one way this test passes for the wrong reason). Expected: `/inspect` on the reply shows a `voice_anchor` section last in the volatile prefix; the reply reads in the card's register — exclamation marks back, no pet name, no courtroom vocabulary, length in the neighbourhood of your message rather than 2× it. Failure: the section is present and the register is unchanged (then the anchor is not enough and Phase 3 moves up), or the section is absent (then the dev card is bare). Report: the `/inspect` link plus a one-line read on the register. **Status: pending.** **Status: RAN 2026-09-07 on prod (owner payload) — anchor present and placed correctly, register NOT recovered (reply 0.00 excl/1k vs card 2.38; history 89k of 106k prompt tokens is the dominant mass). Finding recorded in doc-97 Phase 1; next probe = fresh channel/DM.**
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 (after a surprise weekly reset) — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low. **HANDED TO OPUS 2026-09-09 ~01:29Z** (owner call: Fable past the sustainable burn rate, and the drain is mechanical-class work). Resume point: the medium drain, next unit **TASK-896** (a boot whose command directory yields zero valid files PUTs an empty set and wipes every slash command; `utils/deployCommands.ts` + `deployCommands.test.ts`, the pinned test at the "deploys nothing when every discovered file is invalid" case splits into a store arm and a no-store arm; fix shape in the task, nothing dispatched yet), then **TASK-890** (view page-flip failure gets the ephemeral follow-up the browse surfaces got; mirror `followUpBrowsePageFailure`). TASK-540 and TASK-559 carry product questions and go through AskUserQuestion before any dispatch. Under Opus: nested dispatch for substantive units, single-hop Sonnet allowed for mechanical-class ones (the orchestration skill's mode table); Opus runs at `high` — this window is the effort-sweep sample to mine against tail6b.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped**; **second run 2026-09-09 ~20:05Z (owner-approved via AskUserQuestion): 5 purged (1 grace-expired, 4 never-used, 0 characters), 0 skipped, 1 warning DM enqueued** (`retention:notify` prompts and exits 13 from a non-TTY shell without `--force`, unlike purge — folded into TASK-926) — now 187 users; 2 in grace, 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. **Phase 4 BUILT (#2385 + #2386): the first autonomous run fires ~60s after the beta.222 prod boot (kill switch `RETENTION_AUTORUN_ENABLED=false`); record it here to close TASK-926.**
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
- **#2379 MERGED** (`5fbc2615a`, TASK-920 Done): a compound self-header line — decorated scaffolding then a restamped platform header — reached a user in prod under the global `realMessagesEnabled` setting. `leadingSelfHeaderLineMatcher` closes it, name-scoped so a false strip can never delete character dialogue. **Six review rounds, six real defects**, each caught before merge: no end-of-line requirement, no blank-name guard, no trim, no name boundary (cross-persona collision), a preamble that swept up quoted headers, and case-sensitivity. The last three completed the port of `matchesPersonality`'s four guards, which this matcher was modelled on. **Root observation, recorded on TASK-921 and TASK-923**: every defect restated a property the render path already knows — the durable fix is to match the header string actually rendered instead of guessing from the roster name. Residuals filed: TASK-921 (name divergence — rename window, non-prefix display name, non-Unicode case folding; medium) and TASK-923 (the sibling matcher has the same end-of-line gap and truncates any reply opening with a bracketed em-dash aside; high, measurement-gated).
