# Current

> **Version**: v3.0.0-beta.223 — "the retention follow-up set, the vision refusal fix, the bookkeeping tools" (11 PRs / 10 runtime / 145 range files; one additive migration premigrated to prod; PR #2405 merged 2026-09-12 ~15:10Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.222 — "retention goes autonomous, prompt hygiene, undo counts" (10 PRs / 9 runtime / 152 range files, 2026-09-11 ~13:45Z; no migrations).

---

## 🙋 Owner queue (the human-only items, consolidated 2026-09-08 — everything below also lives in its long-form bullet under Open items)

- **Smoke pass on dev (one sitting, report pass/fail by number; owner holds it for last)**: (1) `/shapes export` on an imported shape → ZIP of per-section JSON+Markdown with a README first; (2) ~~`/history purge` with scope Everyone's from an account WITHOUT Manage Messages~~ PASS 2026-09-13: refused before the modal, the message names Manage Messages; (3) ~~Emily in a FRESH channel or DM after dev has deployed #2412~~ DONE 2026-09-13, PASS on the mechanism: two payloads by accident (cross-channel history ON then OFF) — ON: 0 exclamations/1k, 698 chars, "sweetheart" from the history block; OFF: 4.1/1k, 241 chars, card register back. The anchor and directive work; cross-channel history is the drift vector; `doc-97` Phase 4 (history-side treatment) OPENED, numbers there. (4) The masked-link `/inspect` error render (#2259) is carried as a watch, not asked for.
- **TASK-907 (owner review)**: commitment facts are live on prod since beta.219 with no switch; `/memory facts` filtered by the `commitment:` tag — do they read as commitments? Also note any welcomed pet name that was NOT extracted (the TASK-950 gap in the wild).
- **Rollout state (2026-09-13)**: memory-archive switches ON on dev and prod; Emily (`emily-tzudad-seraph-ditza`) listed in the prod render switch BEFORE her pre-warm (owner call); the 644-row pre-warm enqueued on prod 15:0xZ, gate 21.1% at 15:45Z with 4 dead rows (agent reads the error classes when it finishes). `realMessagesEnabled` confirmed ON on dev and prod. The dev before/after baseline SKIPPED by ruling. The dev stray `archive-summary` probe job removed (C2 cleanup done).
- **Leisure (unchanged, do not re-surface)**: the voice-harness blind review · the slice P 30-row spot check · TASK-104 · TASK-133 · card-level examples for Emily in her own register.
- **Done 2026-09-13**: the Default `system_prompts` row edited on dev and prod (v1 then v2; long-form under doc-97 Phase 2) · `WEEKLY_AUDIT_GH_TOKEN` set (Dependabot row reads; deletion-safety row fixed by #2413) · `TZUROT_RAILWAY_API_TOKEN_DEV`/`_PROD` in the local `.env` (tooling reads them after the TASK-62 follow-up) · the TASK-798 prod measurement · the stale `.bashrc` OpenRouter key revoked · the Waffles Share-Memories question CLOSED by ruling (deal with it if it recurs) · the privacy-policy z.ai summaries bullet committed.
- **Agent-run, no owner action**: the doc-17 gap-bucket cache read · the Emily gate and dead-row read · TASK-702 probe · TASK-62 env-suffixed token read · TASK-951 `.env.example` guard · TASK-838 cadence ledger then the doc-61 economy pass (ruled 2026-09-13, Opus lane) · the #2270 link-share watcher · the five `state:observable` watches TASK-952–956.

## 🙋 2026-09-13 — owner action day (Fable-driven; the owner worked the queue live)

**Shipped**: #2412 (TASK-949 part 2 + the relationship carve-out: the anchor drops its length clause and pet names, carves out forms of address recorded in the facts; four prompt snapshots regenerated; two review rounds — a stale JSDoc my exact-phrase grep missed, and the reviewer's catch that the PR body recorded only the v1 row edit) · #2413 OPEN (TASK-898 second half: `delete_branch_on_merge` read over GraphQL because REST withholds it from fine-grained tokens even with `permissions.admin: true` — owner-run probes settled it; worker-built, canary red proven). **Data edits (owner grant)**: the Default `system_prompts` row on dev and prod — Internal Consistency scoped to facts, Voice Fidelity added (v2 with the relationship carve-out), Precedence Over History names the anchor, TASK-159 renames; before/after JSON under `docs/local/default-prompt-*-2026-09-13.json`. **Rulings**: 702 / 559 / emphasis residual / dev baseline (session start); relationship facts are facts and may develop (→ v2 + TASK-950 widened to forms of address, shared references, disposition); dev-only `pnpm ops run` allow-rule; z.ai summaries privacy bullet; Waffles closed; TASK-838 then the doc-61 economy pass scheduled on the Opus lane. **Filed**: TASK-950, TASK-951 (`.env.example` key-set guard; measured 6 vs 16 keys of drift), TASK-952–956 (the 🚨 watches moved off `now.md`). **Environment**: `~/.local/bin/ws-node-exec` resolves the WebStorm node bundle at call time in every shell (root cause: the desktop session's PATH still carried the dead WebStorm 2025.3 dir); the classifier blocked prod runs on 4 of 6 attempts. **Honest ledger**: I worked around the PATH friction silently four times before the owner named it; I printed `~/.bashrc` whole and copied a stale API key into the transcript (owner revoked it); I grepped source but not snapshots for the old anchor sentence and the pre-push caught it; I read CURRENT.md after the hook had already injected it (CLAUDE.md now says not to); the first prod v2 apply was reported as pending in a PR body that also said "Residue: None". Range after #2412: 7 PRs / 6 runtime.

## 🚀 2026-09-12 — beta.223 CUT (PR #2405 `b487cc688`; Fable-driven, nested dispatch for every unit)

**Released** (cut on the ~10 backstop by owner ruling; the three carried readings move to beta.224 unchanged): the retention follow-up set (#2394 report replay + cooldown-first nag, #2395 the reminder DM with `users.retention_reminded_at` premigrated to prod, #2396 persona-DM unreachability stamped at the send site); the prod vision fix (#2399: terminate set = `media_not_found` only; TASK-937 stays observable); prompt hygiene (#2400 header strip requires end of line, #2402 the tsx-unloadable re-export); #2398 personality default-config routes (slice 2 = TASK-939); tooling (#2397 `worktree:transfer` + `unit:closeout`, #2401 resolved-tree advisories, #2403 uncached depcruise + turbo lint keyed on the eslint config, #2404 the gateway-calls budget split). Per-PR round history lives in the PR bodies. The orchestrators corrected my specs seven times this window (two nonexistent paths, a unit-tier canary, a two-versus-six consumer grep, a false turbo.json premise, a warm-cache STOP that would have closed TASK-902 as not reproducible, an omitted interface pair); every review finding was fixed or dispositioned in its body. Filed: TASK-938–943. **Honest ledger**: three watchdog-killed gate runs from skipping the lint warm-up; a "seven runtime plus three tooling" hand count where `release:range` read 9 runtime (root config and the lockfile count); `release:bump` rewrote the live TASK-941 worktree's manifests (TASK-943); the premigrate needed `--force`, was classifier-blocked, and ran on the owner's explicit instruction. **beta.224 opened the same afternoon: #2406 (TASK-941, the adapter re-export cleanup) MERGED after a post-cut rebase; #2408 (TASK-943, the walkers skip `.claude`) MERGED; #2407 (TASK-938, one personality fetch per edit route) MERGED after three rounds. The ai-worker drain batch 1 (TASK-889 + TASK-749 + TASK-799) MERGED as #2409, one review round; it surfaced TASK-944 (test-utils' colocated unit tests never run in CI, medium) and TASK-945. TASK-798 and TASK-702 wait on owner-side inputs (a prod usage query, a paid probe). The silent-gate batch (TASK-944 + TASK-792 + TASK-849 + TASK-610) MERGED as #2410 — four gates the repo believed were running and were not: test-utils' three colocated unit files, `tests/e2e`'s typecheck, a repo-wide format check, and the drift-ignore schema now all execute, each with a canary proving it bites. One review round, three nits declined on merits. Two spec premises of mine were falsified by probe: `.mypy_cache` was never gitignored (so the ignore-path fix I imagined would not have worked), and any explicit `--ignore-path` drops prettier's implicit `.prettierignore` lookup. Filed from it: TASK-946 (the rules text calls `structure.test.ts` per-package; it is one cross-package scanner) and TASK-947 (the `adm-zip` advisory has no reachable fix). Drain batch 3 (TASK-590 + TASK-743) MERGED as #2411 `49f6adeaf` — `customFields` round-trips through both user routes behind a 16 KiB input bound (response side left unbounded so pre-existing rows stay readable), and the gateway now subscribes to the persona channel, evicting the same shared resolver its two call sites obtain; `historyContextResolver` built a fresh resolver per call and so never served a cached persona, which only the subscription makes safe. Filed TASK-948. **Range: 6 PRs / 5 runtime / 87 files.** Session findings, all recorded in their own records: a debug payload showed the voice anchor and protocol directive 17 contradicting on register (TASK-949; the anchor drops emoji and pet names and loses the metaphor system), owner ruled sidecar prompts are a tiered CASCADE field (doc-26, TASK-110 shrinks accordingly), and the verified prompt source map is in doc-26 — `system_prompts.content` is JSON with three arrays, rendered to escaped XML in code, and has NO write path. Owner rulings: TASK-621 render the trace collapsed, TASK-540 return 503 for catalog-unavailable.**

**beta.223 watch checklist (observability, not smoke; no owner round requested):**

1. **Vision refusal advance** (#2399): a prod ai-worker log showing a `qwen/qwen3.5-397b-a17b` invocation after an `openrouter/auto` `content_policy` advance closes TASK-937 and the 🚨 board entry. Log-closable by me. **Status: pending.**
2. **Retention reminder DM** (#2395): the first cohort can form on or after 2026-10-04; the run report counts reminders sent. **Status: cohort-gated.**
3. **Persona-DM unreachability stamp** (#2396): a permanent persona-DM failure in prod stamps silently; `Failed to stamp persona-DM unreachability` appears only on a gateway error. Log-closable. **Status: pending.**
4. **Header strip counter** (#2400): `headerLinesStripped` after this deploy counts standalone header lines only; a rise means new leaks, never truncated replies (carries the 222 checklist item 3). **Status: pending.**
5. Default-config routes (#2398): no caller until TASK-939; tests only. High.

## 🚀 2026-09-11 — beta.222 CUT (PR #2393 `60b9dc9a9`; Fable-driven from 09-10 evening)

**Released**: retention autonomous (#2385 lease, #2386 the daily job; **autonomy ON by owner ruling at the cut**); ai-worker hygiene (#2384 preamble narrowed, #2387 bracket strip narrowed to prompt timestamps, #2392 the OpenRouter extractor on vision + system-model); bot-client (#2389 page-flip failure notice, #2391 `/history undo` counts); #2388 the `no-raw-log-content` rule; #2382 the `sharp` floor (`adm-zip` MEDIUM remains, no fix); #2383 the hooks chore; #2390 (TASK-901) by main-cut. doc-99 measured bookkeeping at 72.6% of main-loop calls → TASK-934. **Honest ledger**: a missed soft-delete filter and a count placed after the commit (#2391), a header read past (#2378), a canary row never re-derived (#2379), a closed-task blind spot (TASK-634), an unthrottled gate OOM, twice-stale dist, the beta.221 step-9 reset skipped and fixed at this cut. **Owner call RULED 2026-09-13**: the emphasis-run residual on the self-header matcher is accepted as-is (preserve short italic action beats; one strip in 171 measured turns).

**beta.222 watch checklist (observability, not smoke; every item HIGH except item 1, which is the release's one runtime-unverified path):**

1. **Retention first autonomous run** (#2386): ~60 s after bot-client's prod boot, the owner-channel run report (purged/failed/halt counts, runId footer); `retention-run:cooldown` set in prod Redis; the scheduler registration line at boot. **Status: PASS — log-closed 2026-09-11 13:39:37Z (scheduler registered live at boot; run report 60 s later: attempted 0, purged 0, eligible 0, one warning DM sent, no halt, no breaker warning, delivered). TASK-926 Done.**
2. `/history undo` copy (#2391): "Restored **N** messages" or the info embed; optional one-command dev check after `/history clear`. High — seam-pinned at route and command.
3. Self-header + timestamp strips (#2384/#2387): the `headerLinesStripped` counter and echo-strip warn; a `[Name — timestamp]` line in a reply is a surviving leak (TASK-923's measurement). High. **Status: MEASURED 2026-09-12 06:25Z — across the readable span of the beta.222 ai-worker deployment (2026-09-11 15:45Z → 09-12 06:20Z, ~14.6 h, 171 generation turns) the strip warn fired ONCE (headerLinesStripped=1, idTagsStripped=0); the first two hours after the deploy sit beyond the 5000-line cap. The counter cannot tell a standalone header from a header with same-line reply text, so the same-line-leak question is bounded (at most one candidate in 171 turns), not answered. Owner ruled 2026-09-12 for the end-of-line lookahead; shipped as #2400, TASK-923 Done. The counter stays worth a glance after the beta.223 deploy: a rise in `headerLinesStripped` would now mean standalone header lines, never truncated replies.**
4. Extractor parity (#2392): `response_metadata.openrouter` on vision/system-model responses, no reader yet; an empty-content vision reply now yields promoted reasoning text instead of `EMPTY_RESPONSE`. High.
5. beta.221 carry-overs all log-closed or high: boot command-registration line (#2377), log-preview opt-in inert (#2373), mention character (#2375), browse truncation (#2374/#2376).

## 🌇 2026-09-08 — post-OOM resume: #2371 merged, beta.220 CUT on the backstop, the doc-80 owner call, beta.221 planned (Fable-driven)

**Resumed after the Steam Deck OOM reboot**: #2371 merged (round 3: a Medium on `LOG_PROMPT_ASSEMBLY`, an Info on two raw `NODE_ENV` reads — correct-as-is; owner call: merge, the deployed-service refusal for local-only flags rides the `doc-80` sweep), TASK-594 Done. **beta.220 CUT** as #2372 on the backstop (`release:range` read 11 runtime PRs where my hand count said 7; owner approved): no migrations, no advisories, holistic review clean. **doc-80 owner call**: one local-only `LOG_CONTENT_PREVIEWS` flag behind a shared helper, lengths always-on, digests where a site diagnoses duplication. Environment: the reboot swapped WebStorm to 2026.1 (`.bash_profile` fixed since).

## 📋 Open items (near-term)

- **beta.219 CUT 2026-09-07 ~22:03Z** — `release:range` 8 PRs / 7 runtime / 206 files (the PR body had said 189; the holistic review counted 206 and it was right — the docs commits after the body was written count); no advisories; three additive migrations premigrated to prod with `release:premigrate --force` (non-TTY) BEFORE the merge; notes verified exactly-once; the holistic review found no blocking findings and one method note (`gh pr diff` 406s past 20,000 lines on a 206-file PR — TASK-911); the review action went silent once on the release PR (TASK-901, fourth data point); owner approved via `AskUserQuestion`; merged without `--delete-branch`, `release:finalize --yes` re-aligned develop (13 skipped cherry-picks, one replayed), `release:publish` tagged `latest` and demoted 218. **What the release changes at runtime today: the voice anchor only.** Every memory-archive switch ships OFF; the rollout sequence below is the owner's. #2353's workflow hunk landed on main via #2361 (main-cut) with develop resynced.
- **#2362 MERGED 2026-09-07 ~23:40Z (TASK-912)** — the settings dashboard renders an empty slug list as `(none)` and lets the modal clear it (a new `SettingType.LIST`; the owner had seen a bare `**` on the Memory Archive page). Three review rounds: round 1 caught a real crash (select-menu descriptions had no 100-char guard, now truncated for every setting type) and a comment naming a caller that did not exist (dead branch removed); rounds 2 and 3 nits, no bugs. Rollback of a character flip now works from the dashboard, not only via `/admin settings set`.
- **beta.220 → beta.221 drain (2026-09-07 → 09-09)**: #2354, #2363–#2371, #2373–#2378 all merged and shipped; per-PR detail in the release notes and PR bodies. Residue filed then: TASK-913 (vitest-5 watch), TASK-916 (Done-ruled), TASK-917 (Done, #2376), TASK-918 (partial-invalidity boot wipe, medium), TASK-919 (fifth private `createTestCharacter`, low); TASK-890 unit B spec at `spec-890b-fix.md` (four touched lines, no browse-helper reach-across).
- **🔥 beta.219 opener, HIGH: character voice drift (`doc-97`)** — owner intake 2026-09-05 with a handoff and debug payload (both under `docs/local/handoffs/`, gitignored). Owner decisions (2026-09-05, binding): cut 218 first (done); the memory overhaul (`doc-8`) is the active epic again through this theme, anchor first and caching Phase 2 behind it; LID starts at the re-entry design pass; backfill back on the table; no model-parameter experiments. Measured from prod memories (read-only, aggregates only): the card register faded through spring and collapsed in August 2026 (exclamations per 1k chars 0.7–1.0 in Apr–Jul → 0.06 Aug → 0.01 Sep; courtroom vocabulary zero through July, then rising; reply length 830 → 1,652 chars). The cross-channel history feature shipped 2026-02-28, so it is not the August trigger alone; August is also the heaviest memory month (529 rows), and the character's preset history (model change?) is unchecked. Phase 1 = a cache-neutral `<voice_anchor>` before the current turn (`PromptBuilder.ts`, targets in the theme doc). The one-off curve script is stored under `docs/local/handoffs/` and copied into `scripts/analysis/` to run (workspace packages resolve only from there; run with `pnpm ops run --env prod --force tsx <path> <slug>`).
- **beta.218 CUT 2026-09-05** — `doc-97` Phase 1 MERGED as #2348 (the `voice_anchor` V-tier section; TASK-900 filed for the catalog-wide size measurement); the Phase 2 directive text is drafted in the theme doc for the owner to apply; validation is smoke item 4 below. `docs/proposals/backlog/memory-archive-format.md` ACCEPTED 2026-09-05 (four-model council + owner pass). Owner steps outstanding: the `WEEKLY_AUDIT_GH_TOKEN` secret (TASK-898 closes on the first run with it), the `TZUROT_RAILWAY_API_TOKEN` in the local `.env` plus the two `AUTO_DEPLOY_COMMANDS` deletes (TASK-62 closes on that run). TASK-880 stays a prod watch.
- **Memory-archive build (`doc-8`) COMPLETE and LIVE**: slices P/A/B1/B2/C1/C2 + the commitment fact type shipped 2026-09-06→07 (#2349–#2352, #2356–#2358; detail in the PR bodies, the design in `docs/proposals/backlog/memory-archive-format.md` §8, residue in TASK-901/902/904/905/906/910). Owner rulings: commitment facts prompt-level only; retrievals stamped. **Rollout state 2026-09-13**: the enqueue and model switches ON on dev and prod; Emily listed in the prod render switch; her 644-row pre-warm enqueued on prod (`pnpm ops memory:summarize --env prod --personality emily-tzudad-seraph-ditza --dry-run` reads the ≥95% gate; 21.1% at 15:45Z, 4 dead rows to classify); the other characters flip one at a time as their gates read READY (D3, per character on purpose: the summary arm loses ~18 points of correctness to verbatim, so a healthy character pays that for nothing). The privacy-policy z.ai summaries bullet landed 2026-09-13. Dev's stray C2 probe job removed.
- **Real-messages flip (prompt-assembly Phase 2) — ON in prod (log-confirmed 2026-09-08) and dev (owner-confirmed 2026-09-13).** What remains is the doc-17 READING: the multi-day gap-bucket sweep across prod deployments (agent-run; the first 11-hour read was too thin — one `<2 min` turn, 30 in 2–10 min at 36% hit, 23 beyond 10 min at 56%). The dev before/after baseline was SKIPPED by owner ruling 2026-09-13; the snapshot review (the voice harness on Emily, third-person self-reference as the guard) has no pre-flip baseline; the fresh-thread probe (2026-09-13) showed the anchor holding with cross-channel history off and failing with it on, so the review is no longer gated but still lacks the baseline the skip removed — owner call whether it runs at all. Rollback is the same setting back to false, live.
- **beta.219 watch list**: the retrieval stamp PASSED (log-closed 2026-09-08, 974 retrievals, zero `Retrieval stamp tail rejected`); the commitment-facts review is TASK-907 on the owner queue.
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low (used 2026-09-09 → 09-10 for the drain; back on Fable 2026-09-10 evening at ~11% daily headroom against the 14.3%/day pro-rata). TASK-540 (ruled 2026-09-12: 503) and TASK-559 (ruled 2026-09-13: filter against gates before sampling) are both drain-ready.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped**; **second run 2026-09-09 ~20:05Z (owner-approved via AskUserQuestion): 5 purged (1 grace-expired, 4 never-used, 0 characters), 0 skipped, 1 warning DM enqueued** (`retention:notify` prompts and exits 13 from a non-TTY shell without `--force`, unlike purge — folded into TASK-926) — now 187 users; 2 in grace, 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. **Phase 4 SHIPPED in beta.222 (2026-09-11, autonomy ON by owner ruling; kill switch `RETENTION_AUTORUN_ENABLED=false`). First autonomous prod run 2026-09-11 13:39:37Z, 60 s after boot, mode live: attempted 0, purged 0, eligible 0, 1 warning DM sent (0 bounced), no halt, no breaker warning, report delivered — TASK-926 CLOSED on that observation.** Runs hourly; the 23 h cooldown gates the next live one. **Reminder DM shipped 2026-09-11 (#2395, TASK-331; `retention_reminded_at` on dev, prod at the beta.223 premigrate): one reminder at day 23–29 of grace; the first prod reminder cohort can form on or after 2026-10-04.**
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
- **#2379 (TASK-920 Done, beta.221)**: the compound self-header leak is closed by `leadingSelfHeaderLineMatcher`, name-scoped; six review rounds, six real defects, all pre-merge. Residuals TASK-921 (name divergence, medium) and TASK-923 (Done 2026-09-12 via #2400); the durable direction, match the rendered header string rather than guess from the roster name, is recorded on both tasks.
