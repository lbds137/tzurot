# Current

> **Version**: v3.0.0-beta.174 (released 2026-07-23 ~00:15 UTC) — **the Wave-3 breaking-rename major-ping beta**: 15 command renames/moves + 5 option renames + fresh mode replacing focus mode + `/admin`·`/deny` picker-hiding (#1747–#1751, #1753); Edit button on `/character view` (#1755) + `/help commands`/`getting-started` split (#1750); fixes — Mistral limit/offset pagination + MP3 MIME alias (#1752), deferUpdate-clobber ack-tracking (#1756), admin-section leak (#1755); branded `IsAdmin` guard (#1757), per-env help branding (#1754); security — transitive protobufjs 7.6.5 + body-parser 2.3.0 overrides (#1759) + `pnpm ops security:advisories` tooling (#1760); crypto.randomInt random pick (#1762, retired a CodeQL false-positive). No migrations. Prod verified healthy (bot logged in as Tzurot, 20 commands re-registered globally, ~1h propagation lag). Holistic review approved twice; the 2 dependabot advisories from beta.173 are FIXED (#1759) and auto-close on the next main rescan. _Prior: beta.173 (2026-07-20 — vocabulary + enforcement)._

---

## Unreleased on Develop

- **#1764** — retention epic **Phase 1a**: `User.lastActiveAt` + `dmUndeliverableSince` (additive migration, applied to dev) + `pnpm ops retention:backfill-last-active` (dev backfilled **271 users**; idempotent re-run verified). No behavior change — the write paths land next (1b forward activity stamp via the gateway `getOrCreateUser` cache-miss seam; 1c undeliverable stamp on 50278/50007 + clear-on-reach). CPD baseline 1750→1762 (env-scoped-op preamble clone; extraction filed as a follow-up). Prod backfill sequenced AFTER 1b deploys — see `active-epic.md`.

## UX Epic — Phase 3 IN FLIGHT (2026-07-20 → )

**Phase 3 plan APPROVED via plan mode** (`~/.claude/plans/radiant-tickling-candle.md`) — 6 parallel census agents + trio council. Train + per-PR log: `cold/epic-log.md` § Phase 3. **Waves 0–2 ✅ RELEASED (beta.173)**: PR-0 #1740 · PR-1 #1741 · PR-2a #1743 + PR-2b #1744 · PR-3 #1745. **Wave 3 — the breaking rename batch — ✅ RELEASED (beta.174, 2026-07-23, six PRs)**: PR-4 #1747 (`/settings preset`→`/preset override`) · PR-5 #1748 (grammar sweep + selector registry + manifest-walk invariant test) · PR-5b #1749 (`/chat` + `/random` extracted, avatar/voice `set|clear` groups, turn engine → `services/character/characterTurn.ts`, placement rule doc) · PR-5c #1750 (`/help commands` + `getting-started`) · PR-5d #1751 (owner walkthrough + council trio: `/chime-in` extracted, `/preset set-default|clear-default` to root, `/history hard-delete`→`purge`, perms `'0'` on /admin+/deny, description ride-alongs) · PR-5f #1753 (`/memory focus`→`/memory fresh`) + the release ride-alongs #1754–#1762. 15 old→new rows shipped. **NEXT: Waves 4–6** (PR-6a factory core+pilot → 6b destructive preset → 7 /deny redesign → 8 remaining picker hygiene → 9/10 factory sweep + router adoption) as normal minors; post-batch class slices now schedulable (burn-down trigger fired). Command-propagation lag (~1h) is expected in prod post-release.

**Release-blast "transient failure" mystery SOLVED (owner screenshot)**: the recurring `0 sent, 1 transient-failed` on every dev blast is the KNOWN Rotzot quarantine (Watches below) surfacing as `discord-20026` on createDM — runtime-confirmed in dev logs (prewarmer hits the same 403). Not transient, not new; the classifier gap is filed as a Quick Win (bot-level class, sibling of the shipped 50278 fix).

**Wave-3 owner smoke IN PROGRESS (2026-07-21, phone/remote)** — exercised so far: `/voice voices purge` (typed-phrase flow ✅; output surfaced a REAL bug), `/character voice set` (surfaced a second bug), `/chat` in an activated thread (works; raised a design question). **Both bugs fixed same-session — #1752 MERGED to develop** (2 review rounds, non-blocking only): (1) both Mistral voice-list walkers paginated with `page`/`page_size` — response-only fields Mistral ignores as request params → same window every request, 10 voices duplicated into a 200-entry list, purge reporting 190 spurious 404 "failures", and the false `truncated` flag REFUSING new Mistral voice clones; now documented `limit`/`offset` + dedupe + no-new-ids guard, bulk-clear 404s count as already-gone. (2) `voice set` rejected a normal MP3 tagged `audio/mpeg3` (client-claimed MIME alias); alias family now normalized to `audio/mpeg` at intake. **Post-deploy re-checks**: purge again (expect clean "Deleted N"), re-upload the same MP3, then a NEW character speaks on Mistral (clone-refusal gone — was blocked by the false truncation). Remaining smoke: per the scratchpad checklist (history purge, /preset override + set-default, avatar/voice groups, /random, /chime-in, /help, Tier 2–3). Design threads from the smoke: slash-turn/activation parity (owner directive, filed in `now.md` Untriaged) + incognito/focus redesign (SHIPPED — PR-5f, below).

**PR-5f ✅ MERGED: #1753** (2026-07-22, 2 rounds; r1's two semantic asks — `Promise.allSettled` account-delete sweep + status-query Zod parse — owner-approved "both", r2 "Nothing blocking found") — `/memory focus` → `/memory fresh` (owner-decided via trio council + AskUserQuestion; owner's condition: copy makes the meaning unmissable — "memories are kept, just not used" repeated on every surface). Fresh moves OFF the config cascade onto Redis TTL sessions mirroring incognito: `fresh:` prefix, `IncognitoSessionManager` generalized to `MemoryModeSessionManager`, shared status/enable/disable handler factories (`forget` stays incognito-only); duration choices (30m/1h/4h/forever) + `"all"` scope; BOTH statuses are filterable overviews (discharges the walkthrough follow-up row — struck); settings-dashboard focus knob REMOVED (persistent boolean contradicts timed sessions); JSONB focus flags DROPPED not migrated (needs the Breaking Changes line — release-table row staged in scratchpad); `MemoryRetriever` fresh-gate runs BEFORE persona resolution (seam-pinned); 🌱 footer ships, legacy 🔒 footer strip kept for old history. No Prisma migration (Zod strips stale JSONB keys). **Merge note**: codecov's backend was down/stuck (~1h, report never posted) — owner-approved merge on 25/25 checks green ×2 full runs. Character-owner memory config (council: separate control plane) filed in `cold/ideas.md`. **New smoke items**: fresh enable → 🌱 footer on a chat turn → status → disable; incognito status with the character filter.

## UX Epic — Phase 2 ✅ COMPLETE (2026-07-17 → 2026-07-20)

**Board promotion executed** (7591b0395): Platform-Portable UX Layer = active epic (beta-exit gate); memory overhaul parked → `cold/themes/memory-system-overhaul.md` (re-entry triggers recorded); epic-log reset; facts-quality Untriaged item routed to the parked theme.

**Phase 2 plan APPROVED via plan mode** — `~/.claude/plans/radiant-tickling-candle.md` (grounded by 3 code agents; trio council GLM 5.2 · Kimi K2.7-code · Qwen 3.7 Max, all folds/rejections recorded in the plan). **Owner calls locked**: global aliases bot-owner-only · reverse shadow warn-don't-block (global rows only) · resolver stays names-first + ⚠️ shadowed badge in owner's own browse. PR train: 1a/1b (confirmation+router) → 2 (browse builders) → 3a (alias migration+resolver) → 3b (gateway CRUD+policy) → 4 (alias pilot UI) → 4b (browse retrofit sweep, committed) → 5–9. Key artifact drift found at grounding: 4 confirmation impls (not 3), 11 modal sites (not 9), D14 already shipped for settings, Components-V2 + Label-modal API all present in installed discord.js 14.26.5.

**PR-1a ✅ MERGED: #1697** (2026-07-17, 3 review rounds, r3 "nothing blocks merge") — `utils/confirmation/` Tier A+B factories; Cancel→Danger fixed at all 4 sites; history+voice migrated; **live bug fixed**: `/voice voices clear` typed confirmation was silently dropped (modal routed to /settings via config-rebuild — now the modal customId DERIVES from the button's own customId, killing the class). Review arc worth remembering: r1 caught my invoker-in-customId design overflowing Discord's 100-char limit (owner-approved pivot to `interactionMetadata`-based ownership); r2 caught my PR body mis-stating deploy-boundary behavior (corrected); r2's escapeMarkdown nit applied. Follow-ups filed: Tier-B fail-open ephemeral-only guard (check at PR-1b); pre-existing 30–50-char-slug customId overflow. ux:literals 101 (−1; develop sat AT the 102 ceiling pre-merge).

**PR-1b ✅ MERGED: #1698** (2026-07-17, single clean review "looks ready") — `createComponentRouter` (ordered declarative tables, kind-aware match, acked unrouted fallback), memory's chain adopted, purge + account-delete onto Tier-B (wire-contract phrases untouched via override; 2 duplicate invoker-guards + 4 raw `split('::')` sites retired; purge's uncapped-label throw fixed in passing; retired prefixes out of componentPrefixes, manifest regenerated). Fail-open follow-ups row checked: both sites ephemeral, trigger not fired. PR-7 scope grew: settings' third chain + its no-ack fallthrough (review obs), action-cast note.

**PR-2 ✅ MERGED: #1700** (2026-07-18, 3 rounds) — shared browse list-embed builder (absolute numbering owns the select-menu invariant; §2.1 titles; D19 empty states); character+preset adopted; filter-row builder honestly CUT (no consumer existed). r2's review caught a real stacked-empty-state regression (fixed+pinned); r3's minors → PR-4b scope. **Side-quests same day**: #1699 weekly-audit hygiene MERGED (per-metric security degradation, cpd step w/ isolation guard, broadcast verb; alias vocab warning deliberately left as the epic's tracker) · DuplicateDetectionFlow P2002 flake recurrence filed as Quick Win · dev autocomplete outage diagnosed (suspected zombied ws, restart-cured, shard-observability follow-up filed) · adm-zip dependabot triaged (transitive via onnxruntime, not user-reachable, Quick Win).

**PR-3a ✅ MERGED: #1701** (2026-07-18, 2 rounds; r2 full-pass "no blocking issues" — verified tier fallthrough, cache cross-user isolation, migration safety) — `user_id` nullable FK + lower() partial uniques (hand-SQL, drift-ignore, PGLite regen), per-scope uuid seeds, two-lookup resolver (personal→global, access filter each step; `resolveAliasUserUuid` kept separate so the bot-owner bypass doesn't disable the owner's own personal aliases), PGLite invariant component test (distinct-explicit-id trick proves the INDEX rejects, not the PK seed). **Dev migration applied** (clean — index creation itself proved no case-dupes). r1 codecov red = missing direct unit test for the new uuid fn (added, autosquashed). **PR-3b ride-alongs filed in epic-log**: single user-row lookup in `loadFromDatabase` (hot-path double round-trip) + collapse the `scope` union param to `userId: string | null`.

**PR-3b ✅ MERGED: #1702** (2026-07-18, 2 rounds; r2 traced the full auth model — "none of these block merge") — tiered gateway policy (visibility-gated verbs, bot-owner-only globals, user-scope default so the transitional UI writes personal rows), my-aliases endpoint with per-caller shadowed marking, reverse-shadow ⚠️ advisories in create/rename/import (shared sender + `CATALOG.info.warning`), both #1701 ride-alongs (single-lookup resolver seam-pinned; scope-union collapse), codegen `z.input` request bodies. r1's mutation-floor red was real (identity 77.25 < 77.48 floor) — killed with 2 targeted tests (→78.24), no baseline edit. Follow-ups filed: shadow-probe scale bound, own-personal-alias rename warning, DELETE scope array-param 400. No migration.

**PR-4 ✅ MERGED: #1703** (2026-07-18, **1 round, clean** — review verified select-value identity against the DB indexes, deferral traces, per-handler 3s compliance; "no blocking issues") — **THE PILOT SHIPS**: `/character alias` → subcommand group (browse + Tier-0 add; action-multiplex + retype-remove deleted). Two browse modes (per-character / my-aliases with ⚠️ shadowed), **first in-place filter toggle** (all→mine→global coordinate-flip button — owner call 4 delivered), select→Tier-A-confirm→explicit-tier-remove with footer-carried state (newline-alias round-trip pinned), module-internal `createComponentRouter`, autocomplete widened owned→visible, gateway `?scope=` array-param 400 ride-along. Session hygiene note: the develop-commit guard (#1696) fired FOR REAL mid-build (PR-4 was staged on develop — blocked, branched, committed properly), and the temporal-marker hook caught PR-ref archaeology in doc comments. Both structural fixes earned their keep in one PR.

**PR-4b-1 ✅ MERGED: #1707** (2026-07-18, 3 rounds) — `filterRowBuilder` extracted from the pilot (nextFilter cycle + coordinate-flip toggle button); character (all→mine→public), preset (two per-axis toggles — the multi-dimensional DECISION: cycle buttons over filter select), deny (retrofit + 3-value toggle), channel (retrofit + owner-gated toggle; browseHelpers extracted) adopted; always-render rule on filter-bearing browses; #1700 r3 double-blank-line fix. r2/r3 caught two real bugs of mine (zero-activation crash made reachable by always-render; non-owner dead toggle).

**PR-4b-2a ✅ MERGED: #1709** (2026-07-18, 4 rounds, r4 "no blocking issues") — six no-filter surfaces onto the builder: apikey/shapes/voice-voices/persona/models/inspect (freelance titles + colors normalized, escapeMarkdown sweep incl. review-caught voice slug, techId backtick-strip hardened in the builder + voiceId metadata guard, models empty/no-match tests). Round-4 minors routed: badge legend 👁️/🎨 gap + pagination-silent-failure toast → 4b-2b riders (follow-ups row).

**SAME DAY — 🚨 prod db-sync incident diagnosed + fixed (#1708, merged)**: see Unreleased above. Manual prod unblock (delete doomed row `13656360-…0001`) offered, NOT authorized — moot at next release.

**PR-4b-2b ✅ MERGED: #1710** (2026-07-18, 3 rounds, r2+r3 both "no blocking issues") — builder `serverPage` mode (the #1700 r3 pre-flight trigger FIRED: memory family is server-paginated; search's rolling-window synthesizes totalItems, reviewer-verified algebraically) + memory browse/facts/search + admin/servers + overrideBrowse retrofits + riders (footer-on-empty suppression, code-point-safe truncation, models legend, channel-helper tests, tonos fix). r1 caught a REAL regression of mine (server-mode blank embed on a stale page past a shrunk total — fixed with empty-page degrade + regression test); stale-page source fix (route the 3 pagination handlers through fetchPageWithEmptyFallback) filed in follow-ups. **PR-4b IS COMPLETE — every browse surface renders through the shared builder** (census 17/17 accounted: 4b-1 four + toggles, 4b-2a six, 4b-2b five + the two delegators on overrideBrowse). Remaining browse-domain item on the board: the pagination-silent-failure toast decision (follow-ups, catalog-level).

**PR-5a ✅ MERGED: #1711** (2026-07-19, 2 rounds, both "no blocking issues") — `utils/modal/toolkit.ts`: typed `ModalItem` union (text/select/radio/checkboxGroup/checkbox/fileUpload + non-interactive display blocks), Label-based `buildToolkitModal` with D15 inline-docs descriptions, kind-aware `extractSubmission` (skips display per council note), text validation; every builder/getter probed against installed discord.js 14.26.5, build tests gate payloads via `toJSON()`; 100% line coverage. Exemplars adopted: character/create + preset/create (incidental fix: preset now respects `field.style`). r2's hardening trio (catch-narrowing, shared safe-truncation, split watch) filed in follow-ups, promote at the factory migration.

**PR-5b ✅ MERGED: #1714** (2026-07-19, 1 round, "no bugs found") — ModalFactory→Labels flip (owner's create-modal smoke was the gate; signatures/customIds unchanged, one test-helper edit total) + hardening trio (a–c struck from follow-ups; (d) extended with the reviewer's convergence note — live path still reads via extractModalValues' blanket catch, converge at first non-text consumer). **Owner smoke suggestion**: any dashboard section edit on dev (e.g. `/character edit` → section) — same fields, Label-styled.

**PR-5c ✅ MERGED: #1716** (2026-07-19, 3 rounds, r3 "no bugs found") — the D15 retry affordance: `utils/modal/retry.ts` (button + message-id-keyed Redis stash + prefilled re-show through the timeout-catch wrappers — the ack-first lint rule caught my first draft's bare replies), shared `replyWithModalRetry`, builder↔guard drift pins with REAL customIds, closure seam tests. r3's transient-failure counter-argument filed in follow-ups (promote at the site-family wave). **Owner smoke**: `/character create` with slug `Bad Slug!` → error → Try again → fields intact.

**PR-5d ✅ MERGED: #1718** (2026-07-19, 2 rounds, r2 explicit "approve") — the site-family wave: all 7 remaining hand-rolled sites migrated (memory facts-correct + edit, deny detail-edit, apikey set, shapes auth, persona create + override-create via the shared field builder → toolkit `TextModalField[]`); **the modal wave (④) is COMPLETE — zero hand-rolled `ModalBuilder` sites remain** (grep-verified, reviewer-confirmed). Retry swept to the persona family (stash `meta` carries the override customId's personality UUID; rebuild uses generic title — name unrecoverable at submit time, filled fields hide placeholders); transient-catch retry extension shipped (row struck); deliberate non-extensions documented (paste-fields; outcome-uncertain memory writes). Tier-B fail-open + non-text-validation triggers checked: NOT fired, stay parked. r1 codecov 5-line gap closed via final-round one-push (override builder null-branch + unmapped-failure tests → 100% lines; my first assertion guessed the classifier's copy wrong — the classifier surfaces the gateway's own message for fail-arm 500s). **Owner smoke**: `/persona create` with a blank name → Try again → fields intact; any deny/memory/apikey/shapes modal opens Label-styled.

**PR-6 ✅ MERGED: #1719** (2026-07-19, 3 rounds; r3 "no blocking issues") — `buildEntityDetailCard` in `utils/detailCard.ts` (G5): zero-callback plain-data scaffold (title/color/footer caller-composed — four divergent footer shapes made any callback the wrong abstraction), fields as slots with null/false skipping + `'spacer'` sentinel (deny's grid), cap+notice as a type-forced pair, code-point-safe truncation provably ≤ cap with a returned flag (memory's View Full driver). Six sites retrofitted output-identical — all 19 pre-existing embed tests passed untouched. **character/view DECLINED** per the 2-callback ceiling (PAGE_BUILDERS = page-select + truncation-track + expand-config; recorded in module JSDoc — the plan's "multi-page variant" idea dies with it). Ride-along: voice/view catalog stragglers fixed (ux:literals 101→98). **r2 caught a real bug of mine**: both new voice classifier calls omitted `operation: 'read'` → a network blip would render write-uncertainty copy ("your change may still be applying") on a pure read; verified against classify.ts + 52 read sites, fixed + kind-aware test. Notice-vs-cap dev assertion filed in follow-ups.

**PR-7 ✅ MERGED: #1720** (2026-07-19, 2 rounds, both "no blocking issues") — character + settings chains onto `createComponentRouter` (memory was already the reference; grounding corrected the plan's stale "migrate memory" line). Character's edit dashboard = deliberate `unrouted` fallback, existing routing tests passed UNMODIFIED; **settings' silent no-ack fallthrough fixed** (the PR-1b review observation — unrouted now acks via `replyValidationError`, three per-kind pins added; old tests asserted the silent behavior and were rewritten into pins). r1's leave-it-better item taken: preset-override + account-delete routing pins built from REAL `DestructiveCustomIds` (drift pins), character alias-delegation + browse-pagination pins. **D14 CLOSED**: all generic dashboards counted (max 5 vs ~6 bar), threshold documented on `DashboardConfig.sections`. Settings `execute()` action-casts examined, deliberately unchanged (codebase-wide mixed-mode idiom — cross-command sweep, not a rider). Reviewer verified: route order 1:1, no double-ack risk, `settings::` auto-prefix routing.

**PR-8a ✅ MERGED: #1724** (2026-07-19, 3 rounds; r2×2 + r3 all "no blocking issues") — **D16 SHIPS: "Inspect Message" context menu**, the bot's first context-menu command. Infra: `defineContextMenuCommand` (minimal, dispatcher defers EPHEMERAL before execute), separate CommandHandler registry, `isMessageContextMenuCommand` dispatch branch (such interactions were silently dropped before), type unions widened, manifest gains `contextMenuCommands` (deploy needed zero changes), knip root-command glob. Reuses `/inspect`'s lookup (targetId snowflake → by-message→by-response fallback — either side of a conversation resolves the same log) + summary render; all errors via catalog. **r1 caught a REAL latent bug I runtime-confirmed**: the command scan had no `.test.ts` exclusion and `inspectMessage.test.ts` was the first root-level colocated test — importing it outside vitest throws, which would have ABORTED the whole `deploy-commands` run; fixed + probe test. r1's codecov red closed with CommandHandler dispatch tests (defer-before-execute, InfraError→transient, typo guard). Model-substitution flag verified ALREADY LIVE in the summary embed (residual item satisfied pre-existing). **Owner smoke**: right-click any bot reply → Apps → Inspect Message → the `/inspect` summary appears ephemerally.

**PR-8b ✅ MERGED: #1725** (2026-07-19, 3 rounds; each strictly smaller — the round-cap convergence working as designed) — **PR-8 / spec §6 COMPLETE**: 📥 Input view (sectioned chunked text, absent sections omitted), 🎛️ Generation Params (named knobs when set + allParams inline-or-defer measured on the RENDERED string), 🔀 Post-Processing (raw vs final with "changed nothing" collapse), and `sendChunkedReply` `maxChunks` + complete-content overflow-attachment tail (reasoning/input/post-processing cap at 3; legacy callers untouched). All views reachable from both `/inspect` and the context menu. **r1 caught a real escaping gap of mine** (transcript + searchQuery inlined raw — searchQuery carries user-typed text; fixed + backtick-laden pin test). r3's `DISCORD_LIMITS.EMBED_FIELD` constant nit DISMISSED as pure-aesthetic convergence tail (rides the next inspect edit). Model-substitution residual verified already-live pre-slice. **Owner smoke**: `/inspect` a recent message → 9-view select; Input + Post-Processing are new; View Reasoning on a long-thinking reply stops at 3 chunks + 📎 tail.

**PR-9 ✅ MERGED: #1726** (2026-07-19 late evening, 3 rounds; r3 "no blocking findings") — **THE V2 PILOT SHIPS; Phase 2's slice list is COMPLETE.** Council-shaped renderer swap (trio pass recorded in the plan file): `viewV2.ts` behind a `USE_COMPONENTS_V2` kill switch (embed path intact, NO runtime fallback — a V2 failure is pilot data), page-0 header Section+avatar Thumbnail (public /avatars URL, encoded), per-field Section-accessory 📖 expand, customIds byte-identical, redacted single-Container variant. **Two real bugs of mine caught across rounds**: r1 — avatar gated on `avatarData`, which `toCharacterData` ALWAYS nulls on reads, so the thumbnail could NEVER render (the §7 seam-bug class: my tests bypassed the real coercion; fixed via `hasAvatar` + a seam test through the real `toCharacterData`); r2 — pagination's 404 branch edited flag-less `content` onto a V2 message → Discord rejects, catch swallows, user sees NOTHING (and the reviewer's own suggested fix was insufficient — V2 forbids `content` entirely; fixed with `buildViewV2Notice` + flag/content assertions). r3 non-blocking: V2 total-text-budget check absorbed into the owner eval (maxed-out synthetic character is already in the protocol); export.ts avatar-URL encoding nit → follow-ups row. **OWNER EVAL NEXT (mobile-first, council protocol, on dev)**: thumbnail scaling + tap targets, separators, accent in light/dark, null/broken avatar, redacted variant, page-flip flag persistence, maxed-out character. Kill switch = one-line revert if the verdict is thumbs-down. Parked riders now filed as follow-ups rows: alias add-from-browse (design-first), inspect attachment metadata (ai-worker plumbing).

**Same night — #1727 hooks cwd fix MERGED** (owner-spotted the Pre/PostToolUse error spray): all 8 `.claude/settings.json` hook commands now `cd "${CLAUDE_PROJECT_DIR:-.}" &&`-wrapped. The cwd-relative invocation silently SKIPPED every guard whenever the shell sat in a package subdir — a protection gap, not just noise. Runtime-probed both halves; review "no changes requested" (its one nit — my PR body overstating the ack-file claim — corrected pre-merge); takes effect at session start, and this session already saw the fixed invocation firing post-merge. **#1728 session-mining skill IN REVIEW** (owner-requested): proceduralizes the mined-corpus friction audit as `/tzurot-session-mining` (extract → mine → synthesize → operationalize; privacy boundary; the violated-rule-vs-missing-rule fork); review = 2 non-blocking asks pending owner call (skill-eval regex word-boundary false-positive; hardcoded-path fallback note). **GitHub platform outage rode through the night** (~00:20–01:55Z: REST 503s; claude-review's self-validation failed closed into green no-op skips on 3 runs — reruns required post-recovery; the "green ≠ posted" rule earned its keep twice).

## beta.167/168 post-release state (2026-07-17)

**Everything in the runbook executed**: premigrate --allow-destructive (both migrations) → merge #1686 → release:finalize (develop SHA-aligned) → prod backfill (107 stamped, 269→major, 4 opt-outs untouched) → prod anchor cleanup (10 deleted) → release:publish → webhook blast **enqueued recipients=107, batches=3** (gate live-verified; beta.166's incident number was 268). GitHub's multi-webhook burst (new release + beta.166 demote) was absorbed by unique-version idempotency — exactly one enqueue.

**Remaining post-release items**:

1. ~~Delete the three one-off scripts~~ ✅ **DONE — #1691 merged 2026-07-17** (all prod runs confirmed; `prisma format` rider included).
2. **Owner website steps**: prod website service exists; tzurot.org/docs now live — verify on prod domain. **Identity verification ✅ DONE 2026-07-17 — BOTH dev and prod apps verified** (legitimacy arc closed; ToS/privacy URLs were among the verify page's required criteria, so they're necessarily set on both apps). Legitimacy umbrella entry removed from the board 2026-07-17 after verification sweep; only the quarantine appeal (watch below) remains open.
3. ~~Blast completion tally~~ **LANDED 04:54 UTC**: `sent=81 failedPermanent=0 failedTransient=26 optedOut=0` — zero closed-DM permanents/auto-disables; all 26 "transients" are discord-50278 (recipient left every shared server — durable, misclassified; Quick Win filed to reclassify as permanent). 81 of 81 REACHABLE deliberate users delivered. Ops embed should be in the feedback channel — owner eyeball pending.
4. beta.166 demote fired extra webhooks — confirmed absorbed; no action.

## Next Session Goal (sequenced — fresh session starts here)

0. **`services/website` (Astro, tzurot.org) — ✅ SHIPPED #1663 2026-07-16** (+ #1664 docs-placement audience-check rule). Brand-per-env (`SITE_BRAND`: dev=Rotzot / prod=Tzurot), hero + embed cards, `docs/legal` glob-loaded single-source, nginx runner, docker-build-smoke leg, eslint-plugin-astro coverage. Legal docs finalized IN EFFECT (2026-07-16) incl. the AI-training disclosure (system OpenRouter key: all four training/publish toggles OFF, verified; z.ai API terms: no training absent explicit agreement; BYOK = user's own settings + shared-channel disclosure). 3 review rounds + 1 infra-flake rerun (SIGABRT in packages coverage — rerun green). **OWNER NEXT (the working line): ① Railway dev env → new service `website`** (repo branch `develop`, Dockerfile `services/website/Dockerfile`, service settings mirror bot-client's; vars: `SITE_BRAND=rotzot`; check `RAILWAY_PUBLIC_DOMAIN` reaches the BUILD — graceful cosmetic fallback if not) → **preview the Rotzot site on the generated domain (phone OK)** → design feedback round if wanted → ② release cut (explicit approval; carries #1653–#1664) → ③ prod service `website` (branch `main`, `SITE_BRAND=tzurot`) + `railway domain tzurot.org` + Squarespace DNS → ④ ToS/privacy URLs on BOTH Discord apps → ⑤ identity verification ✅ **BOTH APPS VERIFIED 2026-07-17**. Website-polish follow-up pair filed (health-block headers, fs.allow narrowing — next site touch).
1. ~~**Prod fact-timestamp repair (the #1644 release step)**~~ ✅ **DONE 2026-07-14 (owner-approved)** — 21,493 facts rewound to evidence time (12,588 were >6mo skewed — the backfill signature); post-run dry-run confirms 0 remaining. Release checklist closed.
2. **beta.165 smoke (risk-derived, three items)** — (a) one normal reply in a long-history channel (#1645 pre-pass main path), (b) one recall probe about something old (proves the dedup-hole fix surfaces past-truncation content and isn't over-filtering), (c) one multi-tag message (#1642 coordinator touch). Owner drives; results land here.
3. **1b next**: slice B (read-side dup collapse) is recommendation-DON'T-BUILD pending the owner's felt-repetition re-measure now that slice A + the dedup-hole fix are in prod; correction detection and the other redirect deferrals sit in `cold/follow-ups.md` with promote-when triggers. Nothing to build until the re-measure speaks.

**Watches**: **dev-bot Discord DM quarantine (340002 'Access to sending DMs has been limited')** — all dev DM sends blocked as of 2026-07-15 00:15Z incl. persona DM replies; leading hypothesis (timing-correlated, unproven): boot-time createDM prewarm sweeps — ~117 channel-opens × 10+ deploys in one day from a 1-server bot, the classic spam-prep pattern even without sends (structural fix SHIPPED: #1650 `OUTBOUND_DM_ALLOWLIST`, set on dev 2026-07-14); now surfacing as app-level 20026 — **appeal FILED by owner 2026-07-15** (support ticket: names the prewarm-without-sends behavior, the dev-deploy amplification, NO MESSAGES SENT, and that the logic bug is fixed) → **appeal DENIED 2026-07-22** (canned reply from Discord Developer Compliance; did not engage with the zero-messages-sent / dev-bot / already-fixed facts — the Rotzot application is PERMANENTLY unable to join new guilds or DM users it hasn't messaged before). **The dev-bot quarantine is now terminal, not pending.** Practical impact is dev-only: prod Tzurot is a SEPARATE application, never quarantined (the bug was caught in dev before prod), and the `OUTBOUND_DM_ALLOWLIST` + prewarmer fix protect prod from tripping the same heuristic. The casualty is **dev outbound-DM testing** — the clean restore path is a FRESH dev Discord application (quarantine is per-app; a new token/app is unencumbered — cost: re-register commands, swap dev env token, re-invite to the dev guild). Until/unless that happens, dev persona-DM replies and dev broadcast smoke stay unavailable; verify those paths on prod (allowlist-guarded) instead. The "1 transient-failed" in every dev blast report IS this quarantine (classifier Quick Win still open — its copy should now read "bot quarantined (permanent)"). **Owner leaning 2026-07-22: delete & recreate Rotzot as a fresh dev app** (waiting on Discord's next reply first, but inclined to proceed — the underlying bug is fixed so it won't recur). When executed: new app + token → set dev bot-client `DISCORD_TOKEN`/`DISCORD_CLIENT_ID` → re-register commands (`AUTO_DEPLOY_COMMANDS` or manual deploy) → re-invite to the dev guild → set `PUBLIC_SITE_URL`/`SITE_BRAND=rotzot` if the new app needs them → re-run the PR-2 broadcast smoke with a fresh label. Owner sequencing decision pending: legitimacy work (ToS/privacy at tzurot.org via GitHub Pages, verification, appeal) BEFORE the release carrying the first prod DM blast — when dev persona DMs work again, re-run the PR-2 broadcast smoke with a fresh label · first prod extraction log post-beta.165 (`valid_from` should stamp source time, not run time) · first deploy-orphan/safety-flush event (#1642 runtime proof — `remainingBudgetMs` + "found a real job outcome" lines) · owner's felt-repetition re-measure (gates 1b slice B + correction detection) · ~~prod voice-engine volume seed~~ ✅ **VERIFIED 2026-07-17** (log sweep: 4 cache-hit wakes that morning, ~50s boots from `/app/voices/model-cache/`, zero downloads — HF board entry removed) · db-sync probes (clean runs accumulating) · prod lock-storm · retention-failure · ~~session-start hook compact branch~~ ✅ **VERIFIED 2026-07-17** (mid-session compaction fired the recovery checklist). **beta.164 voice smoke still open**: one voice message + one long voice reply when convenient. Fable access through July 19.

## Last Session — beta.168 + secrets arc + alias management (2026-07-17)

Full-day arc: `.claude/` deep-dive + board verification sweep → the complete secrets audit-to-machinery arc (R1 dev/prod key split hash-verified; staged BYOK rotation #1690/#1692; R2 executed — prod was running the DEV z.ai key, owner found and fixed; DEFAULT_VOICES removed) → **v3.0.0-beta.168 shipped** (8 PRs, 2 additive migrations premigrated, prod rotation ledger seeded, blast 1/1/0 — minor-level consent model verified) → `@Lila` mystery solved (v2-migrated `personality_aliases` resolve mentions invisibly; writers deleted Nov 2025) → owner picked **v2-parity as next epic** → `/character alias` built + **MERGED #1695** after 5 review rounds (r1 caught a private-character enumeration oracle in my shadow check; r4 caught the missing routing-cache invalidation — both real, both fixed) → **#1696 develop commit guard MERGED** after its own 5 rounds (r2 caught my sed heredoc-strip deleting the entire canonical commit command — probe harness now pins 17 shapes). Misses to own: the alias feature was originally committed straight to develop AND pushed (recovered via revert + proper PR; #1696 is the structural fix), a wrong-branch amend folded hook files into the alias commit (soft-reset recovery, nothing wrong pushed), and two round-cap stretches consciously taken for reviewer-verified bugs. Compaction-recovery hook verified live mid-session.

**Next session**: promote **Platform-Portable UX Layer** → `active-epic.md` (owner re-sequenced 2026-07-17 after the alias UX deviated from 04-discord standards: "no more UX surface until consistency is by construction"; v2-parity PAUSED). Design artifacts already accepted 2026-07-04; the alias redesign + scoping tiers (global bot-owner / user-scoped — recorded in the theme's pilot section) is the pilot surface. Owner smoke of `/character alias` ✅ DONE 2026-07-17 — works, UX critique filed.

_Older session logs live in git history._
