# Current

> **Version**: v3.0.0-beta.208 — "telemetry consumers + carried fixes": doc-12 FULL P1 tier (#2222 telemetry:report · #2223 usage attribution + telemetry:inference + privacy rider · #2224 weekly export-path smoke) + four carried fixes (#2218 MUTE denial copy · #2219 transcript-reply retarget · #2220 invalid-model classify/veto/rescue-reporting · #2221 hard-failure delivery) + dependabot deps (#2209/#2216) + mining operationalization (#2214). 10 PRs / 8 runtime / 137 files. TWO additive migrations premigrated BEFORE the merge (`add_usage_attribution`, `add_usage_personality_index`). Merged 2026-08-26 00:35 UTC; finalize SHA-aligned develop; tagged + published (latest). Holistic review: no blocking findings (dug the byok×quota-fallback seam independently). Prod deploy: new-build verification via the export-smoke scheduler boot line — see watches.
>
> **Previous**: v3.0.0-beta.207 — "observability + invalidation hygiene" (18 PRs / 16 runtime / 245 files, 2026-08-24). doc-12 P0: command telemetry + error-channel reporter + boot watchdog.

---

## 🚀 beta.208 SHIPPED (2026-08-26) — post-deploy state

Full session arc 2026-08-25→26: four carried fixes → owner picked doc-12 P1 and expanded to the full tier → P1.1/P1.2/P1.3 shipped → cut approved → premigrated → merged 00:35Z → finalized → published. **Prod new-build verified live 00:35:38Z** (shard ready + `export-smoke` scheduler registered — the new code's own boot line). All nested-dispatch; review process caught two Mediums + one HIGH across the feature PRs (byok-stale-across-retarget, sentinel-scoping oracle, zero-count blind spot) — all fixed pre-merge.

**Watches (log/data-signal, no action):**

- **First prod export-smoke run: PASSED 00:36:58Z** (`Export-path smoke passed`, 80s after boot) — the real export pipeline validated end-to-end in prod on first fire. Weekly cadence after; silent = pass, 🧯 owner embed = failure.
- `usage_logs` attribution columns (`latency_ms`/`byok`/`personality_id`) now populating in prod — `pnpm ops telemetry:inference --env prod` becomes meaningful after ~a day of rows; discoverability report likewise accumulating (`telemetry:report`).
- `model_not_found (rescued)` ⚠️ embeds = a delisted-model persona self-healing (GLM now.md entry clears on first observation).
- Error channel: first prod posts should be real system errors only; TASK-754 misdating stop; boot-watchdog standing.
- Carried question watches: glm-5.2 median-4000 maxTokens saturation (owner lever: per-persona maxTokens); Qwen `data_inspection_failed` background rate (owner lever: per-personality visionModel).

**Mining posture: re-test DONE (2026-08-26 run, Fable-driven)** — delegation ratio moved 71/29 → 53/47 (Fable window) / 62/38 (Opus window); both #2214 hooks passed their first live window (details + dispositions in the machine-local mined-corpus README). Capacity figures ADOPTED on two agreeing readings: ~800M all-models / ~180M Fable per week (our weights) — the Fable bucket binds, and the remaining lever is Fable-window DURATION, not delegation ratio. Next mining delta starts after 2026-08-27 01:01Z.

`ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 📋 Open items (near-term)

- **beta.209 in progress (Opus-driver week)**: theme **doc-78 DM context isolation SHIPPED** (#2227, 2026-08-26) — enum `shareHistoryAcrossPersonalities`, default `always`, no migration. Its review established that history is THREE reads (stored · live-channel · reply-references), not one; the live read is dropped in isolated DMs only, reply references are an owner-approved exception. Both decisions recorded in tracker doc-78. **#2228 SHIPPED** (TASK-767 Done) — closed four defects of one class: the reported spurious timeout, the same shape in 4 more recovery classifications, `PENDING_DELIVERY` rows orphaned by every discard path, and a failed BullMQ poll reading as `inFlight` (which re-opened the original bug through a Redis-blip door). Each was found by a DIFFERENT mechanism — incident report, sibling sweep, review, and chasing a worker's own flagged doubt — and no mechanism found more than one; that is the argument for keeping every layer. Follow-up coverage nit deferred at the 6-round review cap: TASK-775. **#2229 SHIPPED** (drain filler, TASK-598) — root `turbo.json` `lint` now declares `@tzurot/tooling#build`. The task's three hypotheses all blamed turbo's cache; the actual actor is tooling's own build script (`rm -rf dist && tsc`) racing lints that had no ordering edge to it, since `dependsOn: ["^build"]` covers a package's dependencies and almost nothing depends on tooling. Runtime-confirmed both ways: `tooling#build` was absent from bot-client's lint graph entirely, and with dist deleted turbo now schedules the build first. CI's lint job was never exposed (it builds in a separate step — reviewer's catch, verified); the exposure is local/pre-push/worktree only. **TASK-598 deliberately stays open** — the worktree-push acceptance clause isn't directly exercised; close it after a stretch of worktree PRs with no hit 10.

Also from this drain pass (tracker notes pushed, no code): TASK-653 is a **partial** — the merge hook's re-derive banner is fixed text, not the per-claim extraction its acceptance wants. TASK-673's blocker is **resolved** — Stryker 10's `--mutate` takes a line range and `--allowEmpty` gives silent-pass; measured 35 mutants/71 lines in 8.1s (~4s fixed), so diff-scoping is ~2 orders of magnitude off the 30-70min whole-package figure. Caveat recorded: measured in config-resolver (small, already configured), NOT tooling (no config, 172 test files) where the defects actually lived. TASK-547/520 were already Done since 08-12 — the sibling lists in TASK-669/673 are stale and now say so.

**#2230 SHIPPED** (drain batch, TASK-612 + TASK-613) — `reconnectStrategy` is node-redis's option name; ioredis reads `retryStrategy` and ignores it, so the bounded give-up policy never applied in any of three paths (including one neither task recorded: `createIORedisClient` never forwarded the option to the constructor while its doc comment claimed it did). DELETED rather than renamed — renaming would newly introduce a permanent give-up, which is worse than the default unbounded capped-backoff retry for a long-running service. Took 129 lines of tests with it that pinned "gives up after max retries" against a mechanism that could not fire; one canaried assertion replaces them. Both bare `new Redis(url)` cache clients now use the factory. **TASK-613 Done; TASK-612 deliberately open** on its runtime clause only (invalidation subscribers still deliver) — close-when trigger on the task. Review flagged `REDIS_TIMEOUT_ANALYSIS.md` as prescribing the exact deleted pattern as "Priority 1: Critical"; deleted as a diverged planning doc, inbound links repointed at the live constants, its one unbuilt idea filed as **TASK-776**.

**#2231 SHIPPED** (owner request) — diagnostic-log retention **24h → 7d** (`RETENTION_HOURS = 7 * 24`). The window is a published commitment, so `docs/legal/PRIVACY_POLICY.md` moved with it (prose + retention table + last-updated), and the constant's comment now names the policy file so the two cannot drift silently. **Owner decision: the beta.209 release notes MUST state the change** — recorded in `now.md` § Deploy notes. **Owner also reversed doc-73's "Closed: no widening"** (2026-08-14) after the review surfaced it pre-merge; reversal recorded in doc-73 with the reasoning — its "25×" contradicts its own measurement (8.5MB→60MB is ~7×, negligible), and its "goes stale in hours" clause is the weak link, since the reports needing deep diagnostics arrive late. Four stale tracker refs swept; three flagged ones deliberately untouched as unrelated 24h values.

**#2232 SHIPPED** — `10-working-posture.md` § "Boards are snapshots" gains the direction that had no tripwire: before changing a documented parameter, search the tracker for a prior DECISION, not just the code. Four review rounds for 16 lines, each finding real: a non-runnable command, a missing `--plain`, doc-73 mislabelled "theme doc" when it is an idea doc, and a search scope narrow enough that following the rule literally could produce the false negative it exists to prevent. Rules budget 2220/2273 lines.

**#2233 SHIPPED** (TASK-764) — `/deny` is scope-first: `add`/`remove` are subcommand GROUPS, scope is the subcommand (`everywhere` · `this-server` · `channel` · `character`), `browse`/`view` stay flat. `type` is gone; each subcommand takes `user:` (native picker) and, on `everywhere` only, `server:`. The old runtime guard that a server denial is bot-wide only is DELETED because the combination is now unrepresentable. Three review rounds, none blocking: r1 found the group-dispatch seam untested (a reversed branch passed all 6672 tests) and that the diff escaped markdown on the user branch but not the free-text `server:` one; r2's `scope: string → DenyScope` tightening immediately broke `typecheck:spec` on a test passing `'INVALID'`, surfacing that the guard is unreachable from typed callers — kept it, cast deliberately; r3 caught two promises I made and did not keep. **TASK-764 stays OPEN on its first acceptance clause** — "owner completes the flows without consulting docs" is experiential and not agent-verifiable. Close-when + the naming question (`everywhere`/`this-server` are the agent's words, not the owner's) recorded on the task.

**#2234 SHIPPED** (mining P1) — `pr-body-ref-gate.sh`: PreToolUse gate on `gh pr create`/`pr edit`/`ops gh:pr-edit` verifying claim-verb tracker refs ("filed as/closes/tracked in TASK-N/doc-N") resolve on origin/develop; fail-open on infrastructure, 25-case canaried probe. Six review rounds converged High→nit (prose `--body-file` abort, lookbehind anchors, chained-id tail, `Closes:` colon, per-id verify output); final three nits parked as TASK-781 at the round cap. Blocked its own test command as a live TP during development.

**#2235 SHIPPED** (owner call at the GLM eval) — `glm-5.3-flash` joins the z.ai coding-plan catalog as its FIRST vision-capable member (docs live under guides/**vlm**/, 1M ctx, launched 2026-08-26). The stale "z.ai = text-only" premise swept from SIX sites across 4 packages (reviewer found 3, the dispatch found a 4th, round-3 nits closed the rest); bot-client's `/models` vision view + api-gateway resolution + ai-worker degraded-mode pattern all now read the entry's own flags, with adversarial both-sources pins that OpenRouter stays authoritative. **Free default UNCHANGED** — model is a day old, zero persona-chat evidence, "notably slow and very verbose" flag; owner probes live at leisure. GLM research summary: cheaper on every path (coding-plan included, ~3x quota vs full 5.3); z.ai has an auth-gated `/models` endpoint — drift-check material only, catalog metadata stays hand-curated.

**Misses worth carrying** (all caught by machinery, none by me):

1. Changed a documented parameter without searching the tracker for a prior DECISION — wrote "storage unmeasured" into a PR body when doc-73 had measured it twelve days earlier, and nearly shipped a silent reversal of a closed decision. Now a rule (#2232).
2. Read an empty SHA-pinned run query minutes after a push as "the review workflow never dispatched", filed TASK-777 on that premise, and it was simply late. `05-tooling.md`'s own not-indexed-yet warning was quoted earlier the same session. Task corrected; the true finding is better — `gh:ci-gate` can fire CI_COMPLETE before the review run exists, because a not-yet-created run is not "in flight".
3. Claimed in #2233's body that a follow-up was "filed" when it was not (now TASK-779) — a PR body is a tracked surface and that was a false claim on it.
4. Marked TASK-764 Done at ship and reverted it within the minute. Shipping code does not satisfy an experiential acceptance clause.
5. (2026-08-26 night, Fable) Swept a falsified premise by PROXIMITY, not by grep — "z.ai = text-only" survived in 4 more sites until a reviewer and a dispatch found them. The sweep rule already says grep the tokens; re-read is what I did instead.
6. (Same night) Stale counts hit durable surfaces twice more (a "18 cases" body claim aged across rounds; a test count off by the round's own addition) — both machine-caught. The class is now consistently reviewer/gate-caught, never self-caught; R1's harder-gate question accumulates evidence at the next mining pass.

Remaining for the release: the owner's deny-UX taste round on TASK-764/765, then drain slices as filler.

- **Usage posture (through the Sun 02:00 ET reset)**: Fable meter over trajectory at the 2026-08-25 21:02 reading (53% vs ~37% pro-rata; all-models 41%) → **the Opus-driver backup lane is the standing driver for routine work this week**; Fable reserved for beta.209 design/verification passes. Durable operationalization SHIPPED in #2226 (flip trigger = `/tzurot-usage-audit` § Step 4a, batching/compaction economics in `10-working-posture.md`).

- **Retention: FULLY caught up 2026-08-26** (owner-approved): 11 never-used accounts purged, then the last pending warning DM **bounced** (DMs closed / left every shared server) → that user flipped to unreachable and was purged too. Userbase 212 → 200; 0 eligible, 0 awaiting DM, 7 in grace (~09-08 expiries are the next retention event).
- **Housekeeping**: BOTH large session JSONLs are now fully mined and are disk-space deletion candidates — `015f3cbc` (152MB, mined through 2026-08-17) and `0059bca0` (147MB, session ended 2026-08-24, mined to end). Owner's explicit okay still needed. (The previously-listed `3f50da50.jsonl` was already deleted 2026-08-16.)

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog composition: ~393 open per the 2026-08-23 digest (state split last measured at the ~321 mark: 107 `ready` · 97 `observable` · 57 `dependent` · 45 `owner` — only ~1/3 agent-drainable); filing rate (not staleness, measured ~3%) drives growth. **Themed batches over per-task PRs** (4-5 closes per cycle vs 1). Pre-grounded remainder: 199 solo (bare-run HEALTH_TOOLS shape; its batch-mates 200/349/457 shipped — verified Done in tracker 2026-08-23); held for owner: 527 (copy), 540 (HTTP contract), 559 (pick), 531 (process design); TASK-599 = the batched owner-decision pass over `state:owner`. Query, do not browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (all one shape — a check that can't distinguish measured-fine from didn't-measure)**: publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green (check other probes for `2>/dev/null` in invoke paths).

**Open rule-shaped gap, not yet drafted**: five of #2097's last six defects entered _while fixing a previous finding_ — every countermeasure fires at authoring time, none at correction time. Wants a council pass (TASK-531 adjacent).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. Judge preview: A-vs-B 5/5/8, B-vs-B′ control 8/18 (the pre-registered under-power flag may trip — read the noise-floor line before trusting a PASS). Artifacts backed up (`voice-consistency-2026-08-04.tar.gz`). **Owner's leisure — do not re-surface as a recommendation.** The #1317 cluster + TASK-165 stay gated behind it.
- **Opus trial record**: TASK-513 (owner decision surface) + TASK-487 (Sonnet-tier evidence ledger). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness spinoff; license recorded), doc-65 (private brain), doc-66 (message coalescing), doc-67 (tag-scoped sharing), doc-70/71/72 (tag mgmt/dashboard nav).
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (beta.191 threads; bullmq/ioredis v6 pending, dependabot-ignored until then) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528, key by personaId) fix shapes recorded.
- **Retention is calendar-only**: books as of 2026-08-09 — 221+ users, 5 in earlier grace (~08-26 expiries) + 2 warned (~09-08). Phase 4 autonomy parked BY DESIGN. Dev nag now silenced by #2120's prod gate.
- **doc-78 — DM Context Isolation: DESIGN SETTLED + GROUNDED** (enum `shareHistoryAcrossPersonalities`, default `always`; no migration). Full design + 7 registration sites + runtime evidence in tracker doc-78.
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time — owner's catch); a concrete argument on doc-8's FOR side.
