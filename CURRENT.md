# Current

> **Version**: v3.0.0-beta.214 — "vision attribution + custom fetch everywhere + refusal retarget + channel dashboard scope + process batch" (14 PRs / 7 runtime / 160 range files, no migrations, merged 2026-09-03 ~01:12Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.213 — "gateway watchdog + cache-invalidation single-flight + identity floors + drain batches + vision fallback fix" (16 PRs / 13 runtime, 2026-09-02 ~02:53Z).

---

## 🌙 2026-09-04 (morning) — beta.215 slate closed: the last drain item, the TASK-880 diagnostic, and a ref-gate race (Fable-driven, nested dispatch)

**Merged: #2322** (TASK-642 Done — `grep-escaped-dollar-guard.sh` blocks a double-quoted backslash-dollar grep pattern; the mechanism turned out to be grep-ENGINE dependent: the host's `/usr/bin/grep` is ugrep, where the incident reproduces live, while GNU basic mode matches the eaten form by accident — measured in the dev container; four review rounds, each a real gap with a canary: quote-scoped `-F` scan, git global options, brace form + heredoc-body strip via the shared helper gated behind a substring check, a bare `&` and group openers; 32 probe cases; residue TASK-885) · **#2323** (TASK-880 step 1 — the custom fetch logs body-arrival timing and named parse failures with `bodyChars`; the clone hypothesis was falsified by a standalone probe (8 MB in 48 ms) and the task is now a watch the next prod occurrence reads directly; one review round). **Open: #2324** (TASK-884 — the PR-body ref gate lost a resolvable id to a SIGPIPE race under pipefail, diagnosed with an instrumented copy at 1 in 60 and reproduced deterministically in the probe at 144 KB; round 1 is the class sweep across every pipe-fed `grep -q` in the pipefail hooks). Tracker: TASK-877 closed on #2322's claim/canary set (the first unit from the amended template); TASK-884/885 filed and grounded; the driver call (Fable until the reset) is in memory.

**Honest ledger.** The first TASK-642 dispatch stopped on my L1 premise because it measured GNU grep while my shell was ugrep — both right for their engine, and the spec now carries the split. My TASK-884 spec fixed the two incident lines without the class sweep the remediation skill asks for first; the reviewer caught it. The ref gate blocked me three times before I had the mechanism, and I dismissed the SIGPIPE hypothesis twice on idle-trial evidence before instrumenting the hook itself. One lossy-pipe trip (a `tail` on a push), one pipe-guard trip on a `gh` read, and my own `cd` into the scratchpad broke a chained branch creation once.

## 🌙 2026-09-03 (late) — Opus-window verification + mining run + two process PRs (Fable-driven)

**Verified the Opus window on live state** (#2319 merged, probe 65/65, tracker filings, board) — clean; one footnote: CI on the merge SHA was cancelled by the doc pushes a minute later, the next run green. **Effort research (Anthropic docs, fetched):** Fable 5.1 stays `high` (documented default); **Opus 5 `medium` → `high`** — medium was an unmeasured carry-over, the docs say start at the default and step down on evals; the NEXT Opus window is the sweep (compare spec-defects/dispatch and rounds/PR against tail6b). `settings.json` + statusline updated. **Session mining (tail6, 44 h, three driver windows):** zero owner friction (4th consecutive clean set), zero worker defects to develop, spec origin dominant under both drivers; the concrete mechanical difference between the windows was ledger coverage — 30/30 Fable dispatches via spec file vs 3/23 Opus (inline prompts the gate skipped by design). All six dispositions shipped: **#2320** (ledger gate covers inline dispatches; phantom `pnpm --filter` scripts and wrong-branch base SHAs block; context hook mutes post-compact; two skill lines — SEVEN review rounds: cap fired, round 6 to a fresh-context implementer who rewrote the phantom walk pnpm-correctly, round 7 escalated to the owner → merge + **TASK-883**) and **#2321** (TASK-877 items 1–4; clause 5 stays open on the task). Closed TASK-495 (shipped as the context hook). Two product-feedback drafts queued (`/feedback`): stale gitStatus injected into every subagent; SendMessage-resume of a worktree-removed agent runs in the main tree.

**Honest ledger.** Rounds 1–2 of #2320 were my defects (a control-flow gap in the spec; a bypass my own inline allowlist introduced) and rounds 5–7 descended from round 2's fix — the self-fed shape; the cap and the fresh reader did their jobs. The new gate blocked three of my own dispatches, all correctly (twice for a phantom quoted as an example, once by exhibiting the round-6 bug on my prompt). One lossy-pipe trip, one commitlint subject-case trip, one broken heredoc fallback, one stale "700k context" claim repeated from a pre-compaction hook banner (the owner caught it; R3 fixes the hook).

## 📋 Open items (near-term)

- **beta.215 cut next**: every waiting-on line on the board is satisfied (TASK-642 shipped #2322; TASK-880 step 1 shipped #2323 and is a watch); #2324 (TASK-884) is the one open PR — cut after it merges, or without it if it stalls. `release:range` before #2322/#2323: 10 PRs / 3 runtime / 58 files. The Opus effort sweep runs on whichever unit the next Opus window takes — mine it with the effort-tag lens.
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 (after a surprise weekly reset) — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped** — now 188 users; 2 in grace (~09-08 expiries are the next event), 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. Phase 4 autonomy parked BY DESIGN.
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
