# Current

> **Version**: v3.0.0-beta.165 (released 2026-07-14) — the memory-epic must-fix release: **STM/LTM dedup hole closed** (#1645 — history-first allocation, exact shipped boundary, ID-authoritative dedup filter; epic review item 8 SHIPPED), multi-tag deploy-orphan wedge fixes (#1642 — safety-flush re-poll, original-deadline re-arm, TTS audio TTL), fact `valid_from` = evidence time (#1644 — 1b redirect slice A), committed 1b weight-sim runner (#1643). Release review: LGTM, zero blocking. _Prior: beta.164 (2026-07-13 night — HF outage + voice close-out day)._

---

## Unreleased on Develop

- **#1662** `fix`: review-nit batch 3 — **MERGED 2026-07-16** (3 rounds; r2 caught a real facts-leak in my memoryLimit-0 fix, r3 LGTM). Four fixes: `memoryLimit: 0` now actually disables LTM (was silently retrieving 10 via the query-builder default; skip also suppresses facts via personaId omission, focus-mode parity); atomic `deleteMany` prune (`Prisma.AnyNull` for the JSONB slice, PGLite-proven, `satisfies` tie to `PersonalityConfigSlices`); export enqueue-failure now marks the row failed (was stuck-pending 409ing 24h; stored message sanitized — shapes list route returns it verbatim); `atLimit` telemetry snapshots pre-filter. Follow-up filed: ai-worker job handlers still write raw error.message into the user-visible errorMessage field.
- **#1661** `fix`: #1658 cache-eviction follow-ups — **MERGED 2026-07-16** (2 rounds, both LGTM). TOCTOU generation guard on `UserService` cache writes (in-flight read racing a deletion can't re-populate the dead id); `invalidateAll`/`'all'` wired to new `clearCache()` (the "no bulk-evict API" comment was wrong — `TTLCache.clear()` existed); broadcast-failure catch tested + blast-radius documented. Follow-up filed: api-gateway doesn't subscribe to its own broadcast (matters only multi-replica).
- **#1660** `refactor`: #1659 follow-ups — **MERGED 2026-07-16** (LGTM r1). Memory defaults compiler-tied to `AI_DEFAULTS` (`typeof` derivation, can't drift); dead `crossChannelHistoryEnabled` bot-client plumbing removed (ai-worker re-resolves from configOverrides; `/settings` write path untouched).
- **#1659** `refactor`: source memory/context settings from the config cascade, not `LlmConfig` columns — **MERGED 2026-07-16** (4 review rounds, all LGTM; reviewer independently re-verified the dead-code claim against ConfigCascadeResolver). Retires the 5 vestigial `LlmConfig` columns (`memoryScoreThreshold`, `memoryLimit`, `maxMessages`, `maxAge`, `maxImages`; KEEP `contextWindowTokens`) — removes every read + write across all 4 services; the cascade (`configOverrides`) is the sole source. `MemoryRetriever` → `configOverrides ?? AI_DEFAULTS` only (prod baseline == AI_DEFAULTS 0.5/20, so behavior-identical dead-code removal). Surfaced + fixed two real bot-client gaps (owner-approved): (a) `/character chat` never resolved the cascade → ignored user/channel LLM overrides — now resolves it via a new shared `chatConfigResolution` helper both chat.ts and `PersonalityChatManager` use (dedup); (b) the slash path (`/chat`/`/random`/`/chime-in`) bypassed the denylist + NSFW age-gate the message pipeline enforces — new `slashChatGates` + extracted `evaluateNsfwGate` close it. Net −256 lines; new colocated tests for both new modules; all tiers green locally. **The physical `DROP COLUMN` is a release-gated follow-up** (`cold/follow-ups.md`): run AFTER this ships to prod → no maintenance window (expand/contract).
- **#1658** `fix`: cross-process user-cache eviction on account deletion — **MERGED 2026-07-16** (4 rounds, all LGTM). The initial fix evicted only api-gateway's `UserService` provisioning cache; claude-review correctly flagged ai-worker's two independent long-lived instances (`PrismaContextDataSource`, `LLMGenerationHandler`) still hand back the dead `discordId→userId` within the ~1h TTL and FK-violate on the usage-log insert (same class as the export_jobs P2003). New `UserCacheInvalidationService` (Redis pub/sub) broadcasts eviction after the synchronous local one; ai-worker subscribes and evicts its shared instance; `getOrCreateUserService` lifted from `AuthMiddleware` into `@tzurot/identity` so every process shares ONE instance per PrismaClient (registry invariants tested against the real WeakMap there). Regression guard per 02 §7: `delete.component.test.ts` drives the real shared `UserService` over PGLite (delete→export→**202**), verified RED (500) with the eviction neutered. **Follow-ups filed** (`cold/follow-ups.md`, r4 non-blocking): in-flight-read TOCTOU re-populate race, untested broadcast-failure catch + 1h-TTL blast-radius comment, `invalidateAll()` no-op/`TTLCache.clear()` wiring.
- **#1657** `fix`: account-export hygiene (PR-A) — MERGED 2026-07-15. Export now carries the personal config-cascade defaults (`configs/user-defaults.json` ← `users.config_defaults`) + the superuser's global `AdminSettings` (`account/admin-settings.json`, metadata-only, no secrets); all-null `UserPersonalityConfig` anchor rows are filtered from `configs/personality-overrides.json` AND pruned on every clear-path (`pruneEmptyPersonalityConfig` at the 6 slice-nulling sites) so set-then-clear stops leaving dead rows. Additive, no migration. **Follow-ups filed** (`cold/follow-ups.md`, 2026-07-15): PR-B dead-column drop (destructive, maintenance-window release), one-off empty-row cleanup (dev now / prod counts+go), deleteMany-atomic prune optimization, channel-settings empty-anchor scrutiny.
- **#1656** `feat`: `/feedback` intake (epic PR-4 — also the legitimacy gate's contact channel: legal docs will name `/feedback` + GitHub issues). Gates cheap-first in api-gateway (Zod 2000-cap → 300s Redis cooldown, armed only post-store → 5/day attempt-counting cap → normalized-sha256 dedupe over 7d with the composite index PR-1 deferred); genuinely fail-open on Redis errors (r1 review caught my fail-closed divergence from the approved plan — fixed + test-pinned). Accepted submissions post ONE silent embed (`allowedMentions: parse:[]`, escapeMarkdown'd) to the new optional `FEEDBACK_CHANNEL_ID`; unset = store-only. New `/help` Feedback category. **Dev migrated 2026-07-15.** 2 rounds; r2 LGTM "ready to merge". Dedupe-TOCTOU accepted gap filed with observe-trigger.
  **PR-4 dev smoke: COMPLETE 2026-07-15 (owner screenshots)** — ① `FEEDBACK_CHANNEL_ID` points at the **Tzurot Log THREAD** (owner-confirmed — bot→thread send live-proven, same surface as the weekly audit webhook) ② ephemeral confirm + silent owner embed landed, @mention rendered as pill WITHOUT pinging (parse:[] live-verified) ③ resubmit at +2min → "try again in 177 seconds" (exact remaining-time copy live-verified). ④ archived-thread auto-unarchive verifies organically on the first post-archive submission (benign failure: row stored + warn-logged; one-line explicit-unarchive fix if it ever fails).
- **#1655** `feat`: `/settings data delete` (data-rights PR-B) — full-account erasure via the purge-pattern handshake: impact preview (counts + per-owned-character cross-user reach) → typed `DELETE MY ACCOUNT` modal → single-use `acctdel_` token → ONE deferred-constraints transaction (60s budget; client outwaits via LONG_SYNC). `user.delete()` cascade + the three sweeps the FK graph can't express (case-insensitive entity-tag facts across ALL scopes, both pending_memories arms, diagnostic logs by discordId); parallel post-tx cleanup (incognito, personality caches, avatar unlink). Superuser 403 at preview/token/in-tx. Zero-residue PGLite proof + route-level handshake component test. 2 review rounds: r1 caught client-timeout(30s) < tx-timeout(60s) — the db-sync false-failure class, worse here (single-use token) — owner-approved LONG_SYNC + parallel cleanup, `LONG_SYNC_EXEMPT` ratchet extended with its decision/trigger pairing documented; r2 LGTM (independently verified the deferred-FK correctness — `users.default_persona_id` is the schema's only RESTRICT FK and is already DEFERRABLE). Exact-match tag-vocabulary gap filed in follow-ups (disclosure belongs in the legal-docs finalization pass).
  **PR-B dev smoke (owner-driven, after dev deploy — use a THROWAWAY ALT account, never your main)**: on the alt: `/settings data delete` → danger embed shows counts + owned-character reach + export nudge; Proceed → modal → type the phrase → success embed with deletion summary; then message any character on the alt → expect a fresh empty account (no memories, no personas). On YOUR account: `/settings data delete` → expect the superuser 403 ("bot owner… cannot be deleted"). **Masking**: none — the deletion is real, which is why the alt. Report: pass/fail per step + whether the warning embed copy reads right.
- **#1654** `feat`: account export v2 — ZIP with per-section JSON+Markdown files (owner-directed after the 80MB single-JSON smoke). fflate ZIP stored in new `export_jobs.file_data` BYTEA (additive migration — **dev migrated 2026-07-15**); layout: README (counts/definitions/exclusions) + profile/personas/characters as json+md pairs + conversations/memories/facts foldered by character slug **including unowned characters** (personality-directory lookup fetches name/slug only, `unknown-<id8>` fallback) + configs/account JSON-only (shapes-mappings added so export map = deletion map). Riders absorbed: 24h cooldown off completedAt (failed exempt; bot's 409 shows the live link), errorMessage off the status wire (server-log-only), status enum. Review r1 LGTM zero blocking; two cosmetic observations folded into the smoke below (markdown escaping fidelity, channel-section ordering).
  **Export v2 dev smoke (owner-driven, after dev deploy)**: `/settings data export` → embed describes a ZIP; download → opens as a valid ZIP; README counts/definitions read right; open one `conversations/<slug>.md` → transcript readable (day headers + `[HH:MM]` clock times are a judgment call — say if the style or channel ordering reads wrong; formatter-only fix); personas/characters files present; size sanity vs the 80MB baseline (text zips well). **Masking**: a 409 with your PREVIOUS export's link + "once every 24 hours" copy is the new cooldown correctly seeing your old completed export — wait out the window or ask to clear the dev row. Report: pass/fail + how the transcripts read.
- **#1653** `feat`: `/settings data export` (data-rights PR-A) — full-account JSON export riding the shapes-export spine (export_jobs sentinel `sourceService='account'`, ai-worker `JobType.AccountExport`, public 24h download URL). Every section cursor-swept (nothing silently truncates); secrets/embeddings/binaries excluded and DISCLOSED in meta.notes, pinned by a PGLite test asserting seeded secret values never appear serialized. 2 review rounds, both LGTM; r1 caught my swallowed-retry bug (handler never re-threw, so BullMQ's 3 attempts were dead config — fixed to shapes-helper semantics) + the silent 1000-row caps (all sections now sweep).
  **PR-A dev smoke (owner-driven, after dev deploy)**: `/settings data export` → expect an embed with a download link; open it → JSON with profile/personas/characters/history/memories/facts sections and `meta.notes` listing exclusions; search the JSON for your API key material → MUST be absent (metadata only). Re-run the command while pending → status embed instead of a new job. Report: pass/fail + anything odd in the payload.
  **Owner dev smoke result: export WORKS but 80MB single JSON was user-hostile → SHIPPED as #1654 (export v2, above). PR-B `/settings data delete` SHIPPED as #1655 (above) — the data-rights pair is complete; the privacy policy's ⏳ export/delete clauses flip true at the release that carries these.**
- **#1652** `fix`: retrieval queries no longer embed prompt scaffolding — the reference formatter's search text was tag-stripped from its own formatted XML, so instruction boilerplate + dedup-stub placeholders rode into every reply-shaped memory/fact embedding query (runtime-observed in the owner's debug export). Formatter now returns `{formatted, searchText}` built from raw pieces; content-less stubs contribute nothing. **Note for the 1b felt-repetition re-measure: post-release, reply-shaped retrieval behaves differently (cleaner queries) — treat pre/post as separate measurement regimes.**
- **#1651** `feat`: release pipeline (epic PR-3) — GitHub `release:published` webhook (HMAC over path-scoped raw body, public section) + hourly reconcile sweep (ai-worker cron `:41` → internal route; 24h lookback, 3-per-run cap, `lookbackHours≤168` manual lever) + changelog classifier/formatter (Breaking→major, Features→minor, else patch via shared `RELEASE_LEVEL_SECTIONS`; DM = title + curated sections + release link under the 1800 cap). Review: LGTM round 1, zero blocking.
  **PR-3 dev smoke — EXECUTED 2026-07-15 03:18Z, pipeline PASS / DM blocked by quarantine**: manual reconcile with `lookbackHours:168` returned `{checked:11, announced:["v3.0.0-beta.165"], skipped:10, capped:false}` — the demote-to-prerelease interplay correctly made only the newest release announceable across real GitHub data. Worker consumed the batch, classified Discord's still-active quarantine correctly (`code="discord-20026"` → transient, no auto-disable), ledger recorded it, announcement completed (`updated=1 completed=true`). Boot logs also show the #1650 allowlist live: prewarm `before=117 after=1`. **Remaining unverified: the literal DM landing** — blocked on the quarantine (now surfacing as 20026, appeal via dis.gd/app-quarantine). When it lifts, the beta.165 row is already terminal, so the DM re-smoke needs a fresh version: next release's reconcile, or an `/admin broadcast` with a fresh label.
  **PR-3 prod deploy steps (ride the carrying release)**: ① MOVE `GITHUB_WEBHOOK_SECRET` to prod api-gateway (owner-approved: copy the verified 64-hex value currently mis-scoped on bot-client, then delete bot-client's copies; use THIS value in the GitHub webhook form at creation) + set `GITHUB_API_TOKEN` (fine-grained PAT, repo lbds137/tzurot, Contents: Read-only) on prod api-gateway ② at merge time verify no OTHER non-prerelease release published within 24h (`gh release list`) ③ merge release ④ DELETE the two dead v2 release webhooks first (`gh api -X DELETE repos/lbds137/tzurot/hooks/550691068` and `.../550693280` — both point at torn-down v2 Railway domains, same `release` event as the new pipeline), then add the GitHub repo webhook (payload URL `https://<prod gateway domain>/webhooks/github/release`, content-type application/json, secret, Releases events only; ping should show 200) ⑤ `release:publish` → **the carrying release self-announces to all opted-in users (~268; owner-decided 2026-07-15)** — write its release notes to LEAD with the announcement feature + `/notifications` opt-out.
- **#1650** `fix`: `OUTBOUND_DM_ALLOWLIST` — optional env var gating all three proactive-DM points (StartupDMPrewarmer, `/internal/users/recent`, broadcast recipient resolution at the SQL WHERE). Unset (prod) = unrestricted; set = fail-closed narrowing (garbage parses to empty Set, never falls open). **Set on dev (bot-client + api-gateway, owner's ID only) 2026-07-14** — the env-change redeploy was the first dev boot that doesn't sweep prod users' DMs. Structural fix for the 340002 quarantine. 3 review rounds, LGTM; r2 caught my hand-rolled snowflake regex drifting from the canonical schema.
- **#1647** `fix`: BullMQ worker lock 20min→5min — deploy-killed jobs stall-recover in ~6 min (real replies) instead of wedging to the 18-min flush. `MAX_JOB_RUNTIME` decouple keeps the job-timeout clamp at 20 min; `stalled`-event logging added (the deploy-orphan recovery trail). Runtime proof: first prod deploy that catches an in-flight job. Closes wedge sub-question (b).
- **#1649** `feat`: release-broadcast pipeline (epic PR-2) — gateway recipient resolution + announcement/ledger rows + batched `release-broadcast` queue; bot-client's FIRST BullMQ Worker (per-send ledger reporting, 50007 classification, 2-consecutive-permanent auto-disable, throw-before-spend/absorb-after-spend seam discipline); `/admin broadcast` with dry-run + `confirm:true` double-key (owner-decided). 4 review rounds — r1 caught my batch-report crash window, r2 caught my fails-closed comment describing behavior that didn't exist. Contract-test pair + per-producer topology keying shipped for the new cross-service seam.
  **PR-2 dev smoke (owner-driven, the designed test vehicle)** — all three via Discord on DEV:
  1. `/admin broadcast message:"smoke test" dry-run:true` → expect an ephemeral "dry run" embed with eligible count ≥1 (you) + username sample; nothing sent. (Invariant: resolution respects `/notifications` prefs; masking: none — dry-run writes nothing, rerunnable.)
  2. `/admin broadcast message:"smoke test" label:"smoke-1" confirm:true` → expect "Broadcast enqueued …1 recipient(s)" AND a DM within ~seconds containing the message + the `-# Manage these DMs with /notifications` footer, no pings. (Failure looks like: enqueued-embed but no DM → check dev bot-client logs for `Broadcast batch processed`.)
  3. Re-run step 2 with the SAME label → expect a clean "already announced — pick a different label" rejection, no second DM. (Invariant: unique-version double-send protection.) Report: pass/fail per step.
- **#1648** `feat`: notification-prefs foundation (epic PR-1) — full epic schema in two additive migrations (users.notifyEnabled/notifyLevel, release_announcements, release_delivery_log, user_feedback; first Prisma enums, userId FK indexes per convention) + `/notifications view|enable|disable|level`. **Dev migrated 2026-07-14.** No DMs sent until PR-3. **Prod ALREADY migrated 2026-07-14 (owner-run, additive-ahead-of-code) — no release step remains; prod sync warnings until next release are the known timing shadow.** 5 review rounds — r3 caught a real read-vs-write error-copy bug in view.ts; r4 rightly overturned the userId-index deferral.

## Release confidence-check (owner-requested, 2026-07-16 — do BEFORE proposing the cut)

After #1671 (release-DM cleanup) merges, the owner wants a **final confidence-check conversation**: full risk & coverage appraisal of the beta.166 release (per the release skill's §0 — risk level with basis, runtime-unverified paths + blast radius, smoke scope derived from the release diff, observability-instead-of-smoke offers). Constraint: **dev bot is DM-quarantined — no DM-path smoke is possible**; the appraisal must lean on suite coverage + prior epic-PR smokes (PR-3 pipeline pass 2026-07-15, feedback smoke, export/delete smoke) and name what only prod will verify (the literal DM landing + first webhook blast). Draft notes: scratchpad `release-notes-beta166-draft.md`.

**OWNER-APPROVED (2026-07-16): prod owner-first DM staging** — replaces the missing dev smoke. Sequence is load-bearing (allowlist gates eligibility at ENQUEUE; a completed announcement can never retro-widen — if the real release announces while the allowlist is set, the blast is permanently one-person): ① release merges → prod deploys, **NO GitHub webhook created yet** ② set `OUTBOUND_DM_ALLOWLIST=<owner discord id>` on prod api-gateway (redeploy settles) ③ `/admin broadcast` with a throwaway test label → owner verifies DM landing + footer + `/notifications cleanup` by hand ④ **CLEAR the allowlist** (redeploy settles; verify via `railway variables` AND an `/admin broadcast` dry-run showing ~268, not 1) ⑤ only then create the GitHub webhook + publish the release → real self-announce to all opted-in users. Bonus: the owner's test DM becomes their standing prior DM, so the real announcement live-verifies #1671's delete-previous path.

## beta.167 release runbook (post-blast-radius-incident release — 2026-07-16/17)

**Scope (owner-picked)**: #1679 blast-radius fix (merged; dev migrated + dev backfill RUN: 117 stamped / 269 total / 152 passive stay null) · #1678 Astro 7 (merged; clears the 10 default-branch alerts at release) · #1680 dep-latest rule (merged) · #1681 memory edge trio (in review) · DB-hygiene PR (column drop + empty-anchor cleanup script — this branch) · PR-5 reconcile hardening (next build, plan-mode).

**At the cut (order matters)**:

1. Release notes MUST disclose: everyone's `notifyLevel` reset to `major` (previously-customized users must re-run `/notifications level`), and the eligibility gate (only deliberate-use evidence gets release DMs).
2. `pnpm ops release:premigrate --allow-destructive` — carries the additive `notify_opted_in_at` migration AND the destructive `drop_vestigial_llmconfig_columns` (safe without maintenance: prod code post-#1659 never reads the dropped columns).
3. Merge release PR → prod deploys.
4. **Prod backfill** (`scripts/src/db/backfillNotifyOptedIn.ts`): dry-run first → show owner counts (expect: nonzero opted-out from post-blast `/notifications disable` runs) → explicit owner go → real run. Uses the flag-free wrapper trick (`ops run` eats bare flags).
5. **Prod empty-anchor cleanup** (`scripts/src/db/cleanupEmptyPersonalityConfigs.ts`): same dry-run → counts → owner go → run (owner pre-approved 2026-07-15, counts-first was the condition).
6. Delete both one-off scripts in a follow-up commit once prod runs are confirmed.
7. The release self-announces via the webhook — this blast is the runtime verification of #1679's gate (expect ~117-scale recipients, not ~269).

**Dev state**: notify migration applied + backfill run 2026-07-16 evening; column-drop migration applies to dev at this PR's merge (`pnpm ops db:migrate --env dev`), then dev run of the cleanup script.

## Next Session Goal (sequenced — fresh session starts here)

0. **`services/website` (Astro, tzurot.org) — ✅ SHIPPED #1663 2026-07-16** (+ #1664 docs-placement audience-check rule). Brand-per-env (`SITE_BRAND`: dev=Rotzot / prod=Tzurot), hero + embed cards, `docs/legal` glob-loaded single-source, nginx runner, docker-build-smoke leg, eslint-plugin-astro coverage. Legal docs finalized IN EFFECT (2026-07-16) incl. the AI-training disclosure (system OpenRouter key: all four training/publish toggles OFF, verified; z.ai API terms: no training absent explicit agreement; BYOK = user's own settings + shared-channel disclosure). 3 review rounds + 1 infra-flake rerun (SIGABRT in packages coverage — rerun green). **OWNER NEXT (the working line): ① Railway dev env → new service `website`** (repo branch `develop`, Dockerfile `services/website/Dockerfile`, service settings mirror bot-client's; vars: `SITE_BRAND=rotzot`; check `RAILWAY_PUBLIC_DOMAIN` reaches the BUILD — graceful cosmetic fallback if not) → **preview the Rotzot site on the generated domain (phone OK)** → design feedback round if wanted → ② release cut (explicit approval; carries #1653–#1664) → ③ prod service `website` (branch `main`, `SITE_BRAND=tzurot`) + `railway domain tzurot.org` + Squarespace DNS → ④ ToS/privacy URLs on BOTH Discord apps (names freeze post-verification — confirm final) → ⑤ identity verification. Website-polish follow-up pair filed (health-block headers, fs.allow narrowing — next site touch).
1. ~~**Prod fact-timestamp repair (the #1644 release step)**~~ ✅ **DONE 2026-07-14 (owner-approved)** — 21,493 facts rewound to evidence time (12,588 were >6mo skewed — the backfill signature); post-run dry-run confirms 0 remaining. Release checklist closed.
2. **beta.165 smoke (risk-derived, three items)** — (a) one normal reply in a long-history channel (#1645 pre-pass main path), (b) one recall probe about something old (proves the dedup-hole fix surfaces past-truncation content and isn't over-filtering), (c) one multi-tag message (#1642 coordinator touch). Owner drives; results land here.
3. **1b next**: slice B (read-side dup collapse) is recommendation-DON'T-BUILD pending the owner's felt-repetition re-measure now that slice A + the dedup-hole fix are in prod; correction detection and the other redirect deferrals sit in `cold/follow-ups.md` with promote-when triggers. Nothing to build until the re-measure speaks.

**Watches**: **dev-bot Discord DM quarantine (340002 'Access to sending DMs has been limited')** — all dev DM sends blocked as of 2026-07-15 00:15Z incl. persona DM replies; leading hypothesis (timing-correlated, unproven): boot-time createDM prewarm sweeps — ~117 channel-opens × 10+ deploys in one day from a 1-server bot, the classic spam-prep pattern even without sends (structural fix SHIPPED: #1650 `OUTBOUND_DM_ALLOWLIST`, set on dev 2026-07-14); now surfacing as app-level 20026 — **appeal FILED by owner 2026-07-15** (support ticket: names the prewarm-without-sends behavior, the dev-deploy amplification, NO MESSAGES SENT, and that the logic bug is fixed); awaiting Discord's response — when it lifts, re-run the PR-2 broadcast smoke with a fresh label. Owner sequencing decision pending: legitimacy work (ToS/privacy at tzurot.org via GitHub Pages, verification, appeal) BEFORE the release carrying the first prod DM blast — when dev persona DMs work again, re-run the PR-2 broadcast smoke with a fresh label · first prod extraction log post-beta.165 (`valid_from` should stamp source time, not run time) · first deploy-orphan/safety-flush event (#1642 runtime proof — `remainingBudgetMs` + "found a real job outcome" lines) · owner's felt-repetition re-measure (gates 1b slice B + correction detection) · prod voice-engine volume seed (first wakes ratchet it; remove the board's HF entry when prod `model-cache/` lands — check via `railway ssh -s voice-engine -e production` while awake) · db-sync probes (clean runs accumulating) · prod lock-storm · retention-failure. **beta.164 voice smoke still open**: one voice message + one long voice reply when convenient. Fable access through July 19.

## Last Session — the dedup-hole + release day (2026-07-14)

Release v3.0.0-beta.165 cut and shipped (4 substantive PRs: #1642, #1643, #1644, #1645). Arc: round-4 fixup on #1645 (createdAt string normalization — my round-1 "fails safe" dismissal was wrong; the reviewer's reframe as silent memory-drop was right and the owner approved the fix) → merged → release PR #1646 (holistic review LGTM, zero blocking; praised the real-ContextWindowManager invariant test) → finalize + publish → prod repair dry-run (21,493 rows) with execution pending owner go. The dedup hole was the epic's architectural must-fix (external review item 8): resolved by restructuring the sequence — history-first pre-pass, exact shipped boundary, ID-authoritative filter. Release-review tie-boundary nit filed in `cold/follow-ups.md`.

_Older session logs live in git history._
