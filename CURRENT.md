# Current

> **Version**: v3.0.0-beta.196 — merge-gate hardening (#2009/#2010), the hook-probe harness and context ratchet (#2003–#2008), the first rules economy pass. No migrations. Per-PR detail lives in the release notes; only what OUTLIVES the release is below.

---

## ⏭️ NEXT SESSION STARTS HERE

**2026-08-09 (Fable): Opus-window work AUDITED and the loose ends CLOSED.** Three independent fresh-context reviews of the ~30 PRs Opus shipped Aug 5–9 (prod-runtime #1984/#1986/#1997 · 7 hooks PRs · 13 tooling PRs) found the SHIPPED work sound — every sweep-claim reproduced, prod clean after 36h, no gate silently passing. The audit's own findings shipped as two PRs, both merged: **#2025** (six tooling follow-ups — the CURRENT.md-reset gate for TASK-476, repo-settings headline, GH_READ_PARTS agreement test, fetchRuns timeout, duplicate-task-id lint, two rules clauses) and **#2024** (the nested-`$(…)`-substitution bypass of both blocking guards — TASK-479/482/483, plus Rule-2 gh-read parity). **#2024 took 6 review rounds and the last one caught a genuine HIGH bypass I had introduced**: the whole-command `strip_heredoc_bodies` dropped-to-end on an unterminated `<<WORD`, truncating a real commit span out of the scan — verified empirically (bash ran the commit), fixed to over-arm, canaried at unit + both guard levels. The lesson is the session's own: the process (independent review + empirical verification) caught what my confidence would have shipped. **TASK-453 left OPEN, not marked Done** — the origin-collision check advances it but is best-effort (shallow-clone fail-open, fetch-recency), so its acceptance isn't guaranteed; marking it Done would be the overclaim this session audited. **TASK-484 filed** (merge-gate case-sensitivity, `state:owner`, likely correct-as-is). Session-log mining is the remaining deliverable of the original ask.

**beta.196 is released; this file was NOT reset with it** — step 9 of the release procedure was skipped, which is why it carried two releases' worth of content. Reset done 2026-08-09, and **TASK-476's gate SHIPPED (#2025)** — `release:bump` now refuses when this header ≠ `package.json` (fail-closed, `--allow-stale-current` for the exception); deciding what outlives a release stays human, only "did it happen" is mechanical.

**The develop-deletion exposure is CLOSED and now verified in both directions.** `delete_branch_on_merge` is off, `pnpm ops guard:repo-settings` asserts the invariant in the release preflight, and TASK-461's falsifying probe (2026-08-08, disposable repo) established the half that had only ever been assumed: a deletion rule with ZERO bypass actors DOES stop the admin-privileged path — a ref DELETE issued as owner was refused 422, and a PR whose HEAD was that branch merged with the branch surviving. The guard's hedging comment was replaced with the measured result (#2022). `delete_branch_on_merge: false` stays the primary protection because it assumes nothing about rulesets.

**The tooling queue is DRAINED** (owner directive 2026-08-09, "get this tooling crap done once and for all"). Shipped: lossy-pipe-guard generalized to `gh` reads with nine measured bypasses closed (#2015) · the shell quote scanner extracted to `.claude/hooks/lib/shell_quotes.py` and its last two copies fixed (#2019) · the claim-time trigger for lossy READS (#2020) · the existing-tests canary step in `/tzurot-bug-remediation` (#2021) · TASK-461's close-out (#2022) · the comment-claims rule (#2023). **TASK-476 filed**: release step 9 has no gate — beta.196 was cut without resetting this file, which is why it was carrying two releases at 95% of its byte budget. **TASK-475 RULED OUT on merits** — a shlex tokenizer fixes neither thing it claimed (probed: shlex does not track paren nesting, so the subshell over-block survives; and closing the awk gap needs an awk-semantics parser, not a tokenizer); evidence in the archived task.

**One correction worth carrying: #2015 shipped a false "ReDoS fixed" claim, corrected in #2019 and in #2015's body in place.** The blowup was `-{1,2}` (`--flag` parses two ways), not the flag value, and the "200 flags in 0.17ms" proof measured a single-dash fixture that could not reach the ambiguity. Now `-+[^-\s]\S*`, canaried on both sides. **The general lesson is codified in #2021**: a sweep that enumerates code sites but never asks which fixture exercises each one has checked half the class — revert the fix, and any existing test that stays green was measuring nothing.

**Drain selection is LABEL-DRIVEN — query, do not browse.** Every open task carries exactly one `state:*` (`ready` startable · `observable` a signal arrives on its own · `dependent` · `owner` **your decision queue** · `unreachable`). Two traps, both hit live: **`--priority high` alone returns only Done** — the work moved to the medium tier, so pair `state:ready` with `size:M`; and **cluster-batching is dead** — re-checking all five same-origin clusters found 2 startable members out of 15, the rest `observable`/`owner`/`dependent`/Done. Counts are deliberately not recorded here; they go stale within a session (`pnpm ops backlog:digest` is the live surface). Still-open holds: the **11 too-big items** (TASK-4, 17, 44, 48, 49, 58, 78, 98, 153, 386, 407) need individual treatment; TASK-61 is blocked until the voice gate runs; TASK-107 needs a producer sweep across every test tier including `tests/e2e`.

**TASK-458 remediation SHIPPED (#2002).** Non-blocking PostToolUse output never reaches the agent, **regardless of matcher** — probed directly. `claim-shape-guard` now runs from `.husky/pre-commit` over the STAGED diff (fires before the claim enters history), and the release-finalize reminder folded into the blocking `pr-merge-review-check`, firing BEFORE the merge instead of after. (`fixup-rider-check` moved to `.husky/commit-msg` in the same PR and has since been DELETED — TASK-472 delegates the rider class to review.) **One correction to the recorded design: `pr-monitor-reminder` is NOT inert** — its `gh api` assignee backfill is a real write that works without delivery, so it stays registered; only its banner was dead. **DELETED**: `release-finalize-reminder.sh` (superseded) and `empty-result-stderr-guard.sh` + probe (retired — it needs the post-hoc RESULT, so no channel can ever carry it; that retirement was absorbed-TASK-433's own acceptance criterion). **KEPT unregistered**: `eslint-on-edit` (~4.8s/edit for unread output) — dormant-but-viable, could return if the channel changes. Five false enforcement claims deleted from `05-tooling.md` (×2), `10-working-posture.md`, the git-workflow skill, and `check-monitor-command.ts` — that last one twice, since the first sweep fixed the header and missed the error output. **Do NOT route anything to a non-blocking PreToolUse hook — whether that channel delivers is an open question nothing in the repo answers.** Review rounds 1–2 found a real defect: the release reminder was nested behind the review-existence check, so a release PR whose claude-review posted nothing (documented, observed twice) would merge with no reminder — the exact silent drop this PR exists to end, and my hand-verification table structurally couldn't reach it. Fixed + all six paths re-verified. Also open: TASK-465 (lossy-READ trigger for the existing rule), TASK-464 (claim-shape `cannot be` false positive), TASK-462 (owner-approved 2026-08-07: narrow to truncation-only — block `head`/`tail` on `gh` read commands, allow `grep`/`awk`/`jq`), TASK-438, TASK-461, TASK-463. **TASK-302 member 1 SHIPPED (#2003): `guard:hook-probes`** runs every registered `*.probe.sh` in `pnpm quality` + the CI lint job (unconditional — a diff-keyed gate can drift from its own trigger), plus a bidirectional `HOOK_PROBES` registry over `.claude/hooks/` AND `.husky/` where every hook needs a probe or a written reason. Its FIRST CI run caught a real bug the manual-only era hid: the develop-code-commit probe assumed local `develop`/`main`, which a shallow `actions/checkout` lacks. **New local precondition — `pnpm quality` now needs local `develop` and `main` (`git fetch origin develop:develop`); branches are fabricated ONLY on a real Actions runner (`GITHUB_ACTIONS` set AND `ACT` unset — act sets CI/GITHUB_ACTIONS/ACT alike), never in a working repo.** 20 review rounds; 4 of them fixed defects in code written during the review itself. **TASK-302 is now CLOSED 2026-08-08 — all four members shipped**: the gate (#2003), `.husky/pre-commit` (#2004), `pr-monitor-reminder` (#2005), `pr-merge-review-check` (#2006). `guard:hook-probes` reports **8 probed / 5 registered-unprobed** (~18s). The env-override design recorded earlier was REJECTED and must not be revived: the ack path is `/tmp/.claude_pr_merge_ack.$(id -u)` and `id` is a PATH lookup, so shimming `id -u` beside the `gh` shim redirects it with ZERO production change — adding a bypass surface to the one hook whose job is to be un-bypassable was the wrong trade. **TASK-469 SHIPPED (#2009)** — the merge gate decided its PR from the whole command TEXT (a decoy in a `--body` armed it on the wrong PR, observed in production twice, and a wrong PR with no review on a non-`main` base exits 0 so the real merge proceeds UNREVIEWED). Now tokenized with quote and command-position awareness, plus a **structural backstop**: if `gh pr merge` survives tokenization as three adjacent tokens but the position logic didn't model the shape, it falls back to a permissive scan rather than exiting clean — every future parser gap is a recoverable over-arm, never a silent bypass. Ten review rounds; the backstop was available at round three. **TASK-470** is resolved into doc-61's economy pass (`state:dependent`): only the one-line defense — mutate the code an assertion covers, confirm it goes red — is a candidate for always-loaded space; the three shapes are examples and stay in the task.

**The voice-consistency harness is BUILT (#1910 miner + #1911 arms/judge/gate). NEXT: the operational gate RUN** — owner-in-loop, ~1 sitting. **Mining is DONE (2026-08-04): 18 probes frozen in local `reports/voice-consistency/probes.json`** — the ~Sep 1 retention deadline is defused for these; the remaining steps park until owner bandwidth. **Owner calls RESOLVED 2026-08-04**: (a) persona list approved as mined — lila-elyona-yareakh-yihye-shemesh, lilith-tzel-shani, emily-tzudad-seraph-ditza (all JSON protocol, 6 probes each); (b) JSON-only coverage accepted — the sole legacy-XML personality (calinette-nevae63, activity=1, not the owner's character) has no usable anchors, and the owner only talks to their own (all-JSON) characters, so the legacy rendering path is unused and needs no gate coverage. Nothing deadline-bound remains; generation → judge → owner blind-review sheet run whenever the owner has a sitting (owner 2026-08-05: no committed date, possibly the weekend — do not re-surface it as a recommendation). **All five artifacts are backed up** to `~/.claude/projects/-home-deck-Projects-tzurot/voice-consistency-backup/voice-consistency-2026-08-04.tar.gz` — `/reports/` is gitignored, so a `git clean -fdx` would otherwise destroy ~$7 of generation spend AND `probes.json`, which cannot be re-mined once the pre-beta.190 window ages out. Restore with `tar -xzf <archive> -C .` from the repo root.

1. ~~Mine probes~~ ✅ done (above). To re-mine with different picks: `pnpm ops prompt:mine-voice-probes --env dev --owner <discordId> --personalities <slugs>`.
2. ~~Persona-model census~~ ✅ done 2026-08-04: all three personas run `z-ai/glm-5` (global default config) — family-disjoint from the `anthropic/claude-sonnet-4.5` judge. Generation spend is z.ai plan quota, not dollars; judge ~$2–3 on OpenRouter.
3. ~~Dry run~~ ✅ PASS 2026-08-04: full replay path works (personalities load, retrieval runs, both arms assemble, all 18 probes). Projection: **894,042 input tokens** for A,B,B2 (~half the plan estimate — 18 probes not 24). Env recipe verified: `EVAL_MEMORY_DATABASE_URL` = dev **pgvector** service `DATABASE_PUBLIC_URL`; `OPENROUTER_API_KEY`/`ZAI_CODING_API_KEY` from dev ai-worker variables. NOTE: the dry-run projection line prints from `afterAll` — vitest swallows it without `--disable-console-intercept`.
4. ~~Replay + judge~~ ✅ done 2026-08-04 (owner-approved spend): 54 generations (A/B/B2 × 18), judge `anthropic/claude-sonnet-4.5` seed 190, 36 position-swapped pairs. **REMAINING (the owner's ~15-min sitting): blind-review `reports/voice-consistency/judgment-sheet.md` (15 pairs, one verdict box each + HARD BREAK flags) → `pnpm eval:voice-verdict`** → verdict + numbers into `active-epic.md`. Judge preview (blinded): A-vs-B resolved 5/5/8 left/right/tie — dead even; B-vs-B′ controls 8/18 consistent preferences on identical-arm pairs, so the pre-registered p₀>35% under-power flag may trip — read the verdict report's noise-floor line before trusting a PASS. One probe (d5a31e4e) generated empty on ALL THREE arms (probe-specific GLM quirk, arm-symmetric so unbiased). FAIL path unchanged: `VOICE_EVAL_ARMS=A,C` re-run → `VOICE_EVAL_GATE_KIND=CA`.
   _Carried runner nits (fold into any runner touch-up, not worth a CI cycle alone): `afterAll` disconnect guard when PrismaClient construction throws; retry-once wastes a cycle on deterministic parse failures._

Phase 2 (history extraction) starts only after the gate passes. Fill-in units: the drain campaign (280 open; `pnpm tracker task list -l state:ready -l size:S --plain` — 55 hits. `state:*` is GATED at filing since #1998, so the query can no longer go quietly blind; do NOT drop the `state:ready` term, and note `--priority high` alone returns only Done) and its same-origin clusters (`#1317` 159–162, `#1323` 167–169, `#1321` 163–165, `#1119` 131–133, `#1035` 100–102). **Sequencing**: the #1317 cluster and TASK-165 are prompt-assembly content — gated behind the voice-consistency run so arm B stays beta.190's shape.

**All 2026-08-03/04 wave work (#1915–#1941) RELEASED in beta.191** — per-PR narratives live in the release notes + git history (this file's pre-reset waves record). Open threads that survive the release: **TASK-425** (deduped-quote type guard — promote when DependencyStep emits File entries), **TASK-426** (STT-wrapper no-key log split, only if it proves noisy), **TASK-410** (bullmq/ioredis v6 migration; both majors dependabot-ignored until then, and dependabot reads its config from MAIN), the 55 doubled-transcript rows aging out ~Sep 2 (#1926 fixed forward), and doc-59 (BYOK video processing — owner ask).

**Owner decisions locked** (still governing): no asset table for attachments — council split 2-1 for one, Gemini tiebroke for Kimi's history-row design · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY`, not any user-facing `maxAge` · direct replies always re-vision regardless of age · extended context re-visions only within retention · an undescribed old image renders a content-free presence note, never a filename stub · extended-context quota exemption ships AFTER persistence · `maxImages` is a spend cap persisted images do not consume, with no second render lever.

**2026-07-27 was retention Phase 3's runtime completion, all owner-delegated**: 45 users purged (33 bystanders incl. the `'unknown'` fossil + 12 notify-bounces), beta.181 released (#1818 BullMQ jobId fix — caught by the dev re-route smoke BEFORE prod tripped it), first prod notify run (21 → 9 in grace, expiry ~2026-08-26; 2 warned users reactivated within the hour). Final books: 227 users, 0 purge-eligible, 7 in grace. **Everything retention is now calendar**: grace expiries ~Aug 26 (daily nag surfaces them); Phase 4 parked. **First rolling-cohort run 2026-08-02 (owner-approved, manual-approval-first working as designed)**: the daily nag surfaced 10 new bystanders (aged past 180d since the July run) → all 10 purged (231 → 221 users, zero character impact, 4.3% vs the 25% ceiling). The 3 reachable-inactive users were sent the warning DM — **all 3 bounced** (batch tally: 0 warned, 3 bounced, 0 failed-without-stamp), were reclassified unreachable by the stamp, and were purged same-night on owner call (July's bounce-purge precedent). **Books after the run: 218 users, 0 purge-eligible, 0 awaiting DM, 5 in grace (~Aug 26)** — everything retention is calendar again. **system-model Phase 0 COMPLETE** (map interview + accuracy review applied; scheduled redraw dropped by owner call — informal sketch-check at next design conversation; freshness = epic-close re-touch rule via #1814). Idle-time ride-alongs whenever: drift audit (Quick Win).

**The backlog substrate** → design, councils, owner ratification, and trial record all live in **[`docs/proposals/backlog/backlog-substrate.md`](docs/proposals/backlog/backlog-substrate.md)**. All four steps shipped (#1822 import · #1823 flip · labeling pass · themes/ideas→docs). **The whim constraint still governs the drain campaign**: the owner's priority-jumping is the point — make the haystack cheap to jump around in; acceptance test is "here are the 20 items you could close this week."

**`feat/memory-hybrid-retrieval` (`8acf63ccc`, local + remote) is a DELIBERATE park** — recorded in `tracker/docs/doc-8`; do NOT push, rebase, or delete it. It is the one branch a repo-state sweep will flag, every time. Method note if branch cleanup recurs: `git cherry develop <branch>` is the test, NOT `git branch -d` — rebase-only merges rewrite SHAs, so `-d` refuses every merged branch while `git cherry` compares patches and answers correctly.

## 🧊 Retention & Purge — build-complete, calendar-only

Design ACCEPTED 2026-07-23 → [`inactivity-retention-purge-phase2.md`](docs/proposals/backlog/inactivity-retention-purge-phase2.md); Phases 1–3 shipped (beta.175–181), per-PR detail in git + release notes. **Nothing here needs building.**

Books after the 2026-08-02 rolling-cohort run: **218 users, 0 purge-eligible, 0 awaiting DM, 5 in grace (~Aug 26)**. The daily nag surfaces expiries; activity aborts the clock. Phase 4 (autonomy) parked.

**Owner calls still governing**: no 10013 fast-track (gone is an alternative unreachable signal, still ANDed with the 180d window) · public-then-private characters with historical reach stay retained, because deleting them deletes other users' memories · orphan sentinel + `original_owner_discord_id` · single `retention_purge_log` · mode-aware `eraseAccount`. **D15 refined**: off-DB-before-DB is not implementable (the off-DB work is a function of the transaction outcome), so DB-first stands and the ledger's `pending`/`off_db_pending` is the retry handle.

**Open follow-ups**: `sendCustomSuccess`→`sendContractSuccess` internal-routes sweep · the purge-concurrency assumption (two members, both gated on Phase 4).

## UX Epic — Phase 3 IN FLIGHT (2026-07-20 → )

Waves 0–2 ✅ released beta.173 · **Wave 3 (breaking renames) ✅ released beta.174** — six PRs, 15 old→new command rows. Per-PR detail: `cold/epic-log.md` § Phase 3.

**NEXT: Waves 4–6** — PR-6a factory core+pilot → 6b destructive preset → 7 `/deny` redesign → 8 picker hygiene → 9/10 factory sweep + router adoption. Ship as normal minors; post-batch class slices are schedulable (burn-down trigger fired). Command-propagation lag ~1h in prod post-release.

Phases 1–2 are ✅ COMPLETE (released beta.170–172); their per-PR narratives live in `cold/epic-log.md` and git history.

## 🔬 Dev smoke checklist (owner-driven; this file is the source of truth)

**beta.195 adds L (below); J and K carried from beta.194, H and I from beta.191 — batch them in one sitting.** G (beta.190, optional/low) and 8/13/17 are carried deferrals, independent, droppable. Observability-instead-of-smoke covers the rest of beta.194: guest BYOK audio logs `Resolved audio API key` on first real use (#1974/#1975); persona-named quotes are visible in any `/inspect` prompt view (#1976); wrapper-tag unwrap (#1970) and guest-keying (#1966) log their own paths.

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Status                             |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- |
| L   | **Voice message still transcribes (#1997) — needs-smoke: the detector swap is behavior-preserving by test, not by runtime.** `hasVoiceAttachment` now delegates to the shared `hasVoiceAttachments` detector instead of carrying its own copy. Send a character a **voice message** and, separately, **forward** someone else's voice message to a character. Invariant: both transcribe as before. Masking: transcripts cache for 1h keyed to the message — use a fresh recording, not a re-send. Failure looks like: the voice message reads as a plain attachment / no transcript in the reply. Report: pass/fail.                                                                                                                                                                                                                                  | ⬜ needs-smoke                     |
| J   | **Reply to an image, then `/inspect` the turn (#1978) — needs-smoke: closes your own double-description report.** In prod after the deploy, reply to a message carrying an image and ask about it, then `/inspect` that turn's prompt. Invariant: the vision description appears ONCE — in `<chat_log>` — and the quote shows the stub marker instead of repeating it. Masking: none (each turn renders fresh). Failure looks like: the same description text visible in both `<contextual_references>` and `<chat_log>` (the beta.193 behavior). Report: pass/fail.                                                                                                                                                                                                                                                                                   | ⬜ needs-smoke                     |
| K   | **`/preset create` model autocomplete (#1967) — needs-smoke: slash-option registration is runtime-only.** ~1h after deploy (propagation), open `/preset create` — expect an optional `model` option whose autocomplete lists models; picking one pre-fills the modal's model field. Invariant: option registered + prefill lands. Masking: Discord client caches command shapes — restart client if absent after ~1h. Report: pass/fail.                                                                                                                                                                                                                                                                                                                                                                                                               | ⬜ needs-smoke                     |
| H   | **Breaking command renames (#1915) — needs-smoke: command registration is runtime-only.** In prod, ~1h after the deploy (propagation lag), open `/preset` — expect a `default` group with `set`/`clear` (old `set-default`/`clear-default` gone). Then `/voice tts default` with a provider option — expect it to SET your default; with **no option it CLEARS it** (by design; the confirmation footer says how to restore). Invariant: the new shapes are globally registered. Masking: the Discord client caches command shapes — restart the client if it still shows old shapes after ~1h. Report: pass/fail.                                                                                                                                                                                                                                     | ⬜ needs-smoke                     |
| I   | **Unsupported attachments read as files, not failed voice (#1934) — needs-smoke: closes the prod entry's loop.** Send a character a message with a video attachment (the soundless-MP4 repro; PDF/zip also count — the invariant is any non-image/non-audio type). Expect the reply to treat it as a file it can't watch (`[File: name]` reaches the prompt), NOT "your voice message failed to transcribe" behavior. Masking: none — this path has no caching. Report: pass/fail + screenshot if it still reads as broken voice.                                                                                                                                                                                                                                                                                                                      | ⬜ needs-smoke                     |
| G   | **Stickers on referenced/forwarded messages (#1895) — optional, low.** Reply to (or forward) a message that carries a sticker, in a character conversation. Expect the character to react to what the sticker depicts, not its filename. Unit-covered; this only proves the live Discord payload shape matches the fixtures.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | ⬜ optional                        |
| 8   | **D.** Wave-3 remainder. **Retroactive prod-log sweep 2026-07-31** over every retained deployment (2026-07-23→07-31, i.e. essentially the whole post-Wave-3 window): **`history purge` ✅** (2 × `DELETE /api/user/history/hard-delete`, 2026-07-26, `deletedCount=2` each). **`/preset override` + `set-default` ❌ never invoked** — their endpoints are `PUT /api/user/model-override[/default]`, zero hits window-wide. **`/random`, `/chime-in`, `/help` are NOT log-checkable at all**: modern deferral-mode commands log nothing on success (`index.ts` only warns on unknown), the first two emit chat traffic identical to a normal turn, and `/help` makes no backend call. Avatar/voice groups ambiguous (`PUT /api/user/personality/{slug}` ×9 can't be attributed). Remaining human work: the two `/preset` writes + avatar/voice groups. | 🟡 partial — 1 of 6 closed by logs |
| 13  | **E.** `/history purge` a channel with a derived unlocked memory → memory flips to `visibility='deleted'` (#1796 runtime proof; released beta.177)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ⬜ carried from beta.176           |
| 17  | **A. Extended-context images are described again (#1884)** — post a message carrying an image (a **forwarded** one is the sharpest case: a forward is only attachments, so an undescribed one reads as a blank quote), let it fall into the chat log, then in a **NEW** message ask the character what the image showed. Expect a description. Regression tell: the model sees only `[image/webp: name.png]`. Prod-log confirmation without Discord: `extendedContextAttachmentCount` in ai-worker's `Preprocessing results ready` should be **non-zero** when bot-client's `Fetched and processed channel messages` logged `imageAttachmentCount>0` for the same window — that pair was 67-vs-0 before the fix.                                                                                                                                       | ⬜ not run                         |

Executed in the Wave-3 round: `/voice voices purge` + `/character voice set` (each surfaced a real bug → #1752), `/chat` in an activated thread (works).

## 🔭 Watches

- **Dev-bot Discord quarantine — LIFTED 2026-08-05 (owner report): Discord relented after the 2026-07-22 denial; Rotzot is back to normal.** No recreate needed — the delete-and-recreate leaning is dead. `OUTBOUND_DM_ALLOWLIST` (#1650) stays: it fixed the root cause (boot-time createDM prewarm sweeps) and prevents re-quarantine. Dev outbound-DM testing is available again; the next dev broadcast smoke should now report `sent` instead of `discord-20026` — if 20026 still appears, that's a fresh finding, not the old quarantine. (The "quarantined (permanent)" classifier-copy note died with the premise.)
- **`targeting: 'recent'` rate after #1883 ships** — both trigger-row writes now log which path found the row. A high `recent` share means the exact `triggerMessageId` lookup isn't matching and the robustness win is illusory; grep `Stored built references` / `Updated user message with enriched content` once prod has traffic. (Raised in #1883's sixth review; the fallback itself predates the PR.)
- **Memory-search budget (live on prod since beta.190; #1893 + #1902)**: grep prod ai-worker for `Input exceeded the model window` (should now fire only on pathological reference/multi-part turns) and `attachmentCharsBeforeBudget` (the cap's bite rate — mined data predicts it bites on the MEDIAN attachment turn).
- **Prompt-cache hit rate (live on prod since beta.190; #1903–#1908)**: two consecutive same-persona prod turns → grep `Generated response` for nonzero `cachedPromptTokens` on turn 2; the `/inspect` Cache view (#1933, this release) now shows the realized fraction directly; `pnpm ops cache:prefix-diff --env prod --channel <id>` names any unexpected divergence by section. **Roleplay-quality watch continues**: the restructure moved protocol/output constraints off the recency tail — if voice drifts, the recorded fallback is OUTPUT_CONSTRAINTS back to the tail (~150 re-billed tokens/turn).
- First prod extraction log post-beta.165 (`valid_from` should stamp source time, not run time) · first deploy-orphan/safety-flush event (#1642 runtime proof) · owner's felt-repetition re-measure (gates 1b slice B + correction detection) · db-sync probes · prod lock-storm · retention-failure.

_Older session logs live in git history._
