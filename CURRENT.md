# Current

> **Version**: v3.0.0-beta.189 (cut 2026-08-01) — **the fast follow: "enrichment is data, not cache."** Eleven PRs, 49 of ~76 changed files in `ai-worker` prompt assembly. #1884 fixed a LIVE prod regression (every extended-context image undescribed since beta.188). #1883 gave quoted enrichment a durable home; #1881/#1882 gave a quoted reference one render path; #1887 stopped that path repeating what the chat log carries; #1885 made the token budget measure what the model receives; #1880/#1889 stopped our own control syntax being eaten when a character quotes it. No migrations. No open Dependabot advisories.

---

## ⏭️ NEXT SESSION STARTS HERE

**The fast follow is DONE and released.** TASK-365 PR-1/PR-2, 367, 370, 372 shipped; 368 and 373 had their sweeps done and stay open on named remainders; doc-56 shipped as `docs/reference/architecture/durability-tiers.md`.

**NEXT BUILD UNIT: the drain campaign** (tracker `doc-7` — Follow-Up Pool Drain). Opening surfaces: `pnpm tracker task list -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches. **Best selector found so far (batch 6): cluster open tasks by SURFACING PR number** — `for f in tracker/tasks/*.md; do grep -oE '#1[0-9]{3}' …; done | sort | uniq -c` and take the largest group; one PR's review rounds are genuinely colocated work. Remaining same-origin clusters: `#1317` (159–162), `#1323` (167–169), `#1321` (163–165), `#1119` (131–133), `#1035` (100–102).

**Batch 7 SHIPPED — #1892 (the `#1880` parse-family cluster).** TASK-377 + TASK-388 Done: `isInsideCodeSpan` now models backtick RUNS (double-backtick spans read as code — the failure direction was toward extracting, i.e. deleting a user's quoted example), and `replaceOutsideCodeMarkup` moved to `codeSpanDetection.ts` as the one definition. **TASK-375's evidence sweep answered the opposite way from its hypothesis and that result governs**: `extractOrphanClosingTag` is LIVE — 6 firings across 374 prod generations, all `glm-4.5-air` (not the retired Kimi K2.5 its docstring credited), 3 followed by healthy output. So TASK-373's unfenced-quote gap is **permanent** — retirement-by-construction is off the table, and the proportion guard was already evidence-rejected; do not spend another design pass on a discriminator there. TASK-375's chimera half is now instrumented (`Chimera stutter artifact stripped`) — grep prod after one release: zero retires the pattern, any hit names the model. TASK-376 still needs prod exposure.

**Two prod findings from a `/inspect` sanity check (req `456ec221`), both filed high**: **TASK-392** — `<contextual_references>` ships byte-identical in BOTH assembled messages (7,713 chars each, 82% of the user message, ~1,900 duplicated tokens per referencing request); open question is whether the dual placement is deliberate. **TASK-393** — the memory-search query was 2,093 tokens against bge-small's 512 cap with truncation hardcoded in transformers.js, so **75.5% is discarded before embedding**, and `referencedMessagesText` (appended last) never reaches the embedder despite a log claiming it did. Owner delegated sequencing ("up to you as long as they get resolved"). **393 first** — it is an unambiguous defect with no product question; 392 changes what the model sees and wants a recency-adjacency answer first.

**Sequenced deliberately — 385 before 387.** Both are the same seam (a pipeline step reading shared state before the step that writes it; #1884 was the first instance). 385 restructures WHERE that state resolves, so it may change or absorb 387's answer — re-read 387 after 385 lands rather than planning it now.

**Owner-gated, not agent-callable**: **TASK-371** (reasoning chunk cap → 10) is a user-visible-latency call; #1888 corrected the docstring that claimed the opposite, the cap itself is yours. **TASK-132 + TASK-164** rule-outs are approved on technical merit (councilled 3/3) but not executed.

**Post-release watch, from the release review**: every generation now renders + tokenizes each history entry instead of reading a cached count (#1885 — the cache understated cost by up to 87%). Measured at 50–120 ms end-to-end per request. Worth a glance at prod generation latency now that it is live; the trade is deliberate (correctness over a map lookup) but unverified at prod scale.

_The three approved one-liners are now **TASK-395** — they had ridden two sessions as prose here, and CURRENT.md is reset every release cut, so the tracker is their real home. Their original justification was already unrecoverable from the session logs when filed; the task records the sites found in source and flags the third as unidentified rather than guessing._

**Owner decisions locked** (still governing): no asset table for attachments — council split 2-1 for one, Gemini tiebroke for Kimi's history-row design · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY`, not any user-facing `maxAge` · direct replies always re-vision regardless of age · extended context re-visions only within retention · an undescribed old image renders a content-free presence note, never a filename stub · extended-context quota exemption ships AFTER persistence · `maxImages` is a spend cap persisted images do not consume, with no second render lever.

**2026-07-27 was retention Phase 3's runtime completion, all owner-delegated**: 45 users purged (33 bystanders incl. the `'unknown'` fossil + 12 notify-bounces), beta.181 released (#1818 BullMQ jobId fix — caught by the dev re-route smoke BEFORE prod tripped it), first prod notify run (21 → 9 in grace, expiry ~2026-08-26; 2 warned users reactivated within the hour). Final books: 227 users, 0 purge-eligible, 7 in grace. **Everything retention is now calendar**: grace expiries ~Aug 26 (daily nag surfaces them); Phase 4 parked. **system-model Phase 0 COMPLETE** (map interview + accuracy review applied; scheduled redraw dropped by owner call — informal sketch-check at next design conversation; freshness = epic-close re-touch rule via #1814). Idle-time ride-alongs whenever: drift audit (Quick Win), GLM generic-unwrapper (now.md Untriaged — ride-along, never a dedicated cut).

**The backlog substrate** → design, councils, owner ratification, and trial record all live in **[`docs/proposals/backlog/backlog-substrate.md`](docs/proposals/backlog/backlog-substrate.md)**. All four steps shipped (#1822 import · #1823 flip · labeling pass · themes/ideas→docs). **The whim constraint still governs the drain campaign**: the owner's priority-jumping is the point — make the haystack cheap to jump around in; acceptance test is "here are the 20 items you could close this week."

## 🎯 Active epic — Automated Inactivity Retention & Purge

Design ACCEPTED 2026-07-23 (council trio + 6 owner calls) → [`inactivity-retention-purge-phase2.md`](docs/proposals/backlog/inactivity-retention-purge-phase2.md).

| Slice                                                                                                                                                                                                                                                                                                                  | State                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| Phase 1 (1a–1d) tracking substrate                                                                                                                                                                                                                                                                                     | ✅ released beta.175; prod backfilled 270 users |
| Phase 1.5 CH sync unification (#1778)                                                                                                                                                                                                                                                                                  | ✅ merged                                       |
| PR-A schema (#1779) — `discord_account_gone_at`, `retention_exempt`, `original_owner_discord_id`, `retention_purge_log`                                                                                                                                                                                                | ✅ merged                                       |
| PR-B mode-aware erasure (#1780) — `AccountEraserService` unifies DB+off-DB (D1); `retention` mode re-homes cross-user characters to the Orphaned-Characters sentinel (D11)                                                                                                                                             | ✅ merged                                       |
| PR-C preview (#1781) — `RetentionPurgeService` owns the single D3/D4 predicate; read-only `GET /internal/retention/preview` + `pnpm ops retention:preview`                                                                                                                                                             | ✅ merged                                       |
| **PR-D1 purge capability** (#1795) — per-user `POST /internal/retention/purge` + `retention:purge`/`reconcile-off-db` CLI, in-tx TOCTOU re-check, audit ledger, 25% server-side ceiling, gone-flag self-heal, privacy policy                                                                                           | ✅ merged 2026-07-26; dev migrated              |
| Dev end-to-end purge smoke                                                                                                                                                                                                                                                                                             | ✅ PASS 2026-07-26 (checklist item 10)          |
| **PR-D2 daily nag** (#1797) — `RetentionNagScheduler` (daily + startup, weekly Redis cooldown, silent at 0, breaker warning + per-env CLI commands in the embed); rode with a `createIntervalScheduler` extraction across all three bot-client schedulers (CPD-forced; also fixed the stray-startup-timer-on-stop gap) | ✅ merged 2026-07-26 — **Phase 2 COMPLETE**     |

**Owner calls locked (D1)**: no 10013 fast-track (gone is an alternative unreachable signal, still ANDed with the 180d window — D13's safety argument assumed an activity-clear that didn't exist; it ships now) · the release-cadence coupling is accepted as Phase 2's scope boundary (the 51 inactive-but-reachable are Phase 3's cohort) · public-then-private characters with historical reach stay retained, because deleting them deletes other users' memories. Earlier locks: orphan sentinel + `original_owner_discord_id` · `retention_exempt` · single `retention_purge_log` · mode-aware `eraseAccount`.

**D15 refined, not followed**: off-DB-before-DB is not implementable (the off-DB work is a function of the transaction's outcome), so DB-first stands and the ledger's `pending` + `off_db_pending` is the retry handle. Recorded in the design doc.

**Open follow-ups**: `sendCustomSuccess`→`sendContractSuccess` internal-routes sweep (deliberately not ridden on the destructive PR) · the purge-concurrency assumption (two members, both gated on Phase 4). _(Struck at beta.186: fake-optional columns shipped #1859; bounded reconcile sweep shipped #1856/beta.185.)_

## UX Epic — Phase 3 IN FLIGHT (2026-07-20 → )

Waves 0–2 ✅ released beta.173 · **Wave 3 (breaking renames) ✅ released beta.174** — six PRs, 15 old→new command rows. Per-PR detail: `cold/epic-log.md` § Phase 3.

**NEXT: Waves 4–6** — PR-6a factory core+pilot → 6b destructive preset → 7 `/deny` redesign → 8 picker hygiene → 9/10 factory sweep + router adoption. Ship as normal minors; post-batch class slices are schedulable (burn-down trigger fired). Command-propagation lag ~1h in prod post-release.

Phases 1–2 are ✅ COMPLETE (released beta.170–172); their per-PR narratives live in `cold/epic-log.md` and git history.

## 🔬 Dev smoke checklist — the beta.189 cut (owner-driven; this file is the source of truth)

**Ordered for one sitting**: each group reuses the previous group's setup, so working A→E top to bottom means posting each fixture once. Nothing here is CI-verifiable. Groups A–C are this release; D–E are carried from earlier cuts and are independent, so they can be dropped without affecting A–C. Closed items from the beta.188 round were evicted at this cut — git has them.

**A. Post an image, let it fall into the chat log** (one image covers two items)
**B. Reply to a voice note** · **C. Ask a character to quote our own syntax** · **D. Two `/preset` writes + avatar/voice groups** · **E. `/history purge` with a derived memory**

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Status                             |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 8   | **D.** Wave-3 remainder. **Retroactive prod-log sweep 2026-07-31** over every retained deployment (2026-07-23→07-31, i.e. essentially the whole post-Wave-3 window): **`history purge` ✅** (2 × `DELETE /api/user/history/hard-delete`, 2026-07-26, `deletedCount=2` each). **`/preset override` + `set-default` ❌ never invoked** — their endpoints are `PUT /api/user/model-override[/default]`, zero hits window-wide. **`/random`, `/chime-in`, `/help` are NOT log-checkable at all**: modern deferral-mode commands log nothing on success (`index.ts` only warns on unknown), the first two emit chat traffic identical to a normal turn, and `/help` makes no backend call. Avatar/voice groups ambiguous (`PUT /api/user/personality/{slug}` ×9 can't be attributed). Remaining human work: the two `/preset` writes + avatar/voice groups.                                                                                                                | 🟡 partial — 1 of 6 closed by logs |
| 13  | **E.** `/history purge` a channel with a derived unlocked memory → memory flips to `visibility='deleted'` (#1796 runtime proof; released beta.177)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | ⬜ carried from beta.176           |
| 16  | **B. A quoted voice note survives replay (#1883).** **PASS (owner, 2026-08-01 11:45, req `4fc9051a`)**: voice note "Okay, here's a quick test." posted, replied to, then asked about in a SEPARATE message — COLD answered "the voice message transcript remains in context" and quoted it back. **Caveat worth keeping**: the transcript reached the model via the voice message's OWN chat-log entry, not via the stored reference, so #1883's persistence is not isolated by this run — it only becomes load-bearing once the voice message ages out of the chat-log window while the reply is still in history. Not arrangeable on demand.                                                                                                                                                                                                                                                                                                                        | ✅ **PASS** (outcome)              |
| 17  | **A. Extended-context images are described again (#1884)** — post a message carrying an image (a **forwarded** one is the sharpest case: a forward is only attachments, so an undescribed one reads as a blank quote), let it fall into the chat log, then in a **NEW** message ask the character what the image showed. Expect a description. Regression tell: the model sees only `[image/webp: name.png]`. Prod-log confirmation without Discord: `extendedContextAttachmentCount` in ai-worker's `Preprocessing results ready` should be **non-zero** when bot-client's `Fetched and processed channel messages` logged `imageAttachmentCount>0` for the same window — that pair was 67-vs-0 before the fix.                                                                                                                                                                                                                                                      | ⬜ not run                         |
| 18  | **A. A REPLAYED deduped quote does not repeat its description (#1887).** **PASS (owner, 2026-08-01, reqs `1ab06c8d` then `4fc9051a`)** — proved by A/B across two diagnostics rather than the two-turn recipe originally written here. SAME reference, two renderings: in `1ab06c8d` the LIVE quote (`<contextual_references>`) carried `its media is described here` plus the full 1374-char `<image>`; 20 minutes later in `4fc9051a` the REPLAYED quote of that same message (inside `<chat_log>`) carried a plain marker, no `<image>`, no media clause — while the referenced entry still renders its own `<image_descriptions>`. Whole prompt: 0 media clauses, 3 plain markers. The earlier log is what makes this airtight: it proves the stored reference HAD enrichment to subtract, which absence alone would not. Same subtraction confirmed in the VOICE modality on the same run.                                                                       | ✅ **PASS**                        |
| —   | **Not a smoke item — expected, and now measured.** Replying directly to an image DOES describe it twice (req `1ab06c8d`, 2026-08-01): the identical 1374-char description appears in `<contextual_references>` and again in `<chat_log>`. That is the LIVE path, **TASK-387**, untouched by #1887. It only became visible when #1884 restored the chat-log descriptions — before that the entry was blank, so the live quote's copy was the only one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | ℹ️ known, filed                    |
| 19  | **C. A character quoting our prompt syntax keeps its example (#1889).** **PASS (owner, 2026-08-01)** on the re-ask. Round 1 was inconclusive — COLD described the prompt at length but wrote only OPENING tags, and the gated pattern is closing-form only. Asked again for closing tags specifically, COLD replied with `` `</chat_log>` `` and `` `</protocol>` `` intact. Both are in the five-name unanchored global pattern, the ONLY family that went red when the gate was disabled during #1889. The reply's brevity makes it clean: its entire content is the two tags, so pre-fix it would have arrived as two EMPTY backtick pairs. **Lesson for the next syntax-quoting smoke**: ask for a CLOSING tag by name — an opening tag exercises nothing, because every opening-tag pattern in that file is `^`-anchored and a backtick already breaks the anchor. Item 14's two arms (bare unclosed `<think>`, standalone `</think>`) remain unit-covered only. | ✅ **PASS**                        |

Executed in the Wave-3 round: `/voice voices purge` + `/character voice set` (each surfaced a real bug → #1752), `/chat` in an activated thread (works).

## 🔭 Watches

- **Dev-bot Discord quarantine — TERMINAL, not pending.** Rotzot's appeal was **DENIED 2026-07-22** (canned reply; did not engage with the zero-messages-sent / already-fixed facts). The app is permanently unable to join new guilds or DM users it hasn't messaged before. Root cause was boot-time createDM prewarm sweeps (~117 opens × 10+ deploys/day from a 1-server bot — spam-prep shape even with no sends); structurally fixed by `OUTBOUND_DM_ALLOWLIST` (#1650). **Prod Tzurot is a separate app and was never quarantined.** Casualty is dev outbound-DM testing only.
  **Owner leaning: delete & recreate Rotzot as a fresh dev app.** When executed: new app + token → set dev `DISCORD_TOKEN`/`DISCORD_CLIENT_ID` → re-register commands → re-invite to the dev guild → set `PUBLIC_SITE_URL`/`SITE_BRAND=rotzot` → re-run the broadcast smoke with a fresh label. The recurring dev-blast `0 sent, 1 transient-failed` **is** this quarantine surfacing as `discord-20026` (runtime-confirmed); classifier Quick Win open (copy should read "bot quarantined (permanent)").
- **`targeting: 'recent'` rate after #1883 ships** — both trigger-row writes now log which path found the row. A high `recent` share means the exact `triggerMessageId` lookup isn't matching and the robustness win is illusory; grep `Stored built references` / `Updated user message with enriched content` once prod has traffic. (Raised in #1883's sixth review; the fallback itself predates the PR.)
- First prod extraction log post-beta.165 (`valid_from` should stamp source time, not run time) · first deploy-orphan/safety-flush event (#1642 runtime proof) · owner's felt-repetition re-measure (gates 1b slice B + correction detection) · db-sync probes · prod lock-storm · retention-failure.

_Older session logs live in git history._
