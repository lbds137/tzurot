# Current

> **Version**: v3.0.0-beta.200 — nightly db-sync now attaches its report (#2073), catalog-outage clarity across two services (#2068/#2070), participant keying by personaId (#2067), deploy-window submit retry (#2061), + 10 tooling/rules PRs. No migrations. Released 2026-08-12 ~07:42 EDT (flags verified: 200 latest, 199 demoted; all three prod services SUCCESS).

---

## 🔄 POST-RELEASE DRAIN (2026-08-12) — ALL MERGED

**#2075/#2076/#2077/#2078/#2079 merged**; TASK-530/542/547/548/550/551/553 Done. Carry-forward finding from #2076: prisma splits a P1001 across streams (benign echo on stdout, identifying error on stderr) — a stdout-only read fails quietly with a plausible wrong reason. From #2077: the tracker id allocator ignores `tracker/archive/tasks/`, so archiving the highest-numbered task frees its id for reuse.

## 🧭 2026-08-12 EVENING (Fable): mining run + shipped-work audit + Opus-setup ops

**Mining run complete** (SYNTHESIS-2026-08-12, R1–R7, machine-local). Driver timeline runtime-pinned: Fable+Sonnet through 08-11 01:17Z, then Opus 5 orchestrator ~45h. **Defect review of beta.198–200's ~30 PRs (6 fresh-context reviewers): 0 High · 11 Med · 32 Low — ALL tracked (TASK-556–577; Meds 556-565).** Verdict: merged-output quality driver-comparable (Meds ~6/14 Opus-window vs ~4/18 Fable-window PRs); the Opus gap is process cost (review marathons, orchestrator error volume), not shipped defects. **Owner decisions**: Opus stays drain-orchestrator default WITH a ~6-round hard cap (shipped to skills, #2080); org model = owner → Fable (second-in-command: design/taste/verification passes over Opus output) → Opus (drain engineering manager) → Sonnet workers+reviewers, Haiku for Explore fan-outs. Ops shipped/in-flight: #2080 (round cap + stacked-gate fixture rule), #2081 (context-size reminder hook + tracker-dirty push gate), `release:range` (worker, in flight). Brief intake: observability → doc-12/16/25. New theme: **doc-76** (read-only backlog browser on the website).

**OPUS DRAIN QUEUE (curated 2026-08-12 by Fable — mechanical-first, precise fix shapes; work top-down):**
Wave 1 (high): 558 → 557 → 556. Wave 2 (medium): 560, 564, 562, 565, 561, 533, 535, 545, 563, 546-continuation. Wave 3 (low picks): 324, 336, 354, 486, 304, 271.
**Held for Fable/owner (do NOT pick up in a drain): 527 (product copy), 540 (HTTP contract), 559 (owner pick: redraw vs document), 531 (process design).** Net-shrink reporting per /tzurot-docs applies: every batch report states closed-vs-filed.

## 🔄 RELEASE STATE (2026-08-12 — beta.200 RELEASED)

**v3.0.0-beta.200 is out.** #2074 merged with owner approval after a holistic review with no blocking findings (it verified the cross-PR seams: #2068/#2070's catalog tags consistent end-to-end across two services, #2067's participant re-keying with no stale untyped fixture, #2073's optional `files` param leaving all 9 other call sites untouched). `release:finalize` resynced develop, `release:publish` tagged + flipped flags. **15 PRs, 5 runtime.** No migrations, no advisories, no dependabot queue.

**Post-release verification — ONE item, and it runs itself.** #2073's nightly db-sync fires unattended at the configured UTC hour; the next owner-channel post either carries `nightly-db-sync-report.md` or does not. No manual round is needed for anything in this release: #2061 is exercised by this release's own deploy window, the catalog pair is fail-closed with only messaging changed, #2067 is seam-covered.

**Release-counting method (carry forward).** Derive the runtime-PR count from the **git range**, never from `gh pr list --json mergedAt` compared against the tag date — the tag commit on `main` predates several develop merges, so that filter re-counts PRs that already shipped. It inflated this session's count to 7-8 against a real 5, and the cut-at-ten trigger reads exactly this number.

## 🧪 OPUS 5 ORCHESTRATOR TRIAL — SLATE COMPLETE, all 6 units merged (2026-08-11, Opus 5 driving)

**Six units merged.** #2060 (TASK-516: `ops logs` snowflake truncation — cac/mri coerces all-digit values past 2^53, so an incident dig silently grepped a different id; fixed via `rawOptionValue` + a source-scanning guard, and the class sweep also caught `retention:purge --exclude` CRASHING on its documented single-id case) · #2061 (TASK-515: submit-path retry across a deploy window, narrowed to CONNECTION failures because `generate()` creates a paid job and the gateway's 5s dedup window is shorter than the ~14s backoff) · **#2062** (TASK-32: `guard:build-scripts` — CI fails if a `tsc`-invoking build script does not `rm -rf dist tsconfig.tsbuildinfo` first; owner-approved widening from an allowlist to word-based detection) · **#2063** (TASK-24: `backlog:lint` resolves every relative markdown link plus `doc-N` refs across `tracker/` and `backlog/` — **15 already-dead links found and fixed**) · **#2064** (TASK-178: `test:audit` discovers service dirs; merge-clean) · **#2065** (TASK-139/140: dockerfile-dist guard anchors on the named `runner` stage and hard-fails whenever name and position disagree — 7 rounds, the trial's heaviest).

**Trial verdict is the owner's call** — full record + verdict-at-6-units in TASK-513's notes. Worker tier 6/6 clean (TASK-487 now at **16 units, 0 worker semantic defects**); over-delegation 0; unverified git-state claims accepted 0. Recommendation: **keep**. The failure shape is orchestrator-side, three instances of one family: **a check or claim that inherits the assumption it was meant to test, or outruns the evidence cited beside it** — an ordering test that passed with the sort removed; an "I verified every link" claim built from a grep carrying the implementation's own dot-prefix assumption, blind to 14 dead links; an unqualified "no `--target` anywhere" from an in-repo-only search, inside the JSDoc justifying a hard CI failure. Countermeasure is the canary, not more care.

**#2065's premise correction is the one to carry forward.** TASK-140 assumed last-`FROM` anchoring was the bug; verifying rather than implementing showed the reverse — with no `--target` in the repo Docker ships the LAST stage, so named anchoring would have validated `runner` while the shipped stage went unchecked. The guard now treats either disagreement as a finding. Rounds 3 and 5 each found defects in what the PREVIOUS round added — the rider class.

**Slate EXHAUSTED**; TASK-513 stays open as the decision surface. The owner-widened rules PR **#2071 MERGED** (TASK-520/523/537/538 Done): dependency-behavior comments named as external-system claims, `ops backlog` + commit scopes point at their generated sources instead of re-listing, both ModelCapabilityChecker cache rows, `max-lines` annotated as skipping blanks/comments. Rules budget 147519/154502. **#2072 MERGED — 13 tooling shell-outs bounded; the timeouts were the easy half.** Adding a throw to a `catch` written before throws were possible changes what that catch MEANS: Node attaches partial stdout to a timeout kill, so four sites needed a different "unknown", each picked by which wrong answer is dangerous — `getPendingMigrations`→`null` (was "none pending" mid-DB-hang), `hasNonTestImporters`→`true` (was: live file onto a deletion list), `fetchRegisteredCommands`→throws (was: truncated help as the full command set), `gitHasCommit`→`true` (was: busy git aborting gate arming right after a push). `isTimeoutKill` was **probed, not assumed**: `killed` is UNDEFINED there, so the intuitive guard never fires. **14 rounds — 1-5 scope/claim defects, 6-9 real regressions I introduced, 10-14 none**; that flattening was the stopping signal. TASK-541 stays OPEN (partial pass): ~20 sites remain in **TASK-546**, carrying the hazard, its base rate (3 of 11), and the neutral-answer-vs-claim test. Filed: TASK-542/545/547/548/549/550.

**#2070 merged (TASK-539) — an OpenRouter outage told users to check their model ID.** The task was filed to port #2068's fix and marked its own premise UNVERIFIED; tracing inverted it. `getModelById` does collapse "catalog unreachable" and "model absent" into one `null`, but the save gate REJECTS on null, so api-gateway is fail-CLOSED — no regression, nothing to port, and the filed fix-shape would have been work against a bug that does not exist. What the trace found instead: a docstring promising the OpenRouter path "skips validation and the request proceeds" during an outage (it rejects), and an error telling users a valid model "isn't in the available models list." Fixed with a tagged `lookupModelById` at one call site, `getModelById` rebuilt on top so the two cannot drift; the unavailable case still rejects, pinned — a pass-through would flip a fail-closed gate open. **My consumer table read as an audit and missed two consumers with the identical false-cause message** — `computeRequiresZaiKey` (round 1) and `ensureVisionCapableModel` (round 4), the latter being "THE gate for vision-ness" with no direct test at all, which is why changing its message moved zero tests. Filed TASK-540 (the outage still returns 400, not a transient status — deferred because an HTTP contract change is user-visible).

**#2069 merged (TASK-534) — `pnpm ops backlog` printed a green "in sync" for task files git had never seen.** Now warns (never gates) on untracked/modified `tracker/` files. **Eleven rounds on ~90 lines: low code-defect density, high CLAIM density** — three of my assertions were falsified mid-PR, and round 2's fix introduced round 3's bug. The right move three times was to **dissolve the class rather than handle it** (`status.renames=false`, `--untracked-files=all`, `-z`).

**#2073 merged (TASK-543) — the nightly db-sync announced warnings and shipped no report.** Owner-reported. `buildSyncSummary` promised "full list in the report below" from a helper BOTH surfaces call, and grounding found the slash path wrong too: `/admin db-sync` moved its report behind a **Show details** button after that sentence was written, so "below" held only in the stash-failure fallback. Both sentences now take a `ReportDelivery` (`below|button|attachment`) with **no default**, so a new caller must state its surface rather than inherit a wrong promise; `buildSyncReportText` moved to `utils/` (a `services/` module importing `commands/` is a boundary violation) and the nightly post attaches it. Reviewer's size-cap concern dismissed on measurement: warnings are O(15 tables), deletions hard-capped at 500 — ~100KB worst case against a 10MB floor.

**#2068 merged (TASK-449) — the context window silently unclamping on a catalog miss.** `resolveFromRedis` collapsed three outcomes into one bare `null`, which `clampContextWindow` reads as "no limit". Two are transient; the third is permanent and its unclamped behaviour is CORRECT for non-OpenRouter providers — so the task's own filed fix-shape (fail closed on null) would have clamped every legitimate non-OpenRouter config to an invented floor. Fixed by REMEMBERING instead: tag the three returns, memoize for 24h (TTL **derived** from the catalog's own Redis TTL), serve the memo on a transient miss. Also stopped caching transient results — one Redis blip was buying five minutes of unclamped generation _after_ recovery.

Eight rounds, zero blocking. Two latent bugs came from review, not tests: a memo with no downgrade path, and a wire-level `undefined` slipping past every `!== null` guard into `Math.floor(undefined * fraction)` → NaN. Filed: TASK-537/538/539.

**Post-slate runtime work: #2067 merged (TASK-528).** `participantPersonas` was keyed by persona NAME while its dedup index was keyed by personaId, so two users sharing a persona name overwrote each other and one vanished from `<participants>` — while `PromptBuilder` still told the model to match that message's `from_id` (a UUID) to a participant entry that no longer existed. Now keyed by `personaId` with the display name in the value; both render under the same label (owner decision: no suffixes, no note line). Found by chasing the owner's own caveat on a moderator's support question, not from any board entry. Orchestrator diff-read caught what the worker had not: `delete`+`set` → same-key `set` changes Map iteration order, which IS the roster render order — probed, kept (order should not depend on which display name won), pinned with a canaried test. Also swept a raw-display-name logging class beyond what the reviewer flagged; the owner correctly pushed back that public Discord names are weak PII, and the operative argument became the stronger one: in a PR about two people sharing a name, the name is demonstrably the wrong identifier and `resolvedPersonaId` was already on every line.

**Five rounds, none blocking — and rounds 3 and 4 CONTRADICTED each other.** Round 4 asserted all four dedup branches were pinned; round 3 had correctly found one that was not. Round 3 was only read because a mid-round monitor stop surfaced both on the next fetch — reading only the latest would have shipped the gap under a confident all-clear. Enumerating the branch condition by hand settled it. Concrete instance behind the standing "claude-review has no special epistemic authority" position, and a sharper reason to read every `claude[bot]` entry rather than the newest.

**Branch survival ROOT-CAUSED and FIXED — #2066 merged (TASK-521 + TASK-524 Done).** `gh pr merge --delete-branch` fails the local delete when the head branch is checked out ANYWHERE: a worktree (the orchestration skill mandates one per file-mutating worker, so every delegated unit lands there) or the main checkout after a local review. Observed alongside it, mechanism deliberately left open in the doc: the remote branch survives too, but whether `gh` skips the remote delete _because_ the local one failed was never established — so the skill says verify the ref rather than reason about the tool. Settings unchanged and must stay (`delete_branch_on_merge=false` is the flag that deleted `develop` on 2026-08-07). #2066's own merge was the first run under its own procedure, verified `deleted ✓`.

**Eight rounds on a docs PR; three defects were mine, all in text an earlier round added — the rider class operating ACROSS rounds.** The round-7 **High** carries forward: plain `worktree remove` refuses on a DIRTY worktree, never on an UNPUSHED one, so a committed-but-never-pushed worktree reads as "clean", removal takes it, and `--delete-branch` strands those commits behind a local reflog — the exact TASK-524 scenario the PR exists for. Probed end to end (commit-without-push → `remove` exit 0 → `branch -D` → absent from `git log --all`). **I had read that section an hour earlier and pronounced it coherent**, reasoning "remove refuses on dirty" — true, and the wrong test. My own re-read is not a canary; the probe was. → **TASK-531**.

**Repo-state swept clean.** `fix/task-515-submit-deploy-window-retry` was a real survivor of #2061 (a worktree unit merged before the fix existed) — proven vestigial by patch-id equality with develop's `8f4d67c18`, deleted with owner approval, verified with #2066's own command. Also removed `refs/remotes/pr/2052`, a local-only `gh pr checkout` ref that made the sweep report a phantom survivor. Remaining and both explained: `feat/memory-hybrid-retrieval` (parked, doc-8) and nothing else.

**Also this stretch**: two comments asserted external-library behavior as fact and were wrong both times (cac repeated-flag returns an ARRAY; `AbortSignal.timeout` starts at `fetch()`, so a timeout does NOT imply delivery) — both reviewer-caught, now operationalized in the rules via #2071. Filed: TASK-518, TASK-519.

**Open threads for next session:**

- ~~Own-voice post-release smoke~~ ✅ **CLOSED via retro-log 2026-08-11** (owner-supplied prod diagnostic, requestId `75407676`): all 31 persona `<voice>` tags in the assembled context render the static own-voice description with zero persona transcripts, while user voice messages keep their transcripts — both halves of the invariant runtime-verified in prod.
- **Opus 5 trial: all 6 slate units RUN, merged and recorded (TASK-513, still open as the owner's decision surface).** Filed during the trial: TASK-518/519/520 (units 1-2), **TASK-521** (verify head-branch deletion), **TASK-522** (backlogLint parse-limitation disclosure — bumped to medium after it fired live), **TASK-523** (`05-tooling.md` gate description stale; blocked by the trial's no-rules-edits boundary), **TASK-524** (SendMessage resume drops worktree isolation — high), **TASK-525** (`RUNNER_STAGE_PATTERN` flag-bearing `FROM`, observable), **doc-74** (three guards hardcode 2 of 4 workspace roots). Post-slate: **TASK-530** (hook the branch-checked-out merge guard), **TASK-531** (run every command a skill documents), **TASK-532** (dirty same-tree resume unwind, observable). 521/524 shipped in #2066.
- **From a user's questions (owner-relayed, 2026-08-11) — Xeo asking as a server MODERATOR, not an individual user.** Three asks, all grounded against the code before answering: **TASK-526** (channel-wide conversation reset — real gap; every clear path requires naming one character, though `clearHistory`'s `personaId` is already optional so the service layer is moderator-capable; `state:owner` because the blast radius is a product call), **TASK-527** (settings dashboards never say WHOSE conversations a tier affects), **TASK-529** (**the widest-impact find**: the cascade is `… < channel < user-default < user+personality`, so a moderator's channel value is a DEFAULT that any user override silently beats — mods can suggest, never enforce, and nothing says so; `state:owner`), **TASK-528** (traced from "worth checking" to a confirmed **high**: `participantPersonas` is keyed by persona NAME while its dedup index is keyed by personaId, so two users sharing a persona name overwrite each other and one vanishes from `<participants>` — while `PromptBuilder` instructs the model to match `from_id` to a participant entry that no longer exists). Owner call on 528's fix shape: **both render, same label** — key by `personaId`, display name in the value; no suffixes, no hint line.
- TASK-514: mis-channel WARN watch (state:observable) — Railway grep "Suspect reasoning mis-channel" on recurrence signals.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the venue-scoping design input either way; if they say OFF and never-talked holds, open an investigation with an /inspect evidence request).
- TASK-498: codify the pre-commit review panel (trial data: #2034 caught 3 pre-commit, CI still found 1 mirror-branch Medium; 2 rounds vs #2032's 9). TASK-499/502/503 filed and open.
- Owner-idea council queue: doc-70 (tag count/group-message), doc-71 (/tag management), doc-72 (dashboard index nav — council pass mandated), doc-73 (reasoning-trace access) + the standing doc-64/65/66/67 set.

_This session has spanned THREE releases (beta.198 → beta.200) plus many compactions; a fresh session is a clean continuation point whenever convenient — owner's call._

<details><summary>Previous session log (2026-08-09, beta.197/198 — compressed)</summary>

## Previous session log (2026-08-09 evening)

**Resume-sequence items ①–③ are DONE; doc-60 build is IN FLIGHT.**

- ① **TASK-490 shipped** (PR #2030 merged: 8 memory promotions + AskUserQuestion allowlist rider + TASK-470's mutate-and-confirm-red clause as Core Principle 9; the 8 memory files + MEMORY.md lines deleted, store at 24 memories). **TASK-488 shipped** (PR #2031 merged after 3 review rounds: rule § A Blocking Question Goes Through a Formal Channel + `blocking-question-channel-check.sh` Stop-hook, 19-case probe; round-3 leftovers rode onto TASK-494 as riders). Both Done in tracker.
- ② **doc-64 Phase 0 DONE**: portability inventory shipped as **doc-69** (278 rows, 5 classifier agents, 12 borderline adjudications); doc-64's Phase 0 bullet updated. Next for doc-64: council pass consumes doc-69.
- ③ **doc-60 owner decisions locked via AskUserQuestion** (recorded in the doc): owner-authored tags · global namespace · over-cap `/chime-in` samples cap-many with a notice · **the multi-character cap becomes admin-configurable** (owner addition). Plan approved (plan file `~/.claude/plans/eventual-enchanting-lynx.md`): 3 PRs. **PR 1 = #2032 MERGED** (`08c15b69a`, tags schema+gateway+dashboard; 9 review rounds — notable: admin routes' silent tags drop caught+fixed round 5, Identity emoji → 🪪 swept through page titles/labels, import seam tests added). **PR 2 = #2033 MERGED** (`e1161be1e`, `multiTagMaxCharacters` registry setting + cap threading; INCLUDES the mention-only truncation-detection bug fix — categorize as Bug Fix in release notes, PR body has the callout; round 4 added write-path cache invalidation + `MAX_POTENTIAL_MENTIONS` ceiling-parity test). **Dev migration `20260809185820_add_personality_tags` APPLIED** (prod rides the next release's premigrate — additive). Owner AskUserQuestion decisions this stretch: models/limits page split (9 pages) · Identity → 🪪 · tags count toward PARTIAL badge · truncation bug fixed in-PR · Promise.all cap-fetch fold · round-4 trio (cache invalidation, cap=1 grammar, description annotate). **PR 3 = #2034 MERGED** (`e670b1bc4`, tag command flows: /random tag filter, /chime-in character→optional + tag XOR, capped+sampled tag fan-out with 🎲 notice, tag autocomplete; both promised riders landed. 2 review rounds — round-1 Medium: emptyTagPoolDetail unescaped echo, fixed; round-2 LOW: cap description scoped to /chime-in. First pre-commit review-panel trial ran on this PR: 3 findings fixed pre-commit, 1 mirror-branch Medium still reached CI review — trial data recorded in TASK-498; TASK-499 filed from round-1 nit). **doc-60 COMPLETE.**
- Also this session: **doc-68** filed (backlog-surface consistency pass, from owner musing), **TASK-493** (stale `ops context` doc filenames), **TASK-494** (Stop-hook sidechain filtering + 3 round-3 riders), **TASK-497** (db-sync schema-skew guard during dev-ahead windows).

**Tag-flow dev smoke (one pass, ~5 min, guild channel with some recent messages; all other shipped surfaces are high-confidence — CI/mutation/review-covered, no manual round):**

- [x] **S1 — dashboard tags write** ✅ PASSED 2026-08-09: `/character edit` on an owned character → Preferences & Tags → enter `Fantasy, sci-fi` → save. Proves the Discord modal → union-schema round trip (PGLite covered the gateway; the modal path is runtime-only). Expect: view shows normalized `fantasy, sci-fi`. Fail: 400 on save, or raw casing displayed.
- [x] **S2 — tag autocomplete vocabulary** ✅ PASSED 2026-08-09: type `/random tag:` and wait for the dropdown. Proves tags survive the wire into the cached client list. Masking: autocomplete cache is 60s — a just-added tag can lag up to a minute; re-open the picker rather than judging the first paint. Expect: `fantasy — N characters`-style entries, count-sorted. Fail: empty dropdown after 60s+, or `[Unable to load tags — try again]`.
- [x] **S3 — tag-filtered /random** ✅ PASSED 2026-08-09: `/random tag:fantasy` (any tagged character counts — the invariant is "pick stays inside the tag pool", not which character). Expect: ephemeral 🎲 pick notice; a character carrying the tag replies. Fail: an untagged character replies, or "No characters carry the tag" despite S1.
- [x] **S4 — cap + over-cap fan-out** ✅ PASSED 2026-08-09 in two halves: default-cap fan-out (`tag:helluva-boss`, 10-pool → picked 5, all 5 delivered in character, notice intact) + cap-change propagation (`multiTagMaxCharacters` 5→2 via `/admin settings set`, observed within TTL: `tag:hazbin-hotel`, 24-pool → picked 2, exactly 2 responders). Pool counts cross-checked the bulk-tag corrections (10 and 24 both exact). Original item (for reference): run `/admin settings set multiTagMaxCharacters 2`, wait ~60s (admin-settings cache TTL — the masking state), then `/chime-in tag:fantasy`. Expect: EPHEMERAL notice `🎲 3 characters carry fantasy — picked 2 at random: A, B` (invoker-only by design — other members see only the replies), then exactly those 2 reply publicly in completion order. Fail: 3 replies (cap ignored), no notice, or notice clobbered by an error. Also covers the admin-setting write path end to end.

Report: pass/fail per item is enough; screenshot only if something looks wrong. Then: doc-64 council pass (consumes doc-69) or drain fill-in. Tags prod migration rides the next release's premigrate (additive). **RESOLVED: beta.197 release DMs never sent** — the GitHub release sat prerelease-flagged (and 196 undemoted, the inverse of what `release:publish` produces — evidence points to a manual `gh release create` in the PM-3 release session), so the announce pipeline skipped it by design in both envs. Fixed 2026-08-09 ~20:40 EDT by flipping both flags (197 → latest, 196 → prerelease); the :41 reconcile announces it. TASK-500 filed: hourly sweep should WARN when the newest release is prerelease-flagged.

**The voice-consistency sitting (~15 min) is at the owner's leisure — do NOT nag or re-surface it as a recommendation.** No calendar pressure exists (probes backed up, retention deadline defused); it unblocks the prompt-assembly cluster + caching Phase 2 whenever it happens, and it costs nothing by waiting.

Owner-idea docs awaiting promotion (council pass before plan-mode): doc-64 (meta-harness plugin spinoff; license section records the owner constraint + MIT-everywhere recommendation, decision pending), doc-65 (private brain repo), doc-66 (coalesce queued user messages — collapse by user alternation; council pass for a hybrid reply shape at pickup), doc-67 (tag-scoped sharing of context/memories/facts; sequenced after doc-60's tag substrate).

**2026-08-09 PM-3 (Fable, release session): beta.197 CUT, MERGED, RELEASED.** Range verified mechanically: 57 commits / 16 PRs (#2013–#2028), zero runtime files (pathspec-excluded diff empty) → risk floor, no smoke items, no migrations, no advisories. Three holistic release reviews, zero blocking findings; finalize resynced develop onto main; v3.0.0-beta.197 tagged + published. Owner filings this session: **doc-66** (message coalescing — same-author runs concatenate, different author breaks the stack, memory/history rows untouched; hybrid room-reply shape gets a council pass at pickup) and **doc-67** (tag-scoped sharing theme + queue bullet — off/all/tag-set scope unified across context, memories, and facts, consuming doc-60 tags; "pantheon of characters that follow me around"). License question answered + recorded in doc-64. Process note: each develop push restarts a release PR's CI — the monitor was re-armed twice for doc nudges before switching to hold-and-batch local commits; batching is the default while a release PR is open.

**2026-08-09 PM-2 (Fable): three process PRs merged + memory sweep.** **#2026** (bare-token + queued-receipt hooks; round-6 HIGH refuted by probe — 1,991/1,991 enqueues carry content, verbatim line is probe case 16) · **#2027** (token economy) · **#2028** (doc-61 economy pass: rules corpus 166,827 → 142,502 B ≈ 6k tok/session saved, first `--surface rules` DOWN-write, worktree-isolation mandate — shipped after I ran a same-tree file-mutating worker and branch-hopped under it). Memory Section 0 sweep: 6 deleted, 7 trimmed, 8 MIGRATE files await TASK-490; backup `../memory-backup-2026-08-09.tar.gz`. Honest-ledger: the TASK-470 weigh-in was missed during the pass (now TASK-490 rider 2).

</details>

---

**The voice-consistency harness is BUILT (#1910/#1911); REMAINING: the owner's ~15-min sitting.** Probes mined 2026-08-04 (18 probes, 3 personas, all JSON-protocol — legacy-XML coverage deliberately declined, owner call), census done (personas `z-ai/glm-5`, judge `anthropic/claude-sonnet-4.5` — family-disjoint), replay + judge done (54 generations, 36 swapped pairs). **Next: blind-review `reports/voice-consistency/judgment-sheet.md` (15 pairs) → `pnpm eval:voice-verdict`** → verdict + numbers into `active-epic.md`. Judge preview: A-vs-B dead even (5/5/8); B-vs-B′ controls 8/18 — the pre-registered p₀>35% under-power flag may trip, read the noise-floor line before trusting a PASS. One probe (d5a31e4e) generated empty on all three arms (arm-symmetric, unbiased). FAIL path: `VOICE_EVAL_ARMS=A,C` re-run → `VOICE_EVAL_GATE_KIND=CA`. **All artifacts backed up** at `~/.claude/projects/-home-deck-Projects-tzurot/voice-consistency-backup/voice-consistency-2026-08-04.tar.gz` (`/reports/` is gitignored; probes cannot be re-mined once the pre-beta.190 retention window closes). Owner has no committed date — do not re-surface as a recommendation. Carried runner nits (fold into any runner touch-up): `afterAll` disconnect guard when PrismaClient construction throws; retry-once wastes a cycle on deterministic parse failures.

Phase 2 (history extraction) starts only after the gate passes. Fill-in units: the drain campaign — **query, do not browse**: `pnpm tracker task list -l state:ready -l size:S --plain`; `--priority high` alone returns only Done (the work moved to medium), so pair `state:ready` with size labels; cluster-batching is dead (2 startable of 15 re-checked). Still-open holds: the 11 too-big items (TASK-4, 17, 44, 48, 49, 58, 78, 98, 153, 386, 407) need individual treatment; TASK-61 blocked on the voice gate; TASK-107 needs a producer sweep across every test tier incl. `tests/e2e`. **Sequencing**: the #1317 cluster and TASK-165 are prompt-assembly content — gated behind the voice-consistency run so arm B stays beta.190's shape.

**beta.191 open threads**: TASK-425 (deduped-quote type guard — promote when DependencyStep emits File entries), TASK-426 (STT-wrapper no-key log split, only if noisy), TASK-410 (bullmq/ioredis v6 migration; both majors dependabot-ignored until then — dependabot reads config from MAIN), the 55 doubled-transcript rows aging out ~Sep 2, doc-59 (BYOK video processing — owner ask).

**Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a content-free presence note · extended-context quota exemption ships AFTER persistence · `maxImages` is a spend cap persisted images do not consume.

**Retention is calendar-only**: 2026-08-09 manual run (owner-approved): 3 never-used bystanders purged (0 characters), 2 warning DMs enqueued (grace → ~2026-09-08). Books: 221 users, 5 in earlier grace (~2026-08-26 expiries) + 2 newly warned; daily nag surfaces expiries. Phase 4 (autonomy) parked BY DESIGN — manual-approval-first; tonight's ~3-minute rubber-stamp run is exactly the trust-evidence its promote-gate wants, if such runs keep being routine. system-model freshness = epic-close re-touch rule (#1814).
