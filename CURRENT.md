# Current

> **Version**: v3.0.0-beta.216 — "drain batches 2–3: hooks residue closed, the hysteresis test's cost, `/inspect` finish reason inert" (6 PRs / 2 runtime / 46 range files, no migrations, merged 2026-09-04 ~18:40Z; finalize done, tagged `latest`). Constituent detail: git + release notes.
>
> **Previous**: v3.0.0-beta.215 — "ceiling extractions + review residue + the first drain batch + the vision body-timing diagnostic" (13 PRs / 4 runtime, 2026-09-04 ~11:40Z).

---

## 🌙 2026-09-04 (afternoon) — drain batch 2 closed (PR B), drain batch 3 whole, beta.216 at its cut point (Fable-driven, nested dispatch)

**Merged: #2329** (TASK-879 Done — the board gate unwraps `bash -c`/`sh -c`/`eval` to depth three with a per-level `scan()` over the lib's now-public `wrapped_command_strings`, each level building its own indexed view so a quoted tracker pathspec inside a wrapper still resolves; the orchestrator falsified my spec's W8 premise — a leading env assignment consumed command position in the extractor, so `FOO=1 bash -c "…"` produced no inner segment at all and any env prefix would have defeated the whole unwrap — and the lib now skips assignment words the way bash does, which gave cwd-drift-guard the same coverage with its own probe case; three review rounds — the depth-3/4 boundary pinned against the hook's own recursion, a continuation-space nit, the depth-4 residual moved into the bulleted gaps list; 90 probe cases, 119 lib tests) · **#2330** (TASK-778 + 853 + 873 Done as one seam — the hysteresis case re-tokenized its fixture 26 times; one hoisted pass sliced per iteration, byte-identical assertions, 1532 ms → 62 ms; TASK-853's three-consecutive-runs clause scoped down in the body to the measured reduction) · **#2331** (TASK-831 Done — the `/inspect` finish-reason default arm renders as an inert code span; the reviewer falsified my spec's "short tokens, do not truncate" prior with the discord.js build-time throw, verified on this checkout, so it truncates to 200 like the Error field; four rounds, the last three each one nit; the `substring(0, 200)` pair recorded as a TASK-704 member). **TASK-827** found already shipped (d2d9e58e1, 08-30) and closed. **Filed: TASK-889** (the attachment suite's real 3 s waits; one case timed out at 8 s under full-suite load during the batch 3 gates — the hysteresis class again). Net today: 2 filed, 10 closed. **v3.0.0-beta.216 CUT + MERGED + PUBLISHED (PR #2332)**: owner approved the cut and the merge; holistic review clean apart from my stale "5 PRs" count (real: 6 / 2 runtime / 46 files, re-derived from `release:range` and corrected before the merge); no migrations; finalize aligned develop; beta.215 demoted. Risk low; observability instead of smoke (the first `/inspect` on a provider-specific finish reason renders the code span). The board's 🚢 block is rewritten for beta.217.

**Honest ledger.** Two spec premises of mine were false and caught downstream: the wrapper extractor's assignment handling (orchestrator) and the finish-reason length bound (reviewer) — both were priors about a producer I had not read. The lossy-pipe guard tripped me twice more on push/gh reads; a `cd` into a package killed a monitor arm and a body edit; the board gate I had just hardened blocked my own branch-hop-then-commit compound; the posture gate refused an 8-line inline test until I wrote it as one line for prettier to reflow. One ai-worker flake (TASK-889's suite) cost a full rerun.

## 🌙 2026-09-04 (midday) — drain batch 2 PRs A + C, the attribution chore, and the beta.216–218 horizon (Fable-driven, nested dispatch)

**Merged: #2326** (TASK-881 + 882 Done — the board gate fails open on every tracker filename because a quoted space-bearing add pathspec scanned as the quote placeholder and an escaped one word-split; fix is a resolvable view in the shared lib (`strip_quoted_indexed` + `resolve_placeholders`) with detection still on the stripped view, field 5 one pathspec per line; the `(?i)` guard is an explicit raise; SIX review rounds at the cap — dash-leading pathspec gap, unread fixture files, escaped-blank semantics now bash-faithful, a stray placeholder codepoint refused up front, a two-add index-continuity pin — each distinct, none self-fed; 76 probe cases, 113 lib tests) · **#2328** (TASK-883 Done — ledger gate flag-arity table + one-token latch; the orchestrator found two of my four spec canaries could not redden and added the case that pins the flag-in-value-position exemption; round 2 added the table-wide typo sweep, and a read-from-hook loop was measured blind to its own typo, so the list is restated with an executable drift check; the exemption's pnpm assumption was probed live with a file-writing script) · **#2327** (owner ask mid-turn: `attribution.sessionUrl: false` — user scope took effect without a restart, project scope shipped with the hook kept as backstop). **TASK-886 closed whole with no code change** (clause 2 measured benign, clause 1's pipefail premise false — every `run:` step of the workflow is `bash -e {0}`, read from a run log). **Board**: rolling three-release horizon (216 drain, 217 the shrink release with **TASK-888**, 218 UX slice or more drain); the shrink lever the owner chose is the batched rule-out pass over the 287 priority-low tasks plus a declined-in-PR default for low process residue. Measured: five organic weeks, 517 filed vs 368 closed, ~44% of filings from process areas that are 28% of the pool. Net today: 1 filed (888, owner call), 4 closed.

**Honest ledger.** My PR A spec carried half of TASK-881's mechanism (the quoted spelling collapses to the placeholder; only the escaped one word-splits) — the orchestrator's runtime trace corrected it. Two of my PR C canaries were unfalsifiable as written; the orchestrator measured and fixed both. The board named the wrong hook for PR A until I read the tasks. TASK-886's clause 1 shipped into a plan on the task's own false premise before I probed the run log. The lossy-pipe guard tripped me five times, all on `gh`/push reads. Ruled out, not filed: the pre-push classifier treats a `.sh` hook edit as docs-only and skips local build/tests — CI runs everything, local-convenience gap. A stray subagent wait loop from the previous session's orchestrator ran 55 minutes after its worktree was removed (TASK-887, feedback draft queued).

## 📋 Open items (near-term)

- **beta.217 is the shrink release** (🚢 in `backlog/now.md`): **TASK-888** half (1) is the next unit — the priority-low digest for one owner sitting — with half (2) the review-gated `06-backlog.md` amendment; drain batches 4–5 run beside it (grounded candidates on the board). TASK-880 stays a prod watch. The Opus effort sweep runs on whichever unit the next Opus window takes — mine it with the effort-tag lens.
- **TASK-816 remaining clauses** (a) ratchet classification recorded at definition (b) named recurring trigger (d) audit reports distance-above-floor — the floors themselves are raised and banked; these are the mechanism clauses. doc-63 owns the theme.
- **Prod watchers live (event-driven, quiet)**: restart recovery (#2253) · guest 429 floor · GLM `model_not_found (rescued)` embeds · **NEW: #2270's first real link-share** (provenance render — `/inspect` observability, no smoke needed) · the #2277 deploy-window transient (accepted, self-heals).
- **TASK-791 (high)** — fix half SHIPPED in #2295 (beta.213). The acceptance clause is a prod watch: the first image after this deploy is described, or logs a classified reason from the router alias instead of `empty_response`. TASK-860 (guest free-floor eligibility) is a sibling watch.
- **🔍 SMOKE ITEM, owner-only (#2259 round 2)**: failed `/inspect` diagnostic with a masked link in the provider error → Error field renders inert literal text. The one clause no local test can close.
- **beta.209 open clauses:** TASK-598 (worktree-push, close after a quiet stretch) · TASK-764 (experiential /deny) · TASK-612 (runtime invalidation) · TASK-782 watch · TASK-795 · TASK-775.
- **Usage posture:** owner call 2026-09-01 (after a surprise weekly reset) — Fable drives with nested dispatch; the Opus single-hop lane is the documented backup when Fable runs low.
- `ConversationHistoryEntry` (pipeline/types.ts) Pick/Omit fold still carried for whichever slice next touches the wire shape.

## 🔧 DRAIN CAMPAIGN — standing; batching is the method

Backlog ~427 open (2026-08-29 digest). **Themed batches, 3–5 same-area size:S per PR (owner call 2026-08-28).** Pre-grounded remainder: 199 solo; held for owner: 527 · 540 · 559 · 531; TASK-599 = the batched owner-decision pass. Query, don't browse: `pnpm tracker task list -s "To Do" -l state:ready -l size:S --plain` (the `-s "To Do"` is load-bearing).

**Carry-forwards (one shape — a check that can't distinguish measured-fine from didn't-measure):** publish the command, never the hand-restated number; negative-control a FIX by running it; a probe that swallows stderr certifies broken code green.

**Structural sweep (this session): nothing new needs structure.** Recurrences all landed on existing rails: commitlint trips (documented most-tripped rules), the branch-gate compound-command timing (each gate's own message carries the split fix), the head-SIGPIPE (lossy-pipe rule, self-caught).

## 🧵 Standing threads (durable)

- **Voice-consistency harness BUILT (#1910/#1911); remaining: the owner's ~15-min sitting** — blind-review `reports/voice-consistency/judgment-sheet.md` → `pnpm eval:voice-verdict`. **Owner's leisure — do not re-surface.** The #1317 cluster + TASK-165 stay gated behind it.
- **Orchestration record**: TASK-513 + TASK-487 (Sonnet-tier ledger) + the Fable nested-dispatch standard. Today's data point: ~9 units through nested dispatch, one worker-tier semantic defect total (the #2274 duplicate tests — spec/orchestrator-attributable, reviewer-caught). Orchestrator failure shape: a check that inherits the assumption it tests → canary, Core Principle 9.
- Owner-idea council queue: doc-64 (meta-harness) · doc-65 (private brain) · doc-66 (coalescing) · doc-67 (tag-scoped sharing) · doc-70/71/72.
- Waffles' venue-leak report: awaiting their Share Memories answer (doc-8 carries the design input either way).
- TASK-514 (mis-channel WARN watch) · TASK-425/426/410 (bullmq/ioredis v6 pending) · 55 doubled-transcript rows age out ~Sep 2 · doc-59 (BYOK video).
- **Owner decisions locked** (still governing): no asset table for attachments · descriptions live 30d keyed to `DAYS_TO_KEEP_HISTORY` · direct replies always re-vision · extended context re-visions only within retention · undescribed old images render a presence note · extended-context quota exemption AFTER persistence · `maxImages` is a spend cap · moderator cascade (TASK-529) + shared-persona-name collision (TASK-528) fix shapes recorded.
- **Retention calendar**: **first prod purge run 2026-09-02 ~03:20Z (owner-approved from the log-channel report): 14 purged (5 warned + grace-expired, 9 never-used, 0 characters), 0 skipped** — now 188 users; 2 in grace (~09-08 expiries are the next event), 0 eligible. `retention:purge --env prod --force` from a non-TTY shell; preview before and after. Phase 4 autonomy parked BY DESIGN.
- **TASK-671 — memory recirculates stale facts**: step 0 is one string (`MEMORY_ARCHIVE_INSTRUCTION` never mentions time); a concrete argument on doc-8's FOR side.
