# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
*.tsbuildinfo

# Prevent accidental JS commits in TS source directories
# (TypeScript compiles to dist/, not alongside source files)
services/**/*.js
packages/**/*.js
!**/*.config.js

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/
junit.xml

# Temporary files
tmp/
temp/
ci-log.txt

# Private data (user data, private personalities)
/data/
/tzurot-legacy/data/
backups/
scripts/utils/uuid-mappings.json

# v2 Legacy Codebase
# Note: tzurot-legacy/ is kept for reference only (not actively maintained)
# Excluded from: ESLint, tests, CI/CD
# Can be git-tracked for historical reference

# Allow example personalities at root for deployment
# (private personalities are in tzurot-legacy/data/)
!personalities/

# Prisma generated client (regenerated via postinstall)
packages/common-types/src/generated/prisma/

# npm lock file (we use pnpm)
package-lock.json

# Dev logs
railway_dev_logs.md
