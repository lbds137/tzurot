{
  "name": "@tzurot/e2e",
  "version": "3.0.0-beta.137",
  "private": true,
  "type": "module",
  "description": "Integration + contract test home. Tests run via the root `pnpm test:integration` (vitest.integration.config.ts), which globs `**/*.{integration,contract}.test.ts` repo-wide — not a per-package script. This package exists to declare the workspace deps those tests import.",
  "scripts": {},
  "dependencies": {
    "@tzurot/ai-worker": "workspace:*",
    "@tzurot/api-gateway": "workspace:*",
    "@tzurot/bot-client": "workspace:*",
    "@tzurot/common-types": "workspace:*",
    "@tzurot/test-utils": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "vitest": "^4.1.9"
  }
}
