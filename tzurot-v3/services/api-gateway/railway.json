{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @tzurot/common-types build && pnpm --filter @tzurot/api-clients build && pnpm --filter api-gateway build"
  },
  "deploy": {
    "startCommand": "node services/api-gateway/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
