---
id: doc-14
title: >-
  Theme: Platform-Portable UX Layer (Discord Design System) — ⏸ PARKED (resumes
  as the immediate next epic)
type: other
created_date: '2026-07-28 11:11'
---

### Theme: Platform-Portable UX Layer (Discord Design System) — ⏸ PARKED (resumes as the immediate next epic)

**⏸ PARKED 2026-07-23** mid-Phase-3, to let the Automated Inactivity Retention epic ([`../../active-epic.md`](../active-epic.md)) through. **This is the beta-exit gate and resumes the moment retention ships** — it is next-up, not shelved. State below is current as of parking: Phase 3 Waves 0–3 shipped (beta.173/174), **Waves 4–6 remain** (see the Phase 3 row).

_Focus: lift the bot's UI + messaging vocabulary to the routing layer's standardization level — encode UX **intent** separately from its Discord expression, so the experience is consistent **by construction** (not reconciled by periodic audits) and portable via adapters. Promoted 2026-07-17 (owner re-sequence: the `/character alias` build deviated from the 04-discord subcommand standards despite the rules table being clear — "no more UX surface until consistency is by construction"; v2-parity PAUSED behind this). Memory System Overhaul parked mid-epic at a natural pause → `doc-8`._

**This epic is the beta-exit gate (owner 2026-07-17).** The project stays in beta because the user-facing surface is still in flux — under strict semver every command-structure change is breaking, so 1.0 is only honest once the surface stops moving. Completion bar: not "components exist" but "the command surface is reconciled onto them and enforcement makes regressions structural." Meta-pattern to guard: Phase 1 shipped and the epic then stalled behind successive re-routes — **see it through**; when new work tries to preempt, the preemption gets surfaced against this line, not silently absorbed.

**Governing artifacts (both ACCEPTED 2026-07-04, boulder #1 — two docs, one system)**:

1. [`docs/proposals/backlog/ux-design-system-spec.md`](../../docs/proposals/backlog/ux-design-system-spec.md) — **the WHAT** (normative): tokens (entity emojis, badge glossary + collision rule, colors, list grammar, sentinels/timestamps), components (browse/detail/dashboard/modals/outcome/pending, expand mechanism, button vocabulary), command grammar (verb table, option conventions, IA moves), discoverability, `/inspect` + owner-surface dispositions. All 21 decisions owner-adopted.
2. [`docs/proposals/backlog/platform-portable-ux-design.md`](../../docs/proposals/backlog/platform-portable-ux-design.md) — **the HOW** (machinery): message catalog + outcome-honesty classifier, builder consolidation, enforcement (ESLint + ratchet + depcruise), phasing (§5); the spec's §9 maps decisions onto phases. Gap inventory G1–G11 in its §3.2.

Plan-mode for each phase starts from the artifacts (+ build-time council per project norm). Do not re-derive design calls — §6/§10 decided them.

### Roadmap (machinery §5 · spec §9)

| Phase | Contents | Status |
| --- | --- | --- |
| 1 — catalog + voice | `ux/catalog` + `ux/render`, outcome-honesty classifier over all gateway writes, in-character straggler sites, `ux:literals` ratchet | ✅ COMPLETE 2026-07-08 — ten-PR train #1550–#1561, ratchet **448 → 92** (−79%; residual = documented exemption classes). Per-PR table in [`cold/epic-log.md`](../../backlog/cold/epic-log.md) |
| **2 — components** | Confirmation hierarchy merge + button-order fixes (G6/G7) · detail-card builder + retrofit ×6 (G5) · modal adoption ×9 per-site-fit (G4) + modal form toolkit (D15: right-input-per-type, Label descriptions, preserve-input-on-failure) · browse list-embed/filter-row builders (G9) + list grammar (§2.4) + empty-state CTAs (D19) · dashboard pagination-by-concern (D14: defaults dashboard 3 pages) · Close-button removal (D18 — precondition satisfied) · post-action button-preservation criterion · `/inspect` disposition pass (spec §6) · Components-V2 pilot on character view (D17) | ✅ **COMPLETE 2026-07-20** (released beta.170–beta.172) — every contents item shipped: confirmations+router (#1697/#1698), alias tiers+pilot (#1701–#1703), browse builders 17/17 (#1700/#1707/#1709/#1710), modal wave (#1711–#1718, zero hand-rolled sites), D18 (#1717), detail cards (#1719; character/view declined per 2-callback ceiling), D14+router migration (#1720), `/inspect` incl. D16 context menu (#1724/#1725), V2 pilot (#1726/#1730, kill switch live). Close-out verified: owner V2 eval PASSED (avatar+Tone/Age confirmed on device); post-action button-preservation criterion satisfied in code (`postActionScreen` — success re-renders browse, error keeps Back-to-Browse; verified at preset, the criterion's origin command, + character/deny). Per-PR detail in [`cold/epic-log.md`](../../backlog/cold/epic-log.md) |
| 3 — vocabulary + enforcement | Verb/option renames (§4.1–4.4) · entity emoji + badge registry (§2.1–2.2) · timestamps/sentinels (§2.5) · custom-ID unification (G8) · ESLint/AST enforcement (G10, retires the grep ratchet) · picker hygiene (§5.1) · `/deny` redesign (§4.5) · G11 choice-set constants · parameter-ordering | **Waves 0–3 ✅ RELEASED** (beta.173 waves 0–2; beta.174 wave 3 = the breaking rename batch — 15 renames + fresh mode + `/help` split + tree moves). **Waves 4–6 REMAIN** (PR-6a factory core+pilot → 6b destructive preset → 7 `/deny` redesign → 8 remaining picker hygiene → 9/10 factory sweep + router adoption, normal minors). Plan APPROVED 2026-07-20 (6-agent grounding + trio council + all owner calls resolved). Completion bar: {D1, D2, D3-residual, D6, D8, D9, D10, D11, D13} + {G8, G10, G11} + parameter-ordering + the three design inputs below (all resolved). Wave order (council-resequenced, guards-first): PR-0 cheap ESLint guards → registry + adoption sweeps → AST rule + ratchet retirement → breaking batch (renames + tree moves + /help restructure) → factory core → /deny + pickers → factory sweep + router adoption. Per-PR log: [`cold/epic-log.md`](../../backlog/cold/epic-log.md) |
| 4 — adapter | Second renderer when a concrete second platform is scheduled — until then the depcruise boundary IS the portability posture. **Candidate platform named (owner, 2026-08-09): Stoat (formerly Revolt)** — named, not yet scheduled; the trigger stands. | trigger-gated |

**Component-vocabulary intake (owner framing 2026-08-09)**: the design system is
the "storybook" — the set of generalized patterns any interface implements, the
Discord bot-client first among them. New UX-pattern ideas land ON this
vocabulary, not beside it. Current intake from the doc-60 smoke session:
**doc-72** (two-level index navigation: hub landing + self-paginating index +
back-to-index; council pass at pickup) and **doc-71**'s `/tag assign`
multi-select browse flow — both build on the Phase-2 browse/dashboard builders
and any new primitive they produce (the jump-select control) joins the shared
component set, once, in the utilities.

### Pilot surface: `/character alias` redesign + scoping tiers (owner-directed 2026-07-17)

The alias command (#1695) works but shipped action-multiplexed (`action: list|add|remove` + retype-the-alias-to-remove) instead of the browse/dashboard shape the 04-discord table prescribes — the incident that promoted this theme. It becomes the design system's **pilot**: small, fresh, and exercising exactly the components Phase 2 must produce (browse + select menu, dashboard with per-row remove, destructive confirm). **Redesign it ON the new components, not before them.**

**Scoping tiers ride the pilot** (owner design input 2026-07-17 — redesigning the surface twice is waste; the dashboard is designed against the tiered model):

- **Implicit global register**: character names/slugs — already resolver step 1, always wins, no work.
- **Global aliases**: existing `personality_aliases` table; write gate narrows from character-owner to **bot-owner only**. v2-migrated rows grandfather in as global (all effectively owner-created).
- **User-scoped aliases**: new tier — a user's personal `@mommy` → character mapping affecting only their own resolution. Schema: `userId` on the alias row (null = global) with per-user uniqueness; resolver step 2 checks the mentioning user's aliases before global ones. The bot-client resolution cache is already keyed `(userId, nameOrId)` — per-user resolution is the cache's natural grain.
- **User-scoped, NOT persona-scoped** (owner-decided): persona-scoping gets messy AND makes aliases unstable across persona switches.
- Verified 2026-07-17: **v2 had no scoping at all** — the tiers are new design, not parity.
- Open product call for the design pass: #1695 lets any character owner add global-effect aliases to their own character; under the tiered model this likely goes away (owners get user-scoped like everyone else; global blessing is the bot owner's). Cross-check the reverse-shadow question in [v2-parity](v2-parity-legacy-retirement.md) Phase 1 — shadowing semantics change per-tier.

### Owner design inputs (2026-07-18) — ALL RESOLVED at Phase 3 planning (2026-07-20)

1. **Breaking-release posture — RESOLVED**: all user-visible contract changes (renames + option renames + tree moves + /help restructure) batch into ONE major-ping beta (Breaking Changes leads, old→new table); no interim release while the batch is partially landed. **Beta-exit (3.0.0) comes separately at epic completion** after enforcement + G8 prove the surface stable; post-batch Phase 3 PRs ship as normal minors.
2. **Onboarding command — RESOLVED**: `/help getting-started` subcommand (owner picked GLM's minority council position — /help is the universal first try, no top-level slot spent, extensible sections). Discord constraint: subcommands kill the bare invocation, so /help itself restructures to `/help commands [command]` + `/help getting-started` — a breaking shape change riding the batch. Still coherent with (not duplicating) the parked onboarding-DM + docs-pipeline themes.
3. **Command-tree semantics — RESOLVED**: grouping + invoke-verb extraction (council unanimous on moves-ride-the-batch): `avatar`/`avatar-clear` and `voice`/`voice-clear` become `set|clear` groups; `/character chat` → top-level `/chat`, `/character random` → top-level `/random` (/character 17→13 entries; 16→18 commands). Placement rule documented in SLASH_COMMAND_UX.md: invoke = top-level, configure/manage = under the entity; budget alarms at 80/100 commands, 20/25 subcommands. Bigger re-home (voice→/voice, template moves) declined — splits character setup for headroom we don't need.

### Phase 1 follow-ups still open (tracker tasks)

System-voice straggler wording (STT/top-catch/truncation) · partial-failure errored-slot delivery · `maxRetries:0` metrics watch · `deletePersona`/`getCachedPersonalities` wrapper widening.

_Per-PR slice detail goes to [`../epic-log.md`](../../backlog/cold/epic-log.md). This file is the parked home for the epic's roadmap, pilot, and design inputs; the earlier 5-dimension audit + catalogued-inconsistencies tables live in this file's git history and the design artifacts' grounding sections._

### Absorbed follow-ups (relocated out of `cold/follow-ups.md`, 2026-07-25)

_Owner directive: UX rows belong to the epic that will sweep them, not to a flat follow-up table where they age against a trigger nobody watches. These nine are epic-scoped — each names a UX phase, wave, or primitive (`CATALOG`, Tier-A/B confirmations, the raw-content ratchet, the alias pilot, `/deny`'s redesign). Verbatim, so nothing is lost in translation; they discharge as their wave ships._

| Item | Why deferred |
| --- | --- |
| Rename the denylist `personality` scope + option to `character` (user-facing terminology) | The deny command's `personality` scope choice + `personality` option + the `❌ Personality scope requires the \`personality\` option.` error (`services/bot-client/src/commands/deny/permissions.ts:92`) still say "personality" user-facing, while the rest of the UI moved to "character" (PR2 terminology sweep). Left as-is in PR2 because this isn't a string swap — the error references the literal command option name, so renaming means changing the scope-choice label + the `personality` option to `character` (commandManifest + int-snapshot regen) and likely migrating/aliasing the internal denylist scope enum. **Fix shape**: rename the scope choice + option to `character`, update the error string, regen the int snapshot; decide whether to migrate the stored scope enum or keep it internal. **Promote when**: next touching the deny command, OR a deliberate terminology-consistency pass. Surfaced 2026-06-24 by the PR2 terminology sweep. |
| Shape-B list-collapse exemptions (browse/search/autocomplete) — The wrapper-conversion PR (UX P1 PR-C) converted the definitive-absence wrappers (memory quartet, character fetch, admin settings) to nullOn404/typed-throw semantics but EXEMPTED the list-collapse class — `deny/browse`, `memory/browse`, `memory/search`, `models/browseUserCache`, `preset/autocomplete`, `preset/browse`, `persona/browse`, `preset/override/browse`, `voice/tts/browse` — where `!result.ok → []` renders an infra failure as "no results". Autocomplete is PERMANENTLY exempt (Discord offers no error affordance in the autocomplete UI; empty choices is the only degradation). Browse/search surfaces CAN show a classified error instead of an empty list. **Promote when**: PR-D's outcome-honesty sweep reaches each browse command (natural rider), or a user reports "my stuff disappeared" during a gateway blip. Filed 2026-07-07 (PR-C wrapper audit). | Deliberate soft degradation today; the sweep converts surface-by-surface. |
| `permissionDenied(action)` intent reads circular — The shared `CATALOG.error.permissionDenied(action)` renders "You do not have permission to {action}" — but many call sites pass an action that itself names a permission (e.g. "manage channel context settings — you need the Manage Messages permission"), yielding "...permission to manage... — you need the... permission". Reads circular across channel/settings, channel/browse, channel/activate, deny/permissions. **Fix shape**: either a `permissionDenied` variant that takes a bare requirement clause, or a wording pass on the call sites. **Promote when**: the catalog gets a wording-polish pass (Phase 2), or a 4th circular site appears. Surfaced 2026-07-08 (PR #1559 review). | Catalog copy quality across all permission-denial surfaces. |
| Surfaced 2026-07-17 (#1697 r3 observation) — Tier-B fail-open ownership check has no ephemeral-only guard: `confirmDestructive.ts`'s `assertInvokerOwnership` fails OPEN when `interactionMetadata` is absent, relying on every call site being ephemeral (true today: history/memory/settings/voice all `deferralMode: 'ephemeral'`). Nothing statically stops a future non-ephemeral Tier-B call site from silently inheriting the fail-open check. **Fix shape**: an assertion or type-level marker binding the factory to ephemeral surfaces, or flip to fail-closed once a non-ephemeral surface exists. **Promote when**: the next Tier-B call site is added (PR-1b migrates purge/account-delete — check there), or any destructive confirmation goes non-ephemeral. | Documented tradeoff is memory-dependent; a guard makes it structural. |
| Surfaced 2026-07-18 (PR-4 pilot scope cut; parked in CURRENT.md until 2026-07-19) — alias browse has no in-place "Add" affordance: adding goes only through `/character alias add` slash options (Tier-0, deliberate at PR-4), so browse→add means leaving the screen. **Fix shape**: an Add button on the browse rendering a toolkit modal (character preselected from browse context per-character mode; character select needed in my-aliases mode — the design question that parked it). **Promote when**: next alias-surface touch, or Phase 3's IA/command-tree pass (which reopens surface ergonomics anyway). | Design-first item — the modal's character-context question is the actual work; the button is trivial after it. |
| Surfaced 2026-07-19 (PR-8 grounding, deliberately out of #1725's slice) — `/inspect` Input view shows attachments as description+type only: `inputProcessing.attachmentDescriptions[]` carries no filename/size/mime because the ai-worker's `DiagnosticCollector.recordInputProcessing` never captured them. **Fix shape**: extend the recorded shape at the collector (ai-worker) + render the extra fields in `buildInputView` (bot-client); additive JSONB, no migration. **Promote when**: next DiagnosticCollector or inspect-payload touch. | Presentation was PR-8's scope; this is the one data-plumbing residue it deliberately left. |
| Surfaced 2026-07-20 (Phase 3 planning census) — `/deny`'s gateway contract has no natural key: entries are `(type, discordId, scope, scopeId)` composites, so edit = upsert-new-then-delete-old (`detailEdit.ts:151-172`, orphan risk hand-handled) and mode-toggle re-POSTs the whole entry. Phase 3's PR-7 redesigns the FORM only (deliberate scope cut). **Fix shape**: dedicated update endpoint or a stable row id in the API surface. **Promote when**: the modal-form redesign makes the upsert shape painful to wire, or an edit-orphan actually occurs. | The composite key works; the redesign is presentation-layer — contract churn needs its own justification. |
| Surfaced 2026-07-20 (#1745 — the AST rule's adoption measurement) — 176 grandfathered raw-content literals across 71 files (`raw-content-allowlist.ts`); burn-down plan: (1) Wave 3's PR-5 copy sweep migrates strings it edits anyway onto the catalog (session-expired class especially — `CATALOG.error.sessionExpired(command)` exists and parameterizes the command name, so future renames stop touching those files); (2) post-batch, one adoption slice per class, biggest mechanical class first (~15 sessionExpired, ~11 transient, ~38 validation/gatewayRejection — intents all exist; ~21 success confirmations may need one small intent addition); (3) bespoke tail migrates on-touch under the ratchet. **Promote when**: PR-5c lands (slices become schedulable), or the weekly margins row shows the total static across a month. | The rule + exact-count staleness test guarantee monotonic decrease; the slices convert "eventually" into "scheduled". |
| Surfaced 2026-07-20 (PR-2b sweep, spec-vs-convention check) — §2.3 of the UX spec reserves WARNING for "destructive confirmation" but the codebase-wide convention is ERROR on destructive confirms (`confirmDestructive.ts:117` + the bespoke memory delete / fact forget screens) — the spec's word and the shipped convention disagree. PR-2b deliberately left every destructive-confirm at ERROR (consistency beats letter-of-spec mid-sweep). **Fix shape**: one deliberate call — either amend §2.3 to say ERROR for destructive confirms (matches shipped reality; red = "stop and read") or swap the confirm class to WARNING in one PR (`confirmDestructive` + the two bespoke memory screens). **Promote when**: PR-7's /deny redesign (it rebuilds a destructive confirm anyway) or the epic's enforcement wave. | One glyph-class decision; either direction is a three-file change — what matters is deciding once, not per surface. |
