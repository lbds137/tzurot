---
id: TASK-487
title: Sonnet worker-tier pilot - record per-unit outcomes
status: Done
assignee: []
created_date: '2026-08-09 15:55'
updated_date: '2026-08-10 13:24'
labels:
  - 'area:process'
  - 'size:S'
  - 'state:observable'
dependencies: []
priority: low
ordinal: 487000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Why: the orchestration skill authorizes model: sonnet on mechanical-class worker units as a measured pilot; without a recording surface the pilot produces no evidence trail (review finding on PR 2027).
What: after each Sonnet-tier unit, append to this task notes: unit name, diff-review findings count, CI cycles to green, verdict (clean / defects).
Acceptance: after ~5 units, decide keep / expand / revert-to-Opus from the tally; then close with the decision recorded.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Unit 1 (2026-08-09): apply the approved economy-pass cut list (~60 precise edits, 3 rules files; PR #2028). Model-side defects in diff review: 0 — all cuts applied exactly, overlapping-cut spans merged correctly, one content flag raised (the barrel-removal claim) that turned out pre-verified by the orchestrator but was the RIGHT caution to raise, and the worker correctly recovered its branch after an orchestrator-caused working-tree collision (orchestrator error, not worker; asterisk on this unit's conditions — same-tree spawn, since banned by the skill's worktree mandate). CI cycles to green: pending (PR in flight at record time). Provisional verdict: clean.

Unit 2 (2026-08-10, PR #2038, TASK-500 prerelease-flag guard, 13-file cross-service diff): diff-review defects: 1 comment-level (test fixture comment described its own array order backwards); 0 semantic. Worker correctly flagged 3 deviations incl. a mandatory import ripple the spec's file list missed. CI cycles: 2 (round-2 fixes were REVIEW findings on orchestrator-approved semantics, not worker defects). Verdict: clean.

Unit 3 (2026-08-10, PR #2039, TASK-491 webhook chunking): diff-review defects: 0; worker mutation-proofed its own chunking assertion unprompted and removed a dead lint suppression it had first added. CI cycles: 3 (rounds driven by reviewer polish on orchestrator-written fixups — incl. an orchestrator-introduced cognitive-complexity trip — not worker defects). Verdict: clean.

Unit 4 (2026-08-10, PR #2040, TASK-435 fixture dedupe, incl. a RESUME for the 4-site sweep the review found missing): diff-review defects: 0 across both stretches; per-consumer reconciliation notes accurate (reviewer independently verified every claim); resume-with-context worked exactly as the skill predicts (no re-grounding cost). The missed 4 copies were an ENUMERATION failure upstream of the worker (task file + grounding agent + orchestrator acceptance grep all scoped to the known 3). CI cycles: 3. Verdict: clean.

Tally at 4 units: 0 semantic defects, 1 comment-level, all review rounds attributable to reviewer polish or orchestrator-side scoping — the Sonnet tier is holding for mechanical-class units. One more unit to the ~5 the acceptance asks for.

Unit 5 (2026-08-10, PR #2045, TASK-503 delivered-gated nag cooldowns): diff-review defects: 0 — faithful mirror of the reference pattern, warn fields matched per-scheduler, both new tests assert the seam; reviewer independently verified every claim and raised only one informational item (dispositioned correct-as-is). CI cycles: 1 (cleanest unit of the pilot — zero fixups). Bonus signal: the unit's FIRST spawn landed on a stale worktree base (16 commits behind develop) and the worker correctly hit the spec's stop condition instead of improvising — exactly the contract behavior the tier needs. Orchestrator-side infra issue, not a worker defect; countermeasure (explicit `develop` start point + ancestor check in every spec's branch-setup step) now standard.

DECISION at 5 units (acceptance reached): **KEEP** — 5/5 units clean, 0 semantic defects across ~30 files of diffs, review rounds never attributable to the worker tier. Sonnet is the standing worker tier for mechanical-class units (spec describes the edit precisely); semantic-class units stay on Opus per the skill. The orchestration skill's "measured pilot" framing should be updated to reflect the settled tier — that edit is review-gated (.claude/skills) and rides the next skills PR (e.g. TASK-498's).

Post-decision unit 6 (2026-08-10, PR #2055, TASK-511 own-voice STT class fix incl. a mid-run scope upgrade delivered via SendMessage): diff-review defects: 0; worker absorbed the consolidation scope upgrade cleanly, flagged the api-gateway cost gap as a deviation instead of scope-creeping into it (filed as TASK-512 by the orchestrator), and mutation-proofed every guard including the two pre-existing June tests. CI cycles: 1, review round 1 merge-clean. Consistent with the KEEP decision.

Post-decision units 7-11 (2026-08-10, appended 2026-08-11 after a PR #2059 review caught the record stopping at unit 6 — outcomes sourced from the session ledger in CURRENT.md's beta.199 log): Unit 7 (PR #2048, TASK-243 computeWindowCap inverted-bounds guard): worker semantic defects 0; the reviewer's mock-reset finding was dismissed with evidence (global afterEach isolates). Unit 8 (PR #2049, TASK-305 sendContractSuccess sweep, ten handlers): worker defects 0; round-1 "no actionable items". Unit 9 (PR #2050, TASK-234 batchDelete outcome honesty, 3 rounds): worker defects 0; the two review catches were ORCHESTRATOR-side (spec placed phase='confirm' before the cancel branch; scope-note verification gap → TASK-508). Unit 10 (PR #2053, TASK-508 destructive-render fallback, 3 rounds): worker defects 0; the worker itself caught the spec naming nonexistent subcommands. Unit 11 (PR #2056, TASK-512 own-voice dispatch skip, 2 rounds): worker defects 0; round-2 no blocking findings. Running total: **11 units, 0 worker semantic defects**; every review round across the record attributes to reviewer polish or orchestrator-side spec/scope, never the worker tier. This file remains the tier's evidence ledger — future Sonnet-tier defects get appended here (Done status does not bar appends; edit the file directly, never the CLI --notes flag).

Unit 12 (2026-08-11, PR #2061, TASK-515 submit-path deploy-window retry — first unit of the Opus 5 ORCHESTRATOR trial, TASK-513): worker semantic defects 0. The unit is notable for the STOP rather than the diff: the worker hit the spec's named max-lines landmine (413/400), refused to improvise a fix that was not authorized (no comment-trimming, no allowlisting, no unauthorized extraction), canaried its own tests, and reported an honest deviation list — including two observability regressions its own change had caused (terminal error logs losing `attempt`, the delivery retry WARN losing `releaseId`) that the orchestrator then fixed. Resumed IN PLACE via SendMessage for the extraction rather than respawned; the resume carried full context at no re-grounding cost, as the skill predicts. Both review rounds on the PR found zero worker-attributable defects — round 1's Medium (retry span vs the gateway's 5s dedup window) was an ORCHESTRATOR-side policy gap in my spec, which never told the worker the call was non-idempotent. Running total: **12 units, 0 worker semantic defects**.

Units 13-15 (2026-08-11, Opus 5 orchestrator trial units 3-5): all three merged, worker semantic defects 0 across all three. Unit 13 (PR #2062, TASK-32 `guard:build-scripts`): the worker caught that my spec named two gates the `@tzurot/tooling` package does not define (`typecheck`/`typecheck:spec`) — verified as my error, not its confusion. 5 review rounds, every finding orchestrator-side. Unit 14 (PR #2063, TASK-24 tracker relative-link gate): 7 rounds, the heaviest of the trial, driven by a genuine widening (`backlog/` added mid-review on owner call) plus my own false completeness claim in the PR body, corrected in place rather than edited away. Unit 15 (PR #2064, TASK-178 audit scan-dir discovery): merge-clean.

Unit 16 (2026-08-11, PR #2065, TASK-139 + TASK-140 dockerfile-dist guard hardening — Opus 5 orchestrator trial unit 6): worker semantic defects 0. Two orchestrator-side infra faults in this unit, neither the worker's: my spec's branch-setup ancestor check was written backwards (`git merge-base --is-ancestor origin/develop HEAD` fails on every fresh pooled worktree), and the worker STOPPED on it correctly rather than working around it; then, resuming that worker via SendMessage silently DROPPED the worktree isolation, so the resumed run branched and edited in the main checkout (no damage — tree was clean; filed as TASK-524). Seven CI rounds, every finding orchestrator- or reviewer-side. Running total: **16 units, 0 worker semantic defects.**

Unit 17 (2026-08-11, PR #2067, TASK-528 participant-name collision — first POST-slate runtime unit, ai-worker): worker semantic defects 0. The worker wrote the canary red-before-green as instructed, reported an unrequested extraction it was forced into (`shouldSkipDuplicateParticipant`, needed once the key-choice comment tripped `max-lines-per-function`) and three dedup tests it added on its own initiative, and flagged both honestly. What the ORCHESTRATOR diff-read added, and the worker had NOT flagged: replacing `delete`+`set` with a same-key `set` changes Map iteration order, which IS the `<participants>` render order — probed (`b,c,a2` vs `a,b,c`), kept as the better behaviour, pinned with a canaried test.

Five review rounds, none blocking. Rounds 2 and 3 each found a DIFFERENT untested branch of the same tie-break — first that `<` and `<=` were indistinguishable across the whole suite, then that every test placed the short name FIRST so the replace-because-incoming-is-shorter path never fired. Both pinned and canaried. **The instructive part is round 4 vs round 3: two runs of the same reviewer over the same diff disagreed, and the OLDER one was right** — round 4 asserted all four branches were pinned when one was not. Round 3 was only read because a monitor stopped mid-round surfaced both on the next fetch; reading only the latest would have shipped the gap under a confident all-clear. Enumerating the condition by hand settled it. Neither review was authoritative, which is the standing position on `claude-review` and now has a concrete instance behind it.

Running total: **17 units, 0 worker semantic defects.**

Unit 18 (2026-08-11, PR #2068, TASK-449 transient-vs-permanent capability miss, ai-worker — a SEMANTIC-class unit, so Opus rather than the Sonnet tier): worker semantic defects **0**. The notable engineering was the worker's: it PROBED the spec's assumption that fake timers could expire the capability cache (measured — a value survived a 10-minute advance, because `TTLCache` holds `lru-cache`'s own clock), and when that closed off the obvious test lever it built an LRU-pressure one and made it **fail-loud unprompted** by asserting `redis.get` fired again. That single unrequested assertion is what kept three later review rounds from resting on a vacuous pass.

Eight review rounds, zero blocking findings. The last four each surfaced something CI structurally could not: a memo with no downgrade path; a wire-level `undefined` that slips past every `!== null` guard — including `clampContextWindow`'s — into `Math.floor(undefined * fraction)`, making the effective window NaN; a WARN-volume regression (a `debug` line promoted to `warn` whose sibling already warned, doubling output for two of three transient causes); and a stale cost claim in the PR body.

**The instructive failure of this unit is the ORCHESTRATOR's.** I claimed the `modelCapabilityPatterns.ts` extraction was FORCED by the 400-line `max-lines` error, measuring with `wc -l`. The rule runs `skipBlankLines` + `skipComments`: the file was 395 raw but **196 counted**, under half the limit. I stated it to the owner, put it in the PR body, and two review rounds accepted it — it read plausibly and it was my file. Caught only because I went to verify the same assumption in the spec for the NEXT unit. Root cause: I treated the `max-lines | 400 | Error` row in the rules table as the operational metric instead of reading the rule's options — the same "a declaration is not the producer" failure I spent the session catching reviewers on. Corrected in the PR body in place rather than edited away; the extraction was kept on its own merits; `TASK-538` files the rules-table annotation.

A second orchestrator shape recurred twice in this unit and is worth naming: **correcting a claim in one surface and believing the job done.** Round 3's cost claim was fixed in the code comment but not the PR body (round 8 caught it); round 4's line count was fixed in the PR body but the same stale claim sat in the code comment (round 4 caught that half). A claim that appears in both code and prose needs both edited in the same action.

**Reviewer-citation tally for this unit: four specifics that did not survive checking** — `assertNever` (does not exist in this repo; the idiom is `const _exhaustive: never`), `ai.ts:68` cited for a nullable `context_length` (that line is `OpenRouterModelTopProvider`, a sibling of the interface actually in the path), api-gateway characterized as fail-closed without tracing what a null does at the save gate (filed as TASK-539 with the premise marked UNVERIFIED), and a round-5 fix scoped to the memo that missed the NaN path entirely. The reviewer's PRINCIPLE was right every time; its SPECIFICS were wrong often enough that applying any of them unverified would have shipped at least one wrong change. Together with unit 17's round-3-vs-round-4 self-contradiction, that is the concrete evidence behind the standing position that `claude-review` has no special epistemic authority.

Running total: **18 units, 0 worker semantic defects.**

Unit 19 (2026-08-11, PR #2069, TASK-534 uncommitted-tracker warning, tooling — MECHANICAL class, Sonnet): worker semantic defects **0**. Spec executed exactly; all five required assertions canaried; the worker noticed on its own that `packages/tooling` defines no `typecheck`/`typecheck:spec` script and substituted `build` (which runs `tsc`) rather than silently skipping the gate, and flagged the substitution. Two deviations reported honestly, both defensible on their face; the orchestrator diff-read overturned one of them (a `typeof result === 'string'` guard whose own comment admitted it existed only to satisfy a test double — moved to the test side as a default mock return).

**SIX review rounds on ~90 lines, and they kept earning it.** Round 2's own fix introduced round 3's bug: a `" -> "` split added for path accuracy ran on every line regardless of status, so an untracked file named `… - plan -> execute.md` would report only `execute.md`. The rider class — a follow-up addition dodging the scrutiny the primary change got. Round 3 was resolved not by the suggested conditional but by `-c status.renames=false`, which deletes the split entirely and makes round 2's "never a worktree-column `R`" comment true by construction rather than incidentally.

**The costliest defect of this unit was found by a probe, not a reviewer, and is an orchestrator lesson about probe DESIGN.** Round 1 fixed octal-escaped non-ASCII paths (`core.quotePath=false`) and I probed end to end with `zz-probe-—-em-dash.md`. Green. Two rounds later a probe chasing something else showed git **also C-quotes any path containing a space**, independent of `quotePath` — and every `tracker/` filename is `task-N - title.md`, so the warning would have wrapped essentially every path it printed in quotes, destroying the one property it exists for. Both probe filenames I had invented were space-free: I verified the *mechanism* using an input that lacked the other property. Filed as the machine-local memory `probe-inputs-from-the-real-corpus` (build the fixture from a real specimen, mutate only what the test needs), since the rules surface is closed during the trial.

Running total: **19 units, 0 worker semantic defects.**

Unit 20 (2026-08-11, PR #2070, TASK-539 catalog-unavailable message, api-gateway — Sonnet): worker semantic defects **0**. The unit began by DISPROVING its own task: TASK-539 was filed to port #2068's fix, with the premise explicitly marked UNVERIFIED. Tracing it inverted the conclusion — `getModelById` does collapse unavailable/absent, but the save gate REJECTS on null, so it is fail-closed and there was no bug to port. What the trace found instead were two honesty defects (a docstring claiming the OpenRouter path skips validation during an outage when it rejects, and an outage message telling users to check their model ID). Worth recording as evidence for the "verify the premise before building" step: the filed fix-shape would have been wasted work.

**Two orchestrator misses, both enumeration.** (1) My spec's verification gates omitted `pnpm test:component` for a package with PGLite tests — the unit tier was green while 18 conformance cases failed with `modelCache.lookupModelById is not a function`, because `RouteDeps['modelCache']` is populated behind an `as unknown as` cast the compiler cannot check. Second occurrence of a spec-gate omission (unit 16 omitted `typecheck:spec`). (2) My PR-body consumer table read as an audit and missed TWO consumers with the same wording defect — `computeRequiresZaiKey` (round 1) and `ensureVisionCapableModel` (round 4), the latter being "THE gate for vision-ness" per its own docstring, used by three route files, with **no direct test at all** — changing its message moved zero tests, which is precisely why a false message survived there.

Reviewer-citation note for this unit: round 2's "verified as correct" section stated that no fixture reaches the real `lookupModelById` and concluded "No other gap found." True for the diff it read, and false the moment its own finding 1 was applied — three route tests went red. A reachability proof is scoped to the call graph at the time it ran.

Running total: **20 units, 0 worker semantic defects.**

The tier remains unchallenged; what the trial units 3-5 stretch actually surfaced is an ORCHESTRATOR failure shape rather than a worker one — twice a check inherited the very assumption it was written to test (an ordering test that passed with the sort removed because the filesystem already returned sorted entries; a "every link verified" claim built from a grep derived from the implementation's own dot-prefix assumption, structurally blind to the 14 bare-relative dead links it then missed). Both were caught — one by a self-canary, one by the reviewer. Recorded here because the ledger's purpose is tier attribution, and this stretch's defects attribute cleanly away from the worker.
<!-- SECTION:NOTES:END -->
