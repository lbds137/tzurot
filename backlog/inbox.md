## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

### General

- 🔒 `[CHORE]` **api-gateway: add global rate limiter on public routes** — Surfaced 2026-05-11 user-prompted security audit after redacting hosted-deployment URL from public docs. The 5 unauthenticated routes (`/health`, `/metrics`, `/avatars/:id`, `/voice-references/:slug`, `/exports/:jobId`) have NO global rate limiter — only the admin denylist mutation has one. A motivated attacker can bulk-hammer these for compute/bandwidth waste, OR enumerate predictable slugs on `/voice-references` to scrape voice samples. **Fix shape**: add `express-rate-limit` with the existing Redis backend (`createRedisDenylistRateLimiter` is the reference pattern in `routes/admin/denylist.ts`); apply globally before the public routes mount. Reasonable defaults: ~60 req/min per IP, configurable via env. ~30-50 LOC plus a per-route exemption mechanism if `/health` needs higher allowance for monitoring. **Promote when**: any sign of public-route abuse in Railway logs, OR opportunistic during next api-gateway security pass.

- 🔒 `[CHORE]` **api-gateway: add helmet() + CORS lockdown** — Surfaced 2026-05-11 same audit. `services/api-gateway/src/index.ts` doesn't apply `helmet()` or any CORS middleware. The public routes are HTTP-served and reachable from any browser; without `helmet()` they lack standard security headers (X-Content-Type-Options, X-Frame-Options, Strict-Transport-Security, etc). Without CORS lockdown a browser-based script on any origin can hit the public routes (less dangerous since they're public anyway, but worth being explicit). **Fix shape**: `app.use(helmet())` near the top of the middleware chain; CORS config that explicitly allows only the bot's own origins (none for the bot use case — the api-gateway is server-to-server, not browser-to-server). ~10 LOC. **Promote when**: opportunistic during the rate-limiter PR above (same security pass).

- 🧹 `[CHORE]` **`TRANSCRIBE_RETRY_ATTEMPTS` naming inconsistent with loop semantics** — Surfaced 2026-05-11 in release PR #1022 review (nit). In `GatewayClient.ts`, the retry loop runs `for (attempt = 1; attempt < TRANSCRIBE_RETRY_ATTEMPTS; attempt++)` then calls `fn()` one final time outside the loop. Total: 3 calls, matching constant value 3 — but a reader has to inspect the loop+final-call structure to verify. `TRANSCRIBE_MAX_ATTEMPTS` (conventional name for "total call count") would make the arithmetic self-documenting. **Fix shape**: rename constant + update the 1-2 references. ~3 LOC. **Promote when**: opportunistic during next GatewayClient.ts touch.

- 🧹 `[CHORE]` **`getChannelHistory` cutoff: `gt → gte` semantic shift documentation** — Surfaced 2026-05-11 in release PR #1022 review (nit). The `computeHistoryCutoff` refactor changed the contextEpoch query from exclusive lower bound (`gt: contextEpoch`) to inclusive (`gte: cutoff`). Messages created at exactly the reset timestamp are now included. No practical impact at millisecond precision, but the semantic shift was unintentional and should either be (a) reverted to `gt:` for behavioral parity, or (b) explicitly documented in `computeHistoryCutoff`'s JSDoc as the intended semantic. **Promote when**: someone reports a unexpected message-just-after-`/history clear` showing up, OR opportunistic during next ConversationHistoryService touch.

- 🧹 `[CHORE]` **`/deny` SCOPE_CHOICES: drop tautological "(specific character)" / "(specific channel)" parens** — Surfaced 2026-05-11 in PR #1020 post-autosquash claude-review (non-blocking, deferred). Current entries: `'Bot (bot-wide)'`, `'Guild (this server)'`, `'Channel (specific channel)'`, `'Character (specific character)'`. The Bot/Guild parens add info (scope breadth + which server); the Channel/Character parens are tautological. Reviewer suggests dropping the redundant clarifiers — Channel and Character labels stand alone given the surrounding context. **Fix shape**: change to `'Channel'` and `'Character'` (or pick a more informative paren like `'Channel (this/a specific one)'` if the prompt feels too terse). ~2 LOC. **Promote when**: opportunistic during next `/deny` touch, OR bundled with the handler-rename item below.

- 🧹 `[CHORE]` **Rename `handleBrowseOverrides` / `handleTtsBrowseOverrides` to match new `list` slash naming** — Surfaced 2026-05-11 in PR #1020 round-3 claude-review (non-blocking). After the `browse → list` slash command rename, the handler functions still carry `Browse` in their identifiers (`handleBrowseOverrides` for `/settings preset list`, `handleTtsBrowseOverrides` for `/voice tts list`). Internal-only naming drift — a reader following `list: handleBrowseOverrides` to the function definition finds a name that says "browse" instead of "list." **Fix shape**: rename the functions + their files (`tts/browse.ts` → `tts/list.ts`, `preset/browse.ts` for the override handler — note `/preset browse` for catalog stays separate), update imports in `voice/index.ts` + `settings/index.ts` + tests. ~6-8 file touches, mechanical. **Promote when**: opportunistic during next touch of either handler, OR alongside a related `/voice tts` or `/settings preset` change.

- 🎨 `[CHORE]` **`/inspect` views: inline-render small content instead of always file-attaching** — Surfaced 2026-05-11 user dev-verification feedback. Several `/inspect` panel views unconditionally emit their content as a downloadable file (e.g., reasoning trace) even when the content would comfortably fit in a Discord ephemeral message (<2000 chars for the message body, or within embed-description limits). The "always file" pattern was simpler at implementation time but produces friction for the small-content case — user has to download + open just to read a 500-character reasoning trace. **Fix shape**: in each affected view builder, check content length first; render inline (embed description or ephemeral message body) when under threshold, fall back to file attachment when over. Thresholds need to match Discord limits (~4096 for embed description, ~2000 for message content). Affected views: at least `buildReasoningView`; audit the others in `services/bot-client/src/commands/inspect/views.ts` for the same pattern. ~50-100 LOC across the affected builders + tests. **Promote when**: opportunistic during next `/inspect` UX pass, OR if the friction comes up again.

- 🧹 `[CHORE]` **Pipeline post-GenerationStep throw silently drops diagnostic data** — Surfaced 2026-05-11 in PR #1016 round-4 claude-review (non-blocking). With `diagnosticCollector` now stashed on `GenerationContext` (so TTSStep can record TTS attribution), an exception thrown by any pipeline step _after_ GenerationStep falls into `LLMGenerationHandler.processJob`'s catch block, which returns an error result without calling `storeDiagnosticLog`. The accumulated GenerationStep flight-recorder data is lost. Today no step after Generation/TTS throws (TTSStep is graceful-degradation only), so the gap is theoretical. But the surface area grows with every new post-GenerationStep step, and a future contributor might assume `diagnosticCollector` on context is always persisted on pipeline exit. **Fix shape**: in `processJob` catch block, check `context.diagnosticCollector` and call `storeDiagnosticLog` (with `failedStep`/`lastSuccessfulStep` from the catch) before returning the error result. Or move the success-path persist into a `finally` and gate on `context.result?.success`. ~10-15 LOC + a test asserting the post-Generation-throw path stores. **Promote when**: a new pipeline step lands after GenerationStep, OR a diagnostic gap on a failed pipeline run gets reported.

- 🧹 `[CHORE]` **`AuthStep` constructor — options-object refactor** — Surfaced 2026-05-10 in PR #1008 claude-review (rounds 1-3, non-blocking). `new AuthStep(apiKeyResolver, configResolver, undefined, sttResolver)` passes `undefined` to skip the 3rd positional `providerRouter` slot — a readability smell as the constructor approaches the 5-param max-params limit. **Fix shape**: convert to options object `new AuthStep({ apiKeyResolver, configResolver, sttResolver })`, update test fixtures. ~30-50 LOC across AuthStep + tests + LLMGenerationHandler call site. **Promote when**: a 5th constructor arg is needed (forces the refactor), OR opportunistic during next AuthStep touch.

- ✨ `[FEAT]` **Tag multiple bots in a single message** — Surfaced 2026-04-30 user request. Discord supports multiple `@mention`s per message but Tzurot currently only routes to one. Cap at 5 simultaneous bot mentions (shapes.inc precedent) to prevent abuse; configurable admin setting. **Fix shape**: `MessageHandler` mention resolution needs to enumerate all matched personalities, dispatch each to the queue with shared context (or split into N independent jobs depending on conversation-thread semantics). Decide: do they share short-term memory (one job, multi-personality response) or run independently (N jobs in parallel)? Independent is simpler. **Promote when**: user-facing demand for multi-personality conversations grows.

- 🐛 `[FIX]` **Memory retrieval shows `Found: 20 (scores 0.65-0.71), Included: 0` with Focus Mode off** — Surfaced 2026-05-03 by user diagnostic screenshot. The LLM Diagnostic Summary showed 20 candidate memories scored in the 0.65-0.71 range but zero included, despite Focus Mode being off. Some threshold/budget interaction is silently dropping all 20. Possible causes: (a) similarity threshold cutoff misconfigured (likely 0.72+), (b) token-budget logic returning 0 even when budget exists, (c) ranking → filter pipeline dropping candidates before inclusion. **Fix shape**: trace the `MemoryService.retrieveRelevant()` → `MemoryBudgetManager` → final-include filter chain; add a structured log between each stage so the drop reason is visible in the diagnostic. Also flagged in same screenshot: `History > 70%! Sycophancy risk!` warning fired alongside, suggesting context budget pressure may be related. **Promote when**: a user reports degraded memory recall, OR opportunistic during next memory-pipeline pass.

- ✨ `[FEAT]` **Character browse: toggle to hide character card fields from non-owners** — Surfaced 2026-05-08 user note. `/character browse` currently shows full public character cards to anyone, including the description / system-prompt fields the creator may want kept private. Some creators want characters chat-accessible globally but with the _definition_ hidden. **Fix shape**: add a per-character `visibility: 'public-chat-public-card' | 'public-chat-private-card' | 'private'` field; `/character browse` and `/character view` non-owner reads return only the bot-public fields (name, slug, avatar, ?short blurb) when set to "private-card". ~50-100 LOC across schema + browse handler + view handler. **Promote when**: a creator complains about exposed prompts.

- ✨ `[FEAT]` **Better UX for "truncated" character card fields** — Surfaced 2026-05-08 user note alongside the above. Currently truncated card fields just say "truncated" — uninformative about what was cut or how to see more. **Fix shape options**: (a) "(truncated, X/Y chars — DM the bot for full text)" with a follow-up command surface, (b) inline `view-more` button that opens an ephemeral with the full content, (c) split long fields across multiple embeds. Combines naturally with the above (visibility toggle) — same UX surface. **Promote when**: bundled with #5 (visibility toggle) OR a user complains about lost content.

- 🧹 `[CHORE]` **Consolidate maxAge filter sites around `computeHistoryCutoff`** — Surfaced 2026-05-10 in PR #1011 fix. The "messages older than N seconds are excluded" semantic now lives in three places: `DiscordChannelFetcher.ts:256-261` (post-fetch loop filter on Discord-extension messages), `ConversationHistoryService.getChannelHistory` Prisma `where` clause, and `ConversationHistoryService.getCrossChannelHistory` Prisma `where` clause. PR #1011 added `computeHistoryCutoff` as the shared helper but only wired the two Prisma callers — `DiscordChannelFetcher` still computes its own cutoff inline. **Fix shape**: have `DiscordChannelFetcher` call `computeHistoryCutoff(maxAgeSeconds, contextEpoch)` and use the returned Date for the loop filter. Single source of truth for the semantic; if a future cascade setting also needs to clamp time (per-channel epoch override, daily-rolling reset, etc.), it goes into the helper once. ~10-20 LOC. **Promote when**: a future cascade setting needs the same time semantic, OR opportunistic during next DiscordChannelFetcher pass.

- 🔬 `[CHORE]` **Audit budget-allocation sites for residual-vs-additive design assumptions** — Surfaced 2026-05-10 in PR #1011 fix. The cross-channel-as-residual bug encoded an implicit design choice ("cross-channel only matters when current is sparse") that didn't match user mental model ("cross-channel is part of memory continuity, always relevant"). PR #1011 flipped that one — but the same shape may exist anywhere else multiple data sources compete for a shared cap. Candidates worth auditing: extended-context attachments vs DB history budget; memory inclusion vs system-prompt budget; reasoning-trace tokens vs response tokens. Each implicit allocation rule is a UX decision in disguise. **Method**: list every `Math.min(remainingX, Y)` / `budget - usedSoFar` site in `ContentBudgetManager`, `ContextWindowManager`, `MemoryBudgetManager`; for each, name the implicit assumption and check if it matches user mental model. **Promote when**: a user reports a similar "feature X is silently disabled when feature Y is busy" surprise, OR opportunistic during next budget-pipeline refactor.

- 🧹 `[CHORE]` **Diagnostic surface for "context dropped because X" pipeline decisions** — Surfaced 2026-05-10 by PR #1011 fix; connects to user's 2026-05-03 note "math might not be mathing" (memory `Found: 20 / Included: 0`). Pipeline stages currently make silent skip/drop decisions (cross-channel skipped because residual budget = 0; memories filtered out by similarity threshold; messages truncated by token budget). The LLM Diagnostic Summary surfaces final counts but not the _why_ of intermediate drops. Users can't tell whether their settings did what they expected without grepping logs. **Fix shape**: each pipeline stage that drops/skips emits a structured "decision" entry (stage name + count + reason) into the diagnostic payload. Diagnostic Summary panel renders them as a "Drops" section. ~80-150 LOC across the pipeline + diagnostic UI. **Promote when**: another silent-skip bug surfaces (high probability), OR opportunistic during next diagnostic-summary pass.

- 🐛 `[FIX]` **`GatewayClient.generate` has the same Railway-deploy exposure as `transcribe`** — Surfaced 2026-05-10 in PR #1009 round 4 claude-review (non-blocking). The transient-network retry shipped in #1009 is scoped to `transcribe` because transcription is idempotent (`requestId`-keyed). `generate()` calls `/ai/generate` with the same undici stack, so the same `UND_ERR_SOCKET` / `ECONNRESET` / `ECONNREFUSED` family can fire during a Railway container swap. **Fix shape**: `generate()` is NOT idempotent at the API level (duplicate AI generation has user-visible side effects), so naive retry isn't safe. Two paths: (a) use the same `requestId` keying to make `generate` idempotent server-side then mirror the retry pattern, OR (b) special-case retry to ONLY the connection-refused-immediately codes (server didn't accept the request at all), distinguishing "request never landed" from "request might have landed but socket died." Lower priority than transcribe — `generate()` has a 60s timeout (vs. transcribe's STT_GATEWAY = 120s) and is less user-visible. **Promote when**: a user reports the deploy-window symptom on a non-transcribe path, OR opportunistic during next GatewayClient pass.

- 🔬 `[CHORE]` **Investigate `@discordjs/rest@2.6.1` REST queue stall under sustained rate-limit pressure** — Surfaced 2026-05-08 in PR #1000. Root cause of the voice-transcription hang fixed in #1000 was `await channel.sendTyping()` blocking forever when discord.js's internal REST queue stalled — the `POST /channels/{id}/typing` promise never resolved nor rejected. The PR's fire-and-forget helper + ESLint guard prevents the whole pipeline from hanging on this class of bug, but the underlying queue stall is still upstream behavior we don't understand. **Investigation shape**: (a) reproduce by hammering `sendTyping` against a single channel under rate-limit pressure; (b) add structured logging around the `RequestManager` queue depth + retry counts; (c) check `@discordjs/rest@2.7.x` and newer changelogs for fixes to queue-stall semantics or hang-on-429 paths; (d) decide between upgrading vs. wrapping all `discord.js` REST calls in a `Promise.race` timeout helper. **Why deferred from #1000**: blast radius — touches every REST call pattern in bot-client, not just `sendTyping`. The narrow fix is sufficient for the immediate bug. **Promote when**: another hang of the same class surfaces against a different REST endpoint, OR opportunistic during next discord.js dependency bump. Surfaced 2026-05-08.

- 🧹 `[CHORE]` **Teach temporal-marker pre-commit hook to skip auto-generated files** — Hit during PR #991 commit. The regenerated `packages/common-types/src/generated/commandOptions.ts` contains an auto-generated header `* Generated at: 2026-XX-XX...` which the hook flags as a temporal marker even though it's a generator meta-stamp, not author archaeology. Required `TZUROT_SKIP_TEMPORAL_CHECK=1` to commit. **Fix shape**: in `.husky/pre-commit`, before scanning a file, check whether the file's first ~5 lines contain `AUTO-GENERATED FILE` or `@generated` marker; if so, skip the file. ~5 LOC. Could check via `head -5 "$file" | grep -q '@generated\|AUTO-GENERATED'`. **Promote when**: a second false-positive on a generated file is observed, OR opportunistic during next hook tweak. Surfaced 2026-05-07.

- 🧹 `[CHORE]` **Resolve `personalityTone` UI/API cap mismatch (UI 255 / API 1000)** — Surfaced 2026-05-06 in PR #983 round 2 claude-review (post `SHORT_PARAGRAPH_MAX_LENGTH` constant introduction). After the PR's refactor, `personality.ts` schema now caps `personalityTone` at `SHORT_PARAGRAPH_MAX_LENGTH` (1000) but `services/bot-client/src/commands/character/sections.ts:74` caps the UI input at `255`. The discrepancy is pre-existing but newly-visible because the constant rename made the schema side self-documenting. **Same data-loss vector as PR #983's primary fix**: any tone value 256–1000 chars stored via direct API would be silently truncated to 255 on dashboard edit (the `ModalFactory.ts:98` pre-fill path strips to UI `maxLength`). **Three options on the table, all with caveats**: (a) bump UI cap to 1000 — UX-questionable because the field is `style: 'short'` (single-line input), and 1000-char single-line input is unusable in practice; (b) lower API schema cap to 255 — **requires a data audit first** to confirm no existing character cards have `personalityTone` values >255 chars; truncating those would be silent data loss equivalent to the bug this PR fixed; (c) document the asymmetry as intentional (UI is the deliberate UX constraint, API is permissive for direct-API users) and add a truncation-warning entry on the persona/character dashboard for the tone field specifically. **Fix shape pending option choice**: ~5 LOC for any of (a)/(b)/(c); option (b) requires a preceding data-audit script + migration if any rows violate the new cap. **Promote when**: opportunistic during next character-dashboard refactor pass, OR if a user reports surprising tone truncation on dashboard edit. Surfaced 2026-05-06 by Vladlena.

- ✨ `[FEAT]` **Distinct error category for `JobPayloadTooLargeError` (UX: "attachments too large" not "media not found")** — Surfaced 2026-05-05 in PR #981 round 4 claude-review (out-of-scope for that PR's constant-bump fix). `LLMGenerationHandler` catches `JobPayloadTooLargeError` from the `DownloadAttachments` pipeline step and maps it to `ApiErrorCategory.MEDIA_NOT_FOUND` along with all other download failures. Users consequently see a "media not found" message in the spoiler-tag error block when they actually hit the aggregate-attachment cap — confusing, since the media IS found, it's just collectively too large. **Fix shape**: add `ApiErrorCategory.MEDIA_TOO_LARGE` (or similar), thread it through the catch in `LLMGenerationHandler.processJob`, give it a distinct user-facing string ("Your message has too many large attachments — try removing or shrinking some"). ~30-50 LOC across the error category enum + handler dispatch + bot-client message rendering. **Why deferred from #981**: pre-existing UX issue not introduced by the constant change; the cap raise to 100 MiB makes it less likely to fire but doesn't eliminate the case (20+ large non-image attachments could still hit it). **Promote when**: opportunistic UX-pass on Discord error messages, OR if a user reports the "media not found" wording as confusing on a known-too-large attachment scenario.

- 🐛 `[FIX]` **Audit other z.ai-specific response fields beyond reasoning** — Surfaced alongside the reasoning gap above. The `reasoning_content` mismatch suggests z.ai may use other snake_case / z.ai-specific field names where we currently only recognize OpenRouter's conventions. **Fix shape**: dump a few z.ai responses (one per model: glm-5.1, glm-5-turbo, glm-4.7, glm-4.5-air), inspect `additional_kwargs` keys, compare against what ai-worker reads. Candidates to check: tool calls, finish reasons, content-filter signals, token usage shape. Note any z.ai-only fields and decide which to plumb through. **Promote when**: alongside the reasoning fix, since the investigation overlaps. ~2-4hr including dump capture + ad-hoc fixes for whatever surfaces.

- ✨ `[FEAT]` **Preset dashboard "set as default" button** — Surfaced 2026-04-24 by user. Add a button to the preset dashboard that lets a user adopt a preset as their personal default in one click; admins additionally see a "set as global default" option. Currently the workflow requires navigating to user-settings (or admin-settings) to set defaults, which is friction when browsing presets. **Why Inbox not Quick Wins**: needs design call on (a) button placement in dashboard, (b) confirmation UX for admin global-default (potentially destructive), (c) how the button reflects current state (e.g. disabled when already default). Surfaced 2026-04-24.

- ✨ `[FEAT]` **User-facing notice when partial attachment failures occur** — Beta.106's hotfix (PR #893) lets the conversation proceed when only some attachments succeed (deliberately better than aborting the whole thread on one bad URL). Side effect: a user forwarding a 5-image post where 2 succeed and 3 fail will see the bot respond to only the 2 surviving images with no indication that 3 were dropped. The response may appear unexpectedly sparse. **Fix shape**: emit a lightweight ephemeral notice ("⚠️ 3 of 5 attachments couldn't be loaded — link expired or external host blocked") alongside the normal response, tied to the per-attachment failure list already produced by `DownloadAttachmentsStep`. Surface via `errorInfo`-style structured field on the success result (e.g. `partialFailures?: string[]`), then bot-client renders the notice. **Why deferred**: hotfix scope; need design call on ephemeral vs. inline placement and on per-failure detail vs. summary count. Surfaced 2026-04-25 by claude-bot review on PR #893 (minor).

### Voice / TTS / STT

- 🔊 `[CHORE]` **Mistral TTS quality below desired bar — eval alternates after NeuTTS Phase 2** — Surfaced 2026-05-12 user feedback ("Mistral still kinda sucks. after NeuTTS I may want to look into a better provider (again)"). Mistral was the right pragmatic call for "ship today" when ElevenLabs subscription was canceled (2026-05-08), but the long-term quality bar needs to be higher for a primary BYOK voice provider. **Sequence**: (1) ship TTS Phase 2 (NeuTTS Air) so self-hosted free-tier voice cloning is in place; (2) bake-off OmniVoice (already in inbox below) + whatever else has surfaced by then against Mistral with actual character voices; (3) decide whether to swap primary BYOK or add a third option alongside Mistral. **Promote when**: TTS Phase 2 ships and quality-shopping headspace returns.

- 🔬 `[CHORE]` **k2-fsa/OmniVoice TTS engine evaluation** — Surfaced 2026-05-06 user note ("TTS to try. Output isn't bad at all"). [k2-fsa/OmniVoice](https://github.com/k2-fsa/OmniVoice) — high-quality voice cloning TTS for 600+ languages. Potential alternative or addition to NeuTTS Air / Pocket TTS in the self-hosted TTS lineup. Evaluate quality, model size, license, GPU requirements, voice-cloning fidelity. **Promote when**: NeuTTS Air phase evaluation reveals quality gaps OR opportunistic during TTS Phase 2 planning OR alongside the Mistral-replacement bake-off above.

- 🔒 `[CHORE]` **`/voice-references/:slug` enumeration risk — switch to UUID or token-gate** — Surfaced 2026-05-11 same audit. Per the route's own docstring, voice references are "intentionally semi-public — anyone with the personality slug can retrieve them." Slugs are predictable (e.g. `lilith`, `abigail`), so an attacker can enumerate the entire voice-clone library by scanning common slugs. The reference audio was uploaded by character creators as a sample, so for public characters this is acceptable; for private characters, a guessable-slug leak is a real privacy concern. **Fix shape options**: (a) switch the public endpoint to `/voice-references/:uuid` keyed by an opaque UUID, with the slug→UUID lookup happening through an authenticated path; (b) keep slug routing but require the voice-engine service secret on this route too (downgrades the "anyone can fetch" property the docstring leans on); (c) add per-character visibility controls and 404 private-character fetches. Each option trades off voice-engine integration shape vs privacy. **Promote when**: a creator reports voice-sample exposure, OR opportunistic alongside the rate-limiter PR above, OR bundled with the future `character visibility toggle` work.

- 🎨 `[CHORE]` **`/voice view <character>` makes no sense as a character-scoped view** — Surfaced 2026-05-11 user dev-verification feedback ("it asks me to pick a character but then I get this view"). Three layered UX problems on the same embed: **(a) Character isn't named anywhere visible** — title is generic "🎙️ Voice Settings" not "Voice Settings for [CharacterName]". User picks a character, gets back something that reads as global. **(b) Cascade source labels conflate "what" with "why"** — phrases like "your TTS default" / "your transcription preference" describe which cascade tier was hit but don't communicate "this is the setting resolved FOR THIS CHARACTER (it falls through to your default because no character-specific override exists)." Reader reasonably concludes the view is showing personal defaults, not character-resolved state. **(c) Cloned Voices section doesn't belong** — the listed voices are user-scoped (your library, 40 entries), nothing to do with the picked character. Showing them under what's labeled "Voice Settings" in a per-character context creates a false implication of association. **(d) Footer is the only character-scoping clue** — "Per-personality settings win over your defaults" hints at scope but doesn't name the character either. **Fix shape**: (1) embed title `🎙️ Voice Settings for **${characterName}**`; (2) annotate each setting line with whether it's character-specific or fell through to default — e.g., `mistral-voxtral-mini _(Mistral, falls back to your default; no per-character override)_` vs `_(set for this character)_`; (3) drop the Cloned Voices section from this view entirely (or move it to a separate `/voice library` / `/voice voices list` command — bundles naturally with the `/voice voices browse` UX overhaul item below); (4) drop or rewrite the footer (it's no longer load-bearing once the lines are clear). ~50-100 LOC in `services/bot-client/src/commands/voice/view.ts`. **Promote when**: opportunistic during next `/voice` UX pass — bundles naturally with the `/voice voices browse` overhaul (both touch voice-library presentation).

- 🎨 `[FEAT]` **`/voice voices browse` UX overhaul: rename + add interactive select menu** — Surfaced 2026-05-11 user dev-verification feedback ("kinda clunky - both the naming scheme and the fact that you can't interact with any of the voices like we usually can with browse commands"). Two interlocking problems: **(a) naming**: `/voice voices browse` stutters ("voice voices") AND `browse` is list-mine semantic (same class as #1020's `browse → list` rename); should likely become `/voice voices list` for naming consistency, OR collapse the doubled noun (e.g., `/voice cloned list`, `/voice library list`). **(b) interaction gap**: the current handler paginates a static list with no select menu, so users have to type voice names verbatim into a separate `/voice voices delete <voice>` subcommand to act on a row. `/character browse` is the codebase's reference pattern: paginated list + select menu where picking a row opens a detail view with actions (delete, etc). **Fix shape**: (1) decide on the naming (rename probably bundles naturally with the interaction work to minimize churn — ASK user before starting); (2) replicate the `/character browse` pattern: select menu on the list embed, picking a voice opens a detail view with `▶ Preview` (if feasible — ElevenLabs provides sample URLs), `🗑 Delete`, `Back to List` buttons; (3) keep the legacy `/voice voices delete <voice>` subcommand as a power-user shortcut OR remove it once the select-menu path covers the use case. ~150-300 LOC across browse handler + new detail handler + tests + customId routing. **Promote when**: user gets sufficiently bothered to prioritize, OR opportunistic during next `/voice` surface touch (likely overlaps with TTS Phase 2 NeuTTS Air work since the voice library will see more use).

- 🎨 `[CHORE]` **`/inspect`: STT/TTS attribution is buried under Token Budget view** — Surfaced 2026-05-11 user dev-verification feedback. The TTS attribution line (added in #1016: `TTS: mistral (via fallback)`) and any STT attribution lives in `buildTokenBudgetView`, which is semantically about prompt-token consumption / cascade allocation — not about voice-pipeline provider routing. User said: "I don't love that the STT/TTS stuff is buried under token explorer or whatever. that's very unintuitive." **Fix shape**: extract voice attribution into its own `/inspect` view (e.g., `buildVoiceAttributionView` rendering both `sttProviderUsed` and `ttsProviderUsed` with fallback annotations) and add a corresponding select-menu entry to the inspect dropdown. Token Budget view keeps the prompt-token data only. Consider including transcript content + audio key + duration in the new view for a complete picture of the voice turn. ~80-150 LOC across views.ts + select menu wiring + tests. **Promote when**: opportunistic during next `/inspect` UX pass — bundles naturally with the inline-render item above.

- 🧹 `[CHORE]` **`sttProvider.ts` JSDoc lists dropped DB columns** — Surfaced 2026-05-11 in release PR #1022 review (minor). The "Stable contract" comment in `packages/common-types/src/types/sttProvider.ts` lists three columns (`users.default_provider`, `users.default_stt_provider_id`, `user_personality_configs.stt_provider_id`); two were dropped in migration #1007. Only `users.default_stt_provider_id` survives. **Fix shape**: update the JSDoc to list only the surviving column. ~2 LOC. **Promote when**: opportunistic during next sttProvider.ts touch.

- 🧹 `[CHORE]` **CHECK constraint will drift from `STT_PROVIDERS` tuple without a guide** — Surfaced 2026-05-11 in release PR #1022 review (observation). `add_stt_provider_check_constraint` migration hardcodes the three valid STT provider values inline. When a 4th STT provider is added to `STT_PROVIDERS`, a developer needs to add a new migration to update the CHECK constraint — there's nothing in the code pointing them there. **Fix shape**: add a one-line cross-reference comment in `sttProvider.ts` near `STT_PROVIDERS` pointing at the migration file path, AND a comment in the migration body pointing back. Adds 0-cost discoverability for the next contributor. **Promote when**: bundled with the existing "STT CHECK constraint companion migration when 4th provider ships" backlog entry, OR opportunistic during next sttProvider touch.

- 🧹 `[CHORE]` **STT CHECK constraint companion migration when 4th provider ships** — Surfaced 2026-05-10 in PR #1008 round-3 claude-review (informational). The `valid_default_stt_provider_id` CHECK hardcodes `'mistral' | 'elevenlabs' | 'voice-engine'`. When a 4th STT provider lands (e.g. Groq, Deepgram, OpenAI Whisper-direct), the migration that adds the new provider needs a companion `ALTER TABLE users DROP CONSTRAINT valid_default_stt_provider_id; ALTER TABLE users ADD CONSTRAINT ... IN (..., 'new-provider')`. **Promote when**: a 4th STT provider is on the roadmap.

- 🧹 `[CHORE]` **Extract `SttDispatch` named type alias to common-types** — Surfaced 2026-05-10 in PR #1008 claude-review (rounds 1-3, all non-blocking). The shape `{ provider: SttProvider; apiKey?: string }` appears inline in 10+ files (`pipeline/types.ts`, `AuthStep.ts`, `DependencyStep.ts`, `GenerationStep.ts`, `autoPromotionFallback.ts`, `MultimodalProcessor.ts`, `AttachmentProcessor.ts`, `ConversationInputProcessor.ts`, `ConversationalRAGTypes.ts`, `ReferencedMessageFormatter.ts`). Works fine today, but if the shape ever grows a third field (e.g. `timeout`, `region`), 10 sites need synchronized edits. **Fix shape**: add `export type SttDispatch = { provider: SttProvider; apiKey?: string }` to `@tzurot/common-types`, sed-replace the inline copies. ~10 LOC. **Promote when**: the shape needs a 3rd field, OR opportunistic during next ai-worker pipeline touch.

- 🧹 `[CHORE]` **Degraded fallback for `sttResolver.resolveProvider()` failures** — Surfaced 2026-05-10 in PR #1008 round-3 claude-review (non-blocking). If the STT resolver throws (DB error, network blip), the exception propagates through `AuthStep.process()` and fails the entire pipeline job — even though STT dispatch is only needed for attachment paths. **Fix shape**: wrap `resolveSttDispatch` in try/catch and degrade to `{ provider: 'voice-engine' }` on resolver error, logging the failure. ~10 LOC. **Promote when**: a real STT-resolver failure causes a user-visible LLM job failure on a non-attachment turn, OR opportunistic during next AuthStep touch.

- 🧹 `[CHORE]` **Startup-time canary for Mistral STT model alias** — Surfaced 2026-05-11 in PR #1015 (Mistral STT model ID fix). The default model is `voxtral-mini-latest` — Mistral's `*-latest` aliases have historically broken on occasion (this exact bug shipped a model ID that was never valid). Without monitoring, those breakages get silently absorbed by the voice-engine fallback path and become invisible to operators (which is what hid this bug for as long as it was hidden). PR #1014 added structured `requestedSttProvider` vs `actualSttProvider` logging that makes _individual_ silent-fallbacks visible in production logs, but a startup-time canary would catch model-alias breakage proactively. **Fix shape**: at ai-worker boot, send a tiny canary audio (~1s of silence or a baked-in WAV byte string) through `mistralTranscribeAudio` with the default model + a system-key fallback. Log `warn` on 4xx/5xx; structured event for downstream alerting. ~30 LOC. **Promote when**: a future model-alias break causes user-visible failure, OR opportunistic during next MistralSttClient touch.

- 🧹 `[CHORE]` **Provider-error `userNotice?: string` field on TtsProviderError base class** — Surfaced 2026-05-10 in PR #1010 (multiple round reviews). Today `TtsDispatcher.buildAttemptNotice` `instanceof`-checks `MistralReferenceAudioTooLongError` directly, coupling the dispatcher to one specific provider's error class. When a 2nd provider error needs a notice (e.g. `ElevenLabsQuotaExceededError`), the dispatcher needs another import + branch. **Fix shape**: add `userNotice?: string` to `TtsProviderError` (or a sibling base class like `NoticeableProviderError`); have provider implementations populate it when constructing the error. `buildAttemptNotice` becomes `error instanceof Error && 'userNotice' in error ? error.userNotice : undefined`. The `NOTICE_CASES` extensibility test in `TtsDispatcher.test.ts` would still apply (just with provider-attached notices instead of dispatcher-side branches). **Promote when**: a 2nd provider error needs a notice, OR opportunistic during next dispatcher refactor.

- 🧹 `[CHORE]` **Make `-# transcribed by X` consistent with LLM model footer (clickable link)** — Surfaced 2026-05-10. The LLM model footer uses `Model: [name](<url>)` (clickable link to the model card — see `buildModelFooterText` in `packages/common-types/src/constants/discord.ts:251`); the STT attribution added in PR #1006 is bare text (`-# transcribed by Mistral`). Make them consistent: `-# Transcribed by [Mistral](<https://mistral.ai/news/voxtral>)` (or wherever the official Voxtral page lives), `-# Transcribed by [ElevenLabs Scribe](<...>)`, `-# Transcribed by [Self-hosted (Parakeet TDT)](<https://huggingface.co/nvidia/parakeet-tdt-0.6b-v2>)`. **Fix shape**: vendor STT engines (Mistral, ElevenLabs) — URL belongs alongside the existing `sttProviderDisplayName()` mapping, since vendor model pages don't change per release. Self-hosted engines (voice-engine → Parakeet TDT today, NeuTTS Air on the Phase 2 roadmap) — hardcode the project page (HuggingFace card, GitHub README, etc.) since there's no API-discoverable URL. Modify `formatProviderAttribution` in `services/bot-client/src/services/VoiceTranscriptionService.ts:30` to emit the markdown link form. ~20 LOC + URL constants. Reuse the existing model-footer admin + user toggle (`showModelFooter` from the config cascade — see `DiscordResponseSender.ts`) to gate the STT attribution too: when a user disables the model footer, the transcribed-by line goes with it. Today the STT attribution is unconditional. **Promote when**: opportunistic during next VoiceTranscriptionService touch, OR if a user asks "what model is this?" about a transcript, OR if a user complains about not being able to suppress the attribution.

- 🧹 `[CHORE]` **Rename `settings-voices` customId prefix to `voice-voices` after stub removal** — Currently the voice browse pagination customIds use the prefix `settings-voices::*` (defined in `services/bot-client/src/commands/voice/voices/browse.ts:36`). PR-TTS-Phase-3-PR-1 (`/voice` consolidation) preserved the prefix as-is to avoid deploy-window in-flight pagination breakage — pre-deploy `/settings voices browse` embeds keep working post-deploy because the prefix routes to `/voice` via the prefix transfer in `componentPrefixes`. **Fix shape**: now that `/settings voices` deprecation stubs are removed (PR #1021), there are no more legacy entry points creating `settings-voices::*` customIds; safe to rename the prefix to `voice-voices` for consistency with the new command path. ~3 LOC across `voices/browse.ts` (prefix string) + `voice/index.ts` (componentPrefixes entry). Update related test fixtures. **Promote when**: opportunistic during next voices/browse touch (likely bundled with the `/voice voices` UX overhaul above). Surfaced 2026-05-09 in TTS Phase 3 PR 1 plan-mode pass.
