## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🧹 `[CHORE]` **`/user/voices/clear` double-lists; count can drift mid-clear** — Surfaced 2026-05-02 in PR #960 round 2 (claude-review). The clear handler lists the user's tzurot-prefixed voices once for the count message and a second time inside the delete loop. If Mistral's account-level voice slate mutates between the two calls (concurrent clone from a parallel session, eviction, etc.), the reported "deleted N voices" count drifts from what was actually deleted. **Fix shape**: PR 3b's `/settings tts` UX puts voice-management inside a dashboard that owns a single `fetchAllTzurotVoices` call and renders both the count and the per-row delete actions from the same snapshot. The double-list disappears as a side effect. **Promote when**: PR 3b begins, OR a user reports a count mismatch in production. Surfaced 2026-05-02 in PR #960.

- ✨ `[FEAT]` **`/user/voices` response surfaces per-provider warnings** — Surfaced 2026-05-02 in PR #960 round 1 (claude-review). The new provider-fanout in `fetchAllTzurotVoices` silently skips providers whose API rejects the key (logs a warn but doesn't tell the user). A user with an expired Mistral key just sees fewer voices and has no signal that something's wrong. **Fix shape**: extend the response shape to include `warnings: string[]` listing skipped providers ("⚠️ Mistral voices unavailable — key expired?"); bot-client browse handler renders the warnings inline below the voice list. **PR 3b absorbs**: when `/settings tts` UX is built, this warning channel can land alongside the voice management UX (same response surface). **Promote when**: PR 3b begins, OR a real user reports the silent-skip confusion. Surfaced 2026-05-02 in PR #960.

- 🧹 `[CHORE]` **`TtsConfigService` must enforce `isTtsProviderId` on write** — Surfaced 2026-05-02 in PR #960 round 1 (claude-review design note). `TtsConfigUpdateSchema` intentionally accepts any string for `provider` (uses `optionalString(40)` for the empty-string-preserve semantics that `LlmConfigUpdateSchema` uses). The schema test pins this looseness explicitly. The service layer (PR 3b's `TtsConfigService.update`) MUST therefore validate the resolved provider against `isTtsProviderId` before writing to the DB — otherwise bad data can reach the `tts_configs.provider` column. **Fix shape**: in `TtsConfigService.update`, after merging the patch with the existing row but before the Prisma write, call `isTtsProviderId(merged.provider)` and throw on false. Add a unit test covering the bypass-attempt path. **PR 3b implements**: this is a service-layer requirement for the PR 3b TtsConfigService itself, not a separate fix-up — flagging here so the implementer doesn't miss it. **Promote when**: PR 3b implementation begins. Surfaced 2026-05-02 in PR #960.

- 🧹 `[CHORE]` **Document BYOK key flow at `dispatchTts` call site** — Surfaced 2026-05-02 in PR #959 final review (claude-review). `MistralTtsProvider.prepare` has a `byokKey === undefined` guard that's effectively dead in production: `runWithTimeout` builds the base ctx without a `byokKey` field, and `buildCtxForProvider` inside the dispatcher hydrates it from `audioProviderKeys` before reaching `synthesize`. The guard only fires in direct-call test paths. A future maintainer adding a provider that bypasses `buildCtxForProvider` will be surprised the safety net isn't there. **Fix shape**: doc comment on the `dispatchTts(...)` invocation in `TTSStep.runWithTimeout` noting "BYOK keys flow via audioProviderKeys → buildCtxForProvider, not the base ctx." **PR 3 absorbs**: when `/settings tts` UX adds new BYOK plumbing, this doc note clarifies the convention. **Promote when**: PR 3 modifies the BYOK threading or adds a new provider type. Surfaced 2026-05-02 in PR #959.

- 🧹 `[CHORE]` **Make `TtsDispatcher.isPrimaryAttempt` invariant explicit** — Surfaced 2026-05-02 in PR #959 final review (claude-review, Medium). The current expression `candidateId === primaryProviderId && attempts.length === 0` relies on `buildFallbackChain`'s Set-based dedup keeping the primary id from appearing twice. Currently safe but load-bearing-invisible. **Fix shape**: introduce an explicit `let primaryAttempted = false` flag and check `!primaryAttempted && candidateId === primaryProviderId`; set the flag after the first match. ~3 lines in `dispatchTts`. **PR 3 absorbs**: minor invariant-clarity refactor; ride along with any other dispatcher work. **Promote when**: any dispatcher touch in PR 3, or if the chain-construction logic ever changes (then this becomes load-bearing). Surfaced 2026-05-02 in PR #959.

- 🐛 `[FIX]` **`MistralApiError(200, ...)` reads as success in log triage** — Surfaced 2026-05-02 in PR #959 final review (claude-review, Minor). Three sites in `MistralTtsClient.ts` (`mistralTTS`, `mistralCloneVoice`, `mistralListVoices`) throw `MistralApiError(response.status, ...)` for malformed-response-body cases inside `if (response.ok)` blocks — so `response.status` is `200`. Log analysis sees `MistralApiError(200, ...)` and reads it as a successful response. **Fix shape**: either use a sentinel status (`0` or `−1`) for "malformed response body" errors, or add a separate error class (`MistralResponseShapeError`) / message prefix. Doesn't affect runtime behavior. **PR 3 absorbs**: when `/settings tts` UX needs better error categorization for user-facing messages, this cleanup naturally lands there. **Promote when**: a real production case where the misleading 200 hampers triage, OR when PR 3 standardizes audio-provider error messages. Surfaced 2026-05-02 in PR #959.

- 🧹 `[CHORE]` **Test for `TtsDispatchError` empty-chain via `dispatchTts`** — Surfaced 2026-05-02 in PR #959 final review (claude-review, Minor). The path where `dispatchTts` is called with a fully-empty chain (e.g., all providers' `canHandle` returns false simultaneously) isn't directly tested. Registry tests cover the self-hosted-omitted case, but the dispatcher's own empty-chain branch (`chain.length === 0`) isn't exercised through `dispatchTts`. ~10-line addition to `TtsDispatcher.test.ts`. **PR 3 absorbs**: any dispatcher test work in PR 3 should add this case. **Promote when**: opportunistically when next touching `TtsDispatcher.test.ts`. Surfaced 2026-05-02 in PR #959.

- 🧹 `[CHORE]` **Comment on `BaseConfigResolver.getExtractSource(_extracted)` `_` prefix** — Surfaced 2026-05-02 in PR #959 final review (claude-review, Nit). The default base implementation doesn't use `_extracted`; only `TtsConfigResolver` overrides do. Per `02-code-standards.md` the `_` prefix is for "cases where you don't control the signature" — this is an abstract-hook pattern where the base receives a parameter only the override needs. Adding `// parameter available to subclass overrides` clarifies it's intentional, not dead code. **PR 3 absorbs**: trivial comment ride-along with any common-types touch. **Promote when**: opportunistically. Surfaced 2026-05-02 in PR #959.

- 🧹 `[CHORE]` **Split `apiKeyValidation.ts` per provider** — Surfaced 2026-05-02 in PR #959 round 4 (claude-review). The file is at 384/400 lines; the next provider validator pushes it past the ESLint `max-lines` error threshold. Per-provider files (`apiKeyValidation/openrouter.ts`, `elevenlabs.ts`, `mistral.ts`, `zaiCoding.ts`) with a barrel re-export keeps each focused and testable. **PR 3 absorbs**: any new provider validator added there should land alongside the split rather than as a 401-line file in violation. **Promote when**: PR 3 starts touching `apiKeyValidation.ts` for any reason. Surfaced 2026-05-02 in PR #959.

- ✨ `[FEAT]` **`/user/voices` route covers Mistral, not just ElevenLabs** — Surfaced 2026-05-02 in PR #959 round 4 (claude-review). The route's `fetchTzurotVoices` only queries the ElevenLabs API. Users with Mistral BYOK can synthesize via Voxtral but can't list/delete their Mistral clones through the bot. **PR 3 absorbs**: the `/settings tts` UX in PR 3 needs to surface voice management for both providers, so this route refactor (or a parallel Mistral voices route) lands there. **Fix shape**: extract a provider-agnostic `fetchUserClonedVoices(userId, providerId)` helper that fans out to whichever providers have BYOK keys; aggregate results. Or two parallel routes. **Promote when**: PR 3 begins. Surfaced 2026-05-02 in PR #959.

- 🐛 `[FIX]` **`filenameFromContentType` has no `audio/pcm` case** — Surfaced 2026-05-02 in PR #959 round 4 (claude-review nit). `services/ai-worker/src/services/voice/MistralTtsClient.ts` falls through `audio/pcm` to `reference.wav` extension. Since Mistral uses the filename extension for format detection, raw PCM audio shipped with a `.wav` extension may parse incorrectly (a WAV file has a 44-byte RIFF header that PCM data lacks). **Practical risk**: low — current frontends produce `audio/wav`/`audio/mpeg`/`audio/ogg`/`audio/m4a`. PCM uploads aren't a real codepath today. **Fix shape**: empirical test whether Mistral handles PCM-as-WAV gracefully or rejects it. If it fails, add a sentinel filename or reject `audio/pcm` upstream in the gateway. **PR 3 absorbs**: as the settings UX expands and admin-facing voice-upload paths surface, retest this and harden. **Promote when**: a real PCM upload happens, OR when PR 3 touches the voice-reference upload path. Surfaced 2026-05-02 in PR #959.

- 🧹 `[CHORE]` **Promote self-hosted-omitted log to startup-level surface** — Surfaced 2026-05-02 in PR #959 round 1 (claude-review). `services/ai-worker/src/services/voice/ttsProviderRegistry.ts` warns when `VOICE_ENGINE_URL` is unset and self-hosted is omitted from the provider chain — but the warn is buried inside the lazy `buildInstances()` path (only fires on first registry access). If self-hosted is the _only_ available provider (no BYOK keys set either), the chain is empty and `TtsDispatchError` throws with no obvious-from-logs cause. **Fix shape**: surface the misconfiguration earlier — either at ai-worker startup (eager registry instantiation in bootstrap, log once at warn level) or at first dispatch attempt (decorate `TtsDispatchError` with the specific cause "no providers registered: VOICE_ENGINE_URL unset and no BYOK keys configured"). Tradeoff: eager startup logs even when the bot has BYOK users for whom self-hosted is irrelevant; lazy-but-decorated error is quieter. Probably the latter is better. **Promote when**: a misconfiguration actually happens in dev/prod and the root cause takes >2 min to track from logs. Surfaced 2026-05-02 in PR #959.

- 🐛 `[FIX]` **TtsConfig system-globals bootstrap path for fresh DBs** — Surfaced 2026-05-02 in PR #958 round 3 (claude-review). The `add_tts_configs_cascade` migration's seed inserts are gated by `WHERE EXISTS (SELECT 1 FROM users WHERE is_superuser = true)` — if a developer sets up a fresh dev DB AFTER this migration lands but BEFORE creating their superuser row, the system-global TtsConfigs (`kyutai-self-hosted`, `elevenlabs-multilingual-v2`, `mistral-voxtral-mini`) never get seeded. Result: `TtsConfigResolver.getFreeDefaultConfig` returns null, tier 5 fallback kicks in (logs a warning, returns hardcoded self-hosted defaults). Silent-but-functional degradation. **Fix shape**: add bootstrap-on-first-run logic somewhere — candidates are (a) `/settings tts default` command in PR 3 of TTS Phase 1 detecting the empty TtsConfig table and creating the system globals on demand, (b) a CLI command (`pnpm ops db:seed-tts-globals`) for manual bootstrap, (c) ai-worker startup creating them if missing. Option (a) likely cleanest since it's gated on actual user need. **Promote when**: PR 3 of TTS Phase 1 (the settings UX), or whenever a fresh dev DB needs seeding and the developer hits the warning. Surfaced 2026-05-02 in PR #958.

- 🐛 `[FIX]` **`wrapUrlsForNoEmbed` walker doesn't cut on `<`** — Surfaced 2026-05-02 in PR #957 round 1 (claude-review). The new character-level walker (post-PR-#956) handles `>` as a cut-point but not `<`. Old regex used `[^\s<>)]+` to exclude both. Input like `https://example.com<rest` produces `<https://example.com<rest>` instead of cutting at `<`. **Practical risk: near-zero** — RFC 3986 says raw `<` should be percent-encoded in URLs, so this only triggers on malformed input; reviewer classified as nit. **Fix shape**: one new branch in the walker — `else if (ch === '<') { cutAt = i; break; }`. Plus a regression test for `<rest` shape. **Why deferred from beta.114**: nit-level, release-frozen until TTS epic completes, can ride along with any future PR touching `wrapUrlsForNoEmbed.ts` (Phase 1 PR work likely will). **Promote when**: opportunistic — next time someone touches the URL-wrapping code, OR if a real-world `<` shape produces visible weirdness in Discord. Surfaced 2026-05-02 in PR #957.

- 🐛 `[FIX]` **Mistral TTS list-voices pagination loop** — Surfaced 2026-05-02 during smoke test. PR 2 of TTS Phase 1 ships with a single-page assumption: `GET /v1/audio/voices?page_size=50` and find-by-name in `items[]`. If a user accumulates >50 cloned voices, a list-and-find on cache miss will silently fail to find the existing voice → re-clone → duplicate `tzurot-X` voices in the account. **Fix shape**: replace single-page list with a pagination loop that walks `total_pages` until target name found OR loop ends. ~10 LOC in `MistralTtsClient.listVoices`. **Why deferred**: at one-voice-per-personality scale, the bot's persona count is well under 50; this would never trigger in current usage. **Promote when**: PR 2's monitor warning ("pagination boundary reached without finding voice X") fires once in real usage. Until then, dead code. Surfaced 2026-05-02.

- 🧹 `[CHORE]` **Mistral slot quota empirical verification** — Surfaced 2026-05-02. PR 2 ships defensive eviction code + mutex modeled on ElevenLabs' "musical chairs" pattern, but Mistral's account-level voice quota (if any) is undocumented and the smoke test (4 clones) was insufficient to probe limits. The eviction code may never fire in practice. **Fix shape**: post-deploy, attempt to clone many voices in a single Mistral account and observe error shape + threshold. Document findings in `docs/research/voice-cloning-2026.md`. If quota exists, validate eviction code paths in production. If not, the eviction code is dead but harmless — keep the mutex anyway (defends against concurrent first-create races). **Promote when**: PR 2 has been in production for 2+ weeks without eviction firing, AND we want to know whether to keep or remove the dead-on-arrival code. Surfaced 2026-05-02.

- ✨ `[FEAT]` **TTS Budget Guard decorator** — Surfaced 2026-05-01 in Kimi K2.6 council review of TTS Phase 1 plan. After Mistral BYOK ships and the cost telemetry log lines (`tts.synthesize` events) are flowing, wrap the provider stack in a `BudgetTtsProvider` that reads `MONTHLY_TTS_BUDGET_CENTS` from config. Counts characters via cost telemetry; force-switches synthesis to free self-hosted (Kyutai/NeuTTS Air) once the budget is exceeded. Directly serves cost-reduction goal as a hard safety net — Mistral cost stays bounded even if usage spikes. **Why deferred from Phase 1**: requires the cost telemetry to have been running long enough to validate accuracy AND a way to aggregate the log events (Railway log queries, or a Redis counter). Implementation belongs after Phase 1 ships and the abstraction is proven. **Promote when**: ~1 month post-Phase-1 deploy, after telemetry has accumulated enough data to set a sensible default budget. Surfaced 2026-05-01.

- ✨ `[FEAT]` **TTS Canary synthesis (parallel-run validation)** — Surfaced 2026-05-01 in Kimi K2.6 council review. When Mistral Voxtral first lands in production, run both Mistral and ElevenLabs in parallel for 1% of TTS requests (behind a flag), compare latency + audio quality empirically, but only upload the Mistral result to Discord. Log the delta. **Why valuable**: empirically de-risks the "85% cost reduction with comparable quality" claim with real-traffic data. Without this, you're trusting blind-test marketing benchmarks. **Why deferred from Phase 1**: requires Mistral to be live first (chicken-and-egg). **Fix shape**: ~50 LOC dispatcher modification to fork the synthesis on a sample, plus a log-event for the comparison. **Promote when**: 1-2 weeks post-Phase-1 deploy, before deciding to fully commit to Mistral as the BYOK default. Surfaced 2026-05-01.

- 🏗️ `[LIFT]` **Voice fingerprint / speaker embedding alongside reference audio** — Surfaced 2026-05-01 in Kimi K2.6 council review. Future-facing design: `fetchVoiceReference(slug)` could eventually return `{ buffer, embedding: Float32Array }` instead of just `buffer`. The embedding field is unused today, but unlocks next-generation providers that accept speaker embeddings rather than raw reference audio (e.g., Zonos's speaker embedding pathway, future Voxtral variants). **Why deferred**: no current provider needs the embedding; YAGNI. **Promote when**: a provider lands that natively accepts embeddings, OR when reference-audio uploads become the bandwidth bottleneck and pre-computed embeddings would shrink payload size. Surfaced 2026-05-01 in Kimi K2.6 council review of TTS Phase 1 plan.

- ✨ `[FEAT]` **OpenRouter TTS as budget tier (preset voices, no cloning)** — Surfaced 2026-05-01 during TTS Phase 1 planning. OpenRouter exposes preset-voice TTS models at very low cost (Kokoro 82M $0.62/1M chars, GPT-4o Mini TTS $0.60/1M, etc.) but does NOT support voice cloning (only the `/audio/speech` proxy, no voices management API). Could plug into the TTS provider abstraction as a 4th provider category for users who want maximum cost savings and accept losing per-persona voice differentiation. **Fix shape**: new `OpenRouterTtsProvider` class (parallel to MistralTtsProvider but stateless — `prepare()` does nothing, `synthesize()` POSTs to `/audio/speech` with preset `voice` field). The TTS preset cascade already supports per-persona provider routing — users could mix-and-match (cheap personas on OpenRouter preset, important personas on Mistral cloning). **Why deferred from Phase 1**: voice cloning is the user's primary requirement; preset-voice tier is a nice-to-have for budget personas. **Promote when**: a budget tier is desired OR a specific persona is appropriate for preset voices. Surfaced 2026-05-01.

- 🧹 `[CHORE]` **Buffer-hash → in-memory LRU cache for TTS text recovery** — Council-suggested upgrade path during PR #955 design review. Today (post-PR-#955) the receive-side handler emits a placeholder text (`🔁 *Forwarded voice message originally spoken by \`{slug}\`...\*`) instead of the actual TTS source text. The placeholder is sufficient for the LLM context but loses fidelity vs. seeing the real utterance. **Fix shape**: at TTS synthesis time (`services/ai-worker/src/services/voice/`), compute a hash of the audio buffer (e.g. `sha256(buf).slice(0,16)`) and store `hash → originalText`in an in-memory LRU cache (single-instance bot-client deployment, so memory is fine;`lru-cache`is already a transitive dep). On the receive side in`VoiceTranscriptionService.handleOwnBotAudio`, after classifying the attachment as bot-authored, download the buffer (already done by the existing STT path), hash it, look up in the cache. Cache hit → use real text. Cache miss → fall back to the current placeholder. **Why deferred from PR #955**: the v1 placeholder is sufficient; the recovery cache requires plumbing across ai-worker → bot-client (where to populate? bot-client uploads, ai-worker synthesizes) and a download step we currently skip. **Promote when**: a user reports the placeholder feels too information-poor in practice, OR alongside any voice-engine refactor that naturally exposes the synthesis-side hash injection point. Surfaced 2026-05-01 in PR #955.

- 🧹 `[CHORE]` **`wrapUrlsForNoEmbed` lookbehind doesn't exclude `[` for Markdown link display-text** — Surfaced 2026-05-01 in PR #956 round 1 (claude-review). The regex `(?<![<(])\bhttps?:\/\/\S+` excludes URLs preceded by `<` (already-wrapped) or `(` (Markdown link target). It does NOT exclude `[`, so a URL used as Markdown link display text — `[https://example.com](https://example.com)` — gets matched: `\S+` captures `https://example.com](https://example.com)`, the walker cuts at the first unmatched `]`, giving `<https://example.com>](https://example.com)`. Reviewer classified as "strictly pre-existing — not introduced by PR #956" and "practical impact is low since `[url](url)` syntax rarely appears in error messages." **Fix shape**: add `[` to the lookbehind exclusion → `(?<![<([])`. Plus test for the `[url](url)` Markdown shape. **Promote when**: full Markdown link awareness is desired (e.g., a provider error message starts using `[url](url)` shape and produces visible weirdness in Discord), OR opportunistically when next touching `wrapUrlsForNoEmbed`. Surfaced 2026-05-01 in PR #956.

- 🐛 `[FIX]` **Audit other z.ai-specific response fields beyond reasoning** — Surfaced alongside the reasoning gap above. The `reasoning_content` mismatch suggests z.ai may use other snake_case / z.ai-specific field names where we currently only recognize OpenRouter's conventions. **Fix shape**: dump a few z.ai responses (one per model: glm-5.1, glm-5-turbo, glm-4.7, glm-4.5-air), inspect `additional_kwargs` keys, compare against what ai-worker reads. Candidates to check: tool calls, finish reasons, content-filter signals, token usage shape. Note any z.ai-only fields and decide which to plumb through. **Promote when**: alongside the reasoning fix, since the investigation overlaps. ~2-4hr including dump capture + ad-hoc fixes for whatever surfaces. ~~**Adjacent diagnostic field naming** (observed 2026-04-28 post-PR-#929 dump): `reasoningDebug.hasReasoningInKwargs` / `reasoningKwargsLength` only count the OpenRouter `reasoning` key, not z.ai's `reasoning_content`.~~ — RESOLVED in PR #954: `buildReasoningDebug` now falls back to `additionalKwargs.reasoning_content` when `reasoning` is absent, with `reasoning` winning when both are present. Three new test cases lock in the precedence + each branch.

- ✨ `[FEAT]` **Preset dashboard "set as default" button** — Surfaced 2026-04-24 by user. Add a button to the preset dashboard that lets a user adopt a preset as their personal default in one click; admins additionally see a "set as global default" option. Currently the workflow requires navigating to user-settings (or admin-settings) to set defaults, which is friction when browsing presets. **Why Inbox not Quick Wins**: needs design call on (a) button placement in dashboard, (b) confirmation UX for admin global-default (potentially destructive), (c) how the button reflects current state (e.g. disabled when already default). Surfaced 2026-04-24.

- ✨ `[FEAT]` **User-facing notice when partial attachment failures occur** — Beta.106's hotfix (PR #893) lets the conversation proceed when only some attachments succeed (deliberately better than aborting the whole thread on one bad URL). Side effect: a user forwarding a 5-image post where 2 succeed and 3 fail will see the bot respond to only the 2 surviving images with no indication that 3 were dropped. The response may appear unexpectedly sparse. **Fix shape**: emit a lightweight ephemeral notice ("⚠️ 3 of 5 attachments couldn't be loaded — link expired or external host blocked") alongside the normal response, tied to the per-attachment failure list already produced by `DownloadAttachmentsStep`. Surface via `errorInfo`-style structured field on the success result (e.g. `partialFailures?: string[]`), then bot-client renders the notice. **Why deferred**: hotfix scope; need design call on ephemeral vs. inline placement and on per-failure detail vs. summary count. Surfaced 2026-04-25 by claude-bot review on PR #893 (minor).
