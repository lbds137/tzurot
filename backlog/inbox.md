## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🧹 `[CHORE]` **Type-narrow `generateByokLlmConfigUuid`'s `provider` parameter (mirror to PR #986's TTS narrow)** — Surfaced 2026-05-07 in PR #986 round 3 claude-review. `generateByokTtsConfigUuid` was just narrowed from `provider: string` to `provider: ByokTtsProvider` (`'elevenlabs' | 'mistral'`). The mirror function `generateByokLlmConfigUuid` in the same file (`packages/common-types/src/utils/deterministicUuid.ts`) still accepts `provider: string`. The function's own JSDoc says "Mirrors {@link generateByokTtsConfigUuid}" — same typo-protection rationale applies. Currently test-only (no production callers; LLM has no equivalent BYOK auto-migration today), so the risk is purely future-callers-passing-typos. **Fix shape**: add `export type ByokLlmProvider = 'openrouter' | 'anthropic'` (or whatever providers are eligible for BYOK LLM rows when that lands) and narrow the parameter. ~3 LOC. **Why deferred from PR #986**: scope-focused on TTS narrow + test-isolation + dormant-code removal; LLM mirror has no production callers to gate against, so the symmetry restoration can wait. **Promote when**: a third call site for `generateByokLlmConfigUuid` lands (the LLM BYOK auto-migration if/when it ships), OR opportunistic alongside any deterministicUuid.ts edit. Surfaced 2026-05-07.

- 🐛 `[FIX]` **`genericSelectMenuHandler.ts` showModal lacks 10062 catch (preset dashboard)** — Surfaced 2026-05-07 in PR #985 final round claude-review. `services/bot-client/src/utils/dashboard/genericSelectMenuHandler.ts:106` calls `interaction.showModal(modal)` after an async `fetchOrCreateSession` on line 79 — same 3-second budget risk that PR #985 just guarded in character + persona dashboards. Currently only `preset/dashboard.ts:211` uses the generic handler, so the user-visible impact is preset-dashboard scoped. **Fix shape**: wrap the `showModal` call with the new `showModalWithTimeoutCatch` helper from `utils/dashboard/`. ~5-10 LOC change in genericSelectMenuHandler.ts plus per-test coverage. **Why deferred from PR #985**: PR #985's scope was character + persona handleSelectMenu specifically; the generic handler is a 5th site that the reviewer correctly identified as "can go straight to backlog rather than blocking." **Promote when**: opportunistic alongside any preset-dashboard refactor, OR if a preset user reports "Interaction Failed" on section click under load. Surfaced 2026-05-07.

- 🏗️ `[LIFT]` **Extract `replyError` into shared `utils/dashboard/replyError.ts`** — Surfaced 2026-05-07 in PR #985 round 2 + round 4 claude-review. Both `character/sectionContext.ts` and `persona/sectionContext.ts` now have near-identical `replyError` private functions (the persona version explicitly says "see character for canonical longer rationale"). The logic is non-trivial: three-branch state machine (`deferred && !replied` → editReply / `replied` → followUp / fresh → reply) plus a load-bearing PRECONDITION about ephemeral flag inheritance. Any future fix needs to land in both files. **Fix shape**: extract to `services/bot-client/src/utils/dashboard/replyError.ts` (parallel to `showModalWithTimeoutCatch.ts` extraction in PR #985). The signature accepts `ButtonInteraction | StringSelectMenuInteraction` (already the type both versions use); the helper would be importable from both sectionContext files. ~20-30 LOC consolidated + one shared test file replacing duplicated tests in both sectionContext.test.ts files. **Why deferred from PR #985**: PR #985's scope was the bug fix, not the DRY-up; reviewer flagged it as "private function, different interaction type signatures, but a backlog note to consolidate when the next change touches both files would be worthwhile." **Promote when**: opportunistic alongside any sectionContext.ts edit (e.g., the preset dashboard adopting a similar context resolver), OR proactively if the rule-of-three rationale shifts (a third dashboard adopts replyError). Surfaced 2026-05-07.

- 🧹 `[CHORE]` **Runtime guard on `replyError` ephemeral precondition (or typed `deferReplyEphemeral` wrapper)** — Surfaced 2026-05-07 in PR #985 round 4 claude-review. The `deferred && !replied` path of `replyError` calls `editReply({ content })` which silently inherits flags from `deferReply` — so a future caller who defers without `{ flags: MessageFlags.Ephemeral }` would publicly expose error messages that were intended to be ephemeral. PR #985 documented this in a JSDoc PRECONDITION block on both `sectionContext.ts` files. **Fix shape options**: (a) runtime guard `if (interaction.deferred && !interaction.ephemeral) logger.warn(...)` inside replyError — adds observability into accidental misuse; (b) typed wrapper `deferReplyEphemeral(interaction)` that callers use instead of raw `deferReply`, encoding the precondition at the API boundary. ~5-15 LOC depending on option. **Why deferred from PR #985**: reviewer explicitly said "Not asking for a change in this PR (the docs are clear)" — documentation suffices for current callers; runtime/type-level guards are belt-and-suspenders for future callers. **Promote when**: a non-handleViewFullButton caller adopts `replyError` (real risk surfaces), OR alongside the `replyError` extraction backlog item above (consolidate into one cross-cutting refactor). Surfaced 2026-05-07.

- 🧹 `[CHORE]` **Dashboard action naming convention drift: kebab-case mixed with snake_case** — Surfaced 2026-05-06 in PR #984 round 4 + round 7 claude-review. `DASHBOARD_ACTIONS` in both `character/dashboard.ts` and `persona/dashboard.ts` mixes naming conventions: existing actions use kebab-case (`confirm-delete`, `cancel-delete`) while the truncation gate uses snake_case (`edit_truncated`, `cancel_edit`, `view_full`, `open_editor`). **Pre-existing in character** — the truncation gate was born snake_case, persona mirrored it. The inconsistency is load-bearing now: the strings are encoded into Discord custom IDs in the wild (any persisted message components in user channels), so a rename can't be a simple find/replace. **Fix shape**: pick a convention (likely kebab-case for new actions to match the existing dashboard precedent) and create a migration path — server reads BOTH old and new for a deprecation window, components write only new. ~50-80 LOC across customId parsers + DASHBOARD_ACTIONS sets + tests; deprecation window of ~1 month for active component messages to age out. **Why deferred from PR #984**: scope explosion; the new action names are part of #984's wire format, can't be quietly changed. **Promote when**: a dashboard refactor pass touches customId parsing, OR a third dashboard wants to adopt the truncation gate (forcing the convention question). Surfaced 2026-05-06.

- 🧹 `[CHORE]` **Resolve `personalityTone` UI/API cap mismatch (UI 255 / API 1000)** — Surfaced 2026-05-06 in PR #983 round 2 claude-review (post `SHORT_PARAGRAPH_MAX_LENGTH` constant introduction). After the PR's refactor, `personality.ts` schema now caps `personalityTone` at `SHORT_PARAGRAPH_MAX_LENGTH` (1000) but `services/bot-client/src/commands/character/sections.ts:74` caps the UI input at `255`. The discrepancy is pre-existing but newly-visible because the constant rename made the schema side self-documenting. **Same data-loss vector as PR #983's primary fix**: any tone value 256–1000 chars stored via direct API would be silently truncated to 255 on dashboard edit (the `ModalFactory.ts:98` pre-fill path strips to UI `maxLength`). **Three options on the table, all with caveats**: (a) bump UI cap to 1000 — UX-questionable because the field is `style: 'short'` (single-line input), and 1000-char single-line input is unusable in practice; (b) lower API schema cap to 255 — **requires a data audit first** to confirm no existing character cards have `personalityTone` values >255 chars; truncating those would be silent data loss equivalent to the bug this PR fixed; (c) document the asymmetry as intentional (UI is the deliberate UX constraint, API is permissive for direct-API users) and add a truncation-warning entry on the persona/character dashboard for the tone field specifically. **Fix shape pending option choice**: ~5 LOC for any of (a)/(b)/(c); option (b) requires a preceding data-audit script + migration if any rows violate the new cap. **Promote when**: opportunistic during next character-dashboard refactor pass, OR if a user reports surprising tone truncation on dashboard edit. Surfaced 2026-05-06 by Vladlena.

- 🐛 `[FIX]` **Persona dashboard missing truncation warning system** — Surfaced 2026-05-06 during persona data-loss fix. The character dashboard has `truncationWarning.ts` that detects when existing data exceeds modal limits and shows a warning with "View Full / Cancel / Edit with Truncation" options before the user enters the modal. The persona dashboard (`/persona edit`) does NOT use this system — it goes straight to `ModalFactory` which silently truncates on pre-fill (`ModalFactory.ts:98`). If a user's persona content ever exceeds `DISCORD_LIMITS.MODAL_INPUT_MAX_LENGTH` (4000), the same silent-truncation data loss will recur. **Fix shape**: wire the persona dashboard's section-opening flow through the same truncation-warning pattern that the character dashboard uses. ~50-100 LOC for the integration + tests. **Promote when**: opportunistic alongside any persona-dashboard edit, OR if a user reports truncation at the 4000-char boundary. Surfaced 2026-05-06.

- ✨ `[FEAT]` **Allow tagging multiple bots in a single message** — User request 2026-04-30. Currently users can only tag/mention one bot personality per message. Request: allow multiple bot mentions in one message, capped at 5 (matching shapes.inc's cap) to prevent abuse. Would require changes to the mention-detection pipeline in bot-client and potentially the LLM context assembly in ai-worker to handle multiple personality responses in one turn. Moderate scope (~2-4hr). Surfaced 2026-04-30.

- 🧹 `[CHORE]` **Remove `/settings tts` and `/settings voices` deprecation stubs** — Surfaced 2026-05-05 during Phase 3 `/voice` consolidation planning. PR 1 of Phase 3 will register `/settings tts` and `/settings voices` as ephemeral deprecation stubs that reply with `"This command has moved to /voice ...". Use /voice view to see your voice settings."` instead of doing the actual work, while routing all real handlers to the new top-level `/voice` namespace. Stubs exist to preserve muscle memory during the migration window. **Fix shape**: delete the stub registrations and their handler files entirely (`services/bot-client/src/commands/settings/tts/` and `services/bot-client/src/commands/settings/voices/`); update the index re-exports; verify `CommandHandler.int.test.ts` snapshot reflects the smaller `/settings` tree. ~10-15 file deletions + index/test updates. **Promote when**: ~1 month after Phase 3 PR 1 ships, OR sooner if a user reports the stubs are confusing rather than helpful, OR if the bot's user count is small enough that an immediate cutover with announcement-channel notice is cheaper than maintaining the stub layer. Surfaced 2026-05-05.

- ✨ `[FEAT]` **Distinct error category for `JobPayloadTooLargeError` (UX: "attachments too large" not "media not found")** — Surfaced 2026-05-05 in PR #981 round 4 claude-review (out-of-scope for that PR's constant-bump fix). `LLMGenerationHandler` catches `JobPayloadTooLargeError` from the `DownloadAttachments` pipeline step and maps it to `ApiErrorCategory.MEDIA_NOT_FOUND` along with all other download failures. Users consequently see a "media not found" message in the spoiler-tag error block when they actually hit the aggregate-attachment cap — confusing, since the media IS found, it's just collectively too large. **Fix shape**: add `ApiErrorCategory.MEDIA_TOO_LARGE` (or similar), thread it through the catch in `LLMGenerationHandler.processJob`, give it a distinct user-facing string ("Your message has too many large attachments — try removing or shrinking some"). ~30-50 LOC across the error category enum + handler dispatch + bot-client message rendering. **Why deferred from #981**: pre-existing UX issue not introduced by the constant change; the cap raise to 100 MiB makes it less likely to fire but doesn't eliminate the case (20+ large non-image attachments could still hit it). **Promote when**: opportunistic UX-pass on Discord error messages, OR if a user reports the "media not found" wording as confusing on a known-too-large attachment scenario.

- 🧹 `[CHORE]` **TTS Ouroboros half-deferrable asymmetry vs LLM/persona pairs** — Surfaced 2026-05-05 during TTS final-sweep PR while adding the int-test scenario for `default_tts_config_id`. Migration `20260418010642` (LLM/persona Ouroboros) made BOTH directions of each circular FK pair deferrable: `users_default_persona_id_fkey` + `personas_owner_id_fkey`, and `users_default_llm_config_id_fkey` + `llm_configs_owner_id_fkey`. The TTS follow-up migration `20260504065151` only made the user→ttsConfig direction deferrable (`users_default_tts_config_id_fkey`), NOT the reverse `tts_configs_owner_id_fkey`. **Why this works today**: `DatabaseSyncService` happens to insert `users` rows before `tts_configs` rows, so the user-side FK is the one that needs deferral. **Why it's a latent gap**: if the sync-order ever flips (e.g., during a refactor of `flushWrites`), the missing deferral on `tts_configs_owner_id_fkey` would surface as an FK violation on tts_config insert. The LLM/persona symmetry exists precisely because order-independence is robust; TTS broke that pattern. **Fix shape**: ~5 LOC migration adding `ALTER TABLE "tts_configs" ALTER CONSTRAINT "tts_configs_owner_id_fkey" DEFERRABLE INITIALLY IMMEDIATE` + matching `drift-ignore.json` entry. **Promote when**: any refactor of `DatabaseSyncService.flushWrites` insert ordering is contemplated, OR opportunistically alongside a TTS schema-touching PR. Surfaced 2026-05-05.

- 🧹 `[CHORE]` **Extract conditional-warning response/log helper for admin config-delete handlers (+ companion cleanups)** — Surfaced 2026-05-04 in PR #978 rounds 4 + post-autosquash claude-review. Both `admin/llm-config.ts` and `admin/tts-config.ts` now have identical `responseBody`/`logFields` ternaries that omit `warning` from the response and log fields when null (clean delete returns `{ deleted: true }`; warning-bearing returns `{ deleted: true, warning: "..." }`). Two near-duplicate ~6-line blocks across two route files. **Fix shape**: extract to a small helper in `services/api-gateway/src/utils/` (e.g., `buildDeleteResponse(opts: { configId, name, warning })` returning `{ responseBody, logFields }`) and call from both admin handlers. ~20 LOC + colocated tests. **Companion cleanups to bundle when this lands**: (i) skip the third `prisma.user.count` query in `checkDeleteConstraints` when a blocker is already known, since the warning is dropped at the route layer on the 400 path — currently a wasted DB round-trip on every blocked delete (LLM `LlmConfigService.ts:600-612`, TTS `TtsConfigService.ts:464-480`); (ii) normalize the pre-existing blocker message asymmetry — LLM says `"Cannot delete: config is used..."` while TTS says `"Cannot delete: TTS config is used..."` — pre-existing before #978 but worth aligning when the helper extraction touches both files. **Why deferred from PR #978**: reviewer explicitly flagged the helper extraction as "not worth extracting now, but flagging in case a third config type lands" — pre-extracting at N=2 is YAGNI; the two companion cleanups are adjacent and cheap to bundle when the helper lands. **Promote when**: a third admin config-delete handler with the same warning-discriminator shape lands (e.g., new memory-config or audio-config domain), OR opportunistic alongside any admin-routes refactor pass.

- 🐛 `[FIX]` **Trim Mistral reference audio to ≤30s (or surface user-facing notice on overflow)** — Surfaced 2026-05-04 in PR #974 (#83 investigation). PR #974 added a pre-flight check that skips Mistral cleanly when reference audio exceeds the 30s limit, and falls through to self-hosted (Kyutai) instead. Functionally correct, but a user who configured Mistral primary for a personality with >30s reference audio gets Kyutai-quality output without explicit notice — the personality silently degrades to self-hosted forever. **Fix shape**: three options — (a) silently trim the reference audio to 30s before sending to Mistral (risk: the latter half of the speaker's identifying characteristics is lost, voice quality may suffer); (b) emit a one-time bot-owner-visible notice ("personality X's voice reference is N.NNs and exceeds Mistral's 30s limit — falling back to self-hosted; consider re-uploading a shorter reference") via Discord ephemeral or status panel; (c) accept the silent fallback (current behavior post-PR-#974). ~30-100 LOC depending on option. **Why deferred from PR #974**: product UX call, not a correctness fix. **Promote when**: a bot owner reports surprise about voice quality on a specific personality, OR opportunistic UX-pass on TTS error reporting.

- 🧹 `[CHORE]` **Distinguish transient vs deterministic failures in `MistralTtsProvider` negative cache** — Surfaced 2026-05-04 in PR #974 (#83 investigation). The 5-min negative cache in `MistralTtsProvider` catches all non-rate-limit failures uniformly. PR #974 added one explicit skip for `MistralReferenceAudioTooLongError` (deterministic from input — caching adds nothing because re-running with the same input fails the pre-flight check anyway). Other deterministic failures (e.g., 401 auth errors, malformed reference) are still uniformly cached even though they have the same "won't change on retry" property. Transient failures (5xx, network blips, response-shape errors) genuinely benefit from the cache. **Fix shape**: route the catch block through `error.isTransient` (already exists on `MistralApiError`, `MistralResponseShapeError`, `MistralReferenceAudioTooLongError`). Only set the negative cache when `isTransient === true`; deterministic failures throw through cleanly without poisoning the cache. ~15-20 LOC + tests. **Why deferred from PR #974**: opportunistic refactor — current behavior is functionally correct (just slightly less efficient on retry storms after deterministic failures). **Promote when**: opportunistic, alongside the next `MistralTtsProvider` edit OR if cache-poisoning produces a debuggable user-visible delay.

- ⚡️ `[PERF]` **Per-instance `evictionLock` serializes `prepare()` calls across all slugs** — Surfaced 2026-05-04 in PR #967 round 2 release-gate review (severity: 🟡 Design, non-blocking). Both `ElevenLabsTtsProvider` and `MistralTtsProvider` use a per-instance promise-chain mutex for `prepare()` to prevent concurrent-clone-double-write races. The mutex is correct for that purpose but has a non-obvious implication: ALL `prepare()` calls across ALL slugs serialize on the same lock. If slug `emily`'s clone times out after 60s, slug `alex`'s `prepare()` waits the full 60s for the mutex slot — even though they're unrelated voices. Worst-case TTS latency for any persona becomes `(N_concurrent_slow_prepares × timeout)`. **Fix shape**: per-slug lock map (`Map<slug, Promise<unknown>>`) keyed before the chain. Parallelizes across unrelated slugs while still serializing same-slug races. ~30 LOC across both providers. **Why deferred from PR #967**: works correctly today at current traffic scale. **Promote when**: workload involves simultaneous persona switches with cold caches AND latency complaints surface, OR opportunistic when next touching either provider. Surfaced 2026-05-04 in PR #967.

- 🏗️ `[LIFT]` **Extract `SingletonFlagResolver` class to replace module-level mutable state in {llm,tts}ConfigSingletons.ts** — Surfaced 2026-05-04 in PR #968 round 4 review (pre-existing concern from `llmConfigSingletons.ts`). Both `llmConfigSingletons.ts` and `ttsConfigSingletons.ts` use module-level `let pendingResolutions: SingletonResolution[] = []` for cross-call state. Each module owns its own list (no LLM↔TTS contamination), but if `finalize*` is ever called without a preceding `prepare*` in the same process, it silently no-ops on stale state from a prior run. Tests handle this correctly by sequencing prepare→finalize, but the type system doesn't enforce it. **Fix shape**: extract a generic `SingletonFlagResolver<TConfig>` class with instance-level `pendingResolutions` and a typed `prepare()` / `finalize()` interface. `DatabaseSyncService` constructs one instance per singleton table (LLM + TTS) and the sequencing becomes explicit. ~100 LOC including parallel test file consolidation. **Why deferred**: pre-existing concern not introduced by PR #968; both helpers ship correctly today via the existing prepare→finalize sequencing in `DatabaseSyncService`. **Promote when**: the sync machinery grows a third singleton table (which would make the extraction's value visible), OR opportunistically when next touching either singleton helper. Surfaced 2026-05-04 in PR #968.

- 🏗️ `[LIFT]` **Smart per-user cache invalidation for LLM + TTS configs (cross-cutting)** — Surfaced 2026-05-03 during PR 3b plan. `LlmConfigService.invalidateCacheSafely()` (`services/api-gateway/src/services/LlmConfigService.ts:633-644`) and the upcoming `TtsConfigService` mirror both call `cacheInvalidation.invalidateAll()` unconditionally on every mutation. This nukes every user's resolved-config TTLCache regardless of whether they were affected by the change. A better pattern exists already (`ConfigCascadeCacheInvalidationService` enumerates affected users and publishes per-user events), but neither LLM nor TTS uses it. **Fix shape**: upgrade BOTH services together to (a) enumerate affected users when a global config changes (query `User` + `UserPersonalityConfig` + `PersonalityDefaultLlmConfig`/`PersonalityDefaultTtsConfig` for users referencing this configId), (b) publish `{ type: 'user', discordId }` events per affected user instead of `{ type: 'all' }`, (c) only fall back to `invalidateAll()` for unenumerable changes (e.g., `setAsFreeDefault` since it affects every guest user). ~200-300 LOC across both services + colocated tests. **Why deferred**: same shape as the `clear-default` UX gap — fixing only TTS would create LLM/TTS divergence in cache strategy. The cross-cutting fix is a focused PR after PR 3b lands. **Promote when**: opportunistically when next touching either config service, OR if the unconditional invalidation produces measurable cache-thrash impact in production. Surfaced 2026-05-03.

- 🏗️ `[LIFT]` **Split `LlmConfigService` + `TtsConfigService` into Query/Mutation/Cache services (cross-cutting)** — Surfaced 2026-05-03 during PR 3b council review. Both services hover at ~600+ raw LOC with mixed responsibilities (query / mutation / cache wiring). Currently passes lint at this density (skip-blanks/skip-comments brings content count under 400). The council recommended pre-splitting; user chose to mirror LLM as single-file for now and split BOTH later when the burden becomes real. **Fix shape**: when either service crosses the lint threshold OR adding new methods would push it over, split BOTH into `*QueryService.ts` + `*MutationService.ts`, leave cache wiring inline in the mutation service. Mirror the split for both LLM and TTS in the same PR to maintain symmetry. **Why deferred**: pre-splitting one but not the other creates asymmetry; current single-file mirror is the symmetric choice. Avoiding a YAGNI refactor until the lint threshold actually trips. **Promote when**: ESLint `max-lines` flags either service, OR a new feature requires expansion that would push either over 400 content-lines. Surfaced 2026-05-03.

- 🐛 `[FIX]` **`wrapUrlsForNoEmbed` walker doesn't cut on `<`** — Surfaced 2026-05-02 in PR #957 round 1 (claude-review). The new character-level walker (post-PR-#956) handles `>` as a cut-point but not `<`. Old regex used `[^\s<>)]+` to exclude both. Input like `https://example.com<rest` produces `<https://example.com<rest>` instead of cutting at `<`. **Practical risk: near-zero** — RFC 3986 says raw `<` should be percent-encoded in URLs, so this only triggers on malformed input; reviewer classified as nit. **Fix shape**: one new branch in the walker — `else if (ch === '<') { cutAt = i; break; }`. Plus a regression test for `<rest` shape. **Why deferred from beta.114**: nit-level, release-frozen until TTS epic completes, can ride along with any future PR touching `wrapUrlsForNoEmbed.ts` (Phase 1 PR work likely will). **Promote when**: opportunistic — next time someone touches the URL-wrapping code, OR if a real-world `<` shape produces visible weirdness in Discord. Surfaced 2026-05-02 in PR #957.

- 🧹 `[CHORE]` **Mistral slot quota empirical verification** — Surfaced 2026-05-02. PR 2 ships defensive eviction code + mutex modeled on ElevenLabs' "musical chairs" pattern, but Mistral's account-level voice quota (if any) is undocumented and the smoke test (4 clones) was insufficient to probe limits. The eviction code may never fire in practice. **Fix shape**: post-deploy, attempt to clone many voices in a single Mistral account and observe error shape + threshold. Document findings in `docs/research/voice-cloning-2026.md`. If quota exists, validate eviction code paths in production. If not, the eviction code is dead but harmless — keep the mutex anyway (defends against concurrent first-create races). **Promote when**: PR 2 has been in production for 2+ weeks without eviction firing, AND we want to know whether to keep or remove the dead-on-arrival code. Surfaced 2026-05-02.

- ✨ `[FEAT]` **TTS Budget Guard decorator** — Surfaced 2026-05-01 in Kimi K2.6 council review of TTS Phase 1 plan. After Mistral BYOK ships and the cost telemetry log lines (`tts.synthesize` events) are flowing, wrap the provider stack in a `BudgetTtsProvider` that reads `MONTHLY_TTS_BUDGET_CENTS` from config. Counts characters via cost telemetry; force-switches synthesis to free self-hosted (Kyutai/NeuTTS Air) once the budget is exceeded. Directly serves cost-reduction goal as a hard safety net — Mistral cost stays bounded even if usage spikes. **Why deferred from Phase 1**: requires the cost telemetry to have been running long enough to validate accuracy AND a way to aggregate the log events (Railway log queries, or a Redis counter). Implementation belongs after Phase 1 ships and the abstraction is proven. **Promote when**: ~1 month post-Phase-1 deploy, after telemetry has accumulated enough data to set a sensible default budget. Surfaced 2026-05-01.

- ✨ `[FEAT]` **TTS Canary synthesis (parallel-run validation)** — Surfaced 2026-05-01 in Kimi K2.6 council review. When Mistral Voxtral first lands in production, run both Mistral and ElevenLabs in parallel for 1% of TTS requests (behind a flag), compare latency + audio quality empirically, but only upload the Mistral result to Discord. Log the delta. **Why valuable**: empirically de-risks the "85% cost reduction with comparable quality" claim with real-traffic data. Without this, you're trusting blind-test marketing benchmarks. **Why deferred from Phase 1**: requires Mistral to be live first (chicken-and-egg). **Fix shape**: ~50 LOC dispatcher modification to fork the synthesis on a sample, plus a log-event for the comparison. **Promote when**: 1-2 weeks post-Phase-1 deploy, before deciding to fully commit to Mistral as the BYOK default. Surfaced 2026-05-01.

- 🏗️ `[LIFT]` **Voice fingerprint / speaker embedding alongside reference audio** — Surfaced 2026-05-01 in Kimi K2.6 council review. Future-facing design: `fetchVoiceReference(slug)` could eventually return `{ buffer, embedding: Float32Array }` instead of just `buffer`. The embedding field is unused today, but unlocks next-generation providers that accept speaker embeddings rather than raw reference audio (e.g., Zonos's speaker embedding pathway, future Voxtral variants). **Why deferred**: no current provider needs the embedding; YAGNI. **Promote when**: a provider lands that natively accepts embeddings, OR when reference-audio uploads become the bandwidth bottleneck and pre-computed embeddings would shrink payload size. Surfaced 2026-05-01 in Kimi K2.6 council review of TTS Phase 1 plan.

- ✨ `[FEAT]` **OpenRouter TTS as budget tier (preset voices, no cloning)** — Surfaced 2026-05-01 during TTS Phase 1 planning. OpenRouter exposes preset-voice TTS models at very low cost (Kokoro 82M $0.62/1M chars, GPT-4o Mini TTS $0.60/1M, etc.) but does NOT support voice cloning (only the `/audio/speech` proxy, no voices management API). Could plug into the TTS provider abstraction as a 4th provider category for users who want maximum cost savings and accept losing per-persona voice differentiation. **Fix shape**: new `OpenRouterTtsProvider` class (parallel to MistralTtsProvider but stateless — `prepare()` does nothing, `synthesize()` POSTs to `/audio/speech` with preset `voice` field). The TTS preset cascade already supports per-persona provider routing — users could mix-and-match (cheap personas on OpenRouter preset, important personas on Mistral cloning). **Why deferred from Phase 1**: voice cloning is the user's primary requirement; preset-voice tier is a nice-to-have for budget personas. **Promote when**: a budget tier is desired OR a specific persona is appropriate for preset voices. Surfaced 2026-05-01.

- 🧹 `[CHORE]` **Buffer-hash → in-memory LRU cache for TTS text recovery** — Council-suggested upgrade path during PR #955 design review. Today (post-PR-#955) the receive-side handler emits a placeholder text (`🔁 *Forwarded voice message originally spoken by \`{slug}\`...\*`) instead of the actual TTS source text. The placeholder is sufficient for the LLM context but loses fidelity vs. seeing the real utterance. **Fix shape**: at TTS synthesis time (`services/ai-worker/src/services/voice/`), compute a hash of the audio buffer (e.g. `sha256(buf).slice(0,16)`) and store `hash → originalText`in an in-memory LRU cache (single-instance bot-client deployment, so memory is fine;`lru-cache`is already a transitive dep). On the receive side in`VoiceTranscriptionService.handleOwnBotAudio`, after classifying the attachment as bot-authored, download the buffer (already done by the existing STT path), hash it, look up in the cache. Cache hit → use real text. Cache miss → fall back to the current placeholder. **Why deferred from PR #955**: the v1 placeholder is sufficient; the recovery cache requires plumbing across ai-worker → bot-client (where to populate? bot-client uploads, ai-worker synthesizes) and a download step we currently skip. **Promote when**: a user reports the placeholder feels too information-poor in practice, OR alongside any voice-engine refactor that naturally exposes the synthesis-side hash injection point. Surfaced 2026-05-01 in PR #955.

- 🧹 `[CHORE]` **`wrapUrlsForNoEmbed` lookbehind doesn't exclude `[` for Markdown link display-text** — Surfaced 2026-05-01 in PR #956 round 1 (claude-review). The regex `(?<![<(])\bhttps?:\/\/\S+` excludes URLs preceded by `<` (already-wrapped) or `(` (Markdown link target). It does NOT exclude `[`, so a URL used as Markdown link display text — `[https://example.com](https://example.com)` — gets matched: `\S+` captures `https://example.com](https://example.com)`, the walker cuts at the first unmatched `]`, giving `<https://example.com>](https://example.com)`. Reviewer classified as "strictly pre-existing — not introduced by PR #956" and "practical impact is low since `[url](url)` syntax rarely appears in error messages." **Fix shape**: add `[` to the lookbehind exclusion → `(?<![<([])`. Plus test for the `[url](url)` Markdown shape. **Promote when**: full Markdown link awareness is desired (e.g., a provider error message starts using `[url](url)` shape and produces visible weirdness in Discord), OR opportunistically when next touching `wrapUrlsForNoEmbed`. Surfaced 2026-05-01 in PR #956.

- 🐛 `[FIX]` **Audit other z.ai-specific response fields beyond reasoning** — Surfaced alongside the reasoning gap above. The `reasoning_content` mismatch suggests z.ai may use other snake_case / z.ai-specific field names where we currently only recognize OpenRouter's conventions. **Fix shape**: dump a few z.ai responses (one per model: glm-5.1, glm-5-turbo, glm-4.7, glm-4.5-air), inspect `additional_kwargs` keys, compare against what ai-worker reads. Candidates to check: tool calls, finish reasons, content-filter signals, token usage shape. Note any z.ai-only fields and decide which to plumb through. **Promote when**: alongside the reasoning fix, since the investigation overlaps. ~2-4hr including dump capture + ad-hoc fixes for whatever surfaces. ~~**Adjacent diagnostic field naming** (observed 2026-04-28 post-PR-#929 dump): `reasoningDebug.hasReasoningInKwargs` / `reasoningKwargsLength` only count the OpenRouter `reasoning` key, not z.ai's `reasoning_content`.~~ — RESOLVED in PR #954: `buildReasoningDebug` now falls back to `additionalKwargs.reasoning_content` when `reasoning` is absent, with `reasoning` winning when both are present. Three new test cases lock in the precedence + each branch.

- ✨ `[FEAT]` **Preset dashboard "set as default" button** — Surfaced 2026-04-24 by user. Add a button to the preset dashboard that lets a user adopt a preset as their personal default in one click; admins additionally see a "set as global default" option. Currently the workflow requires navigating to user-settings (or admin-settings) to set defaults, which is friction when browsing presets. **Why Inbox not Quick Wins**: needs design call on (a) button placement in dashboard, (b) confirmation UX for admin global-default (potentially destructive), (c) how the button reflects current state (e.g. disabled when already default). Surfaced 2026-04-24.

- ✨ `[FEAT]` **User-facing notice when partial attachment failures occur** — Beta.106's hotfix (PR #893) lets the conversation proceed when only some attachments succeed (deliberately better than aborting the whole thread on one bad URL). Side effect: a user forwarding a 5-image post where 2 succeed and 3 fail will see the bot respond to only the 2 surviving images with no indication that 3 were dropped. The response may appear unexpectedly sparse. **Fix shape**: emit a lightweight ephemeral notice ("⚠️ 3 of 5 attachments couldn't be loaded — link expired or external host blocked") alongside the normal response, tied to the per-attachment failure list already produced by `DownloadAttachmentsStep`. Surface via `errorInfo`-style structured field on the success result (e.g. `partialFailures?: string[]`), then bot-client renders the notice. **Why deferred**: hotfix scope; need design call on ephemeral vs. inline placement and on per-failure detail vs. summary count. Surfaced 2026-04-25 by claude-bot review on PR #893 (minor).
