## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

### PR #1074 follow-ups (one PR bundling all)

Items surfaced during PR #1074 (the audit-hardening implementation) that landed-as-out-of-scope. To be tackled together in a single follow-up PR per user decision.

- **[LIFT] Extend middleware-composition test pattern to 4 remaining admin route test files** — `services/api-gateway/src/routes/admin/{diagnostic,llm-config,stopSequences,usage}.test.ts`. PR #1074 added structural router-stack-inspection tests to `denylist.test.ts` and `tts-config.test.ts` (walks `router.stack`, asserts each route layer has ≥2 handlers). The other 4 files only got the `vi.mock(AuthMiddleware)` addition — sufficient to keep tests passing but **does not regression-guard against a future `requireOwnerAuth()` removal**. A removed middleware just means the mock is never invoked; nothing fails. **Fix shape**: copy the stack-inspection test from `tts-config.test.ts` into each of the 4 files, adapting the router factory call. ~80 LOC of repetitive test code. **Promote when**: bundled into the PR #1074 follow-up. Surfaced 2026-05-21 by PR #1074 round-4 claude-bot review.

- **[LIFT] Extract `RouterLayer` interface to a shared test helper** — `services/api-gateway/src/routes/admin/denylist.test.ts` and `services/api-gateway/src/routes/admin/tts-config.test.ts` both define their own `RouterLayer` interface (nearly identical shape, slightly different `stack` element types). When the structural test pattern extends to 4 more admin test files (item above), this duplication multiplies. **Fix shape**: extract to `services/api-gateway/src/test-helpers/routerInspection.ts` (or similar) and import from all 6 test files. **Promote when**: bundled with the structural-test extension above — natural moment to dedupe. Surfaced 2026-05-21 by PR #1074 round-5 claude-bot review (post-autosquash).

- **[LIFT] Migrate `settings.ts` to `requireOwnerAuth()` middleware pattern** — `services/api-gateway/src/routes/admin/settings.ts`. The one admin route file that uses inline `isAuthorizedForRead` / `isAuthorizedForWrite` guards inside handlers rather than `requireOwnerAuth()` middleware at route registration. Pre-existing inconsistency (predates PR #1074); deliberately out of scope for the audit-hardening PR which targeted the 6 explicitly-flagged routes. **Why it matters**: settings.ts is invisible to a future "grep for `requireOwnerAuth` to audit admin coverage" pass. The structural enforcement that PR #1074 established for the other 12 admin routes won't extend to settings.ts until this migrates. **Fix shape**: convert `isAuthorizedForRead` / `isAuthorizedForWrite` call sites to use `requireOwnerAuth()` at route registration, preserving the existing service-only-GET pattern (some GETs need to be reachable without owner-auth for ai-worker reads of admin settings — check call sites in ai-worker before migrating). Need to verify no service-only callers break. **Promote when**: bundled into the PR #1074 follow-up. Surfaced 2026-05-21 by PR #1074 round-4 claude-bot review.
