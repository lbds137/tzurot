## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🏗️ `[LIFT]` **Migrate `GatewayClient` hardcoded `AbortSignal.timeout(N)` calls to named `TIMEOUTS.*` constants** — `services/bot-client/src/utils/GatewayClient.ts` has 8 hardcoded numeric timeout values inline (varying across endpoints). PR #1072 established the pattern for admin paths (`TIMEOUTS.ADMIN_GATEWAY`); the rest of GatewayClient should follow. Surfaced 2026-05-21 by PR #1072 claude-review. **Start**: grep `AbortSignal.timeout(` in `GatewayClient.ts`; introduce one `TIMEOUTS.*` constant per logical endpoint group (probably 2-4 constants, not 8 — some share semantics) in `packages/common-types/src/constants/timing.ts`.

- 🏗️ `[LIFT]` **Tighten `adminApiClient` `INTERNAL_SERVICE_SECRET ?? ''` fallback** — Now that `validateInternalServiceSecret()` runs at bot-client startup (PR #1069), the `?? ''` fallback in `adminApiClient.ts:63` is redundant defensive code. An empty `X-Service-Auth` header is a valid HTTP value that produces a generic 401, masking what should be an obvious misconfig. **Fix shape**: `config.INTERNAL_SERVICE_SECRET!` with a `// validated at startup` lint-disable, OR a typed `getValidatedConfig()` helper that narrows after the startup check. Low priority — current code is functionally correct, just semantically loose. Surfaced 2026-05-21 by release PR #1071 review.

- 🐛 `[FIX]` **`/admin metrics` non-OK branch `editReply` is outside the outer catch** — `services/bot-client/src/commands/admin/metrics.ts` calls `await context.editReply({ embeds: [embed] })` inside the `if (!response.ok)` branch BEFORE the closing `}` of the outer `try`. If Discord interaction has expired by the time editReply fires (e.g., very slow gateway), the exception propagates uncaught. With PR #1072's 10s `adminFetch` timeout the practical window is small but non-zero. **Fix shape**: drop the inline `editReply` in the non-OK branch, let it fall through to the unified `editReply` at the bottom — same pattern as `/admin health`. ~5 LOC. Surfaced 2026-05-21 by release PR #1071 review.
