## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🧹 `[CHORE]` **Trim 7 over-cap voice references (audit tool shipped 2026-05-13)** — Audit ran via the new `pnpm ops voice-refs:audit --env prod` command. Found **7 personalities silently failing Mistral cloning** (refs >30s, fall through to self-hosted on every TTS call): `rich-fairbank-meshavesh-astrategi` (235.63s outlier), `verosika-hi-sukubusit` (43.15s), `baphomet-ani-miqdash-tame` (39.94s), `machbiel-ani-mistori` (33.62s), `shekhinah-hi-akhat-kan` (33.38s), `ha-shem-keev-ima` (31.78s — the one from prod logs), `sir-pentious-malakh-nachash` (30.35s). Plus 1 right at the cap (`lilith-tzel-shani` 29.99s) — could trip on encoding rounding, worth a defensive re-trim. **Action**: owner-driven re-upload of trimmed clips for each of the 8 personalities. No code work needed; the audit tool stays available for ongoing checks. **Re-run the audit** via `pnpm ops voice-refs:audit --env prod` after each trim batch to verify.

- 🧹 `[CHORE]` **Audit api-gateway key validators for 5xx-as-transient classification** — Surfaced 2026-05-17 by PR #1044 review. The deleted ai-worker `KeyValidationService` had `ProviderUnavailableError` as a distinct error type for 5xx responses, preventing a good key from being flagged invalid during an upstream provider outage. The current gateway validators (`services/api-gateway/src/utils/apiKeyValidation/{elevenlabs,mistral,openrouter,zaiCoding}.ts`) may not all share this 5xx-vs-401 distinction. **No current bug** — the validators run on user submission, and a transient 5xx producing a "key invalid" message is annoying but recoverable (user resubmits). **Promote when**: runtime key-health reporting gets wired up (e.g., surfacing key health to users via the bot, or a periodic background re-validation pass) — at that point, mis-classifying a transient 5xx as "key bad" would silently corrupt the health signal. **Fix shape**: audit each of the 4 validators for HTTP status handling; introduce a `ProviderUnavailableError` (or equivalent typed result) to distinguish 5xx from 401 across them; centralize via a shared helper if per-provider differences are minimal. Start: `services/api-gateway/src/utils/apiKeyValidation/types.ts` (shared types) + each provider file.

- 🧹 `[CHORE]` **Normalize non-standard IP formats in `publicRouteKeyGenerator`** — Surfaced 2026-05-17 by PR #1046 final review. Two edge cases where the same client lands in different Redis buckets depending on format: (a) **Bracket+port form** — `X-Forwarded-For: [2001:db8::1]:8080` (RFC 7239 §6.3 bracket+port; rare but valid in some Nginx configs). Current `startsWith('[') && endsWith(']')` check leaves brackets intact because `endsWith(']')` is false, producing key `ratelimit:public:[2001:db8::1]:8080` instead of `ratelimit:public:2001:db8::1`. (b) **IPv4-mapped IPv6** from `socket.remoteAddress` — Node exposes IPv4 clients via `::ffff:127.0.0.1` on some platforms. The same host's `127.0.0.1` direct connection would land in a different bucket. **Promote when**: Railway logs show non-standard XFF formats, or when adopting a non-Railway hosting environment. **Won't surface in current Railway deployment.** **Fix shape**: regex-based strip in the XFF path (`last.replace(/^\[([^\]]+)\].*$/, '$1')` to handle bracket+port), and an `::ffff:` prefix strip on `socket.remoteAddress`. Start: `services/api-gateway/src/utils/RedisRateLimiter.ts` `publicRouteKeyGenerator`.

- 🧹 `[CHORE]` **Decide `/metrics` exposure model (auth or tighter rate limit?)** — Surfaced 2026-05-17 by PR #1046 review. `/metrics` is currently unauthenticated and only rate-limited at 60 req/min/IP (same as `/avatars`, `/voice-references`, `/exports`). If `/metrics` exposes operational data (queue depth, uptime, error rates), a scraper could enumerate internal state at that rate. Two design options to weigh: (a) move `/metrics` behind `requireServiceAuth()` so only authenticated callers (bot-client, internal probes) can hit it; (b) keep public but tighten the rate limit (e.g., 10 req/min/IP) since legitimate consumers are few. **Promote when**: doing the next auth-related audit, or if Railway logs show unexpected `/metrics` traffic. **Decision input needed**: which fields does `/metrics` actually expose? Read the metrics router to inventory. Start: `services/api-gateway/src/routes/public/metrics.ts`. **Bundle**: when doing this work, also consider scoping helmet's `crossOriginResourcePolicy: 'cross-origin'` to only `/avatars` + `/voice-references` (the actual cross-origin-fetched media routes) — currently it's global but only those two need it. Per-route `res.set('Cross-Origin-Resource-Policy', 'same-origin')` on `/metrics` and `/exports` would tighten the surface.
