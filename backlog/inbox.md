## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🐛 `[FIX]` **Mistral TTS guardrail violations silently fall through to self-hosted; user invisible** — Surfaced 2026-05-13. Production logs show Mistral returning `403 guardrail_violation code 1920 "Request blocked by guardrail policy"` on innocuous content (concrete example: a Monty Python reference about African vs European swallows + "capital of Assyria" joke). The dispatcher correctly falls through to self-hosted, but the user has no idea their content is being content-policed by Mistral. Two distinct concerns: **(1) UX/visibility**: bot-owner-visible notice for guardrail-violation falls (similar to the existing `MistralReferenceAudioTooLongError` notice path) so the operator at least sees what's being blocked. **(2) Product/strategy**: this is evidence Mistral's content policy is overly restrictive for our use case (humor, irreverent personalities), reinforcing the [Self-Hosted TTS + BYOK Re-Evaluation](future-themes.md) theme's need to bake-off other BYOK options. **Fix shape (visibility)**: in `MistralTtsClient.ts`, detect the 403 + `code === 1920` shape, throw a new `MistralGuardrailError` (sibling to `MistralReferenceAudioTooLongError`) with a `userNotice` field; thread through `TtsDispatcher.buildAttemptNotice`. ~30-50 LOC + test. **Promote when**: opportunistic during next MistralTtsClient touch, OR alongside the BYOK re-eval work.

- 🧹 `[CHORE]` **Audit voice references for Mistral 30s cap violations** — Surfaced 2026-05-13. Production logs show at least one personality (`ha-shem-keev-ima`, 31.78s reference) silently falls through to self-hosted on every TTS call because Mistral rejects refs >30s. Probably more such personalities exist that haven't been logged yet. **Fix shape**: one-shot script that queries `Personality.voiceReferenceData IS NOT NULL`, fetches each via `/voice-references/:slug`, computes duration via `soundfile.info()`, reports any >30s. Can run as a `pnpm ops` command or one-off. Output: list of (slug, duration_s) for the owner to decide which to trim. Trimming itself can be a follow-up bulk operation OR per-personality user-driven re-upload. **Why this matters**: every voice reply on a >30s-ref personality is silently downgrading from premium-BYOK to self-hosted quality. Owner doesn't know.
