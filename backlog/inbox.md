## 📥 Inbox

_New items go here. Triage to appropriate section weekly._

- 🐛 `[FIX]` **BYOK key rotation false-positive rate-limit block (up to 24h)** — Surfaced 2026-04-29 in PR #943 round 11 (claude-review). The rate-limit cache uses `user:<discordId>` as the scope identifier, which means if a user rotates their OpenRouter API key after their original key hit the daily quota, the `user:<discordId>` Redis bucket stays cached as rate-limited until the original reset window expires (up to 24h via clamped TTL). The new key has its own independent quota, so the block is incorrect. **Why deferred**: rare in practice — users almost never rotate keys mid-quota. **Fix shape options**: (a) add a `keyRotatedAt` timestamp to the BYOK user record; `deriveCacheKeyId` returns `user:<discordId>:<rotation-epoch-seconds>` so a new key gets a fresh cache scope; (b) add a manual operator escape valve via `pnpm ops` (e.g., `pnpm ops cache:clear-rate-limit --user-id <id>` does `DEL ratelimit:openrouter:user:<id>:*` via Redis). Option (b) is cheaper if the manual escape is acceptable; option (a) is correct without operator intervention. **Promote when**: a user reports being rate-limit-blocked after rotating their key, OR alongside an api-key-management UX overhaul. Surfaced 2026-04-29 in PR #943.

- 🧹 `[CHORE]` **Reconcile triple `MemoryDocument` definition across the ai-worker service** — Surfaced 2026-04-29 in PR #944 round 1 (claude-review). After PR #944 deleted `PromptContext.ts`'s duplicate, two definitions remain: `services/ai-worker/src/services/ConversationalRAGTypes.ts:21` (RAG context layer, named-fields metadata: `id?` / `createdAt?` / `score?`) and `services/ai-worker/src/services/PgvectorTypes.ts:71` (storage layer, `metadata?: Record<string, unknown>`). Reviewer's framing: the duality may be intentional ("different semantic layers — low-level storage vs. RAG context") but two non-identical interfaces sharing a name in the same service creates ambiguity at import sites. **Fix shape options**: (a) unify into one `MemoryDocument` with the wider `Record<string, unknown>` metadata, accepting the looser typing at the RAG layer; (b) rename the storage-layer one to `PgvectorMemoryDocument` and keep the RAG-layer one as `MemoryDocument`; (c) add a narrowing-conversion step at the `PgvectorMemoryAdapter` boundary that maps storage-shape → RAG-shape, keeping both types but making the seam explicit. Option (b) is the lowest-risk pure rename. **Promote when**: a future contributor confuses the two at an import site (real ambiguity manifests as a bug), OR a memory-pipeline refactor naturally surfaces the seam. Surfaced 2026-04-29 in PR #944.

- 🧹 `[CHORE]` **`wrapUrlsForNoEmbed` truncates URLs with internal closing parens** — Surfaced 2026-04-29 in PR #944 round 1 (claude-review). The regex `[^\s<>)]+?` excludes `)` from URL characters to handle the markdown-link case `[text](url)`. Side effect: a Wikipedia-style URL like `https://en.wikipedia.org/wiki/Foo_(bar)` would render as `<https://en.wikipedia.org/wiki/Foo_(bar>)` — a malformed suppress-wrap that breaks the link if clicked. Reviewer explicitly notes "LangChain troubleshooting URLs don't use this pattern, so it won't affect production." **Fix shape**: replace the simple `(?<![<(])` lookbehind with one that distinguishes "URL preceded by `(` of markdown-link" vs "literal `(` in surrounding text" — likely needs a balanced-paren-aware matcher (more state) OR a heuristic that allows a single internal `)` if there's also a matching `(` earlier in the URL. ~10 LOC + 2-3 test cases. **Promote when**: a parenthesized URL appears in a real provider error message (e.g., a third-party error message vocabulary expanding to include Wikipedia-style links), OR opportunistically when next touching `wrapUrlsForNoEmbed`. Surfaced 2026-04-29 in PR #944.

- 🧹 `[CHORE]` **Make `cacheKeyId` required in `InvokeWithRetryOptions` (or add debug-log on empty opt-out)** — Surfaced 2026-04-29 in PR #943 round 16 (claude-review). `InvokeWithRetryOptions.cacheKeyId` defaults to `''` to keep legacy test fixtures compiling; production callers that forget to pass it silently bypass the rate-limit cache with no observable signal. Currently fine because there's only one production caller (`ConversationalRAGService`), but easy to miss when a second caller is wired up. **Fix shape options**: (a) make `cacheKeyId` required in the type; tests that genuinely opt out pass `cacheKeyId: ''` explicitly; (b) add a `debug`-level log at the `cacheKeyId.length === 0` branch in `LLMInvoker.invokeWithRetry` so the silent opt-out is at least visible in local dev. Option (a) is cleaner; option (b) is cheaper. **Promote when**: a second production caller of `LLMInvoker.invokeWithRetry` is added (audit `services/ai-worker/src/services/` for new `invokeWithRetry` invocations). Surfaced 2026-04-29 in PR #943.

- 🧹 `[CHORE]` **Enforce `buildKey` no-colon invariant in `deriveCacheKeyId` at runtime** — Surfaced 2026-04-29 in PR #943 rounds 12 + 16 (claude-review; first raised as a Medium, softened to Nit by round 16 with explicit "backlog is reasonable" framing). The Redis cache key shape (`ratelimit:openrouter:<cacheKeyId>:<model>`) is unambiguous only because `cacheKeyId` is always `system`, `user:<digits>`, or `''` — no colons in the dynamic segment. The invariant is documented in `buildKey`'s JSDoc but not enforced. A future contributor extending `cacheKeyId` to a non-numeric scope (e.g., `org:<name>` where the name contains colons) would silently corrupt key lookups via collision. **Fix shape**: in `deriveCacheKeyId` (or at the `buildKey` boundary), assert the produced ID matches `/^(system|user:\d+|)$/` (or the future expanded grammar). Throw or `logger.warn` on violation; the hot-path overhead is negligible for a single regex test. ~3 LOC + 1 test. **Promote when**: `deriveCacheKeyId` is extended to support a non-numeric scope, OR opportunistically alongside the `cacheKeyId-required` follow-up above. Surfaced 2026-04-29 in PR #943.

- ~~🐛 `[FIX]` **402 "insufficient credits" caching (per-account credit-exhaustion)**~~ — RESOLVED in PR #945. New `CreditExhaustionCache` class parallel to `RateLimitCache`, with account-scoped key (`nocredits:openrouter:<cacheKeyId>`, no model dimension), default 1h TTL clamped to [60s, 24h], JSON value `{ ts, ttl }` for accurate remaining-time computation on read. Sub-classification of 402s in `parseApiError` distinguishes account-level (cacheable, `CREDIT_EXHAUSTION` category) from request-level (not cacheable, stays `QUOTA_EXCEEDED`). New `USER_ERROR_MESSAGES[CREDIT_EXHAUSTION]` carries the OpenRouter top-up URL. Operator escape valve `pnpm ops cache:clear-credit-exhaustion --user-id <id>` for users who top up before TTL expiry. 46 new tests. Note: original entry's "vision" framing was mis-scoped — production reality is chat completions on `z-ai/glm-4.7`, not vision pipeline. Two follow-up items spun off (regex breadth + max_tokens auto-reduction) — see entries below.

- 🧹 `[CHORE]` **Cache `KEY_PREFIX` constants duplicated across services and tooling** — Surfaced 2026-04-30 in PR #946 (release-PR holistic review). The `nocredits:openrouter:` prefix is defined in both `services/ai-worker/src/services/CreditExhaustionCache.ts:46` and `packages/tooling/src/cache/clear-credit-exhaustion.ts:19`. The `ratelimit:openrouter:` prefix in `RateLimitCache.ts` has the same shape but no second consumer yet. A future rename of either prefix in one place without updating the other would silently break operator tooling (the operator's `DEL` would target a stale key while live writes go to the new key). **Fix shape**: extract a `CACHE_KEY_PREFIXES` constant object to `@tzurot/common-types/src/constants/redis-keys.ts` (or similar location), exported as `CACHE_KEY_PREFIXES.RATE_LIMIT_OPENROUTER` / `CACHE_KEY_PREFIXES.CREDIT_EXHAUSTION_OPENROUTER`. Both services import from there. ~10 LOC + relocate constants. **Promote when**: a third consumer of either prefix emerges (e.g., a `cache:clear-rate-limit` companion to the credit-exhaustion clear command), OR the next time either of the two existing consumers is touched. Reviewer noted "acceptable as-is" given the mirroring comment but flagged as the "highest-friction item if the prefix ever changes." Surfaced 2026-04-30 in PR #946.

- 🧹 `[CHORE]` **`isAccountCreditExhaustion` regex breadth — `/insufficient.*credits/i` may over-match future 402 shapes** — Surfaced 2026-04-30 in PR #945 round 3 (claude-review). The fallback pattern `/insufficient.*credits/i` is broader than the verbatim `/never purchased credits/i` arm; it could match a hypothetical OpenRouter 402 with non-credit-exhaustion meaning that happens to mention "insufficient credits" (e.g., "insufficient platform credits for this tier"). Blast radius is bounded today because `parseApiError` gates on `statusCode === 402 && resolved.category === ApiErrorCategory.QUOTA_EXCEEDED` — but if OpenRouter ever sends a 402 for a different reason with similar phrasing, it would get cached as account-level credit exhaustion and block subsequent calls for up to 24h. **Fix shape**: tighten the fallback to `/your account has insufficient credits/i` or similar account-specific phrasing once we have a real 402-shape sample to anchor against; OR add an allowlist of explicit credit-exhaustion sub-patterns instead of the generic fallback. **Promote when**: a 402 from OpenRouter (or another provider sharing the parser) with a non-credit-exhaustion meaning surfaces in production logs, OR opportunistically when next touching `apiErrorParser.ts`. Surfaced 2026-04-30 in PR #945.

- 🧹 `[CHORE]` **402 max_tokens auto-reduction (request-level credit-exhaustion fix)** — Surfaced 2026-04-30 alongside PR #945. When a 402 says "can only afford N tokens" (request-level, sub-classified as `QUOTA_EXCEEDED` not `CREDIT_EXHAUSTION` in the new pipeline), the request COULD succeed with smaller `max_tokens`. Currently we just fail the user. A per-request retry with `max_tokens = M - safety_margin` would recover automatically. **Fix shape**: in `LLMInvoker`'s catch path, if 402 + `category === QUOTA_EXCEEDED` (i.e., NOT routed to `CREDIT_EXHAUSTION`) + technicalMessage carries an extractable token-budget number, retry once with reduced `max_tokens`. **Why out of scope (deferred from PR #945 with strong reason)**: different mechanism from the credit-exhaustion cache (per-request adjustment, not session-level memoization). No production evidence (10/10 production 402s observed 2026-04-29 were account-level "never purchased credits", zero were request-level "afford"). Token-extraction from free-form provider messages is brittle; needs its own design pass (regex robustness, missing/malformed numbers, retry-loop bounding). **Promote when**: a user reports the request-level 402 pattern in production, OR the next time the credit-cache feature is touched and natural surface area exists to address it. Surfaced 2026-04-30 in PR #945 plan.

- ~~🧹 `[CHORE]` **Wrap bare URLs in error messages with `<…>` to suppress Discord auto-embeds**~~ — RESOLVED in PR #944. New `wrapUrlsForNoEmbed` utility in `packages/common-types/src/constants/error.ts`, applied at `formatErrorSpoiler`'s `technicalMessage` interpolation. Skips already-wrapped URLs and markdown-linked URLs via single-char lookbehind; trailing punctuation stays outside the wrap. 10 unit cases + 1 integration case for the production LangChain MODEL_RATE_LIMIT fixture. Edge case (parenthesized URLs) tracked separately above.

- 🧹 `[CHORE]` **Monitor `ignoreExportsUsedInFile: true` knip config — narrow to per-workspace if signal degrades** — Surfaced 2026-04-29 in PR #941 round 2 (claude-review, sharpened from round 1's softer framing). PR #941 added `ignoreExportsUsedInFile: true` at the **global** knip config level to dampen the dominant false-positive class (option-bag/result-bag interfaces declared next to consumer functions). The flag suppresses findings for any export consumed only in its declaring file, across all packages. Risk: a genuine dead export that happens to also be used internally (e.g., a helper exported for an old caller, now only called by sibling code in the same file) would be silently suppressed too. Knip supports this flag at the per-workspace level as well. **Promote when**: a real dead-export bug that should have been caught is found by other means (review, manual audit, runtime), OR after ~5 PR cycles if the suppression starts feeling too aggressive in routine PR reviews. **Fix shape (when triggered)**: remove the global `ignoreExportsUsedInFile: true`, add it per-workspace to only `services/ai-worker`, `services/api-gateway`, `services/bot-client` (the workspaces where the option-bag pattern dominates); leave `packages/*` workspaces stricter since they have public API surfaces. Surfaced 2026-04-29 in PR #941.

- ~~🧹 `[CHORE]` **Rename / relocate `services/ai-worker/src/services/context/PromptContext.ts`**~~ — RESOLVED in PR #944. Discovery on audit: the file's `MemoryDocument` was actually a duplicate of the canonical `ConversationalRAGTypes.ts:21` definition (just with a dead `[key: string]: unknown` index signature). Stronger fix than the original rename framing — deleted the file entirely, updated `ContextWindowManager.ts` to import from the canonical home that all other consumers in the same `context/` directory already use. Two more `MemoryDocument` definitions remain in `PgvectorTypes.ts` (RAG vs storage layer); tracked as a separate inbox entry above.

- 🧹 `[CHORE]` **Move `NON_LLM_PROVIDERS` / `USER_AUTH_PROBE_PROVIDERS` to a shared module** — Surfaced 2026-04-29 in PR #940 round 1 (claude-review, self-classified non-blocking). PR #940 colocated both constants in `services/ai-worker/src/jobs/ImageDescriptionJob.ts` because that's currently the only consumer (`userHasAnyKey` is the sole probe site). If a second consumer appears — e.g., `visionAuthResolver` needing the same "is this user authenticated for SOME LLM provider?" discriminator — the constants should move to `services/ai-worker/src/services/ProviderRouter.ts` (next to `detectVisionProvider` + `effectiveVisionModelName`) or to a new dedicated `visionConstants.ts` module. **Promote when**: a second consumer is added, OR the constants need to be referenced from outside `ImageDescriptionJob.ts`. Until then, the colocation is correct (premature extraction would be over-abstraction for a single-caller). Pure-relocation refactor when triggered, ~5 LOC. Surfaced 2026-04-29 in PR #940.

- 🧹 `[CHORE]` **Make `visionProvider` required in vision-pipeline option bags** — Surfaced 2026-04-29 in PR #938 round 4 (claude-review). After the cross-provider vision auth fix, `visionProvider?: AIProvider` is threaded as optional through `DescribeImageOptions`, `InvokeVisionModelOptions`, `ProcessAttachmentOptions`, and `ProcessAttachmentsOptions`. The `?` was for backward-compat with existing tests/legacy paths. Risk: a future caller that forgets to pass `visionProvider` gets the env-default `AI_PROVIDER` from `createChatModel`, silently re-introducing the exact bug PR #938 fixes. **Fix shape**: audit every call site of `describeImage`, `processAttachments`, `processAttachmentsParallel`, `invokeVisionModel` to confirm they pass `visionProvider`. Once 100% coverage, drop the `?` and update the JSDoc to remove the "for backward compat" note. ~~**Interim option** (suggested by reviewer in PR #938 round 10): add a `logger.warn` at the `createChatModel` call site when `visionProvider` is undefined~~ — interim warn SHIPPED in PR #940 at `invokeVisionModel` (single chokepoint covering all upstream callers); fires when `provider` is undefined. **Promote when**: a few weeks of clean Railway logs confirm zero callers fire the warn — then drop the `?` from the option-bag types and remove the warn. Surfaced 2026-04-29.

- 🧹 `[CHORE]` **Eliminate double `selectVisionModel` call in DependencyStep** — Surfaced 2026-04-29 in PR #938 round 4 (claude-review). `DependencyStep.processExtendedContextImages` now calls `selectVisionModel(personality, isGuestMode)` to pre-compute the effective model for provider detection, then `describeImage` calls `selectVisionModel` again internally for the same personality. For personalities WITH `visionModel` set (the primary cross-provider case), both calls return after a property check — no I/O, no issue. For personalities WITHOUT an explicit override, both calls reach `hasVisionSupport` → Redis. PR #940 absorbed the **duplication** concern via the `effectiveVisionModelName` helper (now used by both `visionAuthResolver` and `ImageDescriptionJob`); the **double-Redis-read** concern stays open per its original promote-when criterion. **Fix shape (still deferred)**: `describeImage` accepts an optional `usedModel` param to let callers pass the pre-computed value. Wider interface change. **Promote when**: telemetry shows non-trivial Redis hit rate from this duplication, OR the next vision-pipeline refactor naturally touches `describeImage`'s signature. Surfaced 2026-04-29.

- ~~🧹 `[CHORE]` **Extract `effectiveVisionModelName` helper alongside `detectVisionProvider`**~~ — RESOLVED in PR #940. Helper exported from `services/ai-worker/src/services/ProviderRouter.ts`, used by `visionAuthResolver.ts` and `ImageDescriptionJob.ts:resolveVisionApiKey`. Accepts `string | null | undefined` defensively to match upstream `LlmConfigResolver` shape; four edge-case tests in `ProviderRouter.test.ts`.

- ~~🧹 `[CHORE]` **PR #938 round-13 polish — small code-shape items not folded into the merge**~~ — RESOLVED in PR #940 (both sub-items): MultimodalProcessor lazy import hoisted once at top of `processExtendedContextImages`; four `resolveApiKey: vi.fn()` sites converted to `mockRejectedValue` loud-fail in `ImageDescriptionJob.test.ts`.

- 🧹 `[CHORE]` **`USER_AUTH_PROBE_PROVIDERS` provider-list drift risk** — Surfaced 2026-04-29 in PR #938 round 10 follow-up. ~~`ImageDescriptionJob.userHasAnyKey` probes a hardcoded list of providers (`[OpenRouter, ZaiCoding]`)~~ — drift risk addressed in PR #940 via option (a): probe list now derived from `Object.values(AIProvider).filter(p => !NON_LLM_PROVIDER_SET.has(p))`. New LLM providers added to `AIProvider` auto-include; only ElevenLabs (voice-only) is currently filtered. Three invariant tests lock in the contract. **Option (b) remains conditionally deferred**: replacing the heuristic entirely by adding `auth.isGuestMode` to `ImageDescriptionJobData` (api-gateway already knows at job-creation time) would be more correct but requires schema + api-gateway plumbing. **Promote (b) when**: api-gateway auth-resolution refactor naturally surfaces the signal, OR a regression demonstrates the heuristic is insufficient.

- 🧹 `[CHORE]` **Narrow auto-promotion fallback trigger to catalog-drift errors specifically** — Surfaced 2026-04-28 in PR #928 round 3 (claude-review). The current `runWithAutoPromotionFallback` (`services/ai-worker/src/jobs/handlers/pipeline/steps/autoPromotionFallback.ts`) catches ANY error from the promoted z.ai request and retries via OpenRouter. That's defensive but over-broad — a transient z.ai network blip, a temporary 5xx, or a rate limit will all silently route through OpenRouter without the user knowing. The actual catalog-drift signal is "z.ai returned 404 / model not found." **Fix shape**: once z.ai's API error format for "model not on plan" is empirically verified (see existing entry "z.ai's HTTP status for 402 verification" in this file), narrow the catch to that specific error class. Other errors should propagate immediately so the user sees the real failure mode. **Promote when**: we have a real z.ai error response captured (could come from natural production usage, an artificial probe with a known-bad model, or z.ai docs improvement). Until then, the broad catch is the right starting point — over-fallback is preferable to under-fallback while we have no error-shape data. ~5-10 LOC change once the error class is known.

- 🧹 `[CHORE]` **`ResolvedRoute.fallback` invariant via discriminated union** — Surfaced 2026-04-28 in PR #928 rounds 3+4 (claude-review). ~~Part (a): extract a named `FallbackRoute` type alias~~ — RESOLVED in this PR; both `ResolvedRoute.fallback` and `ResolvedAuth.fallback` now reference the `FallbackRoute` interface exported from `services/ai-worker/src/services/ProviderRouter.ts`. Part (b) remains: tighten the optional shape into a discriminated union so the `wasAutoPromoted === true ↔ fallback !== undefined` invariant becomes a static guarantee. **Fix shape (b)**: `BaseResolvedRoute & ({ wasAutoPromoted: true; fallback: FallbackRoute } | { wasAutoPromoted: false; fallback?: never })`. **Caveat**: needs to handle the graceful-degradation path where promotion succeeds but fallback computation fails — possibly a third union variant: `{ wasAutoPromoted: true; fallback: FallbackRoute | undefined }`, which is just back to optional. May need rethinking once we have more data on how often the degradation fires. **Promote when**: feature has stabilized in production for a few weeks AND the graceful-degradation path is well-understood empirically.

- 🐛 `[FIX]` **Audit other z.ai-specific response fields beyond reasoning** — Surfaced alongside the reasoning gap above. The `reasoning_content` mismatch suggests z.ai may use other snake_case / z.ai-specific field names where we currently only recognize OpenRouter's conventions. **Fix shape**: dump a few z.ai responses (one per model: glm-5.1, glm-5-turbo, glm-4.7, glm-4.5-air), inspect `additional_kwargs` keys, compare against what ai-worker reads. Candidates to check: tool calls, finish reasons, content-filter signals, token usage shape. Note any z.ai-only fields and decide which to plumb through. **Promote when**: alongside the reasoning fix, since the investigation overlaps. ~2-4hr including dump capture + ad-hoc fixes for whatever surfaces. **Adjacent diagnostic field naming** (observed 2026-04-28 post-PR-#929 dump): `reasoningDebug.hasReasoningInKwargs` / `reasoningKwargsLength` only count the OpenRouter `reasoning` key, not z.ai's `reasoning_content`. After PR #929, both fields fire `false`/`0` for successful z.ai-direct extractions even though `postProcessing.thinkingExtracted: true`. Strictly cosmetic but misleading to a future reader of the dump. Fix: extend the debug counters to track both field names, OR drop them entirely now that `thinkingExtracted` covers the actual signal.

- 🏗️ `[LIFT]` **`tryResolveUserKey` no-key negative caching** — Surfaced 2026-04-27 in PR #924 round 7 (claude-review). For `zai-coding`-with-no-key (the auto-fallthrough majority path), `tryResolveUserKey` returns `null` without cache write → every request re-reads DB. Currently accepted overhead documented in code comment at `services/ai-worker/src/services/ApiKeyResolver.ts`; resolveApiKey's `source: 'system'` semantics can't be reused because `zai-coding` has no system fallback. Invariant test (do-not-cache-null-path) shipped via PR #925 — locks in current behavior so a future caching change is a deliberate, test-failure-inducing decision rather than a silent regression. **Fix shape (caching)**: introduce a distinct cache state for "no user key configured" (e.g., a `source: 'absent'` discriminant or sentinel value), populate it from `tryResolveUserKey`'s null branch, and short-circuit subsequent reads through the same entry-point. Touches `ApiKeyResolver`'s cache shape; likely needs co-update to `resolveApiKey` callers that peek the cache directly. **Promote when**: production logs show measurable per-request DB-read load on the fallthrough path, OR if a second auto-fallthrough provider lands and the duplicate-DB-read pattern compounds.

- 🏗️ `[LIFT]` **z.ai integration — remaining follow-ups from PR #921 review rounds** — Surfaced 2026-04-27 across 6 review rounds on PR #921. PR #924 (`ProviderRouter` + auto-fallthrough) absorbed the 5xx-classification item; PR #925 cleanup investigation discovered 4 more were already addressed inline during PR #921 review rounds 5-6 but never struck through here. Two real items remain.

  **Medium — validation model hardcoding (`ZAI_VALIDATION_MODEL`)**: PR #926 fixed the immediate day-1 bug (`glm-4.5-flash` was wrong — corrected to `glm-4.5-air` per z.ai's documented coding-plan catalog). The broader concern still stands: if z.ai deprecates or renames `glm-4.5-air`, every valid key will validate as `INVALID_KEY` (silent false-negative). **Fix shape options**: (a) try a list of fallback models in order (`glm-4.5-air` → `glm-4.7` → first-from-`/models`-endpoint); (b) hit a non-chat endpoint (e.g., `/account/balance` if z.ai exposes one — needs research); (c) accept the maintenance risk and add a periodic check to detect catalog changes early.

  **Verification — z.ai's HTTP status for 402 (expired/lapsed subscription)** (round 3): 401/403/429 are accounted for. If z.ai uses 402 for an expired coding-plan subscription, it falls through to `UNKNOWN` (api-gateway) or `InvalidApiKeyError` (ai-worker), neither of which conveys "subscription lapsed; renew" UX. **Research finding (2026-04-27)**: z.ai's API docs at `docs.z.ai/api-reference/llm/chat-completion` only document HTTP 200 + a generic "default error" with structured `{ code, message }` body — they don't enumerate 401/402/403/404/429 explicitly. So a documentation-based answer to "does z.ai use 402 for expired subscriptions" is unobtainable; verification requires probing with a real expired/lapsed coding-plan key, which we don't have. **Fix shape**: when one becomes available (or when the integration scales enough that natural-occurrence of subscription expiration produces a real probe), capture the actual status code z.ai returns for that case and surface "subscription lapsed" as a distinct error code in `mapValidationErrorToResponse`.

  ~~**Medium — ai-worker validators classify 5xx as `InvalidApiKeyError`**~~ — RESOLVED in PR #924 via `ProviderUnavailableError` symmetric across OpenRouter/ElevenLabs/ZaiCoding validators.

  ~~**Medium — explicit omit of `frequencyPenalty`/`presencePenalty` in `buildZaiCodingModel`**~~ — RESOLVED in PR #921 (see `services/ai-worker/src/services/ModelFactory.ts:443-445, 457-463`).

  ~~**Minor — `__includeRawResponse` absence comment in `buildZaiCodingModel`**~~ — RESOLVED in PR #921 (see `ModelFactory.ts:469-473`).

  ~~**Minor — `VALIDATION_MESSAGES.QUOTA_EXCEEDED_ZAI` consolidation**~~ — RESOLVED in PR #921 (see `services/api-gateway/src/utils/apiKeyValidation.ts:28`).

  ~~**UX — QUOTA_EXCEEDED z.ai-specific message at wallet intake**~~ — RESOLVED in PR #921: `validateZaiCodingKey` returns `VALIDATION_MESSAGES.QUOTA_EXCEEDED_ZAI` in `validation.error`, and `mapValidationErrorToResponse` (`setKey.ts:46-49`) returns that string via `paymentRequired()` rather than the generic INSUFFICIENT_CREDITS fallback.

  Surfaced 2026-04-27.

- 🐛 `[FIX]` **ElevenLabs single-attempt retry — measure-then-decide** — Surfaced 2026-04-27 alongside the TTS chunker investigation (chunker defensive truncation shipped via PR #920; chunker measurement edge cases tracked in `backlog/icebox.md`). The still-open concern: beta.97 cut `ELEVENLABS_MAX_ATTEMPTS` from 2 to 1 to reduce worst-case TTS latency. Both TTS failures observed in beta.107 prod logs hit the 60s ElevenLabs timeout on attempt 1 with no retry, falling all the way through to the cold-start voice-engine path (~44s additional). Reverting to `MAX_ATTEMPTS=2` would increase worst-case latency by 60s on persistent failure but improve user-visible success rate on transient failure. **Decision needs telemetry**: how often does attempt 1 fail and the voice-engine fallback succeed (= retry would have helped) vs. attempt 1 fail and voice-engine also fails (= retry wouldn't change outcome). **Adjacent**: STT + voice-engine retry counts may have the same latent bug pattern — see TTS Engine Upgrade Active Epic, "Audit ElevenLabs STT + voice-engine retry counts." **Start**: telemetry already added to `withRetry` in beta.97 — query for ElevenLabs attempt-1 failure rate over a 7-day window before deciding. Surfaced 2026-04-27.

- 🧹 `[CHORE]` **Railway `overlapSeconds: 0` belt-and-suspenders for SIGTERM regression defense** — Originally tracked as branch (a) of the multi-branch DM-fix-verification entry (PR #913 era); branch (a) won — empirically confirmed 2026-04-27 via DM_RAW_GATEWAY_DIAGNOSTIC observations that PR #918's Layer 1 + Layer 2 fixes resolved the post-deploy DM-silence bug. The env var deletion is tracked separately in `backlog/quick-wins.md`. The remaining optional follow-up: configure Railway `overlapSeconds: 0` as belt-and-suspenders so any future SIGTERM-handler regression doesn't re-introduce the bug. Currently the overlap window is enabled, meaning a regression in the SIGTERM handler would silently re-create the orphaned-session symptom. **Fix shape**: Railway dashboard → bot-client service → deployment settings → `overlapSeconds: 0`. **Promote when**: convenient — opportunistic dashboard tweak with no code change. Branches (b) and (c) of the original entry are obsolete now that branch (a) is confirmed.

- 🏗️ `[LIFT]` **Validate other `/wallet/*` and gateway responses against Zod schemas at api-gateway boundary** — Surfaced 2026-04-26 by claude-bot review on PR #908. ~~`/wallet/list`~~ — RESOLVED in batch-1 cleanup PR (parses through `ListWalletKeysResponseSchema` before `sendCustomSuccess`). Same gap applies to other gateway endpoints with hand-coded response shapes against schemas in `common-types/schemas/api/`: `/wallet/test`, `/wallet/:provider` DELETE, the admin/_ and user/_ sub-routes, etc. **Fix shape**: same pattern — `Schema.parse(payload)` before send. Or invest in wrapping `sendCustomSuccess` with a schema-aware variant so the parse happens centrally for all callers. Folds naturally into the Observability & Telemetry theme's API-contract concerns. **Heads-up on the error path** (PR #929 round 1): `asyncHandler` catches thrown errors and includes `error.message` in the 500 response body. A `ZodError.message` includes the full field path and expected/received shapes — fine for authenticated internal endpoints (informative for debugging), but if this pattern is extended to **public-facing** endpoints, the leak surface needs a separate treatment (catch `ZodError` specifically and return a generic 500 message).

- ✨ `[FEAT]` **Preset dashboard "set as default" button** — Surfaced 2026-04-24 by user. Add a button to the preset dashboard that lets a user adopt a preset as their personal default in one click; admins additionally see a "set as global default" option. Currently the workflow requires navigating to user-settings (or admin-settings) to set defaults, which is friction when browsing presets. **Why Inbox not Quick Wins**: needs design call on (a) button placement in dashboard, (b) confirmation UX for admin global-default (potentially destructive), (c) how the button reflects current state (e.g. disabled when already default). Surfaced 2026-04-24.

- ✨ `[FEAT]` **User-facing notice when partial attachment failures occur** — Beta.106's hotfix (PR #893) lets the conversation proceed when only some attachments succeed (deliberately better than aborting the whole thread on one bad URL). Side effect: a user forwarding a 5-image post where 2 succeed and 3 fail will see the bot respond to only the 2 surviving images with no indication that 3 were dropped. The response may appear unexpectedly sparse. **Fix shape**: emit a lightweight ephemeral notice ("⚠️ 3 of 5 attachments couldn't be loaded — link expired or external host blocked") alongside the normal response, tied to the per-attachment failure list already produced by `DownloadAttachmentsStep`. Surface via `errorInfo`-style structured field on the success result (e.g. `partialFailures?: string[]`), then bot-client renders the notice. **Why deferred**: hotfix scope; need design call on ephemeral vs. inline placement and on per-failure detail vs. summary count. Surfaced 2026-04-25 by claude-bot review on PR #893 (minor).
