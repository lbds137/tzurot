## 🏗 Active Epic: Automated Inactivity Retention & Purge

_Focus: a non-commercial solo-operator bot should not retain per-user data (conversations, memories, facts, user-created personas/characters, usage logs) indefinitely for people who stopped using it. Ship an automated retention/purge mechanism keyed on inactivity + unreachability. Promoted 2026-07-23 (owner: "I'm not a commercial entity that can retain user data indefinitely for people who haven't touched the bot in over 3 months"); triggered by beta.174's blast surfacing 26 `50278`-undeliverable users. **Temporarily displaces the UX epic** (parked below) until this ships, then UX standardization resumes._

**Design ACCEPTED 2026-07-23** (owner + council trio GLM 5.2 · Kimi K2.7-code · Qwen 3.7 Max) → [`docs/proposals/backlog/inactivity-retention-purge.md`](../docs/proposals/backlog/inactivity-retention-purge.md). Key locked decisions: **single 180-day window** (not flat-90d — council: too aggressive for user-created content); **`lastActiveAt`** stamped on any interaction + **backfilled from history** (not ship-time — kills the zombie cohort); **undeliverable = `50278`+`50007`, never `20026`** (bot-wide quarantine would false-flag everyone — blast-radius safety); **circuit breaker** (halt+page on a >X% mass-flag run); **manual-approval-first** → autonomous-with-breaker later; privacy-policy entry.

**Phase 2 detailed design ACCEPTED 2026-07-23** (owner + council trio + owner decisions on 6 open calls) → [`inactivity-retention-purge-phase2.md`](../docs/proposals/backlog/inactivity-retention-purge-phase2.md), with a **Phase 1.5** prerequisite (conversation-history sync unification) → [`conversation-history-sync-unification.md`](../docs/proposals/backlog/conversation-history-sync-unification.md). Phase-2 owner calls: orphan-bucket reassignment (a sentinel owner, not the operator's account, `original_owner_discord_id` preserved for reclamation); `retention_exempt` column; stamp Discord `10013` now + immediate-purge-with-persistence-guard; a single `retention_purge_log` table (audit + off-DB DLQ); mode-aware `eraseAccount`; TOCTOU re-check.

### Roadmap (per the proposal's phasing)

| Phase | Contents | Status |
| --- | --- | --- |
| 1 — tracking | **Split into PRs.** (1a) schema + migration + `retention:backfill-last-active` — **✅ MERGED #1764** (dev migrated + backfilled 271 users; idempotent). (1b) gateway forward stamp (`getOrCreateUser` cache-miss, raw SQL so `updated_at` stays off the sync LWW resolver) — **✅ MERGED #1765**. (1c) undeliverable stamp on 50278/50007 + clear-on-activity — **✅ MERGED #1766** (clear folded into the 1b activity stamp, not the notify seams — single-failure users would never clear there). (1d) pure-client activity stamp via new `POST /internal/users/activity` — **✅ MERGED #1767** (fires fire-and-forget for all chat-input commands; no property distinguishes pure-client). No purge — the clock is now running. | ✅ COMPLETE (1a–1d) |
| 1.5 — CH sync unification (prerequisite) | Make `conversation_history` a normal sync table (add `updatedAt` + generalized AFTER-DELETE trigger, retire the bespoke tombstone) so cascade-deletes are sync-safe; also fixed a latent resurrection gap in today's self-serve `/settings data delete`. → [`conversation-history-sync-unification.md`](../docs/proposals/backlog/conversation-history-sync-unification.md). | **✅ MERGED #1778 (2026-07-24)** — round-trip test proves soft-delete (updated_at LWW) + hard-delete (trigger) propagate, no resurrection; net −782 lines. ✅ **Dev migration applied 2026-07-24** (`updated_at` + `sync_tombstone_conversation_history` trigger live; DROP TABLE clean — bespoke tombstones migrated into `sync_tombstones` first). |
| 2 — preview + unreachable purge | `retention:preview` (dry-run) + the daily job's unreachable branch behind manual approval. The 26 = first real preview batch. Detailed design (D1–D15) → [`inactivity-retention-purge-phase2.md`](../docs/proposals/backlog/inactivity-retention-purge-phase2.md). | **DESIGN ACCEPTED 2026-07-23; Phase 1.5 prerequisite ✅ merged.** **PR-A ✅ MERGED #1779 (2026-07-24)** — additive schema (`discord_account_gone_at`/`retention_exempt`/`original_owner_discord_id` + `retention_purge_log` audit+DLQ table) + D10 blast-success clear + D13 `10013` stamp; no purge logic yet. **PR-B ✅ MERGED #1780 (2026-07-24)** — mode-aware `eraseAccount` (`AccountEraserService` = DB+off-DB, D1) + retention re-home of cross-user characters to the Orphaned-Characters sentinel (D11); self-serve unchanged, retention mode inert until PR-D. No migration. **PR-C ✅ MERGED #1781 (2026-07-24)** — `RetentionPurgeService` owns the single D3/D4 eligibility predicate; read-only `GET /internal/retention/preview` + `pnpm ops retention:preview` (first tooling→gateway plumbing, debuted on the safe endpoint because PR-D's purge needs that path anyway); reports per-user character impact (delete vs re-home) + breaker %. **PR-D1 ✅ MERGED #1795 (2026-07-26)** — the purge capability: per-user `POST /internal/retention/purge` + `retention:purge`/`reconcile-off-db` CLI, in-tx TOCTOU re-check, audit ledger (`success`/`done` enums + `off_db_pending`), 25% server-side hard ceiling, gone-flag self-heal, privacy-policy inactivity section; dev migration applied. **PR-D2 ✅ MERGED #1797 (2026-07-26)** — `RetentionNagScheduler` daily owner-channel nag (weekly Redis cooldown, silent at 0, breaker warning + per-env CLI commands), riding a `createIntervalScheduler` extraction across all three bot-client schedulers. **Dev end-to-end purge smoke ✅ PASS 2026-07-26**: two fabricated users through the real purge — cascade, sentinel re-home with `original_owner_discord_id`, audit rows with exact counts, tombstones for every synced row; fixtures + tombstones cleaned, dev back to baseline. **PHASE 2 COMPLETE.** (Historical note: PR-D was unblocked 2026-07-25 when the "re-homed private characters are inaccessible" blocker was retracted on code evidence — the runtime gate is `buildAccessFilter` (`isPublic \|\| ownerId`), so re-homing never removes access anyone had; the accepted residue is over-retention of once-public-now-private characters with historical reach, see D11.) |
| 3 — reachable branch | notify + export offer + grace + notify-send-failure re-route; circuit breaker. ~~Verify `AccountExportJob` delivery path first~~ — grounding resolved it: **no delivery step exists** (link-only, 24h expiry), so the notice invites self-serve `/settings data export`; see the Phase-3 build notes in the phase2 doc. | **IN FLIGHT 2026-07-26.** Owner calls: 30-day grace · single notice · reclamation deferred (triggers filed). **PR-E1 ✅ MERGED #1800 (2026-07-26; dev migrated)**: `retention_notified_at` + grace-expired predicate arm + notify predicates + reachable-branch counts + activity clears; inert until E2 (3 review rounds: countInGrace disjointness guards + graceExpired-only display gate both caught and fixed; round 3 no blocking findings). **PR-E2 ✅ MERGED #1801 (2026-07-26)**: the full notify pipeline — dedicated `retention-notify` queue, `RetentionNotifyService` (allowlist-narrowed cohort, warn/hard breaker, unique jobIds), bot-client worker (pre-send re-check, 1/sec pacing, per-recipient reporting, owner-channel batch tallies), shared `dmFailureStamps` kernel (both DM pipelines stamp through one code→column mapping), `retention:notify` CLI (dry-run-first, two-flag breaker discipline), notice copy with extended-context exclusion, privacy policy rewritten to the two-path form, producer→consumer contract fixture + topology surface. 3 review rounds (round-1 CLI breaker-flow bug fixed; round-2 tightened BOTH recipient schemas to DiscordSnowflakeSchema; round 3 no blocking findings; regex-drift micro-follow-up #1802). **#1805 bystander split ✅ MERGED (2026-07-26, pre-first-notify)**: prod measurement found 32 of the 53-user notify cohort never deliberately used the bot — notify narrows to the `DELIBERATE_USE` fragment (~21 real users), bystander-shaped rows purge silently via a fourth eligibility arm (never-notified-gated, so a served notice makes the 30-day grace floor unconditional in SQL); review rounds added the `personality_owners` co-ownership arm (inert today, mirrors `crossUserReach`) and the grace-floor gate, both pinned by real-SQL regression fixtures. **BUILD COMPLETE — awaiting release (beta.179 needed before the first notify run); the runtime lifecycle (first prod notify run → 30-day grace → first grace-expired purge) is operator-driven and starts after deploy.** (Historical: the original park trigger was found circular — release blasts are opt-in-gated and never touch the inactive cohort, so cohort discovery IS the notify branch.) |
| 4 — policy + autonomous | privacy-policy 180-day-window entry; flip to autonomous-with-circuit-breaker once trusted. | pending Phase 3 |

**Prod-backfill sequencing** (reviewer #1764 finding) — ✅ DONE. The plan: since 1a–1d rode the SAME release (beta.175), run the prod backfill ONCE _after_ that release deploys them all (forward-stamp already live → no coverage gap). Executed 2026-07-23 post-beta.175 deploy: **270 prod users backfilled** (29 <30d · 198 30–180d · 43 already >180d — the zombie cohort correctly gets historical, not fresh, timestamps). Idempotent, so a re-run is safe if ever needed.

---

## ⏸ Parked: Platform-Portable UX Layer (Discord Design System) — NO LONGER next-up

> **Owner call 2026-07-25 — beta-exit is deprioritized; the backlog drain is the focus.** _"I'm not in a huge rush for getting out of beta tbh since it's a small project. what I want to focus on is draining the backlog."_ Supersedes the 2026-07-23 "resumes as the immediate next epic" note. **Sequencing ratified 2026-07-26: release beta.177 → first prod purge → substrate design boulder ([`backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md), one session, migrate nothing) → THEN promote [`cold/themes/follow-up-pool-drain.md`](cold/themes/follow-up-pool-drain.md)** — the drain campaign executes against whichever substrate wins, so the substrate question resolves first.
>
> Consequence worth stating up front: the drain can no longer interleave with UX waves, because there won't be any in flight. Its Phase-1 domain batches must stand alone as PRs that do the work their rows asked for — which was the theme's original shape, but it means the browse/UI cluster no longer gets a free ride on picker-hygiene work.

_The beta-exit gate (owner 2026-07-17). PARKED mid-Phase-3 on 2026-07-23 to let the retention epic through. **Full roadmap + pilot + owner-design-inputs live in [`cold/themes/platform-portable-ux-layer.md`](cold/themes/platform-portable-ux-layer.md)** (the parked home); authoritative design detail in the two ACCEPTED artifacts + the per-PR log._

- **Phase 1 (catalog + voice)** ✅ COMPLETE (beta ≤172). **Phase 2 (components)** ✅ COMPLETE (beta.170–172). **Phase 3 (vocabulary + enforcement)** IN FLIGHT — **Waves 0–3 ✅ RELEASED** (beta.173 waves 0–2; **beta.174 wave 3, the breaking rename batch**). **Remaining: Waves 4–6** — PR-6a factory core+pilot → 6b destructive preset → 7 `/deny` redesign → 8 remaining picker hygiene → 9/10 factory sweep + router adoption (normal minors). Phase 4 (adapter) trigger-gated on a real second platform.
- Artifacts: [`docs/proposals/backlog/ux-design-system-spec.md`](../docs/proposals/backlog/ux-design-system-spec.md) (WHAT) · [`docs/proposals/backlog/platform-portable-ux-design.md`](../docs/proposals/backlog/platform-portable-ux-design.md) (HOW) · per-PR detail [`cold/epic-log.md`](cold/epic-log.md).
- Phase-1 follow-ups + the alias-redesign pilot + scoping-tier design inputs: in the parked-home theme file above + `cold/follow-ups.md`.
