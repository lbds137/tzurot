## 🏗 Active Epic: Memory System Overhaul

_Focus: implement the ACCEPTED memory architecture — typed memories (episode/fact/reflection/canon) with social scoping pools, async extraction, relationship blocks, consolidation, and hybrid retrieval, evolved in-house on pgvector. Promoted 2026-07-06 after the Spinoff-Theme Knockout completed (its two trigger-gated stragglers — PGLite phases 2–3, z.ai samples — returned to the cold queue)._

**Governing artifact**: [`docs/proposals/backlog/memory-architecture.md`](../docs/proposals/backlog/memory-architecture.md) (ACCEPTED 2026-07-05; owner-signed; full-trio council). The eval harness (§3.9) is the load-bearing gate: every phase ships with its before/after corpus run, and phases 3–6 proceed only if the numbers show the prior phase paid off.

### Roadmap (artifact §5 — each phase independently shippable, quality-gated)

| Phase | Contents | Status |
| --- | --- | --- |
| 0 — integrity + eval baseline | visibility filter, re-embed, linkage, delete propagation, scoping columns, golden corpus | ✅ DONE 2026-07-05 (#1490/#1497/#1498) |
| **2 slice 1** — memory_facts schema + types | table + supersession chain + 3-registry-protected ivfflat + JobType/schemas/generator | ✅ DONE 2026-07-07 (#1527; 3 review rounds, all findings applied; dev migrated) |
| **2 slice 2** — extraction worker (shadow) + goldens + tripwire | trigger/budget/service/store/prompt + eval harness + revival semantics | ✅ DONE 2026-07-07 (#1528; 7 review rounds — every round's findings real: crash vector, provenance leak, BullMQ config, PII logs, revival bug; dev shadow-enabled EXTRACTION_ENABLED=true) |
| 1a — hybrid retrieval | dense + FTS-OR + recency RRF | **A/B RUN 2026-07-12 on the real 800-row Lila corpus (20 pooled queries, LLM-judge, `poolScoring`): RRF ≈ dense (recall@10 0.583 vs 0.571; recall@5 0.479 vs 0.496 — RRF slightly WORSE). SECOND near-null after the toy corpus. RRF-as-implemented does NOT clear the bar → owner call: don't un-park standalone.** FTS did genuinely save 2 queries dense missed (rare-term, screenshot) — the lexical arm has value; the fusion dilutes dense's wins. Keep the branch's FTS-index work as INPUT to 1c rather than merging RRF. **Owner-decided 2026-07-12: branch STAYS PARKED (not deleted) as FTS-index input; 1c takes priority.** |
| **2 — typed memories + extraction worker** | Extraction WRITE side shipped shadow in slices 1–2. Remaining = a **3-slice arc** (build-time council 2026-07-09, GLM 5.2 · Kimi K2.7 · Qwen 3.7): **4a** fact READ path behind `FACTS_IN_PROMPT_ENABLED` (dev-on/prod-off) — `FactRetriever` + reserved fact sub-budget + `<facts>` block + recency/salience tiebreak · **quality slice** eval→source-grounded + grow goldens + tune (confirmed the 39% "hallucination" WAS mostly golden under-enumeration; tuned to **10.3% violation / 99.6% recall**) · **correction slice** `/memory correct\|forget` + extraction respects `isLocked`/`forgotten`. Prod-enable after gate + correction + burn-in. Episode `type`/`salience` deferred to Phase 1b. | **✅ DONE 2026-07-13 — 4a ✅#1565, quality ✅#1566, correction ✅#1567+#1568, extraction-cost knobs ✅(60d8bc7c4). PROD-ENABLED: `extractionEnabled=true` verified in beta.162 prod boot logs (post bag-repair) + `factsInPromptEnabled` ON in prod (owner-confirmed intentional, 2026-07-13). Episode `type`/`salience` remain deferred to Phase 1b.** |
| 1b — full composite scoring | type-weights, salience, superseded/contradiction penalties (needs Phase 2's types). **WEIGHT SIM RAN 2026-07-13 night (slice 0 bench #1637 → judging → `pnpm eval:fact-score` via #1643): composite scoring REFUTED.** 40 goldens judged (owner-delegated to Claude, single-judge in-context — same judge/bias profile as the fold A/B; qrels with per-golden reasoning at `reports/goldens-mining/fact-qrels.json`; **owner reviewed the full judging sheet 2026-07-14 and confirmed the calls — verdict is FINAL**). Prod ordering (sim→recency→salience tiebreaks) wins outright: recall@10 0.695, miss@10 0.000 on 35 scored goldens; every pre-registered composite LOSES (sim-heavy −2, rec-heavy −3, balanced −9 net flips; ZERO fixes anywhere); **salience-as-extracted is anti-signal** (weighting it halves recall, triples repetition); corrected-tier facts reached zero pools — probe ran 2026-07-14: only **2 corrected rows exist in all of dev** (vs 21,484 observed), both healthy (embedded/active/persona-linked), so it is a base-rate emptiness, not a reachability bug; corrections only enter the tier via explicit `/memory correct`, never from in-conversation corrections — extraction-time correction detection joins the redirect scope. Third honest null of the epic (RRF, fold, composite) — §3.9's gate working as designed. **Judging surfaced the REAL 1b targets, matching the owner's prod feedback item 1:1**: near-dup rows flooding pools (six "is a trans woman" variants co-ranking; adjacent duplicate "is named Lila" — the "same fact over and over" mechanism is duplicate ROWS, not ranking), event-obsoleted facts co-ranking with successors ("surgery scheduled Mar 2026" beside "has undergone surgery" in post-op threads — supersession chains can't see these), and the wrong-entity/hallucination class reproducing at retrieval (chess-"black" → "user is Black" fact contradicted by "user is white" in the same pool). Full verdict: `reports/goldens-mining/fact-sim-verdict.md` (local). **Redirect: 1b's design session = semantic near-dup suppression + event-staleness handling + extraction-time correction detection, folding the board's Untriaged prod-feedback item as acceptance criteria. Slice 1 as originally planned (wire composite weights) is dead — nothing to wire.** | **sim REFUTED → redirect designed (3-model council) + slice A SHIPPED #1644, released beta.165** (valid_from = evidence time; dev repaired 21,530 rows; **prod repaired 21,493 rows 2026-07-14** — post-run dry-run 0, class closed). **Slice B (read-side dup collapse): offline gate ran — quality-neutral at 0.95 but benefit ≈1 wasted slot/400 on the judged bench (dup stacking lived in the non-shipping display arms, not prod's top-10); at 0.90 it frees 10 slots but produces a real false-collapse. RECOMMENDATION: don't build — owner call pending; deferral triggers filed (probe re-run / felt repetition post-A).** Correction detection, write-side dedup, prompt temporal awareness, revival evidence-gate: all in cold/follow-ups.md with promote-when triggers. |
| 1c — retrieval-QUERY sophistication | **Context-folding ALREADY SHIPS** (2026-07-12 grounding correction): `extractRecentHistoryWindow` (last 3 turns, `RAGUtils.ts:258`) → `buildSearchQuery` (`SearchQueryBuilder.ts:24`) prepends the window, and the folded `searchQuery` feeds BOTH arms (`MemoryRetriever`/`FactRetriever` via `factRetrievalHelper`). The A/B's "6/20 (30%) both-arms-miss" measured the **BARE message** (`goldensPooling.eval.test.ts:154` embeds `draft.message`), which production does NOT do → **we have never measured the real production baseline.** **Owner-decided 2026-07-12: re-baseline HONESTLY first** — mine real Lila turns from dev `ConversationHistory` (owner: dev synced from prod, rolling 30-day window), reconstruct the exact folded query offline, run a paired bare-vs-folded A/B (+ turn sweep 3/5/8) with a non-circularity guard (STM/LTM cutoff + lexical echo). Plan: `.claude/plans/mutable-doodling-thunder.md` (approved). Residual both-miss AFTER folding decides rung-2 (LLM query rewrite/condensation). **Architecture pivot (owner input): shareLtmAcrossPersonalities=ON → retrieval is persona-wide, so the corpus must be the FULL ~19k pool — infeasible to re-embed in PGLite. The runner queries dev's real memory store DIRECTLY (reuses production `PgvectorMemoryAdapter`), which is strictly more faithful and drops corpus mining/embedding.** MACHINERY BUILT + MERGED: PR-1 turn-count param (#1610), PR-2 conversation-goldens miner (#1611), PR-3 fold-aware runner + non-circularity guard (#1612), PR-4 poolScoring extensions + A/B glue (#1613). **RESULT (2026-07-12, 40 goldens hand-judged single-judge in-context, guard-masked, n=37 scored): the fold is NOT a recall win — it is neutral-to-slightly-NEGATIVE.** bare-dense recall@10 **0.436** vs production fold3-dense **0.390**; paired bare→fold3 flips @10 = 6 fixes / 7 breaks (**net −1**, within noise); guard-honest both-arms-miss@10 bare **8%** vs fold3 **19%** (fold ADDS misses); fold-depth sweep monotonically worse (fold3 0.390 > fold5 0.256 > fold8 0.195). **Per-style split is the real signal: fold HELPS short-reactive (both-miss 2/10→0/10) but HURTS content-rich turns (standalone 0/10→3/10, compound 0/9→2/9) by diluting a rich query with recent-context chatter.** This INVERTS the epic's premise: the original "30% both-miss" was the toy-corpus strawman; on the real corpus the residual is ~8% (bare) and the existing fold doesn't close it. **Rung-2 (LLM query rewrite) is NOT supported — more query augmentation is the wrong direction.** Cheaper candidate the data points to: make the fold CONDITIONAL (fold only short/content-poor turns) or drop it. Report: `reports/goldens-mining/fold-ab-result.md` (local). **OWNER DECIDED 2026-07-12: conditional-fold — SHIPPED #1614** (pre-registered gate: fold iff unfolded query < 5 content words; validated offline against the judged pool BEFORE wiring: cond recall@10 0.548 vs bare 0.436 vs fold3 0.390, combined both-miss 1/37, 4 fixes / 0 breaks vs bare, robust across thresholds 3/5/8, no style regresses). Judging also surfaced: 3/40 turns had NO relevant memory in the pool at all (extraction-coverage gaps, not retrieval) — points at 1b + extraction quality as the higher-leverage phases. | **✅ DONE 2026-07-12 — 4/4 slices merged (#1610–#1613) + conditional-fold gate shipped (#1614); rung-2 refuted** |
| 3 — scoping matrix activation | pool blend + community consent + encapsulation toggle + fiction flag. **Owner design input (2026-07-10)**: (a) classify facts at extraction time as OBJECTIVE (persona-global — "has a cat named Miso", true with every character) vs RELATIONAL (relationship-experience — "confided X to this character") so the blend policy can widen them differently; the §2.2 matrix covers where a fact is visible but not this distinction. (b) ~~sharing asymmetry~~ DECIDED+SHIPPED 2026-07-10 (owner: "asymmetry usually bugs me"): facts now honor `shareLtmAcrossPersonalities` exactly like episodes; the cross-personality browse VIEW is a follow-ups row. | evidence-gated |
| 4 — relationship layer | relationship blocks via boulder-#2 V-tier | evidence-gated |
| 5 — consolidation | scheduled jobs, tier aging (light stages first) | evidence-gated |
| 6 — curation surfaces | lore books, pins-generalization, `/memory share` | evidence-gated |

**Minimum-viable milestone**: Phases 0 + 1a + 2 — the smallest system that should visibly improve roleplay quality. **Backfill posture**: no bulk re-extraction of the existing corpus; old episodes get retrieval gains as-is.

### Phase-1a park (full context in the phase entry below)

The A/B on the toy corpus measured ZERO recall lift (both 0.889) — parked per the artifact's own evidence gate. Findings shaping the resume: BGE-small subword tokenization already handles short-query rare-token recall; the one both-modes failure needs BM25-class IDF (vanilla Postgres lacks it — Phase 1b note). Resume gate: real-scale goldens from prod data (owner-involved query construction). Privacy call RESOLVED 2026-07-12 (owner, on real-sample evidence): the corpus is **LOCAL-ONLY, never committed** — content is sensitive beyond entity swaps (identifying narratives, third-party accounts); committed artifacts = the deterministic miner (#1608) + query goldens. Corpus mined (800/19,169 rows, Lila persona, dev). Ride-alongs for the resume touch: goldens `$comment` rewording, `PgvectorTypes` threshold JSDoc reorder.

### Design inputs

[`docs/proposals/backlog/MEMORY_INGESTION_IMPROVEMENTS.md`](../docs/proposals/backlog/MEMORY_INGESTION_IMPROVEMENTS.md) remains the ingestion-side input (artifact §3.1 note) — feeds Phase 2 extraction design.

### Design inputs from the external Fable review (2026-07-06)

- ~~**STM/LTM dedup hole (review item 8)**~~ ✅ **SHIPPED #1645 (released v3.0.0-beta.165, 2026-07-14)** — resolved via the third candidate: restructured the retrieval/allocation sequence (history-first pre-pass — `preselectHistory` computes the exact shipped boundary before retrieval; query cutoff over-retrieves past it; selection-time ID-authoritative filter against shipped `messageIds`). **The invariant is enforced and tested**: `ContentBudgetManager.test.ts` "a budget-dropped message stays LTM-reachable" composes a REAL ContextWindowManager. Runtime verification rides the beta.165 smoke (recall probe over long history).
- **Systemic lens for the epic**: items 7+8 are one bug family — budget-adjacent code trusting PRE-truncation state for decisions whose correctness depends on POST-truncation state. Token-counting is also inconsistent (chars/4 vs tiktoken-on-XML) — unify in the epic. Anywhere consuming `rawConversationHistory` size, `oldestHistoryTimestamp`, or per-entry `tokenCount` for allocation/exclusion is suspect until checked.

### Still-live items not owned by the artifact

- **Cross-channel history — smarter retrieval with limits**: limit messages per channel, prioritize channels with active conversations (automatic retrieval path at generation time; distinct from user-driven `/history range` import).

_Per-PR slice detail goes to [`cold/epic-log.md`](cold/epic-log.md) as phases build. Completed-knockout writeup lives in git history (2026-07-06)._
