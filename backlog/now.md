## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

_Recently resolved items move to the GitHub release notes at ship time — this section stays empty between incidents (history: git + releases)._


- 🐛 `[FIX]` **A vxreddit link renders as an empty embed, so the character cannot see art the owner shared** (owner report 2026-09-19 with two `/inspect` payloads; prod requests 39a90679-300d-40aa-854d-b5a1e8ddba18 and 0d4f851a-2b6b-496a-9117-ae2d64c3906a, the same Discord message re-fetched 12 and 17 minutes after posting, blank both times — channel history is a LIVE REST fetch, so this is not the ordinary MESSAGE_CREATE embed race). ROOT CAUSE established by probe, not inference: vxreddit adopted Discord **Components V2**. A Discordbot-UA fetch of the URL returns a `discord:component-embed` Container tree matching the owner's screenshot line-for-line (`accent_color` 16729344 = 0xFF4500, the orange older vxreddit embeds carried in legacy `<color>`), while `EmbedParser.parseEmbed` reads only legacy `APIEmbed` fields and therefore renders nothing. Rollout is partial — 1 of 2 older vxreddit URLs probed now serves it too — so this gets worse, not better. **TASK-1024 (high, M)**, grounded 2026-09-20: discord.js 14.27 already types the whole tree (`Message.components: TopLevelComponent[]`, union carries `ContainerComponent`), nothing in bot-client reads an inbound `message.components` at all (all 18 non-test hits are outbound reply payloads), and the never-emit-a-bare-`<embed></embed>` guard belongs at the four wrapper call sites rather than inside `parseEmbed`, which returns only the body. WHERE Discord surfaces the tree for an AUTO-GENERATED unfurl is still INFERRED, so the task leads with a diagnostic, not a renderer. **NOT fixed.** **PARTIALLY ADDRESSED 2026-09-20 — PR #2459 (`a82e82b4b`). STAYS OPEN: the character still cannot see the art.** What shipped is the instrumentation that answers the one question blocking the renderer, plus two independent gaps. A no-content embed now renders `<embed rendered="false" keys="...">` instead of a blank one — so "an embed was present" and "an embed rendered content" stop being indistinguishable — and `logEmptyEmbedShape` records the message component-tree shape (numeric types and child counts only, never a value drawn from the message, pinned by a four-value adversarial test). Separately `parseEmbed` now renders `url` standalone on a title-less embed, plus `provider`, `video`, and `type` when not `rich`. **The watch that unblocks the renderer**: a prod log line from EmbedShapeDiagnostics carrying a NON-EMPTY `components` array — `componentCount: 0` is equally informative and means the tree lives elsewhere, not that the diagnostic failed. TASK-1028 trims the temporary half once that lands.


- 🐛 `[FIX]` **Vision chain terminates on a router `content_policy` refusal, so the Qwen tail is never reached** (owner report 2026-09-12; log-verified on the current prod ai-worker deployment: 67 vision invocations in ~11.7 h, ZERO on `qwen/qwen3.5-397b-a17b`; two sensitive images walked glm-flash `provider_content_refused` → advance → `openrouter/auto` `content_policy` → "terminate category, image itself rejected", then negative-cached so two re-asks terminated without a call). Mechanism: `VISION_TERMINATE_CATEGORIES` holds `content_policy` and `censored`, treating a provider's refusal as image-intrinsic, against TASK-747's recorded evidence that lower tiers describe what an upstream filter refused. **TASK-937 (high, S). Owner ruling 2026-09-12: both refusal kinds advance; only `media_not_found` stays terminal.** **Fix MERGED as #2399 (develop `3abdc6ad6`, 2026-09-12; ships with beta.223)**: the terminate set is `media_not_found` only, pinned by the invariant tests, single-hop advance tests for both refusal kinds, the three-hop prod walk, and a real-Redis test where a cached `content_policy` entry on tier 1 advances to a fresh tier-2 call. The two prod images already negative-cached at the router advance on their next ask without a flush (the cache is per model). TASK-899 (a less-filtered tail model) stays separate. **Runtime-UNVERIFIED until the deploy** — remove this entry when a prod log shows a qwen invocation following an auto-router `content_policy` advance (TASK-937 stays open as `state:observable` for that clause).

- **Runtime-unverified fixes and parked incidents moved to `state:observable` tracker tasks (2026-09-13 context-budget trim)**: TASK-952 (#2253 single-job restart recovery) · TASK-953 (#2155 guest floor hop-1 promotion) · TASK-954 (PARKED reply-ping toggle; branch `fix/reply-ping-gate` stays, never merge on the code read) · TASK-955 (#2220 GLM catalog-veto rescue) · TASK-956 (the 2026-07-12 Postgres lock-timeout window; probe DURING the next one). Each carries its watch signal; close the task on the observation.


---



### 🚢 Next Release — beta.227 (theme: the digest's prod rollout read and its data-rights closeout, then the drain)

_beta.226 CUT 2026-09-18 (8 PRs / 8 runtime / 155 range files, 175 in the PR diff with the cut commits; two additive migrations premigrated to prod; PR #2450 `0f1e9cfbc`; finalize done; tagged `latest`). The whole doc-97 Phase 4 build: slice 0 (#2442), 1a (#2444), 1b (#2445), 2 (#2446), the stamp fix (#2447), the dry run + warn line (#2448), the single-quote stripper (#2449), the register telemetry (#2443); riders: the privacy retention row, TASK-1013/1014. **A plan-driven cut on the owner's ruling; neither backstop fired.**_

- **Driver**: Opus from 2026-09-18 ~17:30Z (owner switched mid-session), nested dispatch per `/tzurot-orchestration`. Owner decisions carried (binding): the Emily flip carries to prod (ruled 2026-09-18); the memory-archive flips stay per character on their gates; the owner clears sessions with `/clear`, so every handoff lives on disk.
- **In**: #2451 — TASK-1013 (daily stale-digest retention sweep) + TASK-1014 (digests in the account export, `formatVersion` 3), ai-worker runtime; #2452 — the session-mining process batch (non-runtime); **#2453 — TASK-1015** (opt-in `lineAware` on `sendChunkedReply`, bot-client runtime; merged 2026-09-18 22:36Z, two review rounds, no blocking findings). Residue filed rather than silently accepted: TASK-1020 (fence straddling, the measured cost this fix accepts) and TASK-1021 (empty chunk under an empty header, unreachable today); **#2454 — TASK-968 + TASK-969** (the gateway request log records the real HTTP status, and query-string secrets are redacted fail-closed; common-types + api-gateway runtime, merged 2026-09-18 23:56Z, two review rounds). That one also closed a PII leak it would otherwise have introduced: restoring the response-headers map put the account-export filename, which embeds the Discord username, into the log for the first time — now redacted and canary-proven. Its residue: TASK-1022 (BYOK decrypt failures still 500 or silently skip) and TASK-1023 (the sensitive-key predicate misses hyphenated `api-key`, newly reachable from query strings). **#2455 MERGED 2026-09-19 00:58Z** — TASK-958 (undecryptable shapes cookie answers 401 re-authenticate; three review rounds, the substantive one turning a too-broad catch into a config-vs-credential discriminator so a misconfigured deploy still fails loudly). Residue: TASK-1022 (BYOK sites, fix shape CORRECTED to require the same guard) and TASK-1023. **#2456 MERGED 2026-09-20 12:59Z** — TASK-804's emitter gap (the trigger path built its message with `extractContentDescriptions`, so the shipped provenance constraint keyed on an `[Image: …]` header that path never emitted; ai-worker runtime, six review rounds, three of which found real code defects and four of which found false claims in the PR prose). **#2457 MERGED 2026-09-20 19:13Z** — the `adm-zip` override widened to 0.6.1, clearing both open advisories at once (deps only). **#2458 MERGED 2026-09-20 20:18Z** — TASK-1025 + TASK-841 (bot-client's PERSISTED provenance header is sanitized, and the header helpers plus their label vocabulary hoist into one common-types module; bot-client + ai-worker + common-types runtime, two review rounds). Residue filed rather than accepted: TASK-1026 (two regex consumers still hand-spell the label vocabulary) and TASK-1027 (the sanitizers strip ASCII brackets only — an owner call, with the pass-through half now probed). **#2459 MERGED 2026-09-20 23:11Z** — TASK-1024 steps 1/3/4 (the embed no-content marker, the shape diagnostic, and four legacy `APIEmbed` fields `parseEmbed` had always dropped; bot-client runtime, three review rounds, sixteen canaries). TASK-1024 stays open carrying step 2, the Components-V2 renderer, alone. Residue filed: TASK-1028.



**Drain status at session end (2026-09-19 ~01:00Z): HALF DONE.** Shipped: TASK-1015, 968, 969, 958. **Remaining: TASK-959, 962, 960, 961.** `release:range` = 5 PRs / 4 runtime / 64 files, well inside both backstops. The cut stays gated on the owner's ruling to wait for the full drain; resuming it is the first work of the next session. Owner paused at 93% usage across all models, limits reset Sunday 2026-09-20 02:00 ET. **2026-09-20 update: the drain did NOT advance.** The session went to the two prod bugs the owner reported, which outrank it. Remaining is unchanged — TASK-959, 962, 960, 961 — while the pool grew by four (TASK-1024, 1025, 1026, 1027, of which 1025 is already Done). `release:range` is now **8 PRs / 7 runtime / 89 files**, so neither backstop has fired and the owner ruling still governs the cut. **End of day**: `release:range` = **9 PRs / 8 runtime / 100 files** — still inside both backstops (~10 runtime PRs, ~250 files), so the owner ruling continues to govern rather than a backstop firing. Pool grew by six today (TASK-1024 partial, 1025 Done, 1026, 1027, 1028, 1029); the drain tasks are untouched — and there are FIVE, not four: the Cut-when line below puts TASK-1017 in the ai-worker batch, which this sentence had omitted (corrected 2026-09-20 at the Fable review).


- **Waiting on**: (1) ~~the prod rollout read~~ DONE 2026-09-18 14:30Z (owner pair `done` at the 14:26Z tick; first prod trace 2.04 excl/1k, 1,185 completion tokens, 17 s — beat the ~4.4k / 72 s targets; record in `CURRENT.md` + design doc §6); (2) ~~TASK-1013 + TASK-1014~~ SHIPPED #2451 2026-09-18; (3) the doc-61 economy pass over `.claude/rules` and `CURRENT.md` (`/tzurot-doc-audit` § 3b, ends with `cadence:mark economy-pass`); (4) the medium/S drain in same-area batches of 3–5 (TASK-958, 959, 968, 969, 960–962).
- **Watches (agent-run, no owner action)**: the beta.226 watch list in `CURRENT.md`; TASK-991/992's runtime clause; the doc-17 caching reading; TASK-937; TASK-947 (`adm-zip` — 0.6.1 shipped and the override MERGED as #2457; the alerts sit against the DEFAULT branch, so the one remaining observation is re-running `security:advisories` after this reaches `main`); TASK-901; the Emily pre-warm gate and dead-row read.
- **🧑‍💻 Owner to-do**: (1) ~~the prod flips~~ DONE 2026-09-18 ~13:55Z (both Emily pairs due at the read, the tick awaited); (2) TASK-907: review the commitment facts via `/memory facts`, Sort A-Z, the `{assistant}` block at one end; note any welcomed pet name NOT extracted (TASK-950); (3) TASK-104: trim and re-upload the 8 over-cap voice references, then `pnpm ops voice-refs:audit --env prod`; (4) ~~the archive/digest consolidation idea doc~~ FILED as `doc-107` 2026-09-18 (owner asked); (5) leisure, unchanged: card-level examples for Emily; the voice-harness blind review.
- **Explicitly NOT in**: the memory-archive flips themselves (settings, not code) · browse/UI (`doc-14`, incl. the `/shapes export` UX row) · `doc-86` · TASK-906/TASK-910 residue unless a prompt-version bump is planned · the vitest 5 bump (TASK-913 watches) · further lookahead on the strip matchers (TASK-921/923, TASK-1012) · TASK-933 · the agentic `/memory remember` (the agentic proposal owns it) · a digest prompt-version bump (nothing observed warrants one).
- **Deploy notes**: no migrations pending at the write; TASK-1013's sweep is code only. The digest sweep on prod is OFF until the owner's flip; spend ceiling once on = listed pairs × ≤12/day (D8). The reminder DM (beta.223) forms its first prod cohort on or after 2026-10-04; retention is live and autonomous from beta.222 (kill switch: `RETENTION_AUTORUN_ENABLED=false` on bot-client). Prod ops writes under auto mode: `release:premigrate` and `db:safe-migrate` are `autoMode.soft_deny` at user scope by design; `release:premigrate` now also carries a `permissions.ask` rule, so it prompts (forwarded to the phone) instead of reaching the classifier.
- **Cut when**: **SUPERSEDED 2026-09-20 — owner ruled CUT NOW** (Fable session, after the review of the 09-20 PRs): the drain-first ruling below was taken on a two-PR range, and the range since grew a high-priority security fix (#2458, the persisted header forgery), the diagnostic for the open vxreddit Production Issue (#2459, which cannot produce its capture until it runs on prod) and the adm-zip override (#2457, which clears the advisories only on `main`). Raised once as new information; the owner chose cut-now over hold-for-drain and over cut-after-the-two-investigations. The five drain tasks (959, 960, 961, 962, 1017) move to beta.228 unchanged. Prior criterion, kept for the record: **the medium/S drain batches have landed** — owner ruling 2026-09-18 ~17:35Z, taken against a cut proposal that reported the original criterion already MET (TASK-1013 + TASK-1014 shipped, the prod rollout read recorded, `release:range` 2 PRs / 1 runtime / 38 files, preflight clean, risk low). The ruling supersedes that criterion for this train: the cut waits on waiting-on item (4). Drain batches, by area: **bot-client** TASK-1015 (high) + 959 + 962 · **api-gateway** TASK-958 + 968 + 969 + 961 · **ai-worker** TASK-1017 + 960. TASK-1017 was offered as a pre-cut rider (the scheduled-job dispatch chain this release adds a branch to is untested on every branch) and now rides the ai-worker batch instead. Backstops unchanged (~10 runtime PRs / ~250 files); say which fired at the cut.
- **🗺️ Horizon (rolling three releases, re-touched at every cut)**:
  - **beta.227** — this block.
  - **beta.228** — caching Phase 3 if the doc-17 reading arrives; the remaining characters' archive flips as their gates read READY; slice C's dead-row report; the UX-epic slice (`doc-14` waves 4–6); the archive/digest consolidation pass (`doc-107`) once the digest shape has settled.
  - **Backlog slope**: the 226 window (2026-09-16 cut → 2026-09-18 cut) closed five tasks through its eight PRs (994, 996, 1006, 1007, 1008) and filed up to TASK-1014 from TASK-997 (≤18) — net about +13 on the pool in a two-day window that opened and closed one defect class; re-measure at the next cut.

### 🎯 Current Focus (max 3)

**🐛 `[FIX]` Character voice drift in long conversations — `doc-97` (owner intake 2026-09-05, HIGH; the beta.219 opener and the re-entry into `doc-8`)** — a character's replies no longer match its card after months of use. Measured from prod memories on 2026-09-05 (aggregates only, read-only one-off script stored under `docs/local/handoffs/`, copied into `scripts/analysis/` to run): the card register faded through spring and collapsed in August 2026 — exclamation marks per 1k chars 1.3–3.8 (winter) → 0.7–1.0 (Apr–Jul) → 0.06 (Aug) → 0.01 (Sep); a courtroom vocabulary absent through July appears in August and doubles in September; average reply length 830 → 1,652 chars. Mechanism: the card sits ~45k tokens upstream behind the cache prefix while ~43k tokens of the character's own prose (cross-channel history + memory archive, which stores replies verbatim) sit at the generation point. Phase 1 (the `voice_anchor` V-tier section) shipped in beta.219 (#2348), and every build phase of the memory-archive design (`doc-8`) is on main behind switches that ship OFF; what remains is Phase 2 (the owner-applied directive) and the owner-run rollout in `CURRENT.md`. Owner rulings 2026-09-05: anchor before caching Phase 2, LID starts at the design pass. Evidence, handoff, and payload under `docs/local/handoffs/` (gitignored).

**🧹 `[LIFT]` Follow-Up Pool Drain — standing background (RESUMED 2026-08-16: doc-77 shipped in beta.203)** — tracker `doc-7`: the outflow campaign over the ~321-task pool. Opening surfaces: `pnpm tracker task list -s "To Do" -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches (~13 clusters + scattered singletons, counts in the doc). Boundary reminder: **rule-outs are owner-gated, fail-closed** — the agent ships work and verifies-obsolete by grep; merit-removals surface to the owner (06-backlog § Ruling an item out). Substrate migration COMPLETE (#1822 import · #1823 flip · labeling pass · #1825 themes/ideas→docs); design record: [`docs/proposals/backlog/backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md).

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._

_(2026-09-16: the Dependabot batch + claude-workflow main-cut entry SHIPPED in beta.225 — `claude-code-action@v1.0.222` on both `main` and `develop`, #2424 merged, #2425/#2426 superseded by #2430/#2431; the open `adm-zip` alerts are TASK-947's watch — fixed on develop by #2457, clearing when it reaches `main`. Quick Wins is empty.)_



### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — a tracker task (`pnpm tracker task create`, terse one-liner), a tracker idea doc (`pnpm tracker doc create`, speculative feature), a theme doc + `cold/queue.md` bullet (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(2026-07-17: the prod facts-quality feedback item routed to tracker `doc-8` § design inputs — it's 1b acceptance criteria for the parked memory epic. 2026-08-23: the slash-chat-mirrors-tagging directive routed to idea doc `doc-82` — Untriaged is empty.)_

