## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

_Recently resolved items move to the GitHub release notes at ship time — this section stays empty between incidents (history: git + releases)._

- 🐛 `[FIX]` **Vision chain terminates on a router `content_policy` refusal, so the Qwen tail is never reached** (owner report 2026-09-12; log-verified on the current prod ai-worker deployment: 67 vision invocations in ~11.7 h, ZERO on `qwen/qwen3.5-397b-a17b`; two sensitive images walked glm-flash `provider_content_refused` → advance → `openrouter/auto` `content_policy` → "terminate category, image itself rejected", then negative-cached so two re-asks terminated without a call). Mechanism: `VISION_TERMINATE_CATEGORIES` holds `content_policy` and `censored`, treating a provider's refusal as image-intrinsic, against TASK-747's recorded evidence that lower tiers describe what an upstream filter refused. **TASK-937 (high, S). Owner ruling 2026-09-12: both refusal kinds advance; only `media_not_found` stays terminal.** **Fix MERGED as #2399 (develop `3abdc6ad6`, 2026-09-12; ships with beta.223)**: the terminate set is `media_not_found` only, pinned by the invariant tests, single-hop advance tests for both refusal kinds, the three-hop prod walk, and a real-Redis test where a cached `content_policy` entry on tier 1 advances to a fresh tier-2 call. The two prod images already negative-cached at the router advance on their next ask without a flush (the cache is per model). TASK-899 (a less-filtered tail model) stays separate. **Runtime-UNVERIFIED until the deploy** — remove this entry when a prod log shows a qwen invocation following an auto-router `content_policy` advance (TASK-937 stays open as `state:observable` for that clause).

- **Runtime-unverified fixes and parked incidents moved to `state:observable` tracker tasks (2026-09-13 context-budget trim)**: TASK-952 (#2253 single-job restart recovery) · TASK-953 (#2155 guest floor hop-1 promotion) · TASK-954 (PARKED reply-ping toggle; branch `fix/reply-ping-gate` stays, never merge on the code read) · TASK-955 (#2220 GLM catalog-veto rescue) · TASK-956 (the 2026-07-12 Postgres lock-timeout window; probe DURING the next one). Each carries its watch signal; close the task on the observation.


---

### 🚢 Next Release — beta.224 (theme: the carried readings land, and the medium/S drain continues)

_beta.223 CUT 2026-09-12 (11 PRs / 10 runtime / 145 range files; one additive migration premigrated; PR #2405 `b487cc688`; finalize done; tagged `latest`). The retention follow-up set (#2394, #2395 the reminder DM, #2396), the prod vision refusal fix (#2399), prompt hygiene (#2400, #2402), the personality default-config routes (#2398), the bookkeeping tools (#2397), the resolved-tree advisories (#2401), the gate-cache fixes (#2403), the gateway-calls budget split (#2404). The cut fired on the ~10 runtime backstop by owner ruling; the three carried readings below moved here unchanged for the third time._

- **Driver**: Fable with nested dispatch per `/tzurot-orchestration` for units; the Opus lane for drain days when Fable runs low. **Owner decisions carried (binding)**: everything in the ⚖️ block below, plus **2026-09-11**: retention autonomy stays ON for the first prod run (no report-only staging); the cut on the backstop; session mining deferred to the next 4–6-week window; **the retention follow-up set (TASK-932 → TASK-331 → TASK-5) builds ahead of TASK-934** after the first live run was observed.
- **In**: #2406 (TASK-941: the adapter's three-name type re-export deleted, four consumers import from `PgvectorTypes.js`; two review rounds, no findings; runtime) · #2408 (TASK-943: both package.json walkers skip `.claude`, a canary each; one review round, approved; not runtime) · #2407 (TASK-938: `canUserEditPersonality` takes the loaded `ownerId`, one `personality.findUnique` per edit route; three review rounds, a `get.ts` count pin and a dead-type narrowing, then clean; runtime) · #2409 (TASK-889 + TASK-749 + TASK-799: fake-timer attachment tests, one shared invoke-mock adapter in test-utils by subpath, the vision length-stop warn; one review round, two nits declined or filed; runtime) · #2410 (TASK-944 + TASK-792 + TASK-849 + TASK-610: the silent-gate batch — test-utils gets a test script, `tests/e2e` a typecheck script, the repo a `format:check` in both quality and CI, and `drift-ignore.json` an ajv conformance test; one review round, three nits declined on merits; counts runtime only because seven files were prettier-reformatted) · #2411 (TASK-590 + TASK-743: the unconnected-wires batch — `customFields` now round-trips through both user personality routes behind a 16 KiB input bound, and the gateway subscribes to the persona invalidation channel, evicting the same shared per-`PrismaClient` resolver its two call sites obtain; one review round, one informational nit dismissed with evidence; runtime) · #2412 (TASK-949 part 2 + the relationship carve-out: the voice anchor drops its length clause and pet names and carves out forms of address recorded in the facts; four prompt snapshots regenerated; two review rounds; runtime) · #2413 (TASK-898 second half: `delete_branch_on_merge` read over GraphQL because REST withholds it from fine-grained tokens; not runtime) · #2414 (TASK-621 + TASK-540, drain batch 4: the export transcript renders the reasoning trace as a collapsed `<details>` block with a literal closing tag entity-escaped, and a catalog outage returns 503 while a bad value keeps 400, bot-client verified rather than changed; two review rounds, one do-it-now fix, two nits dismissed; runtime) · #2415 (the weekly audit's deletion-safety headline is one paragraph instead of five hard-wrapped lines; one review round, LGTM; not runtime). **Range: 10 PRs / 7 runtime / 135 files.** In flight: drain batch 5 (TASK-948 + TASK-945, one helper for nullable Json writes across five sites plus the length-stop warn level; runtime) and the security rider TASK-957 (owner ruling 2026-09-13: rides 224).
- **Scheduled units (in this order)**:
  1. ~~**TASK-941** (low, S, ai-worker)~~ DONE 2026-09-12 as #2406 (rebased onto the finalized develop after the cut; the first merge of the range).
  2. ~~**TASK-938** (low, S, api-gateway)~~ DONE 2026-09-12 as #2407 (the task's "aliases" route never used this path; corrected in the PR body).
  3. ~~**TASK-943** (medium, S, tooling)~~ DONE 2026-09-12 as #2408 (`.claude` excluded in the bump and advisories walkers).
  4. **The medium/S drain** (~55; the high/S query is empty): batches of 3–5 same-area tasks per PR per the standing owner call. **ai-worker batch 1 MERGED 2026-09-12 as #2409** (TASK-889 fake timers, 21 s → 41 ms · TASK-749 one shared invoke-mock adapter · TASK-799 the vision length-stop warn, hedged to the OpenRouter route). Filed from it: **TASK-944** (medium: `packages/test-utils` has no test script, so its three colocated unit tests never run in `pnpm test` or CI) · **TASK-945** (low: the text path's length warn logs at info with a WARNING prefix). Held back from that batch: TASK-798 (needs a prod `usage_logs` query the auto-mode classifier blocks — owner runs the measurement or hands over the numbers) and TASK-702 (needs one paid OpenRouter probe call — owner call on spend). **Cross-area batch 2 MERGED 2026-09-12 as #2410** — four tasks sharing one defect class, a gate the repo believes is running and is not: TASK-944 (test-utils had no `test` script) · TASK-792 (`tests/` had an empty `scripts` block) · TASK-849 (no format gate anywhere but lint-staged) · TASK-610 (nothing validated `drift-ignore.json` against its schema). Each ships a canary proving the gate now bites. Filed from it: **TASK-946** (low: the rules text describes `structure.test.ts` as per-package; there is exactly one, a cross-package scanner in common-types) · **TASK-947** (medium, `state:observable`: the `adm-zip` advisory is transitive with no published fix, and the existing override already sits at the top of the vulnerable range, so there is nothing to bump to). **Batch 3 MERGED 2026-09-13 as #2411** — two api-gateway tasks sharing the class "a connection the surrounding structure implies exists and does not": TASK-590 (`customFields` accepted by both input schemas and emitted by export, written by neither user route — the admin path worked, so the data was reachable by everyone except its owner; now forwarded behind a 16 KiB `CUSTOM_FIELDS_MAX_BYTES` input bound, response side deliberately left unbounded so pre-existing oversize rows stay readable) · TASK-743 (the gateway subscribed to the persona channel nowhere, so the shared-accessor swap alone would have fixed nothing; both halves ship together in `bootstrap/personaInvalidationSubscription.ts`, and `historyContextResolver` changed behaviour — it built a fresh resolver per call and so never served a cached persona at all). Filed from it: **TASK-948** (medium: `admin/updatePersonality.ts` passes a plain `null` to a `Json?` column where the user route now writes `Prisma.DbNull`; mechanism recorded as code-read-only, not runtime-confirmed). Batch-3 candidates screened out: **TASK-283** is an owner-reviewed privacy-policy disclosure riding the legal finalization pass, not drain work; **TASK-744** turned out HALF-STALE — the shared helper it asks for already exists as `tryInvalidateCache` with ~15 call sites, so it is a sweep, not a build. ~~**TASK-621 and TASK-540 are now unblocked**~~ **Batch 4 MERGED 2026-09-13 as #2414** on the 2026-09-12 rulings (collapsed trace; 503 for the outage, 400 for a bad value). **Batch 5 IN FLIGHT** (TASK-948 + TASK-945): the five `customFields` write sites disagreed on how a JS `null` reaches a `Json?` column, so one `toNullableJsonInput` helper replaces all five after a PGLite test runtime-confirms the admin-route defect; the text path's length-stop warn moves to warn level without the `WARNING:` prefix, sweeping the one sibling. Filed 2026-09-13 outside the drain: **TASK-957** (high, rides 224 by owner ruling: the gateway request log carries the raw `x-service-auth` header on every line) · **TASK-958** (an undecryptable stored shapes.inc cookie 500s the list route instead of a re-authenticate 401) · **TASK-959** (`/memory facts` has no tag filter; TASK-907's review needs one) · **TASK-960–962** and **doc-100/doc-101** from the shapes.inc announcement mining · **TASK-963** (CLI-driven secret rotation that never prints the value; depends on TASK-62; the owner rotates `INTERNAL_SERVICE_SECRET` only after TASK-957 is fully shipped).
  5. **Process units queued 2026-09-13 (owner rulings), Opus drain lane, in this order**: TASK-951 (`.env.example` key-set guard + the drift sweep) → **TASK-838** (the periodic-pass cadence ledger with an overdue nag) → the doc-61 economy pass over `.claude/rules` and this file (measured 2026-09-13: ~280 KB / ~70k tokens always-loaded; rules regrew 142,502 → 162,211 B since the 2026-08-09 trim) → TASK-62 follow-up (env-suffixed Railway tokens: read `TZUROT_RAILWAY_API_TOKEN_DEV`/`_PROD` by `--env`) → TASK-702 (the approved probe, needs the system OpenRouter key from the dev env) → TASK-559 (filter against gates before sampling) → TASK-798 (cap at 32768, measured).
  6. Owner-side rollouts and readings run alongside (the waiting-on list below); the reading work is agent-run and needs no code.
- **Owner call RULED 2026-09-13**: the emphasis-run residual on the self-header matcher stays as shipped (short italic action beats preserved; the measured leak rate was one strip in 171 turns). No further matcher work without new measurement.
- **Watches**: TASK-937 (closes on the first prod log showing the qwen tail after a router `content_policy` advance — the 🚨 entry above goes with it); TASK-901 (closes when a test-heavy PR's first review run posts); the beta.223 checklist in CURRENT.md.
- **Waiting on** (carried from 221, 222 and 223; two of the three landed 2026-09-13):
  1. ~~**`doc-97` Phase 2 — the `Voice Fidelity` directive**~~ APPLIED 2026-09-13 (v2 on dev and prod; text and before/after in `doc-97` Phase 2).
  2. **`doc-17` prompt-caching Phase 2 — the READING**: the multi-day gap-bucket sweep across prod deployments (agent-run); the dev before/after baseline SKIPPED by owner ruling 2026-09-13; Phase 3 follows the reading.
  3. ~~**The first rollout finding that needs code**~~ ANSWERED by the fresh-thread probe 2026-09-13 (two payloads: cross-channel history ON → 0 exclamations/1k, 698 chars, "sweetheart" lifted from the history block; OFF → 4.1/1k, 241 chars, card register back; anchor, directive, archive, memories, facts identical across arms). The history-side treatment is now **`doc-97` Phase 4**, a design pass on the beta.225 horizon — not a beta.224 criterion. Stop-gap RULED the same day: history stays ON and the drift is accepted until the pass — the owner values the recent-life context the block carries over memory pulls, which misfire (TASK-671); the pass keeps the user side and sheds the character side.
- **Runtime watches carried**: TASK-880 · TASK-791 / TASK-860 · TASK-822 · the beta.218 runtime-unverified paths · the `headerLinesStripped` counter and echo-strip warn (TASK-923's measurement) · **new from 222**: the retention run report (TASK-926), the `openrouter` diagnostics now populated on vision/system-model responses (no reader yet; inert), the `/history undo` count copy.
- **🧑‍💻 Owner to-do** (work only the owner can do; decisions live in the tracker's `state:owner` queue, rendered by `pnpm ops backlog:digest`; ask "what is on my list" and this line plus that query is the answer):
  1. ~~**TASK-898 token**~~ SET 2026-09-13; the deletion-safety row closes with #2413 and the next weekly run.
  2. ~~**TASK-62 token**~~ both env-scoped tokens in the local `.env` 2026-09-13; the tooling read follows (queued above).
  3. **TASK-907**: commitment facts are live on prod (no switch); review them via `/memory facts` filtered by `commitment:`, and note any welcomed pet name that was NOT extracted (TASK-950).
  4. **TASK-104**: trim and re-upload the 8 over-cap voice references, then `pnpm ops voice-refs:audit --env prod`.
  5. **TASK-133**: the shapes import/export round trip, next time at a desktop with shapes.inc auth.
  6. Card-level examples for the drifted character; the smoke pass (three items, `CURRENT.md` owner queue; the owner holds it for last). The memory-archive rollout is LIVE on prod for Emily (switches on, pre-warm running); the privacy-policy z.ai bullet landed 2026-09-13.
- **Explicitly NOT in**: the memory-archive flips themselves (settings, not code) · browse/UI (`doc-14`) · `doc-86` · TASK-906/TASK-910 residue unless a prompt-version bump is planned · the vitest 5 bump (held on the stryker-js 6210 fix; TASK-913 watches) · any further hand-added lookahead on the strip matchers (TASK-921/923 say derive from the render path) · TASK-933 (low; the lint rule's blind spots).
- **Deploy notes**: none known yet. The reminder DM (beta.223) forms its first prod cohort on or after 2026-10-04. Retention is live and autonomous in prod from beta.222 (`RETENTION_AUTORUN_ENABLED` defaults `true`; the kill switch is the same variable set `false` on bot-client); dev runs the weekly rehearsal only; the dev gateway purges only `OUTBOUND_DM_ALLOWLIST` accounts; purge and non-dry notify require a lease `runId`.
- **Cut when**: the three waiting-on readings land, OR the ~10 runtime-PR backstop fires first (`release:range` at cut time: 0 PRs); say which at the cut. Three consecutive cuts (221–223) fired on the backstop with the readings untouched — if 224 does the same, the readings are not release criteria and the plan should say so.
- **🗺️ Horizon (rolling three releases, re-touched at every cut)**:
  - **beta.224** — this block.
  - **beta.225** — caching Phase 2 completing (the reading, then Phase 3) or the UX-epic slice (`doc-14` waves 4–6) if caching stalls on evidence; the remaining characters flip as their gates read READY; slice C's dead-row report.
  - **beta.226** — whichever of the above did not fit; a prompt-version bump if the summaries need one.
  - **Backlog slope**: re-measure at the next cut (git-adds vs Done-flips for W36–W38); the 217 baseline is about +36/week net; the 223 window closed thirteen tasks (926, 932, 331, 5, 934, 46, 923, 929, 930, 902, 940, 927, 936) and filed six (938–943).

- **⚖️ Owner decisions carried forward (binding, do not re-litigate)**: **beta.219 rulings (2026-09-07)** — the commitment fact type is prompt-level only, no `fact_type` column; slice C defines "hot" by stamping retrievals (two additive columns, environment-local); the cut went out before `doc-97` Phase 2 because the range carried the whole archive build. **Voice anchor first, doc-8 active, LID from the design pass (2026-09-05).** **Mutation score is a RISING QUALITY TARGET** (2026-08-30; floors raised and banked in #2275 and #2293 — the posture governs future tranches). **Settled footer wording (2026-08-29)**: `Model: <alias> → <concrete> (routed)` for router turns; free-tier substitution renders bare; `/inspect` shares the vocabulary. **Vision chain (2026-09-02)**: the concrete paid terminal after a router alias is the code default `qwen/qwen3.5-397b-a17b`; the guest floor STAYS the `openrouter/free` router alias because provider eligibility on OpenRouter is circumstantial — a hand-picked free model and a same-tier retry are both ruled out (TASK-860 is a watch, not work). **beta.214 owner-queue calls (2026-09-02)**: TASK-822 measure-first; TASK-835 legitimate, document + pin; TASK-751 accept the hourly re-attempt (archived); TASK-870 accepted risk (archived); 104/133/63 stay filed. **Backlog shrink lever (2026-09-04)**: the batched rule-out pass over priority-low tasks + declined-in-PR default for low-priority process residue (TASK-888, beta.217) — chosen over watch-expiry-only and outflow-only. Deciding any of these twice is how surfaces drift.

### 🎯 Current Focus (max 3)

**🐛 `[FIX]` Character voice drift in long conversations — `doc-97` (owner intake 2026-09-05, HIGH; the beta.219 opener and the re-entry into `doc-8`)** — a character's replies no longer match its card after months of use. Measured from prod memories on 2026-09-05 (aggregates only, read-only one-off script stored under `docs/local/handoffs/`, copied into `scripts/analysis/` to run): the card register faded through spring and collapsed in August 2026 — exclamation marks per 1k chars 1.3–3.8 (winter) → 0.7–1.0 (Apr–Jul) → 0.06 (Aug) → 0.01 (Sep); a courtroom vocabulary absent through July appears in August and doubles in September; average reply length 830 → 1,652 chars. Mechanism: the card sits ~45k tokens upstream behind the cache prefix while ~43k tokens of the character's own prose (cross-channel history + memory archive, which stores replies verbatim) sit at the generation point. Phase 1 (the `voice_anchor` V-tier section) shipped in beta.219 (#2348), and every build phase of the memory-archive design (`doc-8`) is on main behind switches that ship OFF; what remains is Phase 2 (the owner-applied directive) and the owner-run rollout in `CURRENT.md`. Owner rulings 2026-09-05: anchor before caching Phase 2, LID starts at the design pass. Evidence, handoff, and payload under `docs/local/handoffs/` (gitignored).

**🧹 `[LIFT]` Follow-Up Pool Drain — standing background (RESUMED 2026-08-16: doc-77 shipped in beta.203)** — tracker `doc-7`: the outflow campaign over the ~321-task pool. Opening surfaces: `pnpm tracker task list -s "To Do" -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches (~13 clusters + scattered singletons, counts in the doc). Boundary reminder: **rule-outs are owner-gated, fail-closed** — the agent ships work and verifies-obsolete by grep; merit-removals surface to the owner (06-backlog § Ruling an item out). Substrate migration COMPLETE (#1822 import · #1823 flip · labeling pass · #1825 themes/ideas→docs); design record: [`docs/proposals/backlog/backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md).

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._



### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — a tracker task (`pnpm tracker task create`, terse one-liner), a tracker idea doc (`pnpm tracker doc create`, speculative feature), a theme doc + `cold/queue.md` bullet (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(2026-07-17: the prod facts-quality feedback item routed to tracker `doc-8` § design inputs — it's 1b acceptance criteria for the parked memory epic. 2026-08-23: the slash-chat-mirrors-tagging directive routed to idea doc `doc-82` — Untriaged is empty.)_

