## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

_Recently resolved items move to the GitHub release notes at ship time — this section stays empty between incidents (history: git + releases)._

- 🐛 `[FIX]` **Vision chain terminates on a router `content_policy` refusal, so the Qwen tail is never reached** (owner report 2026-09-12; log-verified on the current prod ai-worker deployment: 67 vision invocations in ~11.7 h, ZERO on `qwen/qwen3.5-397b-a17b`; two sensitive images walked glm-flash `provider_content_refused` → advance → `openrouter/auto` `content_policy` → "terminate category, image itself rejected", then negative-cached so two re-asks terminated without a call). Mechanism: `VISION_TERMINATE_CATEGORIES` holds `content_policy` and `censored`, treating a provider's refusal as image-intrinsic, against TASK-747's recorded evidence that lower tiers describe what an upstream filter refused. **TASK-937 (high, S). Owner ruling 2026-09-12: both refusal kinds advance; only `media_not_found` stays terminal.** **Fix MERGED as #2399 (develop `3abdc6ad6`, 2026-09-12; ships with beta.223)**: the terminate set is `media_not_found` only, pinned by the invariant tests, single-hop advance tests for both refusal kinds, the three-hop prod walk, and a real-Redis test where a cached `content_policy` entry on tier 1 advances to a fresh tier-2 call. The two prod images already negative-cached at the router advance on their next ask without a flush (the cache is per model). TASK-899 (a less-filtered tail model) stays separate. **Runtime-UNVERIFIED until the deploy** — remove this entry when a prod log shows a qwen invocation following an auto-router `content_policy` advance (TASK-937 stays open as `state:observable` for that clause).

- **Runtime-unverified fixes and parked incidents moved to `state:observable` tracker tasks (2026-09-13 context-budget trim)**: TASK-952 (#2253 single-job restart recovery) · TASK-953 (#2155 guest floor hop-1 promotion) · TASK-954 (PARKED reply-ping toggle; branch `fix/reply-ping-gate` stays, never merge on the code read) · TASK-955 (#2220 GLM catalog-veto rescue) · TASK-956 (the 2026-07-12 Postgres lock-timeout window; probe DURING the next one). Each carries its watch signal; close the task on the observation.


---


### 🚢 Next Release — beta.225 (theme: the two high memory defects, the Opus drain lane, and the doc-97 Phase 4 design)

_beta.224 CUT 2026-09-13 (12 PRs / 9 runtime / 161 range files; no migrations; PR #2418 `4b1698482`; finalize done; tagged `latest`). The security rider (#2417: the gateway request log redacts `x-service-auth`, the user-context headers and the client-IP carriers at pino-http's own serializer), the nullable-Json helper (#2416), the `customFields` round trip + persona invalidation (#2411), the anchor trim (#2412), the 503 + collapsed reasoning trace (#2414), the silent-gate batch (#2410), the fake-timer tests + shared invoke mock (#2409), one fetch per edit route (#2407), the adapter re-export (#2406), tooling (#2408, #2413, #2415). **Cut on the ~10 runtime backstop for the fourth consecutive time (221–224) with the doc-17 reading untouched — so, as the 224 plan said it would, the carried readings stop being cut criteria: the doc-17 reading is an agent-run watch below, not a gate.**_

- **Driver**: **Opus** for this train (owner ruling 2026-09-13: Fable weekly usage at 9% against a 14.29% daily budget; the handoff is in `CURRENT.md`), nested dispatch for substantive units per `/tzurot-orchestration`; Fable returns for the doc-97 Phase 4 design pass if usage allows. **Owner decisions carried (binding)**: everything in the ⚖️ block below, plus **2026-09-13**: cross-channel history stays ON for Emily — continuity beats register until doc-97 Phase 4 lands (the fresh-thread probe: ON → 0 exclamations/1k, 698 chars; OFF → 4.1/1k, 241 chars); **no manual rotation of `INTERNAL_SERVICE_SECRET`** — TASK-963 ships IN beta.225 (a cut criterion) and its first prod run is the rotation; not urgent (the value has sat in Railway logs for an unknown stretch already, and the one transcript copy goes only to Anthropic), but done once so it never needs thinking about again; the agentic form of `/memory remember` stays with the agentic proposal (`docs/proposals/backlog/agentic-scaffolding.md`); `/shapes export` UX goes to `doc-14`; multi-level undo filed low (TASK-967); the owner clears sessions with `/clear`, so every handoff lives on disk.
- **In**: _(empty at cut; grows as PRs merge)_. **Open**: #2419 (TASK-964, reserved prompt fact slots — two review rounds applied, fixup pending autosquash) · #2420 (TASK-966, purge stops writing the persona-global epoch; partial on the debug-payload clause, which TASK-965 now carries).
- **Scheduled units (in this order)**:
  1. **TASK-964** (high, ai-worker): fact retrieval is top-10 by cosine similarity with no reserved slot for locked, corrected, or relationship facts — the substrate of the relationship carve-out; the probe showed the character surprised by a label the facts already held.
  2. **TASK-966** (high, `state:owner`): `/history purge` writes a persona-global context epoch, so one purge in a one-message thread emptied the cross-channel feed everywhere until `/history undo` restored 1,494 messages. Recommendation recorded on the task: purge stops writing the epoch. Ruling first, then the build.
  3. **The Opus drain lane** (process units, owner rulings 2026-09-13): **TASK-62 follow-up** (env-suffixed Railway tokens) → **TASK-963** (`pnpm ops secrets:rotate-env`: generate locally, write to Railway, redeploy, never print the value; a 225 cut criterion, not urgent — its first prod run is the `INTERNAL_SERVICE_SECRET` rotation) → TASK-951 (`.env.example` key-set guard + drift sweep) → TASK-838 (periodic-pass cadence ledger) → the doc-61 economy pass over `.claude/rules` and this file (~280 KB always-loaded, measured 2026-09-13) → TASK-702 (the approved probe) → TASK-559 (filter against gates before sampling) → TASK-798 (cap at 32768, measured).
  4. **doc-97 Phase 4 — the history-side design pass** (Fable if usage allows, else Opus with a council pass): the continuity-feed premise (the owner keeps cross-channel history for recent-life context, not for the character's own lines); candidates in order — a category-indexed standing block → a recent-days digest → the agentic form deferred on the measured 14–72 s latency; the register-holding cost measured at ~4k reasoning tokens / 72 s per turn is the number to beat. Design lands in `doc-97`; the build is a beta.226 candidate.
  5. **The medium/S drain continues** in same-area batches of 3–5 per PR (the standing owner call). Candidates screened 2026-09-13: TASK-958 (undecryptable stored shapes.inc cookie → 401, not 500), TASK-959 (`/memory facts` tag filter, unblocks the TASK-907 review), TASK-968 (request-log `statusCode: null`), TASK-969 (query-string values past the sanitizer, low), TASK-960–962 (shapes.inc mining).
- **Watches (agent-run, no owner action)**: ~~**TASK-957's runtime clause**~~ CLOSED 2026-09-13 on the live beta.224 prod build — 23 of 23 gateway request lines redact `x-service-auth` and `remoteAddress`; `x-forwarded-for` appeared on zero lines so that carrier stays unverified. The rotation itself still waits for TASK-963. **The doc-17 caching reading** — the multi-day gap-bucket sweep across prod deployments; Phase 3 follows it; a watch, not a criterion. TASK-937 (the qwen tail after a router `content_policy` advance). TASK-947 (`adm-zip`, no fix upstream). TASK-901. The Emily pre-warm gate and dead-row read. The beta.224 checklist in `CURRENT.md`.
- **🧑‍💻 Owner to-do** (decisions live in the tracker's `state:owner` queue — `pnpm ops backlog:digest` renders it):
  1. ~~Rotate `INTERNAL_SERVICE_SECRET` by hand~~ RULED OUT 2026-09-13 evening: no manual rotation — TASK-963 ships in 225 and its first prod run is the rotation. Nothing to do by hand.
  2. ~~**TASK-966** ruling (purge epoch) and **TASK-970** ruling (null in an import file)~~ BOTH RULED 2026-09-13 at the beta.225 session start: TASK-966 takes shape (a), purge stops writing the context epoch (the git log traces the write to `a4230f61a`/`b74ac6fd0` pattern uniformity, not to a case the delete misses); TASK-970 leaves a null alone on re-import, null means skip. Both relabelled `state:ready`; the rulings and their evidence are comments on the tasks.
  3. **TASK-907**: review the commitment facts via `/memory facts`, Sort A-Z, the `{assistant}` block at one end; note any welcomed pet name NOT extracted (TASK-950).
  4. **TASK-104**: trim and re-upload the 8 over-cap voice references, then `pnpm ops voice-refs:audit --env prod`.
  5. Leisure, unchanged: card-level examples for Emily; the voice-harness blind review.
- **Explicitly NOT in**: the memory-archive flips themselves (settings, not code) · browse/UI (`doc-14`, now also the `/shapes export` UX row) · `doc-86` · TASK-906/TASK-910 residue unless a prompt-version bump is planned · the vitest 5 bump (TASK-913 watches) · further hand-added lookahead on the strip matchers (TASK-921/923) · TASK-933 · the agentic `/memory remember` (the agentic proposal owns it) · the doc-97 Phase 4 BUILD (design first).
- **Deploy notes**: none known. The reminder DM (beta.223) forms its first prod cohort on or after 2026-10-04. Retention is live and autonomous in prod from beta.222 (`RETENTION_AUTORUN_ENABLED` defaults `true`; kill switch: the same variable set `false` on bot-client). The one post-224 operational step is the `INTERNAL_SERVICE_SECRET` rotation, and it runs through TASK-963's command once that ships — not by hand.
- **Cut when**: TASK-964, TASK-966 and TASK-963 (the rotation command — owner: in 225 for the sake of safety) have shipped and the first Opus-lane units have landed, OR the ~10 runtime-PR / ~250-file backstop fires first (`release:range` at cut time: 0 PRs / 0 files); say which at the cut. The readings are no longer criteria.
- **🗺️ Horizon (rolling three releases, re-touched at every cut)**:
  - **beta.225** — this block.
  - **beta.226** — the doc-97 Phase 4 build (whichever candidate the design pass picks) or caching Phase 3 if the doc-17 reading arrives first; the remaining characters flip as their gates read READY; slice C's dead-row report; a prompt-version bump if the summaries need one.
  - **beta.227** — whichever of the above did not fit; the UX-epic slice (`doc-14` waves 4–6).
  - **Backlog slope**: the 224 window (2026-09-12 cut → 2026-09-13 cut) closed eighteen tasks through its twelve PRs plus TASK-133 (941, 938, 943, 889, 749, 799, 944, 792, 849, 610, 590, 743, 621, 540, 948, 945, 957, 133) and filed TASK-944–970 (27, five of them the 🚨-board moves 952–956) — net −9 on the pool in a window dominated by the owner's probe day; re-measure at the next cut.

### 🎯 Current Focus (max 3)

**🐛 `[FIX]` Character voice drift in long conversations — `doc-97` (owner intake 2026-09-05, HIGH; the beta.219 opener and the re-entry into `doc-8`)** — a character's replies no longer match its card after months of use. Measured from prod memories on 2026-09-05 (aggregates only, read-only one-off script stored under `docs/local/handoffs/`, copied into `scripts/analysis/` to run): the card register faded through spring and collapsed in August 2026 — exclamation marks per 1k chars 1.3–3.8 (winter) → 0.7–1.0 (Apr–Jul) → 0.06 (Aug) → 0.01 (Sep); a courtroom vocabulary absent through July appears in August and doubles in September; average reply length 830 → 1,652 chars. Mechanism: the card sits ~45k tokens upstream behind the cache prefix while ~43k tokens of the character's own prose (cross-channel history + memory archive, which stores replies verbatim) sit at the generation point. Phase 1 (the `voice_anchor` V-tier section) shipped in beta.219 (#2348), and every build phase of the memory-archive design (`doc-8`) is on main behind switches that ship OFF; what remains is Phase 2 (the owner-applied directive) and the owner-run rollout in `CURRENT.md`. Owner rulings 2026-09-05: anchor before caching Phase 2, LID starts at the design pass. Evidence, handoff, and payload under `docs/local/handoffs/` (gitignored).

**🧹 `[LIFT]` Follow-Up Pool Drain — standing background (RESUMED 2026-08-16: doc-77 shipped in beta.203)** — tracker `doc-7`: the outflow campaign over the ~321-task pool. Opening surfaces: `pnpm tracker task list -s "To Do" -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches (~13 clusters + scattered singletons, counts in the doc). Boundary reminder: **rule-outs are owner-gated, fail-closed** — the agent ships work and verifies-obsolete by grep; merit-removals surface to the owner (06-backlog § Ruling an item out). Substrate migration COMPLETE (#1822 import · #1823 flip · labeling pass · #1825 themes/ideas→docs); design record: [`docs/proposals/backlog/backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md).

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._



### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — a tracker task (`pnpm tracker task create`, terse one-liner), a tracker idea doc (`pnpm tracker doc create`, speculative feature), a theme doc + `cold/queue.md` bullet (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(2026-07-17: the prod facts-quality feedback item routed to tracker `doc-8` § design inputs — it's 1b acceptance criteria for the parked memory epic. 2026-08-23: the slash-chat-mirrors-tagging directive routed to idea doc `doc-82` — Untriaged is empty.)_

