## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

**🐛 `[FIX]` Gateway user-message persist hangs ~20s → user sees "something's slow on our end"** — Surfaced 2026-06-25 (user prod screenshot). bot-client's synchronous `persistUserMessageViaGateway` (POST `/internal/conversation/user-message`) times out at the ~20s WRITE budget; when it's the only summoned slot, `MultiTagCoordinator` emits the "⏳ Couldn't get a response just now…" notice and the user gets NO AI response. **Runtime-confirmed twice today** (13:42:03Z + 18:37:44Z UTC; bot-client error `User-message persist failed via gateway: 0 Request timeout`). Gateway side (req 896): `request aborted, responseTime=19997, statusCode:null` — the single `conversationHistory` INSERT (`ConversationHistoryService.addMessage`) blocked ~20s while the 6 context-read requests in the SAME interaction each completed in <60ms. Gateway up since 04:34Z (no restart), single replica, NO pool-saturation / acquisition-timeout / `[ERROR]` log. **Dig-deeper (2026-06-25) ruled OUT**: (a) pool starvation — 10s acquisition timeout never fired; (b) broad DB/I-O stall — every OTHER request in the 18:35–39 window (reads + the NSFW-verify write) returned in <71ms; (c) legacy dual-write race — bot-client `saveUserMessage` now routes through the gateway (no direct Prisma), so the handler's P2002 fallback + "dual-write window" doc are STALE (cleanup candidate); (d) retention cleanup — admin-only (`/admin/cleanup`), NOT scheduled, no retention activity near either incident. **So it's a block SPECIFIC to the `conversationHistory` INSERT, not systemic.** Three live, un-disambiguated hypotheses (not resolvable from retained logs): **(1) a lock wait** (the exact ~20s-twice = waits-until-client-abort signature favors this, but no holder found — only unique key is the PK, no concurrent same-id writer); **(2) GIN-index pending-list flush** — `messageMetadata` has a `fastupdate`-default-ON GIN index (`conversation_history_message_metadata_idx`); one unlucky INSERT pays the whole pending-list merge, which is table-specific + intermittent; **(3) a dead/stale pooled connection** — query sent to a broken socket hangs until client abort (also fits exact-20s + intermittent). **Aggravating factor**: NO `statement_timeout`/`lock_timeout`/client `query_timeout` anywhere → any of the three hangs silently to the ~20s client abort. **Mitigation implemented (beta.138)**: council pass (GLM-5.2 + Qwen-3.7) → a dedicated **fast pool** for the two conversation-event persist routes ONLY (main pool untouched → legit long ops exempt by architecture), with a staggered `lock_timeout`(2s) < `statement_timeout`(5s) < client `query_timeout`(6s) ladder, a boot `SHOW` probe that fails fast if the GUC `options` startup string didn't apply, and a SQLSTATE classifier that **self-labels** the cause. **Runtime-verified on dev**: GUCs apply through `@prisma/adapter-pg`; `statement_timeout` fired on `pg_sleep(7)` at 5042ms; classifier → `{statement-timeout, 57014}`. Bounds the hang to ~6s (the user sees the notice fast instead of after 20s) AND labels the next occurrence. **Promote-when (root-cause, still open)**: when a `lock-timeout` / `statement-timeout` / `query-timeout-or-dead-conn` label appears in prod logs, do the targeted fix — lock → find the `FOR UPDATE`/DELETE holder on Persona/Personality (the FK-parent `FOR KEY SHARE` surface); slow-work → GIN `fastupdate=off` + scheduled `gin_clean_pending_list`; dead-conn → `idleTimeoutMillis` tuning. _Logs: `scratchpad/gw-prod.txt`._ **Recurrence 2026-06-28** (user prod screenshot, ~18:28 local, a MULTI-character voice scene — so the notice now also surfaces in a multi-slot fan-out, where EVERY slot's submission threw, not just the single-slot case): confirms the beta.138 mitigation bounds-but-doesn't-eliminate. **This sighting IS the promote trigger** — pull prod logs around that timestamp for the beta.138 SQLSTATE self-label (`lock-timeout` / `statement-timeout` / `query-timeout-or-dead-conn`) to disambiguate the 3 hypotheses, then do the targeted fix. **Further recurrence 2026-06-28 ~22:02 EDT (≈02:02Z 06-29)** (user prod screenshot) — another multi-character scene (Hazbin Hotel channel, glm-5.2 via Z.AI). Keeps recurring; the SQLSTATE self-label log-pull is the immediate next step once S2f wraps. User additionally wants (a) this error class eliminated and (b) ANY surfaced error voiced in-character (not a system line) → tracked in `cold/ideas.md` (in-character error delivery). Deferred per user ("file for follow-up after current work").

**🐛 `[FIX]` z.ai coding-plan key not honored when resolving via the GLOBAL/admin default → routes via OpenRouter** — Surfaced 2026-06-29 (user report + screenshot). A user with a z.ai coding-plan key gets z.ai-direct routing when they SET their OWN default to a z.ai model (works — uses their z.ai quota), but when they rely on the **admin/global default** (also a z.ai model) the response footer reads `Model: z-ai/glm-4.7 · via OpenRouter` — auto-promotion to z.ai-direct does NOT fire, so their z.ai subscription quota is bypassed (served via OpenRouter instead). **Runtime-confirmed symptom** (screenshot footer). **Non-catastrophic** — gracefully falls back to OpenRouter (the user still gets a response), just on the wrong provider/quota; not a hard outage, so it needn't block the current epic. **Auto-promotion logic** (`ProviderRouter.ts`): promotes `provider==='openrouter'` + `isZaiCodingPlanModel(model)` + user-has-zai-coding-key → z.ai-direct. `glm-4.7` IS in the z.ai catalog (`constants/ai.ts:310`) and the user HAS a working key (their own-default path promotes), so **the bug is path-specific to global/admin-default resolution** — NOT the catalog or key validity. **Hypothesis (code-read, NOT yet runtime-confirmed which branch)**: the global-default path either (a) doesn't surface the requesting user's zai-coding key to `ProviderRouter`/`ApiKeyResolver` (promotion may be keyed to config OWNERSHIP — applied for the user's own config but not when the resolved config is the admin's global default), or (b) doesn't route the global-default path through `ProviderRouter` at all. **One diagnostic to confirm**: log `ProviderRouter` inputs (`provider`, `model`, `isZaiCodingPlanModel`, `hasZaiKey`) for a global-default resolution by a z.ai-key user → see which predicate is false. **Start**: `services/ai-worker/src/services/ProviderRouter.ts` (promotion predicate) + `AuthStep.ts` (caller) + `ApiKeyResolver.ts` (key resolution) + the LLM-config resolution cascade (user-default vs global/admin-default path). **UPDATE 2026-06-28 (new data point + bot-client trace, refines the framing)**: user reports the divergence tracks **trigger mode**, not (only) config source — a plain message in an **activated channel** promotes to z.ai (footer `glm-4.7 · via Z.AI Coding Plan 📍 auto`), but a **reply** to the persona does NOT (footer `z-ai/glm-4.7 · via OpenRouter`), same user + personality + model (runtime-confirmed, 2 screenshots). This may mean the earlier "global vs own-default" axis was a conflation; the controlled variable to isolate is **trigger mode with config source held constant**. **Bot-client trace (this session) RULES OUT a bot-client-side cause** (so the user's "bot-client DRY violation" hunch doesn't hold): reply (`ReplyResolutionService.resolvePersonality` → `HttpPersonalityLoader.loadPersonality`) and activation (`loadPersonalityStrict`) return the **same** `LoadedPersonality` (`loadPersonality` is just `loadPersonalityStrict` + error-reclassification, `HttpPersonalityLoader.ts:125`), and **both** trigger modes funnel through the SAME `PersonalityChatManager.submitChatJob` (`PersonalityChatManager.ts:206`), which uses `message.author` for BOTH the gateway client (`clientsForUser`) and `context.userId` (via `buildContext`). So `userId` + `personality` reaching the gateway are identical across modes — the bot-client submit layer is DRY-clean and does NOT explain the split. **Therefore the divergence is server-side or a runtime/data factor** (api-gateway `LlmConfigResolver` cascade / ai-worker `AuthStep` → `ProviderRouter` → `ApiKeyResolver`). Note the related latent issue [`cold/follow-ups.md` "ResolvedLlmConfig carries no provider" (#1239)]: promotion keys off the resolved **model name's `z-ai/` prefix**; both modes show `z-ai/glm-4.7` as the configured model, so the ONLY remaining lever is whether `tryResolveUserKey(userId)` resolves the key — yet `userId` is the same human in both. **This contradiction is the signal that a runtime factor is in play; do NOT ship a fix on code-reading alone.** **Sharpened diagnostic**: temporary `debug`-type log at `ProviderRouter.resolveRoute` entry capturing `{ configuredProvider, configuredModel, userId }` (the no-key / whitelist-miss branches already log at DEBUG — bump to INFO or enable debug on dev), reproduce a reply vs an activation for the same user+personality, and diff the three inputs. If `userId` differs → trace where the reply path overrides it server-side; if same but the key isn't found → `ApiKeyResolver` cache/scope; if `configuredProvider`/model differs → the cascade/seed-provider path. The reply also carries `rawReferencedMessages` (activation doesn't) — a candidate server-side variable worth checking in the same probe.

**🐛 `[FIX]` Voice output silently dropped → text-only, when Mistral guardrail-blocks then the self-hosted cold-start eats the TTS timeout** — Surfaced 2026-06-29 (user prod screenshots; char `lilith-tzel-shani`, user has a Mistral BYOK key). Voice-enabled character + Mistral key → **text-only delivered, no audio**. **Runtime-confirmed full trace** (ai-worker prod, 15:47–15:51Z; `scratchpad/tts-prod.txt`): (1) **15:47:23** Mistral TTS (BYOK) → **403 `guardrail_violation`** (`hate_and_discrimination: violated=true`) — Mistral's moderation blocked the *content*, NOT a bad key; `TtsDispatcher` correctly fell back to self-hosted. (2) self-hosted **voice-engine was COLD** (serverless idle-kill) → **~52s cold-start** (`VoiceEngineWarmup`, 17 polls, `warmupElapsedMs=52167`). (3) **15:48:17** self-hosted synthesis began (3491 chars → 2 chunks). (4) **15:51:09** `TTSStep` hit its **240s timeout** → "TTS synthesis failed, delivering text-only response" (`TimeoutError after 240000ms`). (5) **15:51:29** synthesis actually **completed** (`durationMs=190418`, 15MB WAV) — **~20s too late**. (6) **15:51:37** "TTS completed after timeout (**result discarded**)" (audioSize=1188639). **Root cause**: the ~52s serverless cold-start ate the 240s budget, so the inherently-slow (190s) self-hosted synthesis of a long reply finished just past the deadline → text-only + the produced audio discarded. The fallback chain WORKED; it lost a race against the timeout. Compounding: edgy content *always* routes to the slow self-hosted path (Mistral guardrail blocks it), and the prod voice-engine is serverless (cold). **Fix levers (impact order)**: **(1) keep the prod voice-engine warm** (disable Railway serverless idle-kill — a USER-side dashboard toggle, same one that bites dev TTS probes) → removes the ~52s cold-start, the biggest budget-eater (190s + ~10s start would beat 240s). **(2) don't discard the late-completed audio** — deliver it as a follow-up message instead of throwing it away (it finished, just ~20s late) — code change in `TTSStep` (`services/ai-worker/.../pipeline/steps/TTSStep.ts`). **(3) tune the 240s `TTSStep` timeout** — weakest (4 min is already a long wait for text). **Promote-when**: decide with the user whether the voice-engine-warm toggle + the late-audio-delivery code fix land before beta.142 or as a follow-up; toggle #1 can be flipped immediately as a stop-gap. _Logs: `scratchpad/tts-prod.txt`._

**🐛 `[FIX]` Forwarded text message (native Discord forward) → content lost → AI sees "Hello" placeholder, character is blind to the forward** — Surfaced 2026-06-29 (user prod screenshots + debug JSON; char `lilith-tzel-shani`, activated channel). User forwarded a long text message (Discord native "↩ Forwarded"); the character replied as if she couldn't see it ("You said you were forwarding Ardat-Lilin's rant. I'm still here with my hand out"). **Runtime-confirmed** (debug JSON for job `llm-dd5db39b-b602-4018-93b5-0bdd6dc4d620`): `inputProcessing.rawUserMessage = ""`, `referencedMessageIds = []`, `referencedMessagesContent = []`; the assembled prompt's final user turn is literally `Hello` (the `PromptBuilder` empty-message placeholder, `PromptBuilder.ts:327/334/337`). So the forward's text — which lives in `message.messageSnapshots`, not `message.content` — was **never captured**, neither as the user-message content nor as a reference. The forward WAS the trigger (user-message attempted + empty), not extended context (user-confirmed framing). **Ruled out**: NOT an "activated channel skips extraction" bug — the trigger-content build goes through the SHARED `MessageContextBuilder` (`:250` → `extractReferencesAndMentions` → `MessageReferenceExtractor.extractReferencesWithReplacement`, which by design substitutes snapshot text for a forward's empty `content`, `MessageReferenceExtractor.ts:126-128`), used by both reply and activated paths. So the path ran; the snapshot substitution just didn't land. **Open WHY (DEBUG-level, NOT in prod INFO — needs an instrumented repro, do NOT fix on code-reading alone per `00-critical.md`)**: (a) `isForwardedMessage(message)` returned **false** despite snapshots present (the `reference.type`-not-always-populated fragility the code comments warn about — `forwardedMessageUtils` has a `messageSnapshots` fallback, but it may not have fired) → no substitution → empty; OR (b) detected, but the snapshot text didn't propagate into `messageContent` for the trigger. **Next step**: a `debug`-type instrumentation log at ingestion capturing `message.type`, `message.reference`, `message.messageSnapshots?.size`, `snapshot[0].content?.length`, the `isForwardedMessage` result, and the resulting `messageContent` → deploy to dev → reproduce a forward in an activated channel → diff (a) vs (b), then targeted fix. Prior forwarded-message debugging campaign + tests exist (`forwardedMessageUtils`, `MessageReferenceExtractor`, `EmptyMessageFilter` all handle forwards) — this is a gap/regression in that surface. _Artifacts: debug JSON + system prompt provided by user (job `dd5db39b`)._

_Recently resolved:_ the DB connection-pool-starvation timeouts (surfaced 2026-06-11, fixed 2026-06-18 via #1250/#1251 — bounded `pg.Pool` `max=20` + 10s acquisition timeout + saturation gauge) were **confirmed resolved 2026-06-25** (prod logs: both pools configured, zero saturation). NOTE: this new issue is a DIFFERENT failure mode (a single INSERT blocking with the pool healthy), not a pool-starvation regression.

---

### 🎯 Current Focus (max 3)

**🏗️ `[FEAT]` Model Configuration Overhaul — Phase 3: capability-driven config model** → see [`active-epic.md`](active-epic.md). Phases 1–2 (the `kind` discriminator: vision as a first-class config + editing surface) shipped through beta.141; Phase 3 pivots OFF `kind` — a config is `{name, model, params}` and the model's **capability** decides slot-eligibility (fixes the "Config not found" vision-default bug, the misleading 👁 badge, and duplicate configs). S1 (#1384) + S2 (#1385) + S3 (#1388, the "Config not found" bug fix) shipped; **S4 NEXT** = bot-client capability-driven UX (👁 = `supportsVision`, slot-context browse, drop the kind option, derive default badges from the pointers). Then S5 = test sweep + beta.142 release. Holds beta.142. Plan: `.claude/plans/floofy-rolling-crane.md`. _Post-S3-merge: dev migrate pending (`pnpm ops db:migrate --env dev`) + re-set the global vision default after deleting dup configs._

---

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._

_(empty)_

---

### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — `cold/follow-ups.md` (terse, one-liner), `cold/ideas.md` (speculative feature), `cold/themes/` (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(empty)_
