## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

_Recently resolved items move to the GitHub release notes at ship time — this section stays empty between incidents (history: git + releases)._

- 🐛 `[FIX]` **Reply-ping toggle is ignored — replying to a character with the ping OFF still triggers it** (owner intake 2026-08-17, prod). Full analysis + fix shape + the one open runtime question in **TASK-649**. Short form: the inbound ping signal is never consulted (`PersonalityTriggerProcessor.resolveReplyPersonality` gates only on `message.reference`), and `mentions.repliedUser` is NOT that signal — discord.js populates it on every reply, so the discriminator is membership in `mentions.users`. **Do not ship to prod on the code-read alone**: whether Discord lists a WEBHOOK author in `mentions` when the ping is ON needs a dev capture first, because a wrong answer suppresses every reply.

- 🐛 `[FIX]` **OpenRouter catalog has no non-request-driven writer — TASK-639's premise was wrong. FIXED in #2134.** ⚠️ **The probe behind this entry was invalid** — `pnpm ops run` injects only `DATABASE_URL`, so `REDIS_URL` came from the local `.env` and it read `localhost:6379`, not prod. The absent-key observation was never made. The structural gap is still grep-verified (no non-request-driven writer exists) and TASK-639's original prod-log evidence stands; only the direct observation is retracted. Reading prod Redis at all is TASK-652. `OpenRouterModelCache` lives only in api-gateway and every caller is request-driven (llm-config / model-override / autocomplete routes); `refreshCache` has no caller outside its own test, and there is no startup warm or repeatable job. With a 24h TTL, one quiet day expires the key and nothing rewrites it — ai-worker reads that key every generation but cannot populate it, so degraded capability data is the STEADY state, not an incident. Fix shape: give api-gateway a non-request-driven writer (startup warm + a repeatable refresh under the TTL — `setInterval` is a scaling blocker per `04-discord.md`). Unblocks GLM hardening (b).

- 🐛 `[FIX]` **GLM 5.3 fallback — fix (a) SHIPPED in beta.204 (#2128); (b) STILL OPEN.** The category-loss dead end is fixed: `inheritedQuotaCategory` now threads `promotionDemotion` → `AuthStep` → `GenerationStep` → the eligibility gate, which reads `classifyQuotaFailure(originalError) ?? opts.inheritedQuotaCategory` (live classification still preferred). **Runtime-UNVERIFIED** — the beta.204 dev smoke triggered no demotion, so the first real confirmation is a prod recurrence; watch for a retarget succeeding where Turn B previously dead-ended. Remaining, and the reason this entry stays: **(b) hardening — do not demote/fall back to an OpenRouter id absent from the catalog**, which is gated on the catalog-unavailability question (**TASK-639**, HIGH — `ModelCapabilityChecker` logged "Model catalog unavailable — pattern-fallback" throughout the incident window). (b) is filed NOWHERE ELSE, so do not remove this entry until it lands. The same dead-end shape one hop down is **TASK-645**. Original incident analysis below (kept for the mechanism; the fix-shape half is now history).

<details><summary>Original 2026-08-16/17 incident analysis</summary>

three fallback tiers exist: (1) z.ai-direct, (2) `AutoPromotionFallback` → OpenRouter same-model, (3) `QuotaFallback` retarget to the admin default. GLM 5.3 makes tier 2 a guaranteed 400 (staggered OpenRouter release — `z-ai/glm-5.3 is not a valid model ID`). Turn A (01:47:56): live z.ai 429 → tier 2 400s ×3 → tier 3 fires on the propagated **rate_limit** category → retargets to `z-ai/glm-5` → SUCCESS (owner's second screenshot). Turn B (01:53:41): the 429 was CACHED (`RateLimitCache`, user-scoped, 15-min TTL) → `PromotionDemotion` skips z.ai ("known-doomed") straight to tier 2 → 400 ×3 → failure surfaces as **bad_request**, which `QuotaFallback` doesn't react to → dead end, in-character error (ref `mswky34dbpd`). **Root cause: the cached-rate-limit demotion converts a rate-limit situation into a bad_request failure, losing the exact trigger tier 3 needs.** Fix shapes: (a) general — a failure downstream of a `PromotionDemotion(category=rate_limit/quota_exceeded)` retains the demotion's category for QuotaFallback eligibility; (b) hardening — don't demote/fall back to an OpenRouter id absent from the catalog (NOTE: `ModelCapabilityChecker` logged "Model catalog unavailable — pattern-fallback" throughout this window, so (b) needs the catalog-unavailability question answered first — possibly its own bug). Owner worked around via a GLM 5.2 preset (tier 2 succeeds there — 5.2 exists on OpenRouter, verified same window 01:56–01:57). Natural beta.204 rider. Filed 2026-08-16 (night); mechanism pinned same night. **Code-grounded 2026-08-17** (read, not assumed): the dead end is the eligibility gate at `quotaFallbackRunner.ts:123-126` — `const category = classifyQuotaFailure(originalError); if (category === null) throw originalError;`. Classification runs on the error's own message regexes, so Turn A passed (its `originalError` WAS the live 429) and Turn B failed (its `originalError` is the demoted route's 400, because z.ai was skipped proactively and never produced an error). `tryPromotionDemotion` DOES carry the category — it returns `quotaFallback: { category, mode: 'proactive' }`, threaded onto the auth at `AuthStep.ts:193` — but `GenerateAttemptOpts` (`autoPromotionFallback.ts:43-58`) has no field for it, so the fact is dropped before the gate reads it. Fix shape (a) is therefore a three-point thread, not a redesign: add an inherited-category field to `GenerateAttemptOpts`, populate it from `llmAuth.quotaFallback.category`, and read it at the gate as `classifyQuotaFailure(originalError) ?? opts.inheritedQuotaCategory`. **Open call for the owner before building**: this widens tier-3 eligibility so ANY downstream failure after a rate-limit demotion becomes retarget-eligible, including bad_requests unrelated to quota — defensible (the user genuinely IS rate-limited, and it makes Turn B behave like the observed-succeeding Turn A), but it is a semantic widening rather than a bug fix, so it wants a deliberate yes. _(Same log window also closed the #2117 observability item: `cacheHitRatio=0.62`, `promptHash*` fields all live.)_

</details>

---

- 🐛 `[FIX]` **Prod Postgres lock timeouts, 15:24–16:32 UTC 2026-07-12** — recurring `canceling statement due to lock timeout` on gateway writes: ≥6 conversation-history persists failed fail-soft (replies delivered, history rows missing) and a character import timed out at 16:29 (succeeded on 16:35/16:37 retries). NOT db-sync (last completed 14:37), NOT retention (no runs logged). Probe ran (owner-approved, ~17:55 UTC): **no live contention, no in-transaction sessions** — the holder released before observation and is now unidentifiable. Structural finding: server-level `idle_in_transaction_session_timeout=0` (and `lock_timeout=0`/`statement_timeout=0`) — a wedged idle-in-transaction connection can hold locks indefinitely; only our app-pool timeouts contained the damage. Mitigation LIVE IN PROD (#1606, released in beta.161 2026-07-12): main-pool `idle_in_transaction_session_timeout=60s` reaps app-held wedged transactions. Holder identity remains unknown; an EXTERNAL wedged session (DB console) is out of the guard's reach — DB-level `ALTER DATABASE` is the escalation if it recurs post-release. Next occurrence: run the `pg_stat_activity`/`pg_blocking_pids` probe DURING the window (one-off script per the session's `lock-probe.ts`). Filed 2026-07-12.

---

### 🚢 Next Release — beta.205 (theme: NOT YET PICKED — owner call)

_The release plan: what the next cut IS, what it waits for, what it deliberately excludes. Drafted at the beta.204 cut (2026-08-17). The cut-criterion here is the primary trigger; the count/file caps in `10-working-posture.md` § Ship in bounded units are backstops._

- **Theme: OPEN — needs an owner pick, and each substantial pick deserves a council pass before plan-mode** (`06-backlog.md` § Promoting a theme). Candidates surfaced at the beta.204 cut, from `cold/queue.md`: **doc-12 Observability & Telemetry** (carries an explicit owner "committed NEXT" note from 2026-07-20, with mined evidence that ~15 prod/cost/security incidents entered only through the owner's own eyeballs) · **doc-75 Guild/Server Management** (Phase 1 is a live correctness defect — a channel-tier setting silently outranked by both user tiers) · **doc-67 Tag-Scoped Sharing** · **doc-63 Ratchet Bidirectionality**. Not a recommendation — the pick is the owner's.
- **In already**: **TASK-646** — the claude-code-action 1.0.193 bump, which MUST land as a main-cut PR (it touches both self-validating claude workflow files; a develop-first merge silently disables claude-review on every PR until the next release). Now unblocked: `main == develop` after beta.204's finalize, so the cut is trivial. · **Dependabot #2126** (prod deps) / **#2127** (dev deps, 17 incl. 2 changesets majors) — both held out of beta.204 deliberately so their risk did not ride the release; both need one more `@dependabot rebase` before merge, and per the skill a dev-tooling bump means rebasing every other open PR before merging it.
- **Committed by owner directive (2026-08-18)**: **TASK-657** — put characters into the `<participants>` roster the way personas already are. Today `from_id` is emitted only for `role="user"` lines while `ParticipantFormatter` tells the model to match `from_id` against participant ids, so every character line carries an instruction that structurally cannot resolve. Two scope guards from the owner, both load-bearing: **characterInfo only** (the roster is in every prompt, so bloat is paid every turn), and **exclude the character currently speaking** (it already receives its full character card). The second makes the roster response-scoped rather than window-scoped — the same channel assembled for two personalities yields two different rosters — so it must be threaded, not cached per channel. Unblocks the `from_id` PR #2141 emits: correct value today, resolves to a roster entry once this lands.
- **Rider candidates**: TASK-647 (lint-time ban on regex tag-stripping — the structural half of beta.204's CodeQL finding) · TASK-644/645 (the two review findings held out of #2129/#2128) · drain-campaign batches as they land.
- **Waiting on**: the theme pick. (The beta.204 post-deploy read is DONE — see below.)
- **Deploy notes**: unknown until the theme lands.
- **Explicitly NOT in**: memory overhaul (doc-8, parked) · doc-64/65/66/67 idea docs (council queue) · token-budget-layer eviction (stays dormant by design, §2.5.1 — see `active-epic.md`'s scope note; that verdict covers the TOKEN-budget window only, not the count-cap one beta.204 shipped).
- **Cut when**: theme's slices shipped. Backstops: ~10 runtime PRs / ~250 files.
- **✅ BETA.204 POST-DEPLOY READ — DONE 2026-08-18. The hysteresis paid, large.** Channel `1498247824662335608`, 12 pairs: still 12/12 cutting at `H chat_log`, but the offsets moved from a 14-char band at **27,451-27,465** to a spread of **27,350-97,986**, with 9 of 12 above 54,000 and 7 above 76,000. Channel `1481138179917615144`, 8 pairs: 6/8 at `H chat_log` spanning **32,326-101,954** (baseline 7/8 in a 117-char band at 32,334-32,451).
  - **Metric caveat, stated rather than papered over**: the plan said to compare cached-token COUNTS/percentages, not raw offsets — but `cache:prefix-diff` does not emit cached tokens at all (verified via `--help`; no other `ops cache:*` command does either). The baseline's "cached 5,632 tok, 29-32%" must have come from the diagnostic payload's `cacheHitRatio`, not this command, so `now.md`'s "same command both sides" was wrong. What IS comparable is the offset, and the named confound cannot explain this: #2129 inserts a fixed few hundred bytes inside S1, which shifts every offset by a CONSTANT — it cannot turn 27,451 into 97,986. The direction and the magnitude are real; only the exact token figure is unreproduced.
  - **A floor remains**: two channel-A pairs cut at exactly 27,350, essentially the old baseline value. Consistent with count-cap eviction breaking the prefix at the top of `chat_log` — the expected residual, not a failure.
  - **⚠️ NEW OBSERVATION — 2 of 8 channel-B pairs diverge at `S1 participants`, not `chat_log`** (offsets 30,862 / 30,894). A roster change invalidates S1 AND everything after it, so it is the expensive miss. Benign cause: a new human speaking legitimately grows the roster, a one-time cost per new participant. **But TASK-657 slice A (#2143) adds sibling CHARACTERS to that same roster, so a new character speaking now also churns it** — a source of S1 invalidation that did not exist when this baseline was taken. #2143's own framing ("byte-stable turn to turn") is true only while the participant SET is stable; it is one-time per roster CHANGE, and slice A increases how many things change it. Not measured — frequency of character churn is unknown, and this is a code-read plus the two prod data points above, not a runtime attribution. Worth a read after #2143 deploys.

- **PRE-DEPLOY BASELINE for the rollout read (TASK-641, 2026-08-17)** — measured BEFORE the hysteresis reaches prod, so the post-deploy read is a real before/after rather than an absolute number. Same command both sides: `pnpm ops cache:prefix-diff --env prod --channel <id>`. Channel `1498247824662335608`: 12/12 pairs cut at `H chat_log`, offsets 27,451-27,465, cached 5,632 tok, 29-32%. Channel `1481138179917615144`: 7/8 at `H chat_log`, offsets 32,334-32,451, cached 6,656 tok, 29-30%. Success = the cut moves DEEPER into chat_log (offset up, cached up); the mechanism is confirmed, only the size of the win is open.

### 🎯 Current Focus (max 3)

**🧹 `[LIFT]` Follow-Up Pool Drain — standing background (RESUMED 2026-08-16: doc-77 shipped in beta.203)** — tracker `doc-7`: the outflow campaign over the ~321-task pool. Opening surfaces: `pnpm tracker task list -s "To Do" -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches (~13 clusters + scattered singletons, counts in the doc). Boundary reminder: **rule-outs are owner-gated, fail-closed** — the agent ships work and verifies-obsolete by grep; merit-removals surface to the owner (06-backlog § Ruling an item out). Substrate migration COMPLETE (#1822 import · #1823 flip · labeling pass · #1825 themes/ideas→docs); design record: [`docs/proposals/backlog/backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md).

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._



### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — a tracker task (`pnpm tracker task create`, terse one-liner), a tracker idea doc (`pnpm tracker doc create`, speculative feature), a theme doc + `cold/queue.md` bullet (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(2026-07-17: the prod facts-quality feedback item routed to tracker `doc-8` § design inputs — it's 1b acceptance criteria for the parked memory epic.)_

- ✨ `[FEAT]` **Slash chat turns should mirror raw tagging in activated channels** — owner directive 2026-07-21 (Wave-3 smoke): `/chat`/`/random`/`/chime-in` should "behave as similarly as possible to regular tagging," i.e. the channel's activated character replies to a slash turn too (today the bot-authored echo is dropped by `BotMessageFilter`, so activation never fires — one reply instead of the raw-message two). Needs scoping in the shared turn engine (`services/character/characterTurn.ts`): dedup when the invoked character IS the activated one, reply ordering, second model call per turn. Behavior change only — no command-shape change, does NOT need the breaking batch.

