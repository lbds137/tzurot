## Now

_The hot surface — loaded at session start alongside `BACKLOG.md`, `active-epic.md`, `references.md`. Keep it small. Caps: Current Focus ≤ 3, Quick Wins ≤ 5, Untriaged ≤ 10._

---

### 🚨 Production Issues

_Active bugs observed in production. Fix before new features. Cleared issues are removed once released — see git history + the GitHub release notes._

_Recently resolved items move to the GitHub release notes at ship time — this section stays empty between incidents (history: git + releases)._


- 🐛 `[FIX]` **A vxreddit link renders as an empty embed, so the character cannot see art the owner shared** (owner report 2026-09-19 with two `/inspect` payloads; prod requests 39a90679-300d-40aa-854d-b5a1e8ddba18 and 0d4f851a-2b6b-496a-9117-ae2d64c3906a, the same Discord message re-fetched 12 and 17 minutes after posting, blank both times — channel history is a LIVE REST fetch, so this is not the ordinary MESSAGE_CREATE embed race). ROOT CAUSE established by probe, not inference: vxreddit adopted Discord **Components V2**. A Discordbot-UA fetch of the URL returns a `discord:component-embed` Container tree matching the owner's screenshot line-for-line (`accent_color` 16729344 = 0xFF4500, the orange older vxreddit embeds carried in legacy `<color>`), while `EmbedParser.parseEmbed` reads only legacy `APIEmbed` fields and therefore renders nothing. Rollout is partial — 1 of 2 older vxreddit URLs probed now serves it too — so this gets worse, not better. **TASK-1024 (high, M)**, grounded 2026-09-20: discord.js 14.27 already types the whole tree (`Message.components: TopLevelComponent[]`, union carries `ContainerComponent`), nothing in bot-client reads an inbound `message.components` at all (all 18 non-test hits are outbound reply payloads), and the never-emit-a-bare-`<embed></embed>` guard belongs at the four wrapper call sites rather than inside `parseEmbed`, which returns only the body. WHERE Discord surfaces the tree for an AUTO-GENERATED unfurl is still INFERRED, so the task leads with a diagnostic, not a renderer. **NOT fixed.** **PARTIALLY ADDRESSED 2026-09-20 — PR #2459 (`a82e82b4b`). STAYS OPEN: the character still cannot see the art.** What shipped is the instrumentation that answers the one question blocking the renderer, plus two independent gaps. A no-content embed now renders `<embed rendered="false" keys="...">` instead of a blank one — so "an embed was present" and "an embed rendered content" stop being indistinguishable — and `logEmptyEmbedShape` records the message component-tree shape (numeric types and child counts only, never a value drawn from the message, pinned by a four-value adversarial test). Separately `parseEmbed` now renders `url` standalone on a title-less embed, plus `provider`, `video`, and `type` when not `rich`. **The watch that unblocks the renderer**: a prod log line from EmbedShapeDiagnostics carrying a NON-EMPTY `components` array — `componentCount: 0` is equally informative and means the tree lives elsewhere, not that the diagnostic failed. TASK-1028 trims the temporary half once that lands. **CAPTURED 2026-09-21 00:40Z** (prod, the 09-19 message re-rendered from the history window on the first turn after the 227 deploy): `keys="components,content_scan_version,type,url"`, `embedType="components" componentCount=0`. The tree lives ON THE EMBED under `embed.components` with `embed.type` = the literal `components`; `message.components` is empty; the inferred location was wrong. No library or Discord doc types the shape (probed: installed = latest for both discord.js 14.27.0 and discord-api-types 0.38.55, both GitHub mains, three api-docs pages). **Probe DONE 2026-09-21**: the tree is one Container holding three TextDisplays and a MediaGallery; Discord adds `proxy_url` (`images-ext-1.discordapp.net`, off the CDN allowlist but routed through the external-image fallback, so no allowlist change). **FIXED — PR #2461 MERGED 2026-09-21 (develop `9a5788feb`; ships with beta.228)**: the renderer (`embedComponents.ts`: `<text>` lines, `<image filename="embed-N-media-M.png" …>` with alt text and a `spoiler="true"` label — owner ruling: label, never withhold — and one `<color>` line), the extractor feeding gallery media to vision via the external-image fallback, and the TASK-1028 trim; the two snapshot-embed sites aligned on plain `toJSON()` (a snapshot is a `Message`, verified in discord.js source). Five review rounds, every finding dispositioned in the PR body; TASK-1024 and TASK-1028 CLOSED; residue filed as TASK-1030 (multi-node rendering), TASK-1031 (plain-attachment spoiler parity, owner call), TASK-1032 (walk bounds). **Runtime-UNVERIFIED until the deploy** — remove this entry when the owner smoke passes: one vxreddit link where a character replies (dev now, prod after beta.228), and the character describes the art.


- 🐛 `[FIX]` **Vision chain terminates on a router `content_policy` refusal, so the Qwen tail is never reached** (owner report 2026-09-12; log-verified on the current prod ai-worker deployment: 67 vision invocations in ~11.7 h, ZERO on `qwen/qwen3.5-397b-a17b`; two sensitive images walked glm-flash `provider_content_refused` → advance → `openrouter/auto` `content_policy` → "terminate category, image itself rejected", then negative-cached so two re-asks terminated without a call). Mechanism: `VISION_TERMINATE_CATEGORIES` holds `content_policy` and `censored`, treating a provider's refusal as image-intrinsic, against TASK-747's recorded evidence that lower tiers describe what an upstream filter refused. **TASK-937 (high, S). Owner ruling 2026-09-12: both refusal kinds advance; only `media_not_found` stays terminal.** **Fix MERGED as #2399 (develop `3abdc6ad6`, 2026-09-12; ships with beta.223)**: the terminate set is `media_not_found` only, pinned by the invariant tests, single-hop advance tests for both refusal kinds, the three-hop prod walk, and a real-Redis test where a cached `content_policy` entry on tier 1 advances to a fresh tier-2 call. The two prod images already negative-cached at the router advance on their next ask without a flush (the cache is per model). TASK-899 (a less-filtered tail model) stays separate. **Runtime-UNVERIFIED until the deploy** — remove this entry when a prod log shows a qwen invocation following an auto-router `content_policy` advance (TASK-937 stays open as `state:observable` for that clause).

- **Runtime-unverified fixes and parked incidents moved to `state:observable` tracker tasks (2026-09-13 context-budget trim)**: TASK-952 (#2253 single-job restart recovery) · TASK-953 (#2155 guest floor hop-1 promotion) · TASK-954 (PARKED reply-ping toggle; branch `fix/reply-ping-gate` stays, never merge on the code read) · TASK-955 (#2220 GLM catalog-veto rescue) · TASK-956 (the 2026-07-12 Postgres lock-timeout window; probe DURING the next one). Each carries its watch signal; close the task on the observation.


---



### 🚢 Next Release — beta.228 (theme: the drain finishes, and the vxreddit renderer if its capture lands)

_beta.227 CUT 2026-09-21 00:34Z (9 PRs / 8 runtime / 116 PR-diff files; no migrations; PR #2460 `56e36674d`; finalize done; tagged `latest`). Attachment provenance on the trigger path (#2456) and the persisted header (#2458), the embed no-content marker + shape diagnostic (#2459), the gateway hardening (#2454, #2455), the digest retention sweep + export (#2451), the adm-zip override (#2457), lineAware chunking (#2453), the process batch (#2452). **An owner ruling on new information — cut-now over drain-first; neither backstop fired.**_

- **Driver**: Fable from 2026-09-20 (weekly usage reset), nested dispatch per `/tzurot-orchestration`. Owner decisions carried (binding): the drain-first cut criterion carries over from the 227 plan unchanged; the Emily flip is on prod; the memory-archive flips stay per character on their gates; TASK-1027 (fullwidth brackets) is the owner's call and does not gate the cut; the owner clears sessions with `/clear`, so every handoff lives on disk.
- **In**: #2461 (Components-V2 embed render — the vxreddit Production Issue; TASK-1024 step 2 + TASK-1028; runtime) · #2463 (production-deps group: dotenv 18, zod, langchain, onnxruntime-node 1.30, transformers 4.3 — the embeddings-package bumps are the runtime-unverified part; dev boot is the check) · #2464 (dev-deps group: eslint plugins, prettier, knip, dependency-cruiser, turbo, @types/node) · #2466 (TASK-1017, the scheduled-job dispatch map; runtime) · #2467 (TASK-959, `/memory facts tag:` filter — unblocks the TASK-907 review; runtime; three review rounds, residue filed as TASK-1036). Already on `main` outside the train: #2465 (claude-code-action 1.0.230 via main-cut, 2026-09-21; develop resynced with `release:finalize`, #2462 closed as superseded). `release:range` at the write: 4 PRs / 4 runtime / 54 files.


- **Waiting on**: (1) the drain tasks in area batches — **bot-client** ~~TASK-959~~ MERGED 2026-09-21 as #2467 · 962 (per-message cost in `/inspect`; built and reviewed in its worktree, transfer + PR next) · **api-gateway** ~~TASK-961~~ ANSWERED 2026-09-21 (undo restores a cleared epoch and deletes nothing; the turn → memory → fact chain is pinned on hard-delete by `memoryDeletionPropagation.component.test.ts`; the soft-clear-should-forget question is in the owner queue, recommendation keep clear as a context boundary) · ~~**ai-worker** TASK-960~~ ANSWERED 2026-09-21 (read-only trace on the task; the serial-vs-parallel fan-out question is in the owner queue, recommendation stay parallel — no build unless the owner wants one) · ~~1017~~ MERGED 2026-09-21 as #2466; (2) ~~TASK-1024 step 2, the Components-V2 renderer~~ MERGED 2026-09-21 as #2461; (3) the doc-61 economy pass over `.claude/rules` and `CURRENT.md` (`/tzurot-doc-audit` § 3b; not overdue, next due 2026-10-02).
- **Watches (agent-run, no owner action)**: the beta.227 list in `CURRENT.md` (the digest retention tick `48 9 * * *` UTC, the redacted `Content-Disposition`, the 401 path); ~~TASK-947~~ (CLOSED 2026-09-21 — `security:advisories` read zero minutes after the 227 merge); TASK-937; TASK-991/992's runtime clause; the doc-17 caching reading; TASK-901; the Emily pre-warm gate and dead-row read.
- **🧑‍💻 Owner to-do**: (1) ~~the one dev smoke~~ ~~the REST probe~~ DONE 2026-09-21 — nothing owner-side until the renderer PR lands, then: post one vxreddit link where a character replies and confirm the character describes the art; (2) TASK-1027's call: strip look-alike brackets by Unicode category (recommended — fails safe, at the price of a mangled CJK filename) or accept the pass-through; (3) TASK-907: review the commitment facts via `/memory facts`, Sort A-Z, the `{assistant}` block at one end; note any welcomed pet name NOT extracted (TASK-950); (4) TASK-104: trim and re-upload the 8 over-cap voice references, then `pnpm ops voice-refs:audit --env prod`; (5) leisure, unchanged: card-level examples for Emily; the voice-harness blind review; (6) TASK-960's call (owner queue): keep the multi-tag fan-out parallel (siblings blind to each other within the turn, recommended) or serialize it so later slots see earlier replies.
- **Explicitly NOT in**: the memory-archive flips themselves (settings, not code) · browse/UI (`doc-14`, incl. the `/shapes export` UX row) · `doc-86` · TASK-906/TASK-910 residue unless a prompt-version bump is planned · the vitest 5 bump (TASK-913 watches) · further lookahead on the strip matchers (TASK-921/923, TASK-1012) · TASK-933 · the agentic `/memory remember` (the agentic proposal owns it) · a digest prompt-version bump (nothing observed warrants one) · TASK-1026 and TASK-1028 until their triggers fire (a seventh header label; the capture).
- **Deploy notes**: no migrations pending at the write. The digest sweep on prod runs only for the listed pairs (spend ceiling = listed pairs × ≤12/day, D8); retention is live and autonomous from beta.222 (kill switch: `RETENTION_AUTORUN_ENABLED=false` on bot-client); the reminder DM (beta.223) forms its first prod cohort on or after 2026-10-04. Prod ops writes under auto mode: `release:premigrate` and `db:safe-migrate` are `autoMode.soft_deny` at user scope by design; `release:premigrate` also carries a `permissions.ask` rule, so it prompts (forwarded to the phone) instead of reaching the classifier.
- **Cut when**: **the five drain tasks have landed** — the owner's 2026-09-18 drain-first criterion, carried over unchanged after the 227 cut ran ahead of it on new information (ruled 2026-09-20: a high-priority security fix, an open-incident diagnostic and an advisory override outranked five medium/low tasks). Backstops unchanged (~10 runtime PRs / ~250 files; `release:range` at the write: 0 PRs); say which fired at the cut.
- **🗺️ Horizon (rolling three releases, re-touched at every cut)**:
  - **beta.228** — this block.
  - **beta.229** — caching Phase 3 if the doc-17 reading arrives; the remaining characters' archive flips as their gates read READY; slice C's dead-row report; the UX-epic slice (`doc-14` waves 4–6); the archive/digest consolidation pass (`doc-107`) once the digest shape has settled.
  - **Backlog slope**: the 227 window (2026-09-18 cut → 2026-09-21 cut) closed nine tasks through its nine PRs (804, 841 partial, 958, 968, 969, 1013, 1014, 1015, 1025) and filed TASK-1015–1029 (15) — net about +6 on the pool over a window that closed one defect class (header forgery, both services) and instrumented another (Components V2); re-measure at the next cut.

### 🎯 Current Focus (max 3)

**🐛 `[FIX]` Character voice drift in long conversations — `doc-97` (owner intake 2026-09-05, HIGH; the beta.219 opener and the re-entry into `doc-8`)** — a character's replies no longer match its card after months of use. Measured from prod memories on 2026-09-05 (aggregates only, read-only one-off script stored under `docs/local/handoffs/`, copied into `scripts/analysis/` to run): the card register faded through spring and collapsed in August 2026 — exclamation marks per 1k chars 1.3–3.8 (winter) → 0.7–1.0 (Apr–Jul) → 0.06 (Aug) → 0.01 (Sep); a courtroom vocabulary absent through July appears in August and doubles in September; average reply length 830 → 1,652 chars. Mechanism: the card sits ~45k tokens upstream behind the cache prefix while ~43k tokens of the character's own prose (cross-channel history + memory archive, which stores replies verbatim) sit at the generation point. Phase 1 (the `voice_anchor` V-tier section) shipped in beta.219 (#2348), and every build phase of the memory-archive design (`doc-8`) is on main behind switches that ship OFF; what remains is Phase 2 (the owner-applied directive) and the owner-run rollout in `CURRENT.md`. Owner rulings 2026-09-05: anchor before caching Phase 2, LID starts at the design pass. Evidence, handoff, and payload under `docs/local/handoffs/` (gitignored).

**🧹 `[LIFT]` Follow-Up Pool Drain — standing background (RESUMED 2026-08-16: doc-77 shipped in beta.203)** — tracker `doc-7`: the outflow campaign over the ~321-task pool. Opening surfaces: `pnpm tracker task list -s "To Do" -l size:S --priority high --plain` (then medium), the digest's oldest-20, and doc-7's Phase-1 domain batches (~13 clusters + scattered singletons, counts in the doc). Boundary reminder: **rule-outs are owner-gated, fail-closed** — the agent ships work and verifies-obsolete by grep; merit-removals surface to the owner (06-backlog § Ruling an item out). Substrate migration COMPLETE (#1822 import · #1823 flip · labeling pass · #1825 themes/ideas→docs); design record: [`docs/proposals/backlog/backlog-substrate.md`](../docs/proposals/backlog/backlog-substrate.md).

### ⚡ Quick Wins (max 5)

_Small tasks that can be done between major features. Good for momentum._

_(2026-09-16: the Dependabot batch + claude-workflow main-cut entry SHIPPED in beta.225 — `claude-code-action@v1.0.222` on both `main` and `develop`, #2424 merged, #2425/#2426 superseded by #2430/#2431; the `adm-zip` alerts cleared with beta.227 — TASK-947 CLOSED 2026-09-21. Quick Wins is empty.)_



### 📥 Untriaged (max 10)

_New items land here for same-session capture. Route each to its home — a tracker task (`pnpm tracker task create`, terse one-liner), a tracker idea doc (`pnpm tracker doc create`, speculative feature), a theme doc + `cold/queue.md` bullet (multi-phase epic), or Current Focus / Quick Wins — when you get to it. An empty Untriaged is the goal._

_(2026-07-17: the prod facts-quality feedback item routed to tracker `doc-8` § design inputs — it's 1b acceptance criteria for the parked memory epic. 2026-08-23: the slash-chat-mirrors-tagging directive routed to idea doc `doc-82` — Untriaged is empty.)_

