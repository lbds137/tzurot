## ⏸️ Deferred

_Decided not to do yet — either declined, or parked behind a named trigger._

| Item                                                                                               | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Schema versioning for BullMQ jobs                                                                  | No breaking changes yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Contract tests for HTTP API                                                                        | Single consumer, but middleware wiring tests needed (see Inbox). Revisit after wiring audit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Redis pipelining                                                                                   | Fast enough at current traffic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| BYOK `lastUsedAt` tracking                                                                         | Nice-to-have, not breaking                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Handler factory generator                                                                          | Add when creating many new routes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Scaling preparation (timers)                                                                       | Single-instance sufficient for now                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Denylist batch cache invalidation                                                                  | Single pubsub messages handle current scale; premature optimization for bulk ops that rarely happen                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Deny detail view DashboardBuilder migration                                                        | Action-oriented UI (toggle/edit/delete) doesn't fit multi-section edit dashboard pattern; already uses SessionManager and DASHBOARD_MESSAGES                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `memory_only` import ownership check                                                               | Not a bug — memory_only imports should work across personality owners since memories belong to the importing user, not the personality owner                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm/action-setup` v5→v6 upgrade                                                                  | Investigation 2026-04-17: v6 only adds pnpm 11 support; we use pnpm 10.30.3 (`packageManager` in package.json). v6 replaces the bundled pnpm with a bootstrap installer (see compare v5...v6: `dist/pnpm.cjs` removed, new `src/install-pnpm/bootstrap/`), which caused `ERR_PNPM_BROKEN_LOCKFILE` in our CI. Zero benefit for us on pnpm 10.x. Revisit if: (a) we adopt pnpm 11, (b) v5 is deprecated, (c) a v6.x patch fixes the bootstrap's pnpm version resolution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| JobTracker orphan-sweep user-visible message                                                       | When the 40-min orphan sweep fires, `completeJob` silently deletes the "taking longer" notification with no replacement, so the user just sees the notification disappear. Flagged in PR #820 round 2. Decided not to surface a user-visible message because: (a) orphans require a worker crash or Redis partition — rare in practice, (b) the `logger.warn` in `scheduleOrphanSweep` is the correct signal for ops. Revisit if we see the silent-disappear UX cause real user confusion. Start: `services/bot-client/src/services/JobTracker.ts` `scheduleOrphanSweep`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| JobTracker "Completed job" log on orphan-release                                                   | `completeJob` emits `logger.info("Completed job after Xs")` regardless of why it was called, so an orphan-sweep release at 40 min reads like a successful completion in logs. Flagged in PR #820 round 2. The preceding `logger.warn` from `scheduleOrphanSweep` provides correlation context, and passing an `isOrphan` flag (or splitting into a separate `forceReleaseJob` method) adds complexity for a rare path. Revisit if we need to distinguish these in aggregated log queries. Start: `services/bot-client/src/services/JobTracker.ts` `completeJob`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Kimi K2.6 plain-text reasoning leak heuristic extractor                                            | Kimi K2.6 via OpenRouter (`moonshotai/kimi-k2.6`) emits chain-of-thought as plain narration terminated by `"Final decision:\n<answer>"` — no structural markers, OpenRouter doesn't extract it as reasoning. Council (Gemini 3.1 Pro Preview, 2026-04-21) recommended Option 2 (split-on-`"Final decision:"` heuristic) but we **deliberately chose NOT to build** it: (a) false-positive risk on legitimate roleplay ending in "Final decision:" phrasing, (b) reasoning content itself is low-value narration (personality rehashing, format debating) that doesn't justify the cost. **What SHIPPED 2026-04-21**: per-model `reasoning-did-not-engage` warn log in `ResponsePostProcessor.ts` so log searches can grep `@level:warn AND "did NOT engage"` with a `modelName` field. **User-facing action**: recommend `reasoning.enabled: false` on any preset pointing at `moonshotai/kimi-k2.6` — strictly better UX for this model given the content quality. **Upstream**: file OpenRouter issue asking them to recognize Kimi K2.6's `"Final decision:"` delimiter and populate `message.reasoning`. Revisit if (a) a future Kimi release produces high-quality reasoning with the same broken structure, or (b) users report preferring K2.6-with-reasoning despite the cost/quality trade-off. Deferred 2026-04-22. |
| Typed aggregated error for `DownloadAttachmentsStep.downloadAll`                                   | Per-attachment failures inside `downloadAll` are typed (`HttpError`, `AttachmentTooLargeError`), but the step-level aggregation collapses into anonymous `new Error('Failed to download N attachment(s): ...')`. Callers (`LLMGenerationHandler.processJob`'s catch) can't `instanceof`-classify without string-parsing. **Acceptable today**: failure surfaces to user as async error regardless of classification; no retry/backoff policy keys off the aggregated type. **Fix shape when needed**: introduce `AggregateAttachmentDownloadError extends Error` carrying `failures: Array<{ name: string; error: Error }>`, keep message format for log compat. **Promote when**: we want differentiated retry policy ("retry whole job if all failures are transient" vs "fast-fail if any 403") or differentiated user-facing messages by failure class. Surfaced 2026-04-24 by PR #889 Round 6 claude-review. Deferred 2026-04-24.                                                                                                                                                                                                                                                                                                                                                                                        |
| Reconsider hard-fail vs soft-error for attachment download failures                                | PR #889 changed semantics: old `AttachmentStorageService.downloadAndStore` used `Promise.allSettled` and returned the original Discord CDN URL as fallback on per-attachment failure (soft-error; job continued with broken URL); new `DownloadAttachmentsStep.downloadAll` throws on any failure and fails the whole job (hard-fail). New behavior is arguably more correct (old soft-error handed LLM a dead URL that produced weird responses), but it's a visible user-facing change — transient CDN hiccups that were previously silent now produce classified async errors. **Watch-item**: if users report "attachments dropped without warning" → "job failed visibly" complaints, reconsider partial-failure soft-error (continue with successful attachments, surface non-fatal warning for failed ones). **Fix shape when needed**: change `downloadAll` to keep partial successes, attach failure metadata to generation context, surface "couldn't load N of M attachments" notice. **Why deferred today**: no observed user complaints; visible errors easier to diagnose than silent corruption. Surfaced 2026-04-24 by PR #889 Round 5 claude-review. Deferred 2026-04-24.                                                                                                                                    |
| `discordCdnGuard` non-https failure-reason variant                                                 | `validateDiscordCdnUrl` returns `{ ok: false, reason: 'invalid-url' }` for both `http://cdn.discordapp.com/...` (correct host, wrong protocol) and `not-a-url` (URL constructor throws). Tagged-union accuracy nit: a future caller wanting to log "known host but http" differently has no way to distinguish. **Fix shape**: add `reason: 'non-https'` as a third failure variant. Low priority — current 4 callers (avatar/voice/import/jsonFileUtils) all just check `cdnGuard.ok`. **Why deferred**: no current consumer cares about the distinction; revisit when a consumer needs differentiated logging/UX. Surfaced 2026-04-26 by claude-bot review on #905 final round. Deferred 2026-04-26.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `personalityOwnerResolver` lookup cache (TTL ~5min)                                                | `services/ai-worker/src/services/diagnostics/personalityOwnerResolver.ts:resolvePersonalityOwnerDiscordId` fires a `prisma.user.findUnique` on every AI generation request to surface owner Discord ID for diagnostic-meta. Lookup is ~1ms typical (single indexed query) and not on a profiled hot path; cache adds invalidation complexity. Options when promoted: (a) thin in-memory `TTLCache<string, string\|null>` keyed by `personalityOwnerInternalId`; (b) extend `UserService` with a cached `getDiscordIdByInternalUuid` (more reusable if a second consumer surfaces). **Promote when**: generation latency tightening identifies this resolver as a material p95 contributor — profile production diagnostic logs first, optimize second. Surfaced 2026-04-25 by claude-bot on PR #898. Deferred 2026-04-27.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `PipelineStep.status: 'error'` reachable via per-step try/catch                                    | Schema declares `'success' \| 'skipped' \| 'error'` and `extendedViews.ts` renders the ❌ icon for it, but `DiagnosticCollector.recordPostProcessing()` currently only emits `success` / `skipped` because today's post-processing transforms (`duplicate_removal`, `thinking_extraction`, `artifact_strip`, `placeholder_replacement`) are simple string ops that don't realistically throw. If a step did throw today, the whole `recordPostProcessing` call dies and `pipelineSteps` is never set — failure is invisible to the Pipeline Health view rather than surfaced in it. **Fix shape**: refactor the `recordPostProcessing` array-build so each step is wrapped in its own try/catch returning a `PipelineStep` (rather than the positional ternary today); on catch emit `{ name, status: 'error', reason: err.message }`. **Promote when**: pipeline grows steps that do real I/O (vector lookups, external API calls) — i.e. when a step _can_ legitimately fail. Surfaced 2026-04-25 by claude-bot on PR #899. Deferred 2026-04-27.                                                                                                                                                                                                                                                                            |
| `satisfies` constraint on `PgvectorMemoryDocument`                                                 | The implicit `PgvectorMemoryDocument[]` → `MemoryDocument[]` (RAG-layer) assignment at `MemoryRetriever.ts:~221` works today via TypeScript structural-typing widening — `Record<string, unknown>` narrows into `{ id?, createdAt?, score? }` because all RAG-layer metadata fields are optional. Risk: if `MemoryDocument.metadata` ever gains a **required** field, the type error surfaces at the assignment site (consumer) rather than at the type definition (producer). An explicit `satisfies` constraint would make the contract fail-fast at definition time. **Fix shape**: declare a structural-compatibility witness type (e.g., `PgvectorMemoryDocument satisfies StorageNarrowsToRag`) — concrete encoding TBD; needs design pass on whether to express the invariant at the type level or via a compile-time test in `*.test-d.ts`. **Promote when**: `MemoryDocument.metadata` (in `ConversationalRAGTypes.ts`) gains a required field, OR opportunistically when next touching the storage→RAG seam in `MemoryRetriever`. Surfaced 2026-04-30 PR #948 round 3. Deferred 2026-05-01.                                                                                                                                                                                                                         |
| BYOK key rotation false-positive rate-limit block (up to 24h)                                      | Rate-limit cache uses `user:<discordId>` as scope identifier. If a user rotates their OpenRouter API key after the original key hit the daily quota, the `user:<discordId>` Redis bucket stays cached as rate-limited until the original reset window expires (up to 24h via clamped TTL). The new key has its own independent quota, so the block is incorrect. **Why deferred**: rare in practice — users almost never rotate keys mid-quota. **Fix shape options**: (a) add `keyRotatedAt` timestamp to the BYOK user record; `deriveCacheKeyId` returns `user:<discordId>:<rotation-epoch>` so a new key gets a fresh cache scope; (b) operator escape valve via `pnpm ops cache:clear-rate-limit --user-id <id>`. Option (b) is cheaper if the manual escape is acceptable; option (a) is correct without operator intervention. **Promote when**: a user reports being rate-limit-blocked after rotating their key, OR alongside an api-key-management UX overhaul. Surfaced 2026-04-29 PR #943. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                   |
| Make `cacheKeyId` required in `InvokeWithRetryOptions` (option a)                                  | Option (b) — `logger.debug` opt-out log when `cacheKeyId.length === 0` — SHIPPED in PR #947, so the silent opt-out is now visible in local dev. PR #947 also added a consumer-side `assertValidCacheKeyId` call inside the else-branch as a separate guard (shape validation, not required-vs-optional). Option (a) — making the field required in the type so test fixtures must explicitly pass `''` — remains untouched. **Promote (a) when**: a 2nd production caller of `LLMInvoker.invokeWithRetry` is added (audit `services/ai-worker/src/services/` for new `invokeWithRetry` invocations). Originally surfaced 2026-04-29 PR #943; option (b) closed 2026-04-30 PR #947. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `isAccountCreditExhaustion` regex breadth                                                          | Fallback pattern `/insufficient.*credits/i` in `apiErrorParser.ts` is broader than the verbatim `/never purchased credits/i` arm; could match a hypothetical OpenRouter 402 with non-credit-exhaustion meaning that happens to mention "insufficient credits" (e.g., "insufficient platform credits for this tier"). Blast radius bounded today because `parseApiError` gates on `statusCode === 402 && resolved.category === ApiErrorCategory.QUOTA_EXCEEDED` — but if OpenRouter ever sends a 402 for a different reason with similar phrasing, it would get cached as account-level credit exhaustion and block subsequent calls for up to 24h. **Fix shape**: tighten the fallback to `/your account has insufficient credits/i` once we have a real 402-shape sample to anchor against, OR add an allowlist of explicit credit-exhaustion sub-patterns. **Promote when**: a 402 with non-credit-exhaustion meaning surfaces in production logs, OR opportunistically when next touching `apiErrorParser.ts`. Surfaced 2026-04-30 PR #945 round 3. Deferred 2026-05-01.                                                                                                                                                                                                                                                   |
| 402 max_tokens auto-reduction (request-level credit-exhaustion fix)                                | When a 402 says "can only afford N tokens" (request-level, sub-classified as `QUOTA_EXCEEDED` not `CREDIT_EXHAUSTION`), the request COULD succeed with smaller `max_tokens`. Currently we just fail the user. A per-request retry with `max_tokens = M - safety_margin` would recover automatically. **Fix shape**: in `LLMInvoker`'s catch path, if 402 + `category === QUOTA_EXCEEDED` + technicalMessage carries an extractable token-budget number, retry once with reduced `max_tokens`. **Why deferred (strong reason)**: different mechanism from credit-exhaustion cache (per-request adjustment, not session-level memoization); no production evidence (10/10 production 402s observed 2026-04-29 were account-level "never purchased credits"); token-extraction from free-form provider messages is brittle (regex robustness, missing/malformed numbers, retry-loop bounding). **Promote when**: a user reports the request-level 402 pattern in production, OR the next time the credit-cache feature is touched and natural surface area exists. Surfaced 2026-04-30 PR #945 plan. Deferred 2026-05-01.                                                                                                                                                                                                        |
| Monitor `ignoreExportsUsedInFile: true` knip config                                                | PR #941 added `ignoreExportsUsedInFile: true` at the **global** knip config level to dampen the dominant false-positive class (option-bag/result-bag interfaces declared next to consumer functions). Risk: a genuine dead export that happens to also be used internally would be silently suppressed too. Knip supports this flag at per-workspace level. **Fix shape (when triggered)**: remove the global flag, add it per-workspace to only `services/ai-worker`, `services/api-gateway`, `services/bot-client` where the option-bag pattern dominates; leave `packages/*` workspaces stricter since they have public API surfaces. **Promote when**: a real dead-export bug that should have been caught is found by other means (review, manual audit, runtime), OR after ~5 PR cycles if the suppression starts feeling too aggressive in routine PR reviews. Surfaced 2026-04-29 PR #941 round 2. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                                                                                               |
| Move `NON_LLM_PROVIDERS` / `USER_AUTH_PROBE_PROVIDERS` to a shared module                          | PR #940 colocated both constants in `services/ai-worker/src/jobs/ImageDescriptionJob.ts` because that's currently the only consumer (`userHasAnyKey` is the sole probe site). If a 2nd consumer appears — e.g., `visionAuthResolver` needing the same "is this user authenticated for SOME LLM provider?" discriminator — the constants should move to `services/ai-worker/src/services/ProviderRouter.ts` (next to `detectVisionProvider` + `effectiveVisionModelName`) or a new dedicated `visionConstants.ts` module. Until then the colocation is correct (premature extraction would be over-abstraction for a single-caller). ~5 LOC pure relocation when triggered. **Promote when**: a 2nd consumer is added, OR the constants need to be referenced from outside `ImageDescriptionJob.ts`. Surfaced 2026-04-29 PR #940 round 1 (self-classified non-blocking). Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Make `visionProvider` required in vision-pipeline option bags                                      | After the cross-provider vision auth fix (PR #938), `visionProvider?: AIProvider` is threaded as optional through `DescribeImageOptions`, `InvokeVisionModelOptions`, `ProcessAttachmentOptions`, `ProcessAttachmentsOptions`. The `?` was for backward-compat with existing tests/legacy paths. Risk: a future caller that forgets to pass `visionProvider` gets the env-default `AI_PROVIDER` from `createChatModel`, silently re-introducing the exact bug PR #938 fixed. **Fix shape**: audit every call site of `describeImage`, `processAttachments`, `processAttachmentsParallel`, `invokeVisionModel` to confirm they pass `visionProvider`. Once 100% coverage, drop the `?` and update the JSDoc. Interim `logger.warn` shipped in PR #940 at `invokeVisionModel` (single chokepoint covering all upstream callers); fires when `provider` is undefined. **Promote when**: a few weeks of clean Railway logs confirm zero callers fire the warn — then drop the `?` and remove the warn. Surfaced 2026-04-29 PR #938 round 4. Deferred 2026-05-01.                                                                                                                                                                                                                                                                  |
| Eliminate double `selectVisionModel` call in DependencyStep                                        | `DependencyStep.processExtendedContextImages` calls `selectVisionModel(personality, isGuestMode)` to pre-compute the effective model for provider detection, then `describeImage` calls `selectVisionModel` again internally for the same personality. For personalities WITH `visionModel` set, both calls return after a property check — no I/O. For personalities WITHOUT an explicit override, both calls reach `hasVisionSupport` → Redis. PR #940 absorbed the **duplication** concern via the `effectiveVisionModelName` helper; the **double-Redis-read** concern stays open per its original promote-when criterion. **Fix shape (still deferred)**: `describeImage` accepts an optional `usedModel` param to let callers pass the pre-computed value. Wider interface change. **Promote when**: telemetry shows non-trivial Redis hit rate from this duplication, OR the next vision-pipeline refactor naturally touches `describeImage`'s signature. Surfaced 2026-04-29 PR #938 round 4. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                    |
| `USER_AUTH_PROBE_PROVIDERS` provider-list drift risk (option b)                                    | Option (a) RESOLVED in PR #940: probe list now derived from `Object.values(AIProvider).filter(p => !NON_LLM_PROVIDER_SET.has(p))`; new LLM providers added to `AIProvider` auto-include; only ElevenLabs (voice-only) is currently filtered. Three invariant tests lock in the contract. **Option (b) remains conditionally deferred**: replacing the heuristic entirely by adding `auth.isGuestMode` to `ImageDescriptionJobData` (api-gateway already knows at job-creation time) would be more correct but requires schema + api-gateway plumbing. **Promote (b) when**: api-gateway auth-resolution refactor naturally surfaces the signal, OR a regression demonstrates the heuristic is insufficient. Surfaced 2026-04-29 PR #938 round 10 follow-up. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Narrow auto-promotion fallback to catalog-drift errors specifically                                | `runWithAutoPromotionFallback` (`services/ai-worker/src/jobs/handlers/pipeline/steps/autoPromotionFallback.ts`) currently catches ANY error from the promoted z.ai request and retries via OpenRouter. Defensive but over-broad — a transient z.ai network blip, a temporary 5xx, or a rate limit will all silently route through OpenRouter without the user knowing. The actual catalog-drift signal is "z.ai returned 404 / model not found." **Fix shape**: once z.ai's API error format for "model not on plan" is empirically verified, narrow the catch to that specific error class. Other errors should propagate immediately so the user sees the real failure mode. ~5-10 LOC change. **Promote when**: we have a real z.ai error response captured (natural production usage, artificial probe with a known-bad model, or z.ai docs improvement). Until then the broad catch is the right starting point — over-fallback is preferable to under-fallback while we have no error-shape data. Surfaced 2026-04-28 PR #928 round 3. Deferred 2026-05-01.                                                                                                                                                                                                                                                             |
| `ResolvedRoute.fallback` invariant via discriminated union (part b)                                | Part (a) — extracting a named `FallbackRoute` type alias — RESOLVED in PR #928; both `ResolvedRoute.fallback` and `ResolvedAuth.fallback` now reference the `FallbackRoute` interface. Part (b) remains: tighten the optional shape into a discriminated union so the `wasAutoPromoted === true ↔ fallback !== undefined` invariant becomes a static guarantee. **Fix shape (b)**: `BaseResolvedRoute & ({ wasAutoPromoted: true; fallback: FallbackRoute } \| { wasAutoPromoted: false; fallback?: never })`. **Caveat**: needs to handle the graceful-degradation path where promotion succeeds but fallback computation fails — possibly a third union variant `{ wasAutoPromoted: true; fallback: FallbackRoute \| undefined }`, which is just back to optional. May need rethinking once we have more data on how often the degradation fires. **Promote when**: feature has stabilized in production for a few weeks AND the graceful-degradation path is well-understood empirically. Surfaced 2026-04-28 PR #928 rounds 3+4. Deferred 2026-05-01.                                                                                                                                                                                                                                                                     |
| `tryResolveUserKey` no-key negative caching                                                        | For `zai-coding`-with-no-key (the auto-fallthrough majority path), `tryResolveUserKey` returns `null` without cache write → every request re-reads DB. Currently accepted overhead documented in code comment at `services/ai-worker/src/services/ApiKeyResolver.ts`; `resolveApiKey`'s `source: 'system'` semantics can't be reused because `zai-coding` has no system fallback. Invariant test (do-not-cache-null-path) shipped via PR #925 — locks in current behavior so a future caching change is a deliberate, test-failure-inducing decision rather than a silent regression. **Fix shape (caching)**: introduce a distinct cache state for "no user key configured" (e.g., `source: 'absent'` discriminant or sentinel value), populate it from `tryResolveUserKey`'s null branch, short-circuit subsequent reads through the same entry-point. Touches `ApiKeyResolver`'s cache shape; likely needs co-update to `resolveApiKey` callers that peek the cache directly. **Promote when**: production logs show measurable per-request DB-read load on the fallthrough path, OR if a 2nd auto-fallthrough provider lands and the duplicate-DB-read pattern compounds. Surfaced 2026-04-27 PR #924 round 7. Deferred 2026-05-01.                                                                                       |
| z.ai integration validation-model fallback (PR #921 follow-up)                                     | PR #926 fixed the immediate day-1 bug (`glm-4.5-flash` was wrong — corrected to `glm-4.5-air`). Broader concern stands: if z.ai deprecates or renames `glm-4.5-air`, every valid key validates as `INVALID_KEY` (silent false-negative). **Fix shape options**: (a) try a list of fallback models in order (`glm-4.5-air` → `glm-4.7` → first-from-`/models`-endpoint); (b) hit a non-chat endpoint (e.g., `/account/balance` if z.ai exposes one — needs research); (c) accept the maintenance risk and add a periodic check to detect catalog changes early. **Promote when**: the integration scales enough that catalog change becomes a real risk, OR alongside the z.ai 402-status verification work. Surfaced 2026-04-27 PR #921. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| z.ai 402 HTTP status verification for expired/lapsed subscription                                  | 401/403/429 are accounted for in `mapValidationErrorToResponse`. If z.ai uses 402 for an expired coding-plan subscription, it falls through to `UNKNOWN` (api-gateway) or `InvalidApiKeyError` (ai-worker), neither of which conveys "subscription lapsed; renew" UX. **Research finding 2026-04-27**: z.ai's API docs at `docs.z.ai/api-reference/llm/chat-completion` only document HTTP 200 + a generic "default error" with structured `{ code, message }` body — they don't enumerate 401/402/403/404/429 explicitly. So a documentation-based answer is unobtainable; verification requires probing with a real expired/lapsed coding-plan key, which we don't have. **Fix shape**: when one becomes available (or when natural-occurrence of subscription expiration produces a real probe), capture the actual status code z.ai returns and surface "subscription lapsed" as a distinct error code in `mapValidationErrorToResponse`. Surfaced 2026-04-27 PR #921 round 3. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                                                       |
| ElevenLabs single-attempt retry — measure-then-decide                                              | Beta.97 cut `ELEVENLABS_MAX_ATTEMPTS` from 2 to 1 to reduce worst-case TTS latency. Both TTS failures observed in beta.107 prod logs hit the 60s ElevenLabs timeout on attempt 1 with no retry, falling all the way through to the cold-start voice-engine path (~44s additional). Reverting to `MAX_ATTEMPTS=2` would increase worst-case latency by 60s on persistent failure but improve user-visible success rate on transient failure. **Decision needs telemetry**: how often does attempt 1 fail and the voice-engine fallback succeed (= retry would have helped) vs. attempt 1 fail and voice-engine also fails (= retry wouldn't change outcome). **Adjacent**: STT + voice-engine retry counts may have the same latent bug pattern — see TTS Engine Upgrade Active Epic, "Audit ElevenLabs STT + voice-engine retry counts." **Promote when**: telemetry already added to `withRetry` in beta.97 has been queried for ElevenLabs attempt-1 failure rate over a 7-day window. Surfaced 2026-04-27. Deferred 2026-05-01.                                                                                                                                                                                                                                                                                            |
| Validate other `/wallet/*` and gateway responses against Zod schemas at api-gateway boundary       | `/wallet/list` resolved in batch-1 cleanup PR (parses through `ListWalletKeysResponseSchema` before `sendCustomSuccess`). Same gap on other gateway endpoints with hand-coded response shapes against schemas in `common-types/schemas/api/`: `/wallet/test`, `/wallet/:provider` DELETE, admin/_ and user/_ sub-routes. **Fix shape**: same pattern — `Schema.parse(payload)` before send. Or invest in wrapping `sendCustomSuccess` with a schema-aware variant so the parse happens centrally. Folds naturally into the Observability & Telemetry theme. **Heads-up on the error path** (PR #929 round 1): `asyncHandler` catches thrown errors and includes `error.message` in the 500 response body. `ZodError.message` includes the full field path and expected/received shapes — fine for authenticated internal endpoints, but if extended to public-facing endpoints, the leak surface needs separate treatment (catch `ZodError` specifically, return generic 500). **Promote when**: extending response validation to additional `/wallet/*` or admin/user routes, OR alongside the Observability & Telemetry theme. Surfaced 2026-04-26 PR #908. Deferred 2026-05-01.                                                                                                                                            |
| TTS Budget Guard decorator (post-Phase-1)                                                          | Wrap provider stack in `BudgetTtsProvider` reading `MONTHLY_TTS_BUDGET_CENTS`; force-switch to free self-hosted once budget exceeded. Hard safety net for Mistral cost. **Promote when**: ~1 month post-Phase-1 deploy after cost telemetry has accumulated enough data to set a sensible default. Surfaced 2026-05-01 in Kimi K2.6 council review. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| TTS Canary synthesis (parallel-run validation)                                                     | When Mistral Voxtral lands in prod, run both Mistral + ElevenLabs in parallel for 1% of requests (behind a flag); compare latency + audio quality. Only upload Mistral result to Discord; log the delta. Empirically de-risks the "85% cost reduction with comparable quality" claim with real-traffic data. ~50 LOC dispatcher modification + log event. **Promote when**: 1-2 weeks post-Phase-1 deploy, before deciding to commit to Mistral as BYOK default. Surfaced 2026-05-01 in Kimi K2.6 council review. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Per-instance `evictionLock` serializes `prepare()` across all slugs                                | `ElevenLabsTtsProvider` + `MistralTtsProvider` use a per-instance promise-chain mutex for `prepare()` — correct for race protection but ALL slugs serialize on the same lock. If slug A's clone hangs, slug B's `prepare()` waits the full timeout. Worst-case latency `(N_concurrent_slow_prepares × timeout)`. **Fix**: per-slug lock map. ~30 LOC across both providers. **Promote when**: simultaneous persona switches with cold caches AND latency complaints surface, OR opportunistic when next touching either provider. Surfaced 2026-05-04 PR #967. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Extract `SingletonFlagResolver` class (replace module-level state in {llm,tts}ConfigSingletons.ts) | Both files use module-level `let pendingResolutions: SingletonResolution[] = []` for cross-call state. Type system doesn't enforce prepare→finalize sequencing — silently no-ops on stale state if order flips. ~100 LOC including test consolidation. **Promote when**: sync machinery grows a third singleton table (rule-of-three), OR opportunistically when next touching either singleton helper. Surfaced 2026-05-04 PR #968. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Extract `replyError` into shared `utils/dashboard/replyError.ts`                                   | Both `character/sectionContext.ts` and `persona/sectionContext.ts` have near-identical `replyError` private functions (3-branch state machine + load-bearing PRECONDITION about ephemeral flag inheritance). Future fix needs to land in both files. ~20-30 LOC consolidated + shared test file. **Promote when**: opportunistic alongside any sectionContext.ts edit, OR a third dashboard adopts replyError. Surfaced 2026-05-07 PR #985. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Runtime guard on `replyError` ephemeral precondition                                               | The `deferred && !replied` path inherits flags from `deferReply` — a future caller who defers without `MessageFlags.Ephemeral` would publicly expose error messages. Currently documented in JSDoc PRECONDITION block. **Fix shape**: (a) runtime `if (interaction.deferred && !interaction.ephemeral) logger.warn(...)`; (b) typed `deferReplyEphemeral(interaction)` wrapper. ~5-15 LOC. **Promote when**: a non-handleViewFullButton caller adopts `replyError` (real risk surfaces), OR alongside the replyError extraction above. Surfaced 2026-05-07 PR #985 round 4. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Dashboard action naming convention drift (kebab vs snake)                                          | `DASHBOARD_ACTIONS` mixes `confirm-delete`/`cancel-delete` (kebab) with `edit_truncated`/`cancel_edit`/`view_full`/`open_editor` (snake). Pre-existing in character; persona mirrored. Strings encoded into Discord custom IDs in user channels — rename requires a migration path. ~50-80 LOC + ~1 month deprecation window. **Promote when**: a dashboard refactor pass touches customId parsing, OR a third dashboard adopts the truncation gate. Surfaced 2026-05-06 PR #984 round 4 + 7. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Remove `/settings tts` and `/settings voices` deprecation stubs                                    | Phase 3 PR 1 will register these as ephemeral deprecation stubs routing to `/voice`. Stubs preserve muscle memory during migration. ~10-15 file deletions + index/test updates. **Promote when**: ~1 month after Phase 3 PR 1 ships, OR sooner if user reports stubs are confusing, OR if user count is small enough that immediate cutover with announcement is cheaper than maintaining the stub layer. Surfaced 2026-05-05. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Extract conditional-warning response/log helper for admin config-delete handlers                   | Both `admin/llm-config.ts` and `admin/tts-config.ts` now have identical `responseBody`/`logFields` ternaries (clean delete vs warning-bearing). ~20 LOC + colocated tests. Companion cleanups: skip the third `prisma.user.count` query in `checkDeleteConstraints` when blocker known; normalize blocker message asymmetry. Reviewer flagged "not worth extracting at N=2." **Promote when**: a third admin config-delete handler with the same warning-discriminator shape lands, OR opportunistic alongside any admin-routes refactor pass. Surfaced 2026-05-04 PR #978. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Distinguish transient vs deterministic failures in `MistralTtsProvider` negative cache             | The 5-min negative cache catches all non-rate-limit failures uniformly. Deterministic failures (401 auth, malformed reference) get cached identically to transient failures (5xx, network blips). PR #974 added one explicit skip for `MistralReferenceAudioTooLongError`. **Fix shape**: route catch through `error.isTransient`; only set negative cache when `isTransient === true`. ~15-20 LOC + tests. **Promote when**: opportunistic alongside next `MistralTtsProvider` edit OR if cache-poisoning produces user-visible delay. Surfaced 2026-05-04 PR #974. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Smart per-user cache invalidation for LLM + TTS configs (cross-cutting)                            | `LlmConfigService.invalidateCacheSafely()` and the `TtsConfigService` mirror both call `cacheInvalidation.invalidateAll()` on every mutation, nuking every user's TTLCache. Better pattern (`ConfigCascadeCacheInvalidationService`) exists but isn't used. Upgrade BOTH services together: enumerate affected users, publish per-user events, only invalidate-all for unenumerable changes (e.g., `setAsFreeDefault`). ~200-300 LOC across both services + tests. **Promote when**: opportunistic when next touching either config service, OR if unconditional invalidation produces measurable cache-thrash in production. Surfaced 2026-05-03. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Split `LlmConfigService` + `TtsConfigService` into Query/Mutation/Cache services (cross-cutting)   | Both services hover at ~600+ raw LOC with mixed responsibilities. Currently passes lint via skip-blanks/skip-comments. Council recommended pre-splitting; user chose mirror-as-single-file for now and split BOTH later when burden becomes real. **Fix shape**: when either crosses the lint threshold, split BOTH into `*QueryService.ts` + `*MutationService.ts` (cache wiring stays in mutation). Mirror split for both LLM and TTS in same PR for symmetry. **Promote when**: ESLint `max-lines` flags either service, OR new feature requires expansion that pushes over 400 content-lines. Surfaced 2026-05-03. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Mistral slot quota empirical verification                                                          | PR 2 ships defensive eviction code modeled on ElevenLabs' "musical chairs" pattern, but Mistral's account-level voice quota is undocumented. Smoke test (4 clones) was insufficient to probe limits. Eviction code may never fire. **Fix shape**: post-deploy, attempt many clones in single account; observe error shape + threshold; document in `docs/research/voice-cloning-2026.md`. **Promote when**: PR 2 has been in production for 2+ weeks without eviction firing AND we want to know whether to keep or remove the dead-on-arrival code. Surfaced 2026-05-02. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| OpenRouter TTS as budget tier (preset voices, no cloning)                                          | OpenRouter exposes preset-voice TTS at very low cost (Kokoro 82M $0.62/1M chars, GPT-4o Mini TTS $0.60/1M) but doesn't support voice cloning. Could plug into TTS provider abstraction as a 4th category for users wanting max savings + accepting loss of per-persona voice differentiation. New `OpenRouterTtsProvider` class (stateless — `prepare()` no-op, `synthesize()` POSTs to `/audio/speech`). **Promote when**: a budget tier is desired OR a specific persona is appropriate for preset voices. Surfaced 2026-05-01. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Buffer-hash → in-memory LRU cache for TTS text recovery                                            | Today's PR #955 placeholder text (`🔁 *Forwarded voice message originally spoken by ...*`) is sufficient for LLM context but loses fidelity vs seeing the real utterance. Upgrade: hash audio buffer at synthesis → store in LRU; on receive side, look up hash → real text. Requires plumbing across ai-worker → bot-client. **Promote when**: a user reports the placeholder feels too information-poor in practice, OR alongside any voice-engine refactor that naturally exposes the synthesis-side hash injection point. Surfaced 2026-05-01 PR #955. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `detailModals.ts` error branches have async-before-ack 10062 exposure (3 paths)                    | PR #988 wrapped showModal success paths but three error branches (`handleEditButton` null-memory + content-too-long, `handleEditTruncatedButton` null-memory) call `interaction.reply()` / `update()` after `await fetchMemory(...)` without prior ack. Same async-before-ack class as the wrapped sites. **Fix shape**: (a) wrap calls in try/catch + `followUp` fallback (mirrors `showModalWithTimeoutCatch`), or (b) restructure to `deferUpdate()` first then `editReply` (allowed before reply/update, forbidden before showModal — exactly why showModal paths needed the wrap helper). ~15-25 LOC + tests across 3 paths. **Promote when**: alongside the test-coverage entry below (single PR, same file), OR user reports the silent "Interaction Failed" UX. Surfaced 2026-05-07 PR #988. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Add unit tests for `handleEditTruncatedButton` in `detailModals.ts`                                | `detailModals.test.ts` covers `handleEditButton` but `handleEditTruncatedButton` (the second showModal site wrapped in PR #988) has no per-site fixture (truncated content, button-click path, reply on missing memory). Wrapper itself is covered by `showModalWithTimeoutCatch.test.ts`. **Fix shape**: ~3-5 `it()` blocks mirroring the existing `handleEditButton` describe, asserting `showModal` is called with a modal whose value is sliced to `MAX_MODAL_CONTENT_LENGTH`. ~30-50 LOC. **Promote when**: opportunistic during next memory-detail refactor pass, OR alongside the 3-path 10062 exposure entry above (single PR adding the production fix + tests for the new behavior). Surfaced 2026-05-07 PR #988. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Extend temporal-marker pre-commit hook to scan `.py` files                                         | Current hook filters to `*.ts/*.tsx/*.js/*.jsx`, excluding voice-engine (`services/voice-engine/*.py`). Reviewer's framing: "Not critical — primary violation history is TypeScript." **Fix shape**: change file-extension filter to `\.(ts\|tsx\|js\|jsx\|py)$` AND extend comment-shape gate to include Python's `#`: `^[+][[:space:]]*(//\|\*\|/\*\|#)`. ~2 LOC. Worth integration-testing against `services/voice-engine/` to confirm no false positives (some Python comments use `# 2024-01-15: TODO` style). **Promote when**: a temporal marker is observed in a `.py` comment during code review, OR opportunistic during voice-engine refactor pass. Surfaced 2026-05-07 PR #989. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `/character chat` random-pick + load-personality failure UX                                        | When `resolveCharacterSlug` succeeds but `loadPersonality` subsequently fails for the randomly-picked slug, the user sees the existing generic `❌ Character "X" not found.` error which doesn't mention the slug was randomly chosen. They might think their command was malformed. The `isRandomPick` flag is already available in `handleChat` at that point; could surface a "couldn't load the randomly-picked character — try again" message instead. **Fix shape**: add an `if (isRandomPick)` branch in the `!personality` error path of `handleChat`, ~5 LOC + a test fixture. **Promote when**: a user reports confusion about random-pick failures (likely rare since `getCachedPersonalities` returns currently-accessible slugs and load failure between resolve-and-load is a narrow race). Surfaced 2026-05-07 PR #991 round 4. Deferred 2026-05-07.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `deny/detailEdit handleEdit` session-expiry path 10062 exposure                                    | After PR #992's `followUp`→`reply` fix, the session-expired path in `handleEdit` is correct on the happy timing path but still does `await sessionManager.get(...)` (Redis round-trip) before any ack. If Redis is slow under load, the subsequent `interaction.reply()` call lands after Discord's 3-second window and throws 10062 silently — same async-before-ack class as the showModal sites wrapped in PRs #985/#987/#988. The success branch already has the `showModalWithTimeoutCatch` wrapper covering this; the error branch doesn't. **Fix shape**: can't `deferUpdate` first (Discord forbids defer-before-`showModal`, which the success branch needs). Two options: (a) wrap the `reply()` call in a try/catch for `DiscordAPIError(10062)` with `followUp` fallback, mirroring `showModalWithTimeoutCatch` but for non-modal acks; (b) restructure to check the session via a separate ack-and-reply path. ~10-15 LOC + tests. **Promote when**: a user reports the silent "Interaction Failed" UX on a session-expired denylist edit click, OR alongside the `detailModals.ts` 3-path 10062 entry above (single PR could address the same pattern class). Surfaced 2026-05-07 PR #992 round 1. Deferred 2026-05-07.                                                                                         |
| `persona/dashboard.ts handleSelectMenu` empty-values silent drop                                   | If Discord sends a select-menu interaction with an empty `values` array (should never happen per Discord contract, but isn't structurally prevented), `value = interaction.values[0]` is `undefined` and the function silently returns with no log. **Same pattern in `character/dashboard.ts` `handleSelectMenu`**. **Fix shape**: add `if (value === undefined) { logger.warn({ customId: interaction.customId }, 'Select menu arrived with no values'); return; }` as a separate branch from the `!value.startsWith('edit-')` early-return — both dashboards together. ~6 LOC + test fixture per dashboard. **Promote when**: opportunistic during next dashboard refactor, OR alongside any observability-sweep PR. Surfaced 2026-05-08 in v3.0.0-beta.117 release PR #993 review. Deferred 2026-05-08.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `dispatchTruncationGateAction` default case leaves interaction unacknowledged                      | The `default` case in `dispatchTruncationGateAction` logs a warning but takes no action, so Discord shows "Interaction Failed" if a non-whitelisted action ever reaches the switch. The `DASHBOARD_ACTIONS` Set above is supposed to gate before the switch, so the default should be unreachable — but a future contributor adding a new action to the Set without updating the switch would produce a confusing "Interaction Failed" rather than a type error. **Fix shape**: replace the action-string Set + switch with a discriminated union over a `TruncationGateAction` literal-type union; use an exhaustive switch with a `never`-check default. Compile-time-safe against drift. Larger refactor (~30-50 LOC across the gate dispatcher and the action-string definitions). **Promote when**: next time `DASHBOARD_ACTIONS` needs to grow, OR opportunistic alongside any truncation-gate refactor pass. Surfaced 2026-05-08 in v3.0.0-beta.117 release PR #993 review. Deferred 2026-05-08.                                                                                                                                                                                                                                                                                                                       |
| `persona/sectionContext.ts replied===true` path direct unit test                                   | `sectionContext.test.ts` covers most paths of `loadPersonaSectionData` but doesn't have a direct test for the `replied && !deferred` branch in isolation — it's covered indirectly via `persona/dashboard.test.ts` integration. Reviewer marked low-concern. **Fix shape**: add 1-2 `it()` blocks pinning the `replied → followUp` branch directly. ~10-15 LOC. **Promote when**: opportunistic alongside next sectionContext refactor, OR if the integration coverage breaks during a future change and the unit gap becomes load-bearing. Surfaced 2026-05-08 in v3.0.0-beta.117 release PR #993 review. Deferred 2026-05-08.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
