## ⚡️ Quick Wins

_Small tasks that can be done between major features. Good for momentum._

- ✨ `[FEAT]` **Extend `/character chat` to allow omitting the character (triggers random bot)** — User note 2026-05-05. Currently `/character chat <character> <message>` requires specifying a character. Request: allow omitting the character to trigger a random bot response. Implementation: make the `character` option optional (or add a new subcommand), pick randomly from the user's available personalities when omitted. Small UX improvement. Surfaced 2026-05-05. Triaged from inbox 2026-05-06.

- 🐛 `[FIX]` **"Max age: off" should override global setting; separate "off" from "inherit"** — User note 2026-05-02. In the settings cascade, "off" (disabled) and "inherit" (use parent value) are conflated into one state. When a user sets max-age to "off" at a level where the global has a value, "off" should still mean "disabled" — it should not fall through to the global. The same issue may affect other cascade items (TTS provider, LLM config, etc.). **Fix shape**: audit all settings cascade fields for off-vs-inherit semantics; introduce a distinguishable "off" sentinel value (e.g., `null` = inherit, `false` = explicitly off, or a discriminated union). Ensure the resolver treats "off" as a terminal "no max age" rather than "use parent". Scope depends on how many cascade fields have this conflation. Surfaced 2026-05-02. Triaged from inbox 2026-05-06.

- 🐛 `[FIX]` **`deny/detailEdit.ts handleEdit` uses `followUp` without prior ack on null-session path** — Surfaced 2026-05-07 in PR #988 round 3 claude-review. Pre-existing pattern. When `sessionManager.get(...)` returns `null`, `handleEdit` calls `interaction.followUp(...)` without first acknowledging via `deferUpdate` / `reply`. Discord accepts the `followUp` via the webhook endpoint, but because no initial callback response was sent, the user sees BOTH the default "Application did not respond" banner AND the ephemeral follow-up — confusing double-message UX. **Fix shape**: change `await interaction.followUp({ content: ..., flags: MessageFlags.Ephemeral })` to `await interaction.reply({ content: ..., flags: MessageFlags.Ephemeral })` in `handleEdit`'s null-session branch. The `reply` call is the correct idiom for the initial response to an unacknowledged button interaction. ~3 LOC + the `detailEdit.test.ts` fixture update for the null-session case. Worth a quick audit of other handlers in `commands/deny/` for the same pattern before closing out. Surfaced 2026-05-07.

- 🧹 `[CHORE]` **Audit `backlog/deferred.md` for stale entries** — `deferred.md` has accumulated 55+ entries; some triggers may have already fired without notice (related work shipped, conditions met). Risk: backlog rot reduces signal-to-noise — a 55-entry deferred pile is harder to scan for "what should we promote now?" than a 20-entry one. **Fix shape**: walk through each entry in `deferred.md`, for each one: (a) check if its named trigger has already fired (grep recent commits / PRs / current state), (b) if yes, either promote to inbox/quick-wins OR delete if no longer relevant, (c) if no, re-affirm the trigger is still current. ~1-2hr task. **Promote when**: opportunistic between major features OR when planning a next-theme rotation (clear signal-noise before the next planning cycle). Surfaced 2026-05-07 during inbox triage.

- 🧹 `[CHORE]` **Railway `overlapSeconds: 0` belt-and-suspenders for SIGTERM regression defense** — Originally tracked as branch (a) of the multi-branch DM-fix-verification entry (PR #913 era); branch (a) won — empirically confirmed 2026-04-27 via DM_RAW_GATEWAY_DIAGNOSTIC observations that PR #918's Layer 1 + Layer 2 fixes resolved the post-deploy DM-silence bug. The env var deletion is tracked separately above. The remaining optional follow-up: configure Railway `overlapSeconds: 0` as belt-and-suspenders so any future SIGTERM-handler regression doesn't re-introduce the bug. Currently the overlap window is enabled, meaning a regression in the SIGTERM handler would silently re-create the orphaned-session symptom. **Fix shape**: Railway dashboard → bot-client service → deployment settings → `overlapSeconds: 0`. **Promote when**: convenient — opportunistic dashboard tweak with no code change. Branches (b) and (c) of the original entry are obsolete now that branch (a) is confirmed. Moved from `backlog/inbox.md` 2026-04-30 to colocate with the sibling dashboard task.

### 🐛 Detect and Retry Inadequate LLM Responses

LLMs occasionally return a 200 OK with garbage content — e.g., glm-5 returned just `"N" (1 token, finishReason: "unknown"`, 160s duration). Needs compound scoring heuristic + timing data threading through RAGResponse. ~4-6hr feature, not a quick win — moved details to Logging & Error Observability theme.
