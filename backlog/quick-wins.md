## ⚡️ Quick Wins

_Small tasks that can be done between major features. Good for momentum._

- **[CHORE] Remove the forward-shape diagnostics once the forwarded-trigger cause is known** — Four TEMPORARY INFO logs were added to capture forward behaviour (see `backlog/production-issues.md`): (1) PR #1171 — `describeForwardShape` + the empty-content gate in `MessageHandler.handleMessage` (trigger-path wire shape); (2) PR #1173 — the `History-fetched forward — shape diagnostic` log in `DiscordChannelFetcher.convertMessage` (settles whether a bulk-list REST fetch carries snapshot content); (3) the `Authoritative content lost during reference/mention rewriting — diagnostic` log in `ReferenceExtractor.extractReferencesAndMentions` (pinpoints whether the trigger content-loss is the link-replacement clobber or mention resolution); (4) the `Empty DB message not recovered from extended context — diagnostic` log in `historyMerger.recoverEmptyDbContent` (pinpoints why an empty forward is NOT self-healed: extended-map keying miss vs genuinely-empty extended content). Once the real fix ships (`fix/forwarded-message-content-loss`) **and is dev-verified** (forwards persist real content; `recoverEmptyDbContent` heals with `recoveredCount ≥ 1`), **delete** all four log blocks plus `describeForwardShape` and its colocated tests in `forwardedMessageUtils.test.ts`. **Also delete the dead `hydrateForwardedSnapshots` (#1170)** in `forwardedMessageUtils.ts` + its colocated tests — runtime diagnostics confirmed snapshots were never the problem (the clobber + placeholder-blind recovery were), so it never fires. **KEEP** the `DiscordChannelFetcher.test.ts` "includes a forwarded message with empty top-level content in history results" test and the `ReferenceExtractor.test.ts` "threads the effective content to the extractor so forward text is not clobbered" / "returns empty messageContent when mention resolution drops non-empty content" tests, plus the new `historyMerger.test.ts` placeholder-recovery tests and the `NO_TEXT_CONTENT_PLACEHOLDER` value-lock test — all assert real contract invariants that outlive the diagnostics. Dev-only and short-lived; this entry guarantees the diagnostics don't ossify into permanent INFO noise. Surfaced 2026-06-07 by PRs #1171/#1173 review; #1170 dead-code confirmation 2026-06-08.

> Note: 7 items previously filed here all shipped in PR #1082-1084 (Layer 2 + Layer 3 of the periodic-audit-enforcement proposal). The remaining work tracked in [`docs/proposals/backlog/periodic-audit-enforcement.md`](../docs/proposals/backlog/periodic-audit-enforcement.md) is Layers 4-5 (markdown baselines + `ops:health` cron aggregator).

- **[CHORE] Merge stacked JSDoc blocks in `check-duplicate-exports.ts`** — The file uses the stacked pattern `/** description */` + `/** @internal Exported for testing */` on several exported helpers (`isAllowed`, `isSourceFile`, `parseReExportName`, `matchDeclarations`, `matchReExports`, `extractExports`, `findDuplicates`). TypeScript/TSDoc only reads the JSDoc block **immediately preceding** the declaration, so the description blocks are orphaned — they don't appear in IDE hover or generated docs. **Fix**: fold the `@internal Exported for testing` tag into each description block as a trailing `@internal` tag line (same fix already applied to `check-dockerfile-dist.ts` in PR #1148 after claude-review caught the pattern there). Grep `/** @internal Exported for testing */` across `packages/tooling/src` to confirm no other files carry the stacked form. ~10 line-moves, comment-only. Surfaced 2026-06-03 by PR #1148 claude-review.

_Shipped 2026-06-03 (quick-wins sweep, PRs #1147/#1148/#1149): redis removal + test-factories depcruise boundary, `guard:dockerfile-dist`, view.ts coverage + typed preset unflatten pipeline. One new item filed below from the sweep's review cycles._
