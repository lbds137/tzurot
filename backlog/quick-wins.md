## ⚡️ Quick Wins

_Small tasks that can be done between major features. Good for momentum._

- **[CHORE] Migrate `/admin/db-sync` + `/admin/cleanup` to `X-User-Id` header** — both routes pass `{ownerId}` in POST body, which is why `extractOwnerId` retains a body-fallback path. All other admin routes use the `X-User-Id` header convention (post-PR #1081). Migrating these two would let the body fallback come out of `extractOwnerId` entirely. **Start**: `services/bot-client/src/commands/admin/db-sync.ts:137-140` + `commands/admin/cleanup.ts:31` — drop the `ownerId` body field, rely on `adminPostJson` setting the header. Then drop the body block in `services/api-gateway/src/services/AuthMiddleware.ts:extractOwnerId`. ~10 LOC. Surfaced by PR #1081 review.

- **[CHORE] Guard `runEslint` against `respectIgnores: false` with default `targetDirs`** — `respectIgnores: false` passes `--no-ignore` to ESLint, which also defeats the implicit `node_modules` exclusion. The canary caller mitigates this by pairing it with a tightly-scoped `targetDirs` fixture path, but a future caller passing `respectIgnores: false` without a narrow `targetDirs` would scan `node_modules`. **Start**: `packages/tooling/src/lint/complexity-report.ts:runEslint` — add `if (!respectIgnores && targetDirs === DEFAULT_TARGET_DIRS) throw` precondition, or replace the boolean with an explicit `additionalIgnoreOverrides: string[]` option the caller can extend. Surfaced by PR #1082 reviews (rounds 8-15, reviewer-flagged repeatedly).

- **[CHORE] Add direct test for the non-summary path of `checkProposalOrphans`** — the human-readable orphan listing + fix-hint paragraph + non-summary `process.exit(1)` is exercised only implicitly by the real-repo test (which only hits the no-orphans success branch). Detection logic in `findProposalOrphans` is well-tested; the CLI formatter is not. **Start**: `packages/tooling/src/audits/check-proposal-orphans.test.ts` — add a test scaffolding a temp repo with one orphan, invoking `checkProposalOrphans({ summary: false, repoRoot })`, spying on `console.log` + `process.exit`, asserting the orphan path appears in output and exit was called with 1. Surfaced by PR #1082 review round 13.

- **[CHORE] Strengthen orphan-check against single-word proposal slugs** — the word-boundary regex matches the basename as a token, so a proposal named `memory.md` or `shapes.md` would be silently rescued by any markdown file containing those common words in prose. JSDoc warns "multi-segment kebab-case preserves precision" but nothing enforces it. **Start**: in `packages/tooling/src/audits/check-proposal-orphans.ts:findProposalOrphans`, add a static check that warns (or fails) when a proposal basename matches `^[a-z]{1,8}\.md$` — pick threshold per actual proposal naming distribution. Alternative: document the constraint in the human-readable error message so contributors hitting the failure see it directly. Surfaced by PR #1082 review rounds 14-15 (reviewer flagged as "medium concern worth addressing before this enforcement pattern gets applied to more tools").
