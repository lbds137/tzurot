# Memory Archive: EARS Specs — Slices A, B1 and B2

Status: LIVE — slices A, B1 and B2

LLD: [`memory-archive-design.md`](./memory-archive-design.md). HLD:
[`docs/proposals/backlog/memory-architecture.md`](../../proposals/backlog/memory-architecture.md).
Entry point to every `@spec` annotation and proving test:
`grep -rn "MEM-ARCH-" docs/intent services packages`.

| ID           | Requirement                                                                                                                                                                                                                                                                                                                                                                                         | Proving test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MEM-ARCH-001 | While the responding personality's slug is not in `archiveSplitRenderPersonalities`, the memory archive shall render byte-identical to the pre-slice-A verbatim form.                                                                                                                                                                                                                               | `it('MEM-ARCH-001: verbatim mode (no archiveRender) keeps the no-newline single-line shape', ...)` (`MemoryFormatter.test.ts`); `it('MEM-ARCH-001: stamps nothing when the personality slug is not listed (verbatim mode)', ...)` (`factRetrievalHelper.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                     |
| MEM-ARCH-002 | When a memory row is split-mode and parseable, the system shall omit the assistant part of the row from the rendered note, and every user-controlled value in the split body (the speaker label, the user turn, fact statements) shall be XML-escaped.                                                                                                                                              | `it('MEM-ARCH-002: split mode renders the user turn, never the assistant part of pageContent', ...)` (`MemoryFormatter.test.ts`); `it('MEM-ARCH-002: escapes the speaker label — a persona name cannot break out of the note', ...)` (`MemoryNoteSplitRender.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                |
| MEM-ARCH-003 | While rendering a split-mode note, the system shall strip every `> `-prefixed quote line from the user turn and count them.                                                                                                                                                                                                                                                                         | `it('MEM-ARCH-003: strips quote lines from the user turn and counts them', ...)` (`MemoryNoteSplitRender.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| MEM-ARCH-004 | When a split-mode user turn exceeds 3,000 characters, the system shall cap it and append a ` […]` marker, and the cut shall always fall on a code-point boundary (never splitting a UTF-16 surrogate pair).                                                                                                                                                                                         | `it('MEM-ARCH-004: leaves text at exactly the cap unchanged', ...)`; `it('MEM-ARCH-004: a 3000-char fixture is unchanged; a 3001-char fixture is capped', ...)`; `it('MEM-ARCH-004: caps text over the limit, cutting at the last whitespace and appending the marker', ...)`; `it('MEM-ARCH-004: the hard cut steps back rather than splitting an astral emoji surrogate pair', ...)` (`MemoryNoteSplitRender.test.ts`)                                                                                                                                                                                                                                                              |
| MEM-ARCH-005 | The split-mode render shall never render the `[Referenced content: …]` block.                                                                                                                                                                                                                                                                                                                       | `it('MEM-ARCH-005: userTurn excludes the [Referenced content: ...] block', ...)` (`memoryUtils.test.ts` — the actual exclusion point); `it('MEM-ARCH-005: never renders anything beyond the stored userTurn (referenced block excluded upstream)', ...)` (`MemoryNoteSplitRender.test.ts` — a structural sanity check, not independently falsifiable, since the renderer has no `referenced` field to mutate into view)                                                                                                                                                                                                                                                               |
| MEM-ARCH-006 | While a memory row does not match the stored `{user}: … \n{assistant}: …` template, the system shall render it verbatim under the cap and count it as a fallback render.                                                                                                                                                                                                                            | `it('MEM-ARCH-006: falls back to verbatim pageContent when userTurn is absent', ...)` (`MemoryNoteSplitRender.test.ts`); `it('MEM-ARCH-006: leaves userTurn and subjectName undefined for legacy (unparseable) content', ...)` (`memoryUtils.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                |
| MEM-ARCH-007 | When a split-mode note has linked facts, the system shall render them salience-descending beneath the user turn, drawn only from active facts (not deleted, forgotten, or superseded), and shall omit the facts section when there are none. A fact linked to several retrieved memories shall be attributed to at most one note — the most relevant memory that links it.                          | `it('MEM-ARCH-007: renders linked facts salience-descending beneath the user turn', ...)` (`MemoryNoteSplitRender.test.ts`); `it('MEM-ARCH-007: linked facts exclude deleted, forgotten, and superseded rows', ...)` (`FactStore.component.test.ts`); `it('MEM-ARCH-007: a fact linked to two retrieved memories is attributed to the more relevant one only', ...)` (`factRetrievalHelper.test.ts`)                                                                                                                                                                                                                                                                                  |
| MEM-ARCH-008 | While a turn is in split mode, the system shall drop any `<facts>` entry that was rendered inside a surviving split note (matched by fact id); while a turn is in verbatim mode, facts shall be left untouched.                                                                                                                                                                                     | `it('MEM-ARCH-008: drops a fact whose id appears in a surviving note's linkedFacts', ...)` (`dropFactsCoveredByArchive.test.ts`); `it('MEM-ARCH-008: leaves a fact untouched in verbatim mode even when its id would be dropped in split mode', ...)` (`PromptBuilder.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                       |
| MEM-ARCH-009 | When a turn is in split mode, the archive instruction shall be `MEMORY_ARCHIVE_SPLIT_INSTRUCTION`; otherwise it shall be the unchanged verbatim `MEMORY_ARCHIVE_INSTRUCTION`.                                                                                                                                                                                                                       | `it('MEM-ARCH-009: formatMemoriesContext picks the mode from the FIRST doc and uses the split instruction', ...)`; `it('MEM-ARCH-009: formatMemoriesContext uses the verbatim instruction when no doc is in split mode', ...)` (`MemoryFormatter.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                            |
| MEM-ARCH-010 | The system shall read the split-render switch per turn from `archiveSplitRenderPersonalities`, keyed by the responding personality's slug.                                                                                                                                                                                                                                                          | `it('MEM-ARCH-010: stamps every retrieved doc with archiveRender when the personality slug is listed', ...)`; `it('MEM-ARCH-001: stamps nothing when the personality slug is not listed (verbatim mode)', ...)` (`factRetrievalHelper.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| MEM-ARCH-011 | When a linked-facts fetch fails, the system shall degrade to no linked facts and shall never fall back to verbatim mode.                                                                                                                                                                                                                                                                            | `it('MEM-ARCH-011: fails soft — returns [] when the query throws (never falls back to verbatim)', ...)` (`FactRetriever.test.ts`); `it('MEM-ARCH-011: stays in split mode with linkedFacts: [] when the retriever already degraded the fetch failure ...', ...)` (`factRetrievalHelper.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                      |
| MEM-ARCH-012 | The memory budget manager shall size a split-mode note at its rendered split size, not at its `pageContent` size.                                                                                                                                                                                                                                                                                   | `it('MEM-ARCH-012: counts a split-mode note at its SPLIT body size, not its verbatim pageContent size', ...)` (`memoryBudgetSplitSeam.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| MEM-ARCH-013 | When a non-chunked memory is stored and `archiveSummaryEnqueueEnabled` is on, the system shall enqueue exactly one archive-summary job whose BullMQ jobId is the memory's own id; while the switch is off, or when the stored memory was chunked, it shall enqueue nothing.                                                                                                                         | `it('C013: a non-chunked memory enqueues with the deterministic memory id', ...)`; `it('C013: a chunked memory enqueues nothing', ...)` (`PgvectorMemoryAdapter.test.ts`); `it('enqueues a job with the memory id as the deterministic jobId', ...)`; `it('skips entirely when disabled — no enqueue, no SQL', ...)` (`ArchiveSummaryTrigger.test.ts`)                                                                                                                                                                                                                                                                                                                                |
| MEM-ARCH-014 | While the model-call switch is off, the resolved system-model route is not `zai-coding`, or the daily cap is exhausted, the system shall delay the job without consuming a retry attempt and without writing to the memory row.                                                                                                                                                                     | `it('MEM-ARCH-014: the model-switch-off gate delays without a model call', ...)`; `it('the OpenRouter-route gate delays, logs once inside the throttle window, and logs again past it', ...)`; `it('budget-exhausted gate delays without a model call and refunds the denied charge', ...)` (`ArchiveSummaryProcessor.test.ts`)                                                                                                                                                                                                                                                                                                                                                       |
| MEM-ARCH-015 | When a memory's content does not match the stored user/assistant template, the system shall mark the row `dead` with error class `no_template` and shall make no model call and shall consume no budget. The write shall also stamp the prompt version that produced the verdict, so a re-enqueue of the row skips instead of re-billing.                                                           | `it('MEM-ARCH-015: no-template content is marked dead with the invoker never called, and consumes no budget', ...)` (`ArchiveSummaryProcessor.test.ts`); `it('MEM-ARCH-015: a row whose content has no template is marked dead/no_template, the invoker is never called, and the budget is never consumed', ...)`; `it('MEM-ARCH-015: a dead row from no-template skips re-enqueue without a model call or budget consumption', ...)` (`ArchiveSummaryProcessor.component.test.ts`)                                                                                                                                                                                                   |
| MEM-ARCH-016 | The summarizer's own writes to `memories` shall never change `updated_at`.                                                                                                                                                                                                                                                                                                                          | `it('MEM-ARCH-016: the summary write leaves updated_at untouched', ...)` (`ArchiveSummaryProcessor.component.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| MEM-ARCH-017 | A summary of at most 80 tokens shall be accepted; over 80 shall trigger exactly one regeneration; over 120 after regeneration shall be recorded as a billed `overflow` failure; and a summary shall never be truncated.                                                                                                                                                                             | `it('MEM-ARCH-017: a summary still over the hard cap after regeneration is a billed overflow failure, never truncated', ...)` (`ArchiveSummaryProcessor.test.ts`); every `decideLengthState` branch test (`archiveSummaryValidation.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| MEM-ARCH-018 | A first-person pronoun outside a quoted span shall trigger regeneration, and shall be recorded as a billed `first_person` failure if it persists.                                                                                                                                                                                                                                                   | `it('MEM-ARCH-018: a quoted form of address is not a leak, the same words unquoted ARE', ...)` (`archiveSummaryValidation.test.ts`); `it('MEM-ARCH-018: a summary still first-person after regeneration is a billed first_person failure', ...)` (`ArchiveSummaryProcessor.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                  |
| MEM-ARCH-019 | While a memory carries referenced content, the system shall run a referent check, shall regenerate on any dangling referent, and shall accept and count a summary that still dangles after regeneration.                                                                                                                                                                                            | `it('MEM-ARCH-019: the referent check runs only for referenced rows', ...)`; `it('MEM-ARCH-019: the regeneration path carries the dangling names into the invoker prompt', ...)` (`ArchiveSummaryProcessor.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| MEM-ARCH-020 | Three billed failures against the same content and prompt version shall mark the row `dead`; a zero-spend failure shall leave the row untouched and shall be rethrown for retry. The three billed failures shall also record the prompt version that produced the verdict, so the row is skipped on re-enqueue until a content edit or a version bump; either one restarts the attempts count at 1. | `it('MEM-ARCH-020: three consecutive billed failures against the same content mark the row dead', ...)`; `it('MEM-ARCH-020: a dead row from billed failures skips re-enqueue without a model call or budget consumption', ...)`; `it('MEM-ARCH-020: a prompt-version bump restarts the attempts budget instead of re-killing the row', ...)` (`ArchiveSummaryProcessor.component.test.ts`); `it('a rejecting invoker is a zero-spend throw: refunds the budget, writes nothing, rethrows', ...)` (`ArchiveSummaryProcessor.test.ts`)                                                                                                                                                  |
| MEM-ARCH-021 | While a retrieved memory carries a done summary, the split-mode note shall render the user turn followed by the character's name and the summary, with no linked-facts section; while it carries none, the note shall render as slice A's user turn + linked facts.                                                                                                                                 | `it('MEM-ARCH-021: renders the user turn then the character's name and summary, with no facts section', ...)`; `it('MEM-ARCH-021: a doc with no summary renders slice A's user turn + linked facts', ...)`; `it('MEM-ARCH-021: a doc with no userTurn takes the verbatim fallback and ignores the summary', ...)` (`MemoryNoteSplitRender.test.ts`); `it('MEM-ARCH-021: formatMemoriesContextWithStats counts summary-rendered notes in summaryNotes', ...)` (`MemoryFormatter.test.ts`); `it('stamps assistantSummary for a done row whose prompt version is stale', ...)` (`memoryUtils.test.ts` — the stamp that selects the arm)                                                  |
| MEM-ARCH-022 | When a split-mode turn retrieves a non-chunked memory whose summary is absent, failed, or from an older prompt version — including a dead row from an older version — the system shall enqueue it with reason `retrieval` without awaiting; pending rows, dead rows at the current prompt version, chunk rows, and verbatim-mode turns shall enqueue nothing.                                       | `it("MEM-ARCH-022: enqueues every refresh-eligible doc with reason 'retrieval'", ...)`; `it('MEM-ARCH-022: enqueues nothing in verbatim mode', ...)`; `it('MEM-ARCH-022: does not await the enqueue', ...)`; `it('MEM-ARCH-022: an enqueue rejection never propagates', ...)` (`factRetrievalHelper.test.ts`); `it('does not stamp summaryRefreshEligible for a pending row', ...)`; `it('does not stamp summaryRefreshEligible for a dead row at the current prompt version', ...)`; `it('stamps summaryRefreshEligible for a dead row with a stale prompt version', ...)`; `it('MEM-ARCH-022: does not stamp summaryRefreshEligible for a chunk row', ...)` (`memoryUtils.test.ts`) |
| MEM-ARCH-023 | The system shall write one `archive_summary` usage row per model call, including calls whose response failed to parse, each carrying its own `latencyMs`.                                                                                                                                                                                                                                           | `it('MEM-ARCH-023: writes a usage row even when the response fails to parse (a parse failure still spent tokens)', ...)` (`ArchiveSummaryProcessor.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| MEM-ARCH-024 | When a memory's content is edited, the system shall reset every summary column on that row.                                                                                                                                                                                                                                                                                                         | `it('MEM-ARCH-024: resets every summary column on a content edit', ...)` (`memorySingle.test.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| MEM-ARCH-025 | `/memory view` shall show the stored summary when one exists.                                                                                                                                                                                                                                                                                                                                       | `it('MEM-ARCH-025: renders a Summary field when the memory carries one', ...)`; `it('MEM-ARCH-025: omits the Summary field when there is no summary', ...)` (`detail.test.ts`); `it('MEM-ARCH-025: GET returns the stored summary and status', ...)`; `it('MEM-ARCH-025: GET returns null for both on an unsummarized row', ...)` (`memorySingle.test.ts`); `it('MEM-ARCH-025: a payload carrying assistantSummary and summaryStatus survives safeParse with both values intact', ...)` (`memory.test.ts`)                                                                                                                                                                            |
| MEM-ARCH-026 | A fact linked to a summarized memory shall not be attributed to that note and shall remain in `<facts>`.                                                                                                                                                                                                                                                                                            | `it('MEM-ARCH-026: a fact linked to a summarized memory is not attributed to it and flows to the next unsummarized memory that links it', ...)`; `it('MEM-ARCH-026: a fact linked ONLY to a summarized memory is attributed to no note', ...)` (`factRetrievalHelper.test.ts`); `it('MEM-ARCH-026: keeps a fact whose only source memory rendered as a summary', ...)` (`dropFactsCoveredByArchive.test.ts`)                                                                                                                                                                                                                                                                          |
