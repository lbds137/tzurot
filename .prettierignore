# Legacy codebase
tzurot-legacy/

# Build output
dist/
coverage/

# Dependencies
node_modules/

# Lockfile (machine-generated; prettier reformats integrity blocks across
# thousands of lines, drowning real version changes in noise).
pnpm-lock.yaml

# Generated client classes from the route manifest. Prettier mangles JSDoc
# bodies that contain glob-pattern strings (the asterisks confuse the
# JSDoc parser), so the codegen output is excluded from formatting.
_generated/
